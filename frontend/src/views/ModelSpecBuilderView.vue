<script setup lang="ts">
/**
 * `02` §5.3's Model spec builder — `/models/new`.
 *
 * Builds a Model Spec and asks `POST /model-specs/validate` whether it may be fitted,
 * before any compute is spent. This slice (`W6b-4a`) covers the **builtin** objective arm;
 * `W6b-4b` adds custom objectives.
 */
</script>

<template>
  <section>
    <h1 class="text-xl font-semibold tracking-tight">
      New model spec
    </h1>
  </section>
</template>
