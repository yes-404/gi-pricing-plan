# Open Questions

Unresolved design questions. Every question is mirrored from the §10 "Open questions"
section of the spec that raised it. **Phase 0 exit requires this file to be empty or
every remaining entry explicitly marked `deferred` with a target phase.**

**Status values:** `open` · `decided` (link the ADR) · `deferred` (state the phase) ·
`dropped` (state why).

**2026-08-14 — Track A research closed one question and sharpened two others.** OQ-RATE-1 is
`decided`; OQ-MODEL-1 and OQ-RATE-2 carried new evidence and are now `decided` too. See
[`research/track-a-findings.md`](research/track-a-findings.md).

**How to close one:** if the decision constrains more than one module or is expensive to
reverse, write an ADR and set status `decided → ADR-NNNN`. Otherwise record the decision
inline in the owning spec and set status `decided (in spec)`. Never delete a row.

---

## OVR — system level

| ID | Question | Options / trade-offs | Recommendation | Owner | Status |
|---|---|---|---|---|---|
| **OQ-OVR-6** | ~~How are the five workflow journeys (`docs/workflows/wf-01…05`) evidenced?~~ **DECIDED 2026-08-15: the mechanical citation audit now, one end-to-end test per journey as its last module lands, never a marker on an existing test.** | Found by plan review 2: **no test in the repository cites a journey**, and `audit-docs.py`'s "workflow coverage" measures whether a workflow document *mentions* a requirement id, not whether the journey runs. Options: **(a)** mark existing tests with the journey id — cheapest, and the weakest, since a marker on a slice claims the whole; **(b)** one end-to-end journey test per workflow, written when its modules exist — real evidence, but wf-01's modelling half cannot be written until W5; **(c)** a mechanical audit that every step in a journey cites an endpoint, requirement or artifact that exists — catches drift, proves nothing about execution. | **Maintainer accepted the recommendation: (c) now, (b) per phase, never (a).** Specified as FR-OVR-17. The audit is sharper than "cites something that exists" once written down: requirement ids and `§` references are already checked (`audit-docs.py` 2 and 9), so what is **not** checked — and what check 21 adds — is that every **endpoint path and `pricing-core` function** a journey step cites is declared in the owning module's §5.1 or §5.2. Ownership needed no new rule either: "the workstream that completes the last module the journey touches" is in every case the phase whose exit criterion names that journey (`roadmap.md` §12), so `wf-01` is W5's, `wf-02` and `wf-04` A–D are Phase 2's, `wf-05` is Phase 3's, and `wf-03` with `wf-04` E–H are Phase 4's. **Not (a)** remains the load-bearing half: marking `test_the_failure_loop_then_validated` as `wf-01` would claim a journey where one slice is covered. | maintainer | **decided** (in spec) |
| **OQ-OVR-1** | ~~Is `workspace` the tenancy boundary, or is physical isolation needed for hosted multi-tenant use?~~ **DECIDED 2026-08-15: the tenancy boundary is the deployment — one tenant, one deployment.** | (a) Logical only — one schema, `workspace_id` on every row: simple, cheap, but a query bug leaks another insurer's rates. (b) Schema-per-tenant: strong isolation, migration complexity ×N. (c) Deployment-per-tenant: total isolation, highest ops cost. | **Maintainer decided (c), for hosted and self-hosted use alike** — recorded as [ADR-0006](adr/0006-tenant-isolation-is-a-deployment-boundary.md), because it constrains every module and is expensive to reverse. *Beyond the recommendation on file:* the recommendation kept `workspace_id` so that **(b) stayed reachable**, and (b) is now **rejected rather than deferred** — it fans migrations out exactly as (c) does while a single misconfigured connection still crosses the boundary. `workspace_id` therefore stays for a different reason than it was kept: it is the organisational scope for RBAC, settings and the audit chain inside one tenant, and **must never be described as an isolation boundary again** (FR-OVR-13). The isolation invariant is FR-OVR-15; FR-PLAT-56 makes a deployment refuse to start against another tenant's database, which is the one operational mistake the guarantee cannot survive. Two consequences land elsewhere: version skew between tenants is now permanent, so provenance must name the build (FR-OVR-16), and any cross-insurer capability becomes an export-and-aggregate pipeline rather than a query. | maintainer | **decided** ([ADR-0006](adr/0006-tenant-isolation-is-a-deployment-boundary.md)) |
| **OQ-OVR-2** | ~~Project licence.~~ **DECIDED 2026-08-14: Apache-2.0.** | Matches the recommendation. Insurers embedding `pricing-core` in internal systems is the adoption path, and AGPL would block exactly the users the project wants. | **Apache-2.0.** `LICENSE` added at the repo root (canonical text, unmodified except the copyright line). NFR-OVR-11's dependency check now has a concrete target: every runtime dependency must be Apache-2.0 compatible, which rules out AGPL and SSPL transitively. | maintainer | **decided** |
| **OQ-OVR-3** | ~~Multi-currency support in one workspace — Phase 2 or Phase 4?~~ **DECIDED 2026-08-14: deferred to Phase 4; a single workspace currency is enforced now and stored on every artifact, so the later change is additive.** | Now: every money field needs a currency and FX effective-dating, which touches every rate table and trace. Later: a migration of all monetary columns, but no cost until then. | **Maintainer accepted the recommendation.** Multi-currency now means every money field needs a currency and FX effective-dating, touching every rate table and every trace — for a capability nobody has asked for yet. Deferring it is only safe if the deferral costs nothing later, which is why the single currency is *recorded* rather than assumed: `ArtifactEnvelope` carries a `currency` code from today, so Phase 4 adds FX rather than migrating every monetary column. Applied to FR-OVR-7 and `00` §4.3; the workspace setting is renamed `workspace.currency`, because `display.currency` implied it only affected rendering. | maintainer | **decided** |
| **OQ-OVR-4** | ~~Publish `pricing-core` to PyPI from Phase 1?~~ **DECIDED 2026-08-14: not in Phase 1 — an editable-install path is documented now; publish from Phase 2 as `0.x` with an explicit no-stability notice.** | Publishing forces semver stability on an API we are still discovering; not publishing weakens the "verify it in a notebook" story that ADR-0001 exists for. | **Maintainer accepted the recommendation.** Publishing in Phase 1 would force semver stability on an API still being discovered, and every early mistake would become a breaking change for somebody. What ADR-0001 actually needs is that a reviewer can run the maths outside the platform, and `pip install -e packages/pricing-core` delivers that today — documented in CLAUDE.md §11. From Phase 2 it publishes as `0.x` carrying an explicit no-stability-guarantee notice, so the promise made is the one that can be kept. | maintainer | **decided** |
| **OQ-OVR-5** | ~~Notebook escape hatch: embedded JupyterLab or client library only?~~ **DECIDED 2026-08-14: a client library in Phase 1; embedded notebooks revisited in Phase 4 once sandboxing exists.** | Embedded: best UX, but arbitrary code execution inside the platform's security and audit boundary. Client library: users bring their own Jupyter, credentials leave the platform, no audit of what they ran. | **Maintainer accepted the recommendation.** An embedded JupyterLab is arbitrary code execution inside the platform's security and audit boundary — the same problem custom objectives pose, and it should not be solved twice or early. A client library keeps execution outside, at the cost that the platform cannot audit what a user ran on data they were entitled to read, which is the accepted trade. *Consequence to resolve when it is built:* it needs **read-scoped tokens for a human principal**, and W2's Service Accounts are deliberately scoring-only (FR-GOV-6), so a user-token type is a new thing rather than a reuse. Revisit embedded notebooks in Phase 4, when sandboxing for custom objectives has to exist anyway. | maintainer | **decided** |
| ~~**OQ-OVR-7**~~ ✔ | ~~Should `01` FR-DATA-26's one-way row rename `severity_minor` and `burning_cost_minor`, which are **means** and therefore floats, not integer minor units?~~ **DECIDED 2026-08-17: rename to `mean_severity` and `mean_burning_cost`, in the slice that next changes the profile contract.** | Raised 2026-08-15 (W5) by generating `banding` and `grouping` schemas: they embed the one-way row, and the money scan flagged both fields the moment it could see them. `01` keeps them as floats deliberately — rounding a mean to whole minor units would lose the precision the confidence interval beside it expresses — so the values are right and the **names** are what FR-OVR-7 objects to, because `_minor` is reserved for integer minor units. **Rename** (e.g. `mean_severity`, `mean_burning_cost`, both in units of the workspace currency's minor unit): the scan needs no exclusion and a reader stops having to know which `_minor` fields are exceptions — at the cost of a breaking change to `01`'s published profile contract, the frontend that reads it, and the seeded demo. **Keep and exclude**: nothing breaks, and the repository carries a money-discipline check with a hand-maintained allow-list, which is the shape of thing that grows. | **Maintainer accepted the recommendation, 2026-08-17.** Rename, in the slice that next changes the profile contract — not on its own, because the change is mechanical but wide, and not never, because an allow-list on a money check is exactly the kind of exception nobody revisits. Specified as `01` **FR-DATA-46**, so the obligation has a written form, a trigger and an owner rather than living in a comment: the names become `mean_severity` and `mean_burning_cost`, both still in the workspace currency's minor unit, and the values stay floats for the reason `01` always kept them so. ~~**Not delivered** — deliberately, and the requirement says so. Until then the exclusion in `test_contracts.py` stands and **may not grow**: a ratio recognisable by shape is excluded by a rule (`_per_parameter`), and a third hand-written name is the signal that the rename is overdue rather than an entry to add.~~ **Corrected 2026-08-24: delivered 2026-08-18**, the day after this row was written. `01` FR-DATA-46 records the delivery, `OneWayRow` declares `mean_severity` as an optional float in `packages/model-schema/src/model_schema/profiles.py`, and the hand-written exclusion was **deleted rather than grown** — `backend/tests/test_contracts.py:139` cites this question by name as its reason to prefer a shape rule over a name list. The condition the struck sentences set was *discharged, not breached*: the `ratio_suffix` rule now carries three terms, but those are rule terms, not the hand-written names this row warned about, and post-rename `mean_severity` carries no `_minor` to exclude. What the rename never reached is a set of names that pre-dated it in `pricing-core`'s `validate.py`; that is recorded against FR-OVR-7 and asked as **OQ-OVR-11**. | maintainer | **decided** (in spec) |
| ~~**OQ-OVR-8**~~ ✔ | ~~`DecimalStr` is the repository's exact-decimal type, and it silently accepts a `float`. `OneWayRow(exposure_years=0.1+0.2)` returns `Decimal('0.30000000000000004')` — the float's binary error preserved verbatim inside a field FR-OVR-7 defines as exact. Should `DecimalStr` refuse a `float` outright?~~ | Raised 2026-08-19 (W5, the `top_levels` slice), from an implementer's question rather than a spec review. **11 `DecimalStr` fields across 6 modules** are affected. *(Corrected 2026-08-19 from "26 across 7": that figure counted every line mentioning `DecimalStr` — imports, `__all__`, and `money.py`'s own definition — of which there are exactly 26. The audit the decision required is what found it.)* The new `LevelCount.exposure_years` carries a field-scoped validator that does refuse floats, making it the only strict one — a deliberate inconsistency recorded here rather than generalised without a decision. Serialisation is not the hole: `DecimalStr` emits a JSON **string** and declares itself one in JSON Schema, so every round-trip through the contracts is already safe. The exposure is a caller constructing a model in Python, or a hand-written client posting a JSON number. | **DECIDED 2026-08-19: yes — refuse a `float` at validation, and put the rule on the type rather than on the field.** Specified as **FR-OVR-18** and delivered the same day in `model_schema.money`: `DecimalStr` and `Relativity` both carry a `BeforeValidator` that rejects a `float`, `12.0` as firmly as `0.1 + 0.2`. `LevelCount.exposure_years`'s field-scoped validator is **deleted rather than duplicated** — the inconsistency this question recorded is resolved by generalising the strict field, not by leaving 25 lax ones beside it. **The audit was the real work and it came back clean**: every existing caller passes a `str`, an `int` or a `Decimal`, and the one path that computes in float — the profile's exposure sums — already quantised at the boundary via `_stored_exposure`, which is why that helper is now named in FR-OVR-18 as the pattern to copy. No caller needed rerouting; the full suite passed unchanged. `Relativity` was included though the question named only `DecimalStr`: no production field uses it yet, so it was free, and leaving the rating path's factor type lax would have rebuilt the exact inconsistency being removed. *Not adopted:* nothing. The one thing deliberately **not** done is a float-accepting escape hatch — a caller who wants one writes the quantisation, where a reviewer can see the decimal places it chose. | maintainer | **decided** (in spec) |
| ~~**OQ-OVR-9**~~ ✔ | ~~Nothing in this repository cross-checks a spec's declared error codes (each module's §5.1 "Error codes" table) against `backend/src/app/errors.py`, the registry that makes a code raisable. Should one exist, and if so where?~~ **DECIDED 2026-08-21: yes — folded into `audit-docs.py`'s existing error-code pass.** Specified as FR-OVR-19. | Verified twice on the FR-MODEL-96 branch: `02` §5.1 declares `MODEL_APPROXIMATION_INVALID`, `errors.py` registers it in `MODELLING_ERROR_CODES`, and no test, script or audit compares the two lists. `test_errors.py::test_spec_error_codes_are_all_constructible` iterates only `PLATFORM_ERROR_CODES`; `audit-docs.py`'s error-code check (`scripts/audit-docs.py`, ownership check) tests exclusivity **between spec documents** and never opens `errors.py`; `scope-audit.py` has no error-code check at all. The one related test, `packages/pricing-core/tests/test_spec_hash.py::test_every_code_the_fit_path_can_raise_is_registered`, walks one `pricing-core` file's AST for raise sites — code-vs-registry, not spec-vs-registry. **The gap is structural and applies to every module**, not only `02`; `07` gets incidental coverage only because a test happens to name it. **(a)** a script (`scripts/`, following `scope-audit.py`'s shape) that parses each spec's §5.1 error-code table and diffs it against `errors.py`'s `*_ERROR_CODES` frozensets, run in the gate — cheap, mechanical, and it is the same shape as the import-linter contract that was dead for a day and reported success throughout (`CLAUDE.md` §13.4), so it must itself be proven to fail on a deliberately introduced divergence. **(b)** fold the check into `audit-docs.py`'s existing error-code pass, since that already parses every spec's error-code table for the ownership check and only needs a second comparison added. **(c)** a test in `backend/tests/test_errors.py`, parametrised over every module's error-code frozenset the way `test_spec_error_codes_are_all_constructible` already is for `PLATFORM_ERROR_CODES`, extended to read each spec file rather than trusting the frozenset alone. | **Maintainer decided 2026-08-21, at the recommendation — (b).** `FR-OVR-19`: check 10 gains a second comparison — each module's §5.1 owned codes against `errors.py`'s module frozenset, in both directions. Declared-but-unregistered fails (a promise no caller can branch on); registered-but-undeclared fails (a code with no owner). Re-raised codes stay excluded as today; `03`, `04` and `05` declare owned codes while no frozenset exists and are reported with their count rather than failed. The check lands with the six discrepancies that exist today resolved, each with a verdict: `OBJECTIVE_GRAMMAR_VIOLATION`, `OBJECTIVE_NONFINITE_DERIVATIVE` and `OBJECTIVE_NOT_CERTIFIED` are declared-and-unbuilt for Phase 2's `expression` objectives (OQ-MODEL-1) and marked as such in place; `PICKLE_PERSISTENCE_REFUSED` and `TRANSPARENCY_ARTIFACT_REQUIRED` are declared and never raised — the type refuses `pickle` by having no spelling for it, and FR-MODEL-89 checks R3 by query — and are struck with dated notes; `MODEL_SPLIT_REQUIRED` is registered and raised and gains its `02` §5.1 declaration. Proven to fail on a deliberately introduced divergence before it is trusted (`CLAUDE.md` §13.4). **Owner: the maintainer; before Phase 1a's exit demo.** | maintainer | **decided** (in spec) |
| **OQ-OVR-10** | ~~What is a Contents cell in each module spec's §5.3 Frontend views table — an enumeration of what the view must show, or a prose summary of its character — and how does a cell declare which kind it is? Raised 2026-08-24 (W6b) by `02` §5.3's Diagnostics cell, which a filed slice map read as an enumeration and turned into a chart count that then bound a slice.~~ **DECIDED 2026-08-24: a §5.3 Contents cell is prose and binds nothing, with a declared-exhaustive and a declared-prose affordance — FR-OVR-21.** In force now, with a **per-cell** carve-out rather than a suspensive one: it binds every cell except the seven carrying an obligation held in no requirement and no contract, each of which stays binding until discharged, because ruling them non-binding retroactively would repeal all seven with no record. *(Clarified 2026-08-24: the original wording read as suspensive — that the whole rule was inert until all seven discharged — which is not what was decided and would have made both slices below unplannable.)* *Consequences:* `W6b-1b` becomes plannable on the rule; `W6b-9` additionally waits on `02` §5.3 Diagnostics being declared prose, which is discharged in this change; `W6b-1a` was never affected. | **The table is not consistent with itself, and nothing marks which kind a cell is.** In `02` §5.3 alone: Model detail and Diagnostics are category-noun summaries; the Objective certificate row is an exhaustive enumeration of all four `CheckStatus` values with a presentation rule attached; the Peril structure library row enumerates its *negative* space, recording that there is **no usage count** and that its absence is by specification rather than pending. Three kinds of cell, one table, no marker. **The Diagnostics cell was wrong in both directions at once**: it still named double-lift a week after FR-MODEL-50 struck it on 2026-08-17, and it named none of `partial_dependence`, `permutation_importances`, `monotonicity`, `quantile_crossing`, `type_iii_tests`, `vif`, `aliasing` or `leverage_blob`, all of which the generated contract carries. So a reader who trusts the cell can both under-scope a view and re-litigate a decision already written down, and the two failures are opposite. **The problem is cross-cutting rather than local**: `00` §5.6, the canonical view inventory, registers 7 of `02` §5.3's 12 views, omits 5 and carries 1 name with no §5.3 row — and its model row names three views while giving two routes, Diagnostics having none, where every other slash-separated row in that table gives one route per view: `Dataset list / detail / version` gives three routes for three views and `Peril structure library / detail` gives two for two. The model row is not the only one short of route-per-view, which cuts toward this question rather than against it: `Quote sandbox & trace viewer` names two views on one route, so the table has no route-per-view rule for the model row to be anomalous against. **Options.** *(a)* every Contents cell is prose and the generated contract is the only enumeration. *(b)* every cell is exhaustive and a view may show nothing not written in it, so the cell is amended first. *(c)* a cell declares its own kind in the cell, which the Peril structure row already does in prose and could be made the rule. | **Recommended by `w6b-slice-map`, narrowed by OQ-MODEL-15 — (a) plus (c)'s affordance.** The generated contract is the **floor** and the enumeration of what exists; a Contents cell is prose about a view's character and binds no chart list. It is **not a ceiling**: anything a view needs beyond the contract is a new requirement raised at build time, which is exactly OQ-MODEL-15's decided rule of 2026-08-21 — *if the diagnostic view needs those fields, that is a new requirement raised at that point, when the answer stops being a guess about what a reader wants.* A cell that means to be exhaustive says so inside the cell, the way the Peril structure library row already does. **Not acted on — this is a recommendation and the decision is the maintainer's.** ~~*Consequences while it is open:* `W6b-9` is not plannable at all, its scope being the disputed cell, and `W6b-1b` is not plannable for the same reason; `W6b-1a` is unaffected and proceeds.~~ **Two counts in the frozen plan `docs/plans/2026-08-24-w6b-slice-map-revised.md` are retired here**, since a filed plan is frozen at its date and cannot retract its own: its "eight charts, enumerated" section states that *the eight is the number that binds `W6b-9`* — the eight was transcribed from the disputed cell and binds nothing — and its "what can start today" section names eight startable slices including `W6b-9`, whose dependency columns are empty, when by OQ-MODEL-15's rule the chart set is not knowable until `W6b-1b` is built and `W6b-9` is therefore downstream of it.  **Decided 2026-08-24 as recommended, with one addition the recommendation did not carry: the rule is prospective.** Seven §5.3 obligations are held in no requirement and no contract, so applying (a) retroactively would repeal all seven silently; each is enumerated in FR-OVR-21 and must be discharged before the rule takes effect. The citation of OQ-MODEL-15 above is half load-bearing — it supports *the contract is the floor*, and says nothing about a cell being prose; that half rests on the Peril structure library precedent alone. | maintainer | **decided** (in spec) |
| **OQ-OVR-11** | ~~**What governs a monetary value carried inside a `dict[str, Any]` that the generated contract publishes as an unconstrained object, and how far does FR-OVR-7's guarantee reach?** `RuleResult.measured` and `RuleResult.threshold` (`packages/model-schema/src/model_schema/validation.py:455`) carry money-bearing entries into every published validation report, and `docs/contracts/schemas/generated/validation-report.schema.json` declares both as an object with `additionalProperties` true — the contract asserts nothing about what is inside them.~~ **DECIDED 2026-08-24 — option (c), write the boundary down: FR-OVR-7 now states it.** The carve-out reaches statistics, not amounts, and the test is **what a quantity denominates, not how it was computed**; money stays integer minor units or `Decimal` *wherever it appears*, including inside an untyped `dict[str, Any]`, which is what closes this row's actual gap — a boundary scoped to *declared* fields would have left the untyped payload exactly where it was. *Consequences:* option (a), typing the payload, becomes the mechanism that lets the existing checks see the value rather than the thing that creates the obligation, and is now specified rather than guessed at; it is **not** wholly mechanical, because `threshold_minor`'s quantile branch needs a rounding direction that moves the check's own verdict. ~~`audit-docs.py` check 12's appeal to FR-OVR-7 for a `_minor` naming rule FR-OVR-7 does not state is **not** fixed here and must not be read as fixed~~ — FR-OVR-20 records it. *(Second half struck 2026-08-24.)* *Not fixed here* was true of this change; **must not be read as fixed** was already false when written. `d2a4773` (#181) had repaired the citation and is an ancestor of `e527984`, the commit that wrote the warning. Standing rather than scoped, it invites exactly the edit FR-OVR-20 exists to prevent — re-opening a closed repair, or correcting the citation a second time in the wrong direction. It is correct at `scripts/audit-docs.py:547-550` and the failure message emits `(FR-OVR-20)` at `:556`. Found by `w6b-auditor` while verifying an exoneration it was itself the beneficiary of. | Raised 2026-08-24 (W6b-13b) while checking whether that slice's catalogue defaults were safe to publish. **The purpose-built check already exists, already runs on this exact document, and still cannot see the value.** `test_generated_and_authored_agree_on_scalar_types` (`backend/tests/test_contracts.py:1013`, marked for FR-DATA-46) was added in response to `01` FR-DATA-46's second propagation failure — its docstring names the 45812.42 case verbatim — and `validation-report` is in `COMPARED_SLUGS`. Its stated scope is the gap: *“Only paths present on both sides are compared.”* A `dict[str, Any]` published with `additionalProperties` true has no declared path on either side, so the remedy the repository adopted after that failure is **field-shaped**, and this value is not a field. Two further instruments are blind on the same path: `test_no_generated_money_field_admits_a_json_number` (`backend/tests/test_contracts.py:136`) walks generated JSON Schema *property names*, so runtime dict keys are unreachable by construction; and `audit-docs.py`'s check 12 reads markdown and schema files only, matching a `_minor` key against a fractional literal, which no Python source and no runtime payload can present to it. The carrier itself is untyped: `CheckOutcome` (`packages/pricing-core/src/pricing_core/data/validate.py:55`) is a plain frozen dataclass, not Pydantic and not from `model-schema`, so `MoneyMinor` never reaches it. The control sits on the same model — `affected_exposure_fraction` *is* a named, typed property and *is* within the money scan's reach. **The name-shape guard is not the problem:** its own comment says it recognises a *shape* of name “so the next ratio needs no entry and no decision”, deliberately a rule rather than the hand-maintained list OQ-OVR-7 objected to; give the payload a declared shape and that guard arms itself unchanged. **The second half is where FR-OVR-7 stops.** Its positive rule is scoped to “the rating path and all persisted rate tables”, and a validation report is neither, while its second sentence *permits* floats “inside model fitting and diagnostics” — so `threshold_minor` and `largest_minor` published as floats (`validate.py:957`, `:971`, `:978`, `:982`) breach no requirement textually. Yet the platform does not behave as though the carve-out reaches amounts: `OneWayRow` (`packages/model-schema/src/model_schema/profiles.py:141`) is itself a diagnostic artifact and still types `claim_amount_minor` as strict `MoneyMinor`, and `validate.py:918` casts with `int(...)`. That reading — the carve-out covers *statistics*, not *amounts* — is written nowhere. Options: **(a) type the payload**, a declared shape per check family or `MoneyMinor` on the entries that really are amounts, after which both existing checks reach it and no new rule is written; **(b) extend the money scan to dict keys**, cheap, but it must infer which runtime keys are money from names alone, which is the inference that fails below; **(c) write the boundary down only**, amending FR-OVR-7 to say whether a diagnostic payload is in scope and accepting that nothing enforces it. | **(a), with (c)'s amendment alongside it.** A boundary nobody wrote down is what let three separate instruments each behave as though another was covering this path, so typing the payload without also stating where the guarantee stops would leave the next reader making the same assumption. **Why not (b), despite being cheapest:** FR-DATA-46's rename has now failed to propagate three times — twice on 2026-08-19 (`01` FR-DATA-46's amendments) and once in `validate.py` — and not one was caught by an automated check; the two that were caught were read by hand, on the same day. The second instance is the exact inverse of the third: there the *name* was corrected and the *type* left integer, here the type is right and the *name* left `_minor`. One requirement failing in both directions on a single quantity is the argument against any remedy that reasons from names, which is what (b) is and what renaming again would be. **One correction to the recommendation as filed, recorded rather than silently applied:** it resolved the boundary on **division** — a quantity produced by division is a statistic. That is refuted by the requirement it amends. Money divided by a count is still money, so a pro-rata or per-instalment amount would classify as a statistic and be permitted as a float, while FR-OVR-7's own positive rule says floats are *never in a quoted premium*. Division survives as **evidence** of a statistic, not as the test. **This decision releases work explicitly deferred onto it.** `d2a4773` recorded three `..._minor` names published on floats by `_large_loss` and `_severity_range` (`packages/pricing-core/src/pricing_core/data/validate.py`) and scheduled their correction *with this row* rather than inline, because they are rendered verbatim as user-visible labels by `RuleResultRow.vue`. The boundary above settles all three, and **it settles them in two opposite directions** — so a sweep treating them as one population corrects two of them wrongly. `mean_severity_minor` (`:1077`) denominates a statistic, already settled as one by OQ-OVR-7 on 2026-08-17: the float is correct and **the name is the defect**, so it is renamed and its type left alone. `threshold_minor` and `largest_minor` (`:971,978,982`) denominate money — a claim-amount cut and a largest claim amount — so **the name is correct and the type is the defect**, and renaming them would destroy the marker while leaving the float. The fix inverts between the two groups, and the `..._minor` suffix cannot tell them apart because it is the thing under test in one group and the evidence in the other. *(Population corrected 2026-08-24 — it was under-wide by two.)* **The enumeration above is three names; the population is five.** `min_severity_minor` and `max_severity_minor` (`:1072-1073`, echoed at `:1078`) belong in the rename group, and **FR-OVR-20 says so twelve rows above this one** — it cites `:1072`–`:1073` alongside `:1077` and `:1078` as `validate.py` publishing the suffix *"on a mean severity, a ratio"*. This row and its own requirement therefore disagreed while sitting in the same document. The corollary landed in `da7ba2c` settles it independently: both **bound `severity`**, the mean severity at `:1073`, and a bound takes its kind from the quantity it bounds — so they are a statistic's bounds, their float is **permitted rather than a defect**, and the suffix is the defect. Sorted by dimension rather than by whether name and type happen to agree — **rename and keep the float** (bounds on, or measures of, a statistic): `min_severity_minor`, `max_severity_minor`, `mean_severity_minor`; **retype to integer and keep the name** (amounts): `threshold_minor`, `largest_minor`. Every field bounding or measuring mean severity now moves together, where the earlier split separated a measure from the two bounds that constrain it. **Why it was missed is the same trap from the opposite side**: at `:1072-1073` the name and the type agreed with *each other* — a `_minor` suffix on a value the rule catalogue writes as an integer — and agreement between name and type reads as conformance, when the question is whether either matches the **quantity**. Retracted by `w6b-decision-maker`, which had filed the original disposition. **One further surface is a name to neither rule and so is governed by neither**: `detail=f"mean severity is {severity:,.0f} minor units"` (`:1079`) asserts minor units on the same statistic in a user-visible string. FR-OVR-20 reserves *names* and check 12 matches names in markdown and schema files, so nothing reaches it — renaming the three fields and leaving this string still ships a payload telling a reader that mean severity is in minor units. | maintainer | **decided** (in spec) |
| **OQ-OVR-12** | Burning cost — claim amount divided by exposure — is classified **two opposite ways in the same repository**, and FR-OVR-7's boundary (OQ-OVR-11, decided above) forbids both standings at once. `Reconciliation.observed_burning_cost_minor` and `ReconciledPeril.modelled_burning_cost_minor` (`packages/model-schema/src/model_schema/perils.py:290,306-307`) are strict `MoneyMinor` integers carrying the `..._minor` suffix; `OneWayRow.mean_burning_cost` (`packages/model-schema/src/model_schema/profiles.py:162`) is `float \| None` with no suffix, and that class's own docstring (`:145-148`) states the rule as *"they are statistics, not amounts"*. Same quantity, same arithmetic shape, opposite verdicts. FR-OVR-7 now tests **what a quantity denominates**, and what these denominate is identical, so the divergence is no longer expressible. | **(a) Burning cost is an amount.** `mean_burning_cost` becomes `MoneyMinor` and gains the suffix. Costs the confidence-interval precision `OneWayRow`'s docstring says the float exists to preserve, and FR-DATA-46 is cited there for it. **(b) Burning cost is a statistic** — a rate of money per exposure-year, money only once multiplied back by exposure. `observed_/modelled_burning_cost_minor` lose the suffix under FR-OVR-20; whether they also lose the integer type is separable, since FR-OVR-7 *permits* a float for a statistic rather than requiring one, and `perils.py:221-224`'s sum-of-the-rounded-parts invariant is a real reason to keep the integer. **(c) They are different quantities** and the divergence is correct — requires stating the distinguishing predicate, which no artifact currently carries. | **(b), with the integer kept.** Burning cost is a rate by dimension: it is money only after multiplication by exposure, and `Reconciliation` itself uses it as one — its `ratio` divides modelled by observed so the units cancel, which is a thing you do to rates. (a) is refuted by the artifact that would have to change: FR-DATA-46 is cited at `profiles.py:145-148` for the float, so (a) trades a named requirement's precision for a naming convention. **The suffix is the defect, not the type** — FR-OVR-20 governs the name and FR-OVR-7 only *permits* the float, so dropping `_minor` while keeping `int` satisfies both and preserves the rounded-parts invariant. Not decided here: this row is filed by the change that made it visible, and deciding it inside that change would be the silent pick `CLAUDE.md` §10 forbids. **Two objections this row has already drawn, answered here so they are not re-run.** *That they are different quantities — a per-level mean against a part-level total.* The perils figure is not a total: `perils.py:192` computes `_to_minor(float(np.sum(observed)) / exposure_total)`, a weighted mean per exposure-year across the part, and `Reconciliation.ratio` (`:337-338`) divides modelled by observed so the exposure denominators cancel — which is a thing that works because both sides are rates. The two therefore differ in **grouping**, part against level, not in kind. *That `OneWayRow`'s float is deliberate rather than an oversight.* It is, and the row does not claim otherwise: the docstring states the reason and cites FR-DATA-46 for it, which is why option (a) is the expensive one. A stated reason on one side of a divergence settles that side, not the divergence. **The naming half is FR-OVR-20's territory and the typing half FR-OVR-7's**; this row is filed against the pair because the recommendation splits between them. **One side of this has a decision of record, found after filing.** OQ-OVR-7 — maintainer-accepted 2026-08-17 and specified as `01` FR-DATA-46 — decided that `01` FR-DATA-26's one-way row renames `severity_minor` and `burning_cost_minor`, *"which are **means** and therefore floats"*, to `mean_severity` and `mean_burning_cost`. So the `profiles.py` side is not an unresolved standing but a decided one, and its basis — a mean per unit is a statistic — reaches `perils.py`'s figures too, since `:192` and `:217` both divide by `exposure_total`. That makes **`perils.py` the divergent side**, and narrows this row from *which of the two changes* to *whether an aggregate reconciliation figure is exempt from the basis OQ-OVR-7 already decided on*. Recommendation (b) is unchanged and now rests on a precedent rather than only an argument. Raised by `w6b-planner` as evidence rather than as a verdict, after it had been wrong twice on this row. | maintainer | open |
| **OQ-OVR-13** | Should `ChartFigure` relate a row's values to its column headers by **name** rather than by position? The component takes `columns: readonly string[]` and `rows: readonly (readonly (string \| number \| null)[])[]` as two independent props, and "in the same order as `columns`" is a docstring. Every caller reaches it by hand-transcribing an already-correct ECharts option into that pair, which is the one activity that produces a value published under the wrong heading — a table that says something different from the chart beside it, which is the specific failure NFR-OVR-10 exists to prevent. | **(a) Positional rows, with the two checks W6b-9 built** — a dev-only arity guard in `ChartFigure` and `cellUnder(table, row, column)` in `frontend/src/test-tables.ts`, which reads a cell by the header above it. The two catch disjoint classes (a wrong *number* of cells at runtime; a wrong *alignment* under test) and both are proven on deliberately broken input. Neither makes a misaligned table impossible: the correspondence remains a convention each call site re-honours, and the helper only fires where a test was written to use it. **(b) Rows keyed by column name** — `Record<string, string \| number \| null>` per row, keys constrained to `columns` by the type system. Misalignment becomes a compile error and the ordering question stops existing; costs a rewrite of every call site and fixture, and turns display strings into identifiers, so renaming a heading becomes a code change. **(c) A column descriptor** — `{ name, value: (row: T) => Cell }` with rows staying the caller's own domain objects, which is where most table libraries land. Strongest and the only option that removes the transcription step itself, but it makes `ChartFigure` generic and is the largest change of the three. | **(b) or (c), at the next slice that adds a chart — not now, and not "someday".** (a) was chosen for W6b-9 because it lands *with* the retrofit rather than instead of it: the slice's job was to give three existing charts an accessible table, and a prop-shape rewrite of all ten call sites would have displaced that. The reason to revisit rather than close: **the cost of (a) is per-call-site and grows with every new `ChartFigure` caller**, because each one re-performs the transcription by hand, while the cost of (b)/(c) is paid once and falls with each call site that has *not* yet been written. The next chart is therefore the last cheap moment to switch, and each one after it is dearer. | **The next slice that adds a chart**, which revisits this before adding them. | **Deferred** with an owner (CLAUDE.md §13), 2026-08-25, by `w6b-lead` arbitrating Decision 1 of [`2026-08-25-w6b-9-chart-table-retrofit.md`](plans/2026-08-25-w6b-9-chart-table-retrofit.md), which recorded the condition that the rejected option be filed rather than dropped. |
| **OQ-OVR-14** | Where does a view get the **workspace currency** it formats minor-unit amounts with? `ProfileView.vue:22` declares `currency?: string` and `:40` resolves `props.currency ?? "GBP"`; `VersionDetailView.vue:15,25` does the same. Both are mounted only by `router/index.ts:37,43` with `props: true`, which supplies route *params* — `slug` and `version` — so `currency` is never passed by anything and the `"GBP"` fallback is not a fallback but the only path. `formatMinor(row.claim_amount_minor, currency)` therefore prints `£` over a euro-denominated dataset. The defect is invisible because the frontend's hardcoded `"GBP"` matches `Dataset.currency`'s own `@default GBP` in the generated contract, so every dataset that has not changed it renders correctly and the one that has renders wrongly with no warning. The right value exists and is simply not threaded: `Dataset.currency` is a required `string` on the `Dataset` schema. It is **not** on `DatasetVersion`, which is what these views already fetch, so no option here is free. Raised 2026-08-25 (W6b-9), which made `OneWayChart`'s new `currency` prop **required** so the retrofit could not propagate the default further, but did not fix the views — a money defect landing inside an accessibility commit is reviewed by someone looking at table markup. Mirrored in `docs/specs/00-overview.md` §10. | **(a) Thread it through the route** — resolve the dataset before navigation and pass `currency` as a prop. Keeps the views pure functions of their props, which is why `props: true` was chosen, and keeps the money value on the same path as `slug`. Costs a router-level fetch or a navigation guard, and every future view showing an amount must be added to it. **(b) Read it in the view** — call `getDataset(slug)`, which already exists and returns `Dataset`, alongside the existing `getVersion`. Local, needs no router change, and each view that needs currency asks for it. Costs one extra request per page and repeats the fetch in every such view. **(c) Hold it in a store** — a Pinia store keyed by slug, fetched once. Cheapest at the third view and the natural home for a value that is per-dataset and read everywhere; the most machinery to add for the two views that need it today. | **(b) now, (c) when a third view needs it.** (b) is the smallest change that makes the number correct, and correctness here is not a refinement — a euro amount under a pound sign is a wrong number that looks like a right one. (a) is rejected: it puts a network fetch in the router to preserve a purity the views do not otherwise have, and it scales by making every new view a router change. (c) is right eventually and premature at two call sites. Whichever is chosen, **delete the `?? "GBP"` defaults rather than leave them behind the new source** — a fallback that duplicates the contract's own default is what hid this. | **The maintainer**, as a money-correctness decision (FR-OVR-7) rather than a frontend-plumbing one; the slice that fixes it inherits the choice. | **Open** — raised 2026-08-25 by `w6b-executor` during W6b-9 Task 5, which was scoped to file this and not to fix it. |

## DATA — data management ([`01-data-management.md`](specs/01-data-management.md))

| ID | Question | Options / trade-offs | Recommendation | Owner | Status |
|---|---|---|---|---|---|
| ~~**OQ-DATA-7**~~ ✔ | ~~What does a **first** failing validation leave a Dataset Version as?~~ **DECIDED 2026-08-15: `failed`.** | Nothing sets `failed` today, so the version rests in `validating` — a transient state that every screen showing a status reads as "still running". Found by driving the exit demo, not by a test: no requirement named the state, so no marker could be missing. **`failed`**: the enum and `FAILED → VALIDATING` exist for exactly this, and the word tells a reader the run finished and the answer was no. **`draft`**: what FR-DATA-23 does when a *validated* version is re-validated and fails — consistent, but it loses the distinction between "not yet validated" and "validated and refused". | **`failed`.** FR-DATA-23's `draft` is right for its own case: that version *was* good, and returning it to `draft` says so. A first attempt that failed never had that standing, and the state machine already carries `FAILED → VALIDATING` so the version can be re-validated once the data or the rule set is corrected — which is the whole loop `01` §1.3 describes. **Maintainer accepted the recommendation.** Specified as **FR-DATA-43** and delivered the same day: `dataset.validate` now opens `validating` and closes it — a failing first report leaves the version `failed`, a failing *re*-validation returns it to `draft` per FR-DATA-23 and clears the report reference with it. That last part also repairs something else the audit found: a stale `validation_report_id` left a database check constraint unable to bite. A passing report leaves the version `validating`, because promotion is the actuary's act (`01` §1.3) and a job that promoted on a pass would make the gate automatic. | maintainer | **decided** |
| ~~**OQ-DATA-8**~~ ✔ | ~~`sample`, `filter`, `join` and `aggregate` derived versions still inherit their parent's data rather than being produced from it — should they be materialised, and when?~~ **DECIDED 2026-08-17: materialise all four, each in the slice that first needs it, and refuse them until then.** | Raised 2026-08-16 (W5, diagnostics) by needing an honest holdout. `dataset.derive` recorded the operation and set `child.tables = parent.tables`, conflating FR-DATA-34's "inherits schema, Data Dictionary and Rule Set" with inheriting the *rows*. **`split` is materialised now** (FR-DATA-44) because a holdout containing every training row produces diagnostics that look excellent and mean nothing. The other four were left alone: each has its own correctness surface — stratified and exposure-weighted sampling, the restricted filter grammar, join keys and grain — and doing them under a modelling slice would be a large DATA change with no caller yet. The cost of leaving them: a 1 % sample presently holds 100 % of the rows, silently. | **Maintainer accepted the recommendation, 2026-08-17.** Materialise all four, in the slice that first needs one, and refuse them until then rather than leaving the silent version: a derived version that claims an operation it did not perform is worse than one that refuses, because FR-DATA-33's whole purpose is that a derivation is reproducible, and a sample nobody sampled cannot be reproduced or defended. Specified as `01` **FR-DATA-45**, and **the refusal was delivered the same day** — `derive_version` raises `DERIVATION_NOT_MATERIALISED` (501) for every declared operation except `split`, computed as `DERIVED_OPERATIONS - {"split"}` so that materialising one is a single edit and cannot leave a refusal behind. The refused set is covered by a parametrised negative test. Owner: W7 for `sample` (the demo seed's `--rows` path is the first real caller); unowned for `filter`, `join` and `aggregate`, which stay refused until a caller exists. *Found while writing it up:* FR-DATA-33 declares `union`, which the implementation does not accept, and accepts `join` and `aggregate`, which it does not declare — recorded in `01` §3.6 and settled by the slice that materialises the first of them, since the refusal makes the difference unreachable today. | maintainer | **decided** (in spec) |
| **OQ-DATA-1** | ~~Large-loss handling: preparation step or modelling decision?~~ **DECIDED 2026-08-14: modelling decision, applied at fit time.** | Delegated to Claude. Datasets are immutable versioned artifacts, so baking a cap in means a new Dataset Version — plus re-validation and re-profiling — for every capping assumption. Actuaries routinely try several caps when fitting severity, so that cost lands on the most common inner loop in the project. | **Fit time.** A Model Spec carries `loss_treatment`, which forms part of `spec_hash` so two models differing only in their cap cannot collide (`FR-MODEL-73`). Datasets stay assumption-free and one validated dataset serves many capping assumptions. The reconciliation objection is answered rather than dismissed: `FR-MODEL-74` requires the Peril Structure to reconcile *after* restoration against uncapped observed data, and the dossier to state the treatment — otherwise a capped model looks like a modelling error. | Claude | **decided** |
| **OQ-DATA-2** | ~~Incremental/append ingestion, or full snapshot per version?~~ **DECIDED 2026-08-14: full snapshots for Phase 1; append deferred to Phase 2 under a stated constraint.** | Delegated to Claude. Phase 1a's demo dataset is freMTPL2 — small enough that snapshots are free. Append would add real complexity now (validation must reason about which parts are new; profiling and PSI would need incremental variants) to solve a problem the project does not yet have. | **Full snapshots**, per `FR-DATA-2` as already written — no spec change needed, which is itself a point in favour. Append is **not** ruled out: `FR-DATA-40` records the constraint any future append mode must meet — it must still materialise a complete, content-addressed, independently validatable version, so FR-OVR-1 immutability holds and only the *cost* changes. Content-addressing (ID-4) already deduplicates unchanged parquet parts, so most of the saving is available without an append mode at all. | Claude | **decided** |
| **OQ-DATA-3** | ~~Keep the `sql` custom validation check? If so, what control?~~ **DECIDED 2026-08-14: keep it, Admin-authored, single Approver, behind a workspace feature flag defaulting to off. Revisit after Phase 1.** | Keep + dual approval: maximum expressiveness, real sandboxing burden (NFR-DATA-9). Keep + admin-only: less flexible but far fewer reviewers to train. Drop: the declarative checks cover ~95 % of real rules; the last 5 % becomes a feature request. | **Maintainer accepted the recommendation.** The control that matters is how few people can *author* one, not how many must approve it — dual approval on a check nobody may write anyway adds ceremony without adding a decision, and ceremony is what turns review into signature. The flag (`features.sql_validation_check_enabled`, `07` FR-PLAT-46, already shipped in W2 defaulting to off) means a workspace that never needs the escape hatch never carries its risk. Sandboxing is unchanged: a single `SELECT`, a read-only DuckDB connection with no filesystem or extension access, and a hard time budget. Applied to `01` §4.5, which previously required two approvers. **Revisit after Phase 1** with evidence of what it was used for; if the declarative checks could have expressed all of it, remove it. | maintainer | **decided** |
| **OQ-DATA-4** | ~~Where do IBNR / claims-development adjustments live?~~ **DECIDED 2026-08-14: out of scope — the platform consumes developed data supplied by the user, and warns about immature periods.** | Preparation step (developed amounts in the dataset), modelling offset, or out of scope (user supplies developed data). Currently only `VR-ACT-14` warns about immature periods. | **Maintainer decided: out of scope.** Claims development is an actuarial exercise with its own methods, judgement and review, and a pricing platform that half-performed it would produce developed amounts nobody had signed off. The user supplies developed data; `VR-ACT-14` remains the platform's only treatment, warning that the most recent months are immature so that fitting on them is a visible choice rather than an accident. *Not adopted from the recommendation:* a first-class `development` preparation step in Phase 4. If the monitoring work later needs triangles, that is a new question rather than a deferred half of this one. | maintainer | **decided** |
| **OQ-DATA-5** | ~~Ship ONS/ABI reference data, or only loaders?~~ **DECIDED 2026-08-14: loaders for every source; ship data only where the licence is unambiguously permissive. Never ship ABI group tables.** | Shipping is far better UX but the licences (ONS OGL is permissive; ABI vehicle groups are **not** freely redistributable) differ per source and get us wrong quickly. | **Maintainer accepted the recommendation.** Every source gets a loader and a documented fetch step, so a workspace can always obtain the data itself. Actual rows ship only where the licence is unambiguous — ONS NSPL and the bank-holiday calendar under OGL. **ABI vehicle group tables are never shipped**: they are not freely redistributable, and shipping them would put a licence breach in every clone of the repository. Applied to FR-DATA-32. | maintainer | **decided** |
| **OQ-DATA-6** | ~~Acknowledgement granularity — per rule per report, or a time-boxed standing acknowledgement?~~ **DECIDED 2026-08-14: per report, as FR-DATA-18 already specifies, plus a carry-forward affordance that still requires an explicit act.** | Per report (FR-DATA-18): strongest evidence trail, but a known seasonal warning is re-justified every month, breeding rubber-stamping. Standing: less fatigue, weaker evidence, risk of a stale acknowledgement hiding a real change. | **Maintainer accepted the recommendation.** FR-DATA-18 stands unchanged: an acknowledgement is scoped to `(dataset_version_id, rule_id, report_id)` and does not carry forward. What is added is an affordance — the UI may pre-fill the justification from the last acknowledgement of the same rule, while still requiring an explicit, separately audited act. Fatigue is a UI problem and a standing acknowledgement is a governance one; a stale standing acknowledgement hiding a real change is the failure this avoids. | maintainer | **decided** |
| ~~**OQ-DATA-9**~~ ✔ | ~~`01` §5.3 asks the dataset list to display a status badge, a last-validated date and an owner. `Dataset` carries none of the three: status and `validation_report_id` live on `DatasetVersion`, and ownership is only implied by `workspace_id`. Does `Dataset` gain the three fields, or does §5.3 mean the latest version's status and validated-at, plus a workspace-level owner?~~ | Raised 2026-08-18 (W5). Recorded as an unowned divergence in `docs/roadmap.md` since 2026-08-15 and built around in silence, which is the `CLAUDE.md` §0 failure dated amendment notes exist to prevent. `Dataset` is a *container*: `01` §4.1's contract shows no status either, so the divergence is between §5.3 and §4.1 as much as between spec and code. | **DECIDED 2026-08-19: two of the three are projections, one is a new field.** `latest_version_status` and `last_validated_at` are derived by `GET /api/v1/datasets` from the Dataset's versions and never stored — `DatasetVersion.status` with `is_fittable` stays the single answer to "can I fit on this?", and the date is scoped to the most recently *validated* version rather than the latest one, so a fresh `draft` above a `validated` v11 does not render as never validated. `Dataset` gains an explicit **`owner_id`**, not derived from `workspace_id`, because ownership is a fact about the container that no version carries and `06`'s RBAC needs a named subject. Specified as `FR-DATA-50` and `FR-DATA-51`; neither delivered, both W6b's, each carrying its own trigger. | maintainer | **decided** (in spec) |
| ~~**OQ-DATA-10**~~ ✔ | ~~FR-DATA-25 asks for "top-20 levels **by exposure and by count**", and the platform now produces one list — selected by count, each level carrying its `exposure_years` (FR-DATA-49). Should there also be a second, exposure-ordered selection; and should `VR-DST-1`'s PSI weight by exposure rather than by count?~~ **DECIDED 2026-08-19: defer both, together, until a consumer needs an exposure-ordered view.** FR-DATA-25 amended; the deferral, its trigger and its unowned status are FR-DATA-52. | Raised 2026-08-19 (W5, the `top_levels` slice). **The two halves are one question and must be answered together.** A level with few policies but large exposure still never enters the top 20, because selection remains by count — so an exposure-ordered view and an exposure-weighted PSI would need the same underlying selection to be meaningful. Answering only the PSI half would leave the check weighting a set of levels chosen on the other basis, which is worse than either consistent answer. `psi_from_weights` already takes arbitrary weights and needs no signature change; `VR-DST-8` (mix-shift-exposure) already asks for PSI on exposure for a *declared key factor*, so part of the need is met by a different rule. | **Maintainer decided 2026-08-19, as recommended.** `FR-DATA-25` now says one list, selected by count, each level carrying `exposure_years`; `FR-DATA-52` carries the deferral with a named trigger — a reader asking for levels ranked by exposure — and a deliberately **unowned** status, because assigning a workstream would schedule work no consumer has asked for. `exposure_years` carried per level and `psi_from_weights` taking arbitrary weights are what make the deferral cheap to reverse. Re-basing `VR-DST-1` without stating the basis change is what must never happen quietly: a dashboard that moves with no data change and no release note is the failure this row exists to refuse. | maintainer | **decided** (in spec) |
| ~~**OQ-DATA-11**~~ ✔ | ~~`01` §5.3's PSI comparison selector needs somewhere to keep the chosen reference version. §5.3's own note and `docs/roadmap.md`'s Pinia row both predict the frontend's first **Pinia store**, on the premise that this is "the first piece of Profile state that must outlive a route". Is that premise right, or does the reference belong in the **route query**?~~ | Raised 2026-08-19 (W5, the comparison-selector slice), by checking the premise rather than inheriting it. **Nothing requires the reference to survive navigation** — not FR-DATA-28, not §5.3, not any other requirement; the prediction was made before the selector had a design. **Route query `?against=<version>`:** vue-router already owns URL state, so no new state layer; the comparison becomes a link an actuary can send to a colleague, and survives reload. Costs the first `useRoute`/`useRouter` in an app where every view so far takes props, and a `vue-router` mock in one file, `ProfileView.test.ts`, under which its eleven existing tests then run. **Pinia store:** survives navigation between profile routes and matches the prediction, but is global state for a single consumer, is **not shareable**, and is lost on reload. **Local ref:** smallest surface, no persistence at all. | **DECIDED 2026-08-19: the route query.** It delivers everything the store does except cross-route persistence, which nothing asks for, and adds shareability, which is worth more in a platform built around reproducibility than a selection surviving a click into the version detail page. The store's justification — state that must outlive a route — does not survive being checked, and a global store built for one view's `<select>` is a layer nobody can later remove without knowing why it was added. `frontend/src/stores/` stays empty and the first store waits for state that genuinely is global; the workspace selector W6b carries is the likelier candidate. | maintainer | **decided** (in spec) |
| **OQ-DATA-12** | **What is an Offending Sample entry — an opaque string or a keyed object?** FR-DATA-20 and the §2 glossary both say "primary keys of rows" and neither chooses an encoding; §4.6's only example prints `"offending_sample": []`, which settles nothing. The two written sides disagree and have since `validation-report` first gained a generated side (2026-08-24, W32-11): the model says an array of `string`, the authored contract an array of `object`. | **(a) String, as the code emits today.** `_sample` in `pricing_core.data.validate` joins the key values with `\|`. Cheap, already shipped, and readable in a log line — but it is an encoding no specification defines: there is no escaping, so a key containing `\|` is ambiguous; `None` is rendered `""` and cannot be told from an empty string; and the column names are dropped, so a composite key cannot be reassembled without knowing the dataset's key order. A sample exists to be traced back to rows, and this one cannot always be. **(b) Keyed object, as the contract declares.** `{"col": "value"}` is self-describing, unambiguous under composite keys, and distinguishes null from empty. But the contract's declaration is today a bare `{"type": "object"}` with no properties, which constrains nothing a validator could check, so (b) is only real once the item shape is written out. Cost: `pricing-core`'s validation engine, roughly twelve assertions across `test_validate.py` and `test_catalogue.py`, the published contract, the generated frontend type, and §4.6's example. **(c) Keep both — string for display, object for tracing.** Rejected on sight: two encodings of one field is the divergence ADR-0002 exists to remove. | **(b), with the item shape written out.** The deciding argument is not aesthetics but FR-DATA-20's own purpose — the sample is evidence an actuary follows back to rows, and (a) is lossy in three independent ways at exactly that job. It is recommended rather than done because the change spans the engine, its tests, the contract and the frontend, which is a data-model change across the suite and not a contract fix; the slice that found it was scoped to certificate floors and two generated sides. Until it is decided the type comparison is pinned at `results.[].offending_sample.[]` through `UNRESOLVED_TYPE_DISAGREEMENTS`, with a companion test that goes red the day the pin stops earning its place. | maintainer | open |
| **OQ-DATA-13** | **`DatasetVersion` and its published contract disagree on 22 of their 48 paths, and every comparison passes.** Measured 2026-08-24 (W32-11) on the day the slug first gained a generated side. 26 paths are present on both and **none of them disagrees**; the divergence is entirely one-sided and therefore entirely outside what the guard compares (`OQ-PLAT-10`). The contract promises 17 paths the model does not carry — `slug`, `description`, `created_by`, `updated_at`, `archived_at`, `currency`, `labels`, `parent_id`, `schema_version`, and the object forms `derived_from.{operation,params,parent_version_id}`, `period_covered.{from,to}` and `source_fingerprint.{kind,value,extracted_at}`. The model carries 5 the contract does not: `derived_from`, `period_from`, `period_to`, `source_fingerprint` and `tables.[].source_names`. Three of those are the same concept in a different shape — the model holds a scalar where the contract declares an object — so the pair is not merely incomplete on one side, it is structurally different on three fields. A Dataset Version is a Phase-1a artifact that `00` §2 makes the immutable thing modelling references, so this is a live disagreement, not a Phase-2 stub. | **(a) The contract is ahead of the model and the model catches up.** Most likely correct for the flat fields — `slug`, `created_by`, `currency` and the rest read as specified-but-unbuilt rather than as contract error, and `01` describes them. Cost is a data-model change plus a migration. **(b) The contract is wrong and is trimmed to what exists.** Fast and wrong in the governed direction: it deletes the specification to make the tooling agree, which `CLAUDE.md` §0 names explicitly as the thing not to do. **(c) Split the three structural fields from the fourteen flat ones and answer them separately** — the flat ones by (a), the structural ones (`derived_from`, `period_*`, `source_fingerprint`) as a genuine shape question, since a scalar and an object are two designs and not two stages of one. | **(c).** The two halves have different answers and merging them would force one wrong. The flat fields are (a): the contract is the specification and the model is behind it. The three structural fields need a decision — the object forms carry provenance the scalars cannot express (`source_fingerprint.kind` and `.extracted_at`, `derived_from.operation` and `.params`), which argues the contract is right there too, but that is a shape change to a Phase-1a artifact and deserves saying out loud rather than being absorbed. **Not resolved in W32-11**, which was scoped to giving two shapes a generated side; publishing that side is what made this visible, and the slice records it rather than silently widening to fix it. **W32 goal borne on:** FR-PLAT-48 — **dispositioned, not delivered.** | maintainer | open |
| **OQ-DATA-14** | **`pii_class` cannot express "nobody has classified this column", so the platform's two different silences are both stored and displayed as the affirmative statement `none`.** Raised 2026-08-24 (W6b); every measurement taken at `e2ae7c6` and re-checked at `1c80638`, between which no cited file moved. `PiiClass` has five members and `DataDictionaryEntry.pii_class` defaults to `PiiClass.NONE` (`packages/model-schema/src/model_schema/datasets.py:96-115,142-143`), while the field immediately above it, `semantic_type`, is optional. The dictionary is authored, not inferred — its own docstring says so at `:131-135` — so an unauthored field has no observation behind it. **Two distinct silences exist and neither is visible.** *(i) No dictionary entry for the column at all* — 21 of 23 `create_dataset` call sites omit `data_dictionary` entirely, including `backend/tests/test_wf01_journey.py:189`, the dataset carrying the Phase 1b exit criterion. *(ii) An entry exists but was never classified* — `examples/fremtpl2/seed.py:132`'s `DICTIONARY` describes 13 columns and sets `pii_class` on 1; the 12 silent ones include `driv_age`, `area`, `density` and `region`, age plus geography, the textbook quasi-identifier combination, all defaulting to the word `none`. The specification's own canonical example does the same thing (`01-data-management.md:231-234`): four columns, one classified, `driver_age` left silent eleven lines above the sentence that enumerates what `pii_class` may be. **The default is not merely displayed, it is persisted as a positive assertion**: `_dictionary_json` (`backend/src/app/platform/datasets.py:219-220`) is `model_dump(mode="json")` with no `exclude_unset`, so every stored entry carries `"pii_class": "none"` whether or not a human typed it, and the audit chain records that same post-default value in both `before` and `after` (`:294-295`) — the distinction is destroyed at write time, for every row already in every database. Downstream the UI prints it as a considered answer: `frontend/src/views/DatasetDetailView.vue:352` renders `pii_class ?? "none"` beside `:323`'s honest `semantic_type ?? "—"`, same table, same row, opposite conventions. Silence (i) is worse than mislabelled, it is invisible: `:29` derives the displayed column list from `Object.keys(data_dictionary)`, so a column the dictionary never mentions never appears. **What makes this a design question and not a bug is that no requirement is broken today** — FR-DATA-13, FR-DATA-41 and FR-OVR-9 are all phrased on the positive class, "columns *classified*/*tagged* `direct_identifier`", so an unclassified column is outside their scope by their own wording and `_refuse_direct_identifiers` correctly does not fire. This is `CLAUDE.md` §0's "a capability not yet specified", not its "code and spec disagree". **The clearest statement of the gap is a green test the repository already ships**: `backend/tests/test_ingestion.py:468`, marked for FR-DATA-41, uploads a CSV whose `customer_email` column holds an address against a dataset built with an empty dictionary and asserts the rows are written. It is a deliberate negative control and it does prove the check does not refuse everything — but its name is `test_a_column_the_dictionary_does_not_forbid_is_not_refused`, and the fixture supplies a dictionary that forbids nothing **because it says nothing**. The suite's own negative control cannot tell the two silences apart either. *(amended 2026-08-24, the day this row was raised, in two directions — one correcting this row, one recording a change to the artifact it cites.* ***First, this row credited the control with more than it proved.*** *"It does prove the check does not refuse everything" is false, and the control's own docstring made the same claim. Mutating the per-column filter to `present = [c for c in columns]` — refuse every column — left it green, because with no dictionary `forbidden` is empty and the refusal returns before reaching that line. A second mutation, `forbidden = set(dataset.data_dictionary)` — refuse every* ***classified*** *column, which is precisely the confusion between "classified and permitted" and "never classified" that this row is about — left the* ***entire module*** *green at 17 passed.* ***Second, the control has since been strengthened*** *so that its fixture classifies `customer_email` `quasi_identifier`: in the dictionary and permitted. It now fails on the second mutation by its own predicate. The illustration this row drew from it therefore no longer stands at HEAD, and is kept above as the finding's provenance rather than as a current reading of the file. The question is untouched: strengthening the control moved it off silence (ii) onto a classified column, which is why it can now do its job — the platform still cannot express silence (i) or (ii), and no requirement yet makes either consequential.)* | **Read first: a type change is inert on its own.** `modelling_forbidden_columns` (`datasets.py:195-205`) tests `entry.pii_class in MODELLING_FORBIDDEN_PII`, and `None` is absent from that frozenset exactly as `PiiClass.NONE` is; its sole caller `_refuse_direct_identifiers` (`ingestion.py:498-500`) early-returns on an empty result. Every option below makes the silences *distinguishable*; **none makes either one *consequential* unless (e) is decided with it.** A requirement that only widens a type specifies nothing. **(a) Nothing.** Free. Keeps a governed classification whose unset state is stored and shown to an actuary as a decision, in the one screen `01` §5.3 provides for reading it. **(b) A sixth member, `PiiClass.UNCLASSIFIED`.** The house pattern for "no verdict here" is an explicit member on a *required* field, never optional: `RuleOutcome` (`validation.py:64-69`) spells both non-verdict states, `ERROR` and `SKIPPED`, as members; `RuleResult.outcome` is required with no default; and `counts` (`:502-506`) is built from `dict.fromkeys(...)`, so each non-verdict state is positively enumerated rather than inferred from an absence. Contract cost is narrow and known — one string in the flat enum at `docs/contracts/openapi/generated.json:7837-7847` and one property at `:2171-2173`, with no hand-authored contract tier touched and the generated TS field staying required and non-nullable. **Its real cost is the migration**, and the repository has ruled on that shape twice, both times the same way (`backend/migrations/versions/d4e5f6a7b8c9_modelling_diagnostics.py:18-22`, which demotes to `draft` rather than invent an artifact, and `82edffbe1dce_dataset_owner.py:57-60`, which refuses rather than invent an owner). Because `_dictionary_json` already flattened authored and defaulted rows into the same bytes, **no migration can tell existing rows apart**: the honest choices are to relabel every stored `none` as `unclassified` — accurate for the great majority, wrong wherever someone did type it — or to refuse, and neither is free. **(c) Make the field optional** (`PiiClass \| None`). Matches no pattern in `model_schema`. The neighbour that looks like precedent is not one: `DataDictionaryEntry.semantic_type` has **zero runtime consumers** — every consuming site is `ColumnProfile.semantic_type`, which is required and machine-inferred — so its nullability is evidence that nobody had to decide, not that this was decided. It also **recreates the defect one level down**: in JSONB a stored `null` and an absent key both deserialise to `None`, so null-versus-absent replaces unset-versus-`none`. Contract cost is strictly worse than (b) — the field widens from a flat enum to a nullable union and the generated TS field stops being required. **(d) A derived `unclassified_columns`, beside the existing `modelling_forbidden_columns`.** The repository has already answered this exact concern in this exact shape: `ValidationRuleSet.empty_layers` (`validation.py:166-184`, FR-DATA-16) is a contract-visible positive enumeration of *what was not covered*, promoted to a computed field precisely because "silence would let a rule set lose its whole distributional layer in an edit and look complete." Alone among the options it reaches **both** silences, because it is computed against the Version's column list rather than against the dictionary's own keys. No wire-format change, no enum edit, and **no migration** — it is a projection of state already stored, so it is correct on historical rows that (b) and (c) cannot repair. Its cost is that it needs the Version's schema, which is where the scoping question bites. **(e) The behavioural half — orthogonal, and decided with whichever of (b)–(d) is chosen.** Does an unclassified column (i) block the `draft → validated` transition under FR-DATA-17, (ii) warn on the report and in the UI, or (iii) refuse only to be used as a Factor? **The precedent usually cited for (i) settles less than it appears to.** FR-DATA-19's "an unrun rule is never treated as a pass" is scoped to `error`; the glossary at `01-data-management.md:64` separates `error` ("the rule itself blew up") from `skipped` ("a dependency was unavailable"), and `ValidationReport.overall` (`validation.py:541-548`) branches on `FAIL`, `ERROR` and `WARN` and falls through to `PASS` with **no `SKIPPED` branch** — an all-skipped report passes, and `frontend/src/api/validation.ts:57-63` bands `skipped` with `pass`. An unauthored classification is the `skipped` shape, not the `error` shape; followed faithfully the precedent licenses "name the absence and enumerate it" and licenses nothing about blocking. **(f) One cost every option except (a) incurs, and one that no option touches.** `01-data-management.md:241` states the five values as an exhaustive set, so it is amended in the same commit and `generated.json` regenerates under FR-PLAT-48's drift gate. Separately, `frontend/src/api/datasets.ts:79` reads `entry?.pii_class ? MODELLING_FORBIDDEN.includes(entry.pii_class) : false` — **a falsy class fails open**, returning "safe to model" with no type error anywhere, which is exactly what a null would do under (c). That line is a required edit under every option and **no schema change reaches it**; the same file hand-copies `MODELLING_FORBIDDEN` from `datasets.py:113-115`, a shape duplicated against `CLAUDE.md` §2 and worth deriving whatever is decided here. | **(d), paired with (e)(ii) — warn, name the columns, do not block — and (b) in preference to (c) if a stored field-level state is also wanted.** Three things decide it. **Scope:** (b) and (c) reach only silence (ii) and are *mechanically incapable* of reaching silence (i), because a column with no entry contributes no field to be null or unclassified — `modelling_forbidden_columns` iterates `.items()`. Silence (i) is the majority case, the one in the Phase 1b exit dataset, and the one the UI hides completely rather than mislabels; (d) is the only option that reaches both. **History:** because `_dictionary_json` writes the default explicitly, the authored/defaulted distinction is already gone from every stored row, so (b) and (c) fix the shape going forward and leave the existing corpus exactly as ambiguous as it is now, while (d), being derived, is immediately correct about rows written years ago. **Blocking is not licensed:** the precedent that would justify it governs the wrong half of a distinction the spec draws in its own glossary. **The nearest precedent is closer than FR-DATA-19 and should be cited first:** `update_dictionary` is a replace and not a merge, and its docstring gives the reason — "a merge would make a removal indistinguishable from an omission" (`backend/src/app/platform/datasets.py:267-272`). That is this question's argument exactly, already decided in this repository, about this dictionary — decided at the entry level and left open at the field level. Two things this row deliberately does **not** decide. **The scoping question is real and is not settled here:** the dictionary is Dataset-scoped and mutable while the artifact it governs is Version-scoped and immutable, and `update_dictionary` re-checks nothing and demotes nothing, so a classification can be removed after the version it governed was validated. OQ-DATA-9 and FR-DATA-50 resolved the structurally identical asymmetry for status and owner by making the container's fields derived projections of the version — which argues for (d)'s shape, but never touched `pii_class` and does not pre-decide this. **And the modelling-side hole is owned elsewhere:** that a `direct_identifier` column is fittable today because nothing derives `Factor.prohibited` from the classification is filed at `docs/roadmap.md:1106-1137` with a pending §14 disposition and no acceptance line following it. This row cites it and adopts nothing from it — asserting custody over it is the precise error that passage names. | unowned | open |
| **OQ-DATA-15** | **A validation layer that is present but could not run is indistinguishable from one that passed, and the field written to prevent exactly that cannot see it.** Raised 2026-08-25 (W6b docs backlog), while writing FR-DATA-55. When a Rule Set enables distributional rules but pins no Reference Dataset Version, each of those rules returns `skipped` with the reason "no reference version to compare against" (`packages/pricing-core/src/pricing_core/data/validate.py`), which `_run_one` records as `RuleOutcome.SKIPPED`. `ValidationReport.overall` (`packages/model-schema/src/model_schema/validation.py`) branches on `FAIL`, `ERROR` and `WARN` and falls through to `PASS` — so a report whose **entire** distributional layer never ran reads `pass`, `permits_validation` is `True`, and the Dataset Version is promotable on the strength of checks that did not happen. **`empty_layers` does not catch it.** It is `ALL_LAYERS - covered_layers`, and `covered_layers` is taken from the rule set's *enabled* entries, so a layer that is enabled and unrunnable counts as covered. Its own docstring states why it exists — "silence would let a rule set lose its whole distributional layer in an edit and look complete" — which describes this case exactly, measured by the very field written to refuse it. The gap is that FR-DATA-16's completeness test is **rule-set-side**, asking what the set declares, and no **report-side** analogue asks what the run produced. There is no write-path guard either: `replace_rule_set` stores `reference_dataset_version_id` without checking it against the layers its members belong to. **No requirement is broken today.** FR-DATA-19's "an unrun rule is never treated as a pass" is scoped to `error`, and §2's glossary separates `error` ("the rule itself blew up") from `skipped` ("a dependency was unavailable") — an absent reference is squarely the second. This is `CLAUDE.md` §0's "a capability not yet specified", not its "code and spec disagree". *The mechanism is already written down in this document, inside `OQ-DATA-14`, where the `PASS` fallthrough is cited as licence for that row's own recommendation about `pii_class`. It has never been raised as a question in its own right; that is what this row does, and nothing here reopens `OQ-DATA-14`.* | **(a) Nothing.** Free, and defensible on the narrow ground that `skipped` is an honest per-rule verdict already present in `results`. The cost is that the honesty stops at the rule: the layer, the overall verdict and the promotion decision all read as though the checks ran, and an actuary reading a `pass` badge cannot learn that PSI was never computed without expanding every distributional rule individually. **(b) A report-side `unrun_layers`,** computed on `ValidationReport` in the shape `empty_layers` already established — the layers for which every result is `SKIPPED`. Contract-visible, so the screen can name the layer that did not run beside the badge that says `pass`. No wire-format break and **no migration**, because it is a projection of results already stored, which makes it correct about reports written months ago in a way no enum change can be. **(c) A `SKIPPED` branch in `overall`** — either a fifth `OverallOutcome` or banding skipped with `PASS_WITH_WARNINGS`. It reaches the badge with no client change, but it re-grades every report already written, and the frontend bands `skipped` with `pass` deliberately (`frontend/src/api/validation.ts`), so the edit is not confined to one field. It also overstates the common case: one skipped rule among thirty-eight is not a warning about the run. **(d) Refuse at write time** — reject a Rule Set that enables a distributional rule with no reference pinned. Cheap, and it prevents the case rather than reporting it. But it over-refuses, because a Rule Set is routinely authored before the data it will run against exists, and FR-DATA-55 keeps the pin optional on exactly that ground; and it reaches only this one cause, when a rule can be skipped for others. **(e) The orthogonal half, answered together with whichever of (b) to (d) is chosen:** does an unrun layer *block* `draft → validated` under FR-DATA-17, or only warn on the report and in the UI? | **(b), with (e) answered "warn and name the layer, do not block", and (d) rejected.** Three things decide it. **Shape:** (b) is the third instance of one pattern in this module — `empty_layers` under FR-DATA-16, the derived `unclassified_columns` recommended by `OQ-DATA-14`, and now unrun layers. Each enumerates what did not happen instead of leaving it to be inferred from silence, and three instances is enough to argue the repository should own the pattern deliberately rather than grow a fourth ad hoc. **Blocking is not licensed:** the precedent usually reached for, FR-DATA-19, governs `error` and this is `skipped`, a distinction §2's glossary draws in its own words; reading FR-DATA-19 across that line would also re-grade the referential layer, which legitimately runs empty when a dataset has no reference tables. **Cause versus condition:** (d) fixes the one cause visible today, (b) reports the condition however it arises — and since the condition is "this check produced no verdict", it is the condition that governs promotion. Two things this row does not decide. Whether `unrun_layers` should distinguish *wholly* unrun from *partly* unrun is a design detail left to the slice, though the wholly-unrun case is the one with teeth. And the **testing** gap underneath this is filed separately with FR-DATA-55: no Python test sets `reference_dataset_version_id` at all, so neither the pinned path nor the `None` path has coverage, while the distributional rules themselves are well covered by injecting `reference_profile` directly — which is precisely why the hole was invisible. The rules are tested; the wiring that decides whether they run is not. | unowned | open |

## MODEL — modelling ([`02-modelling.md`](specs/02-modelling.md))

| ID | Question | Options / trade-offs | Recommendation | Owner | Status |
|---|---|---|---|---|---|
| ~~**OQ-MODEL-8**~~ ✔ | ~~Does the GLM spine grow to meet `02` §4's field sets, or does §4 narrow to a staged contract?~~ **DECIDED 2026-08-17: §4 is a staged contract, re-widened as each slice lands.** | Found by auditing the spine, 2026-08-15. §4.1 declares `banding_id`, `grouping_id` and `expression` on `Factor`; §4.4 declares `split_ref`, `filter`, `loss_treatment` and a nested `regularisation` block on `ModelSpec`; §4.8 declares `diagnostics_id`, `transparency_artifact_id`, `custom_objective_ref` and `approval_request_id` on `Model`, plus a `status ≥ fitted ⟹ diagnostics_id set` invariant the spine cannot meet because diagnostics do not exist yet. The spine implements a subset and enforces a different invariant (`fitted ⟹ fit_result`). **Grow the code**: the contract is the design and the frontend generates from it, so a field declared and unpopulated is at least visible. **Narrow the spec**: a contract carrying eleven always-null fields teaches a reader that null means nothing rather than not-yet, and `loss_treatment` in particular changes `spec_hash` for every stored model when it arrives. | **DECIDED 2026-08-17 — the staged contract, decided by demonstration rather than by argument.** The pattern this row recommended ran six times before the decision was taken: `banding_id`, `grouping_id`, `split_ref`, `diagnostics_id`, `loss_treatment` and `approval_request_id` each became a live field as its slice landed, and §4 carried a dated note for each. What was left was a list, not a choice — so the row closes on the list, with every residual given a verdict and an owner rather than on the pattern having worked. **`02` FR-MODEL-87** is the rule: a field is shown live only once a slice populates it, and anything else is named in place with a dated note saying it is declared-and-unbuilt and who owns it. It carries the residual list, and keeps the three kinds apart — *absent entirely*: `filter` on `ModelSpec`, `interval_for` (deferred here by FR-MODEL-78), and `GlmSpec`'s `custom_objective_ref`, `select_by` and `cv_folds`, all Phase 1b; *declared and unbuilt*: `transparency_artifact_id` (W5) and `Model.custom_objective_ref` (Phase 1b); *present under a different shape*: §4.4's nested `regularisation` block, which was a **divergence** — written in Phase 0, built to by nothing, and copyable off the page into a body the contract rejects — and is corrected in place to `GlmSpec`'s flat `alpha` / `l1_ratio` / `max_iter` / `tolerance` with a dated note saying which side was wrong. **The precondition this row set for itself is met: `spec_hash` versioning is now `02` FR-MODEL-86**, a numbered requirement that outlives the question — the digest is `v<n>:sha256:…` with `n` inside the hashed payload, any change to the payload's field set increments `n` in the same commit, and `spec_hash_is_current` makes the older rows findable. It had already earned its keep twice (`split_ref` `v1 → v2`, `loss_treatment` `v2 → v3`) while living only in this cell, which closing would have retired. The two sharper residuals get their own requirements rather than a line in a list: **FR-MODEL-88** states `expression`'s verdict — `FactorType.EXPRESSION` is selectable, `Factor` has no field to hold the expression, and `resolve_factors` refuses it **by name** at the boundary where it would matter, so it is contained rather than corrected, with the field owned by Phase 1b; **FR-MODEL-89** restates §4.8's R3 in the direction the link actually runs — the `TransparencyArtifact` carries `model_id`, so "a non-GLM model cannot be approved without one" is checked by querying for an artifact naming the model at the approval transition, not by reading a column nothing writes. That was `diagnostics_id`'s failure arriving a second time, which is the argument for closing on a stated list. | maintainer | decided |
| ~~**OQ-MODEL-9**~~ ✔ | ~~Do the `tree` banding (FR-MODEL-9) and `tree` grouping (FR-MODEL-14) methods justify adding a tree learner to `pricing-core`'s declared dependencies?~~ **DECIDED 2026-08-17: declare `scikit-learn` and implement both with `DecisionTreeRegressor`.** | Raised by implementing the other seven methods, 2026-08-15. **Add the dependency:** both methods are declared in the spec, both are what an actuary reaches for when the response is what should decide the cut, and scikit-learn is already present transitively through `glum` — so the cost is a line in `pyproject.toml` and a row in `skills-map.md`, not a new subsystem. **Leave them refused:** `pricing-core`'s dependency list is the thing ADR-0001 makes reviewable, a transitive dependency is not a declared one, and seven of nine methods already ship. What is *not* an option is the third path taken by neither: returning quantile boundaries under the label `tree`, which records a method as one it is not. | **DECIDED 2026-08-17 by the maintainer, at recommendation (b).** Specified as **FR-MODEL-85** and delivered in the same commit: `pricing-core` declares `scikit-learn>=1.5,<2` (`02` §8, `skills-map.md`), and `propose_banding` / `propose_grouping` fit a depth-limited `DecisionTreeRegressor` on the exposure-weighted response instead of refusing. The declaration installs nothing — 1.9.0 arrives with `glum` 3.4.1 — so what it buys is that a package which *imports* scikit-learn also names it. Option (a), the one-tree booster, was rejected on the reason this row was reframed around: it costs no dependency but selects splits under `lambda`, `min_child_weight` and `gamma`, so shipping them as `tree` mislabels the method exactly as the quantile substitution would have. Each artifact now records its effective `min_samples_leaf` and `random_state`, found while building: a `tree` banding that stored only `n_bands` could not reproduce its own boundaries. | maintainer | decided |
| ~~**OQ-MODEL-10**~~ ✔ | ~~Should the GLM approximation of a GBM (FR-MODEL-34) be persisted as a **Model** in its own right, or carried inside the transparency artifact?~~ **DECIDED 2026-08-18: as a Model, owned by Phase 1b.** **Built 2026-08-19 (W5).** | Raised by building it, 2026-08-17. §4.9's `approximating_model_id` implies the first and the field is `None` in the built artifact. **As a Model:** it becomes citable, versioned and diffable like any other, `02` §4.11's comparison could put a GBM beside its own approximation, and OQ-MODEL-3's question — can a Rating Version rate on the approximation — has an obvious referent if it is ever answered yes. **Inside the artifact:** a Model needs a spec, and this one's response column is *another model's prediction* rather than a dataset column, so `dataset_version_id`, `spec_hash` and R1 all mean something slightly different; it would also need diagnostics of its own to reach `fitted` (§4.8), which are diagnostics of a surrogate against a surrogate target. | **Maintainer accepted the revised recommendation, 2026-08-18.** Specified as `02` **FR-MODEL-96**, and deliberately **not delivered** — the requirement carries its own trigger and owner instead, which is what stops a deferred decision decaying back into a recommendation (the FR-DATA-46 pattern). The argument that settled it came from OQ-MODEL-3's answer rather than from this row: an `approximation`-mode Rating Version pins what it rates on, FR-OVR-14 requires a pin to resolve to an artifact whose status is `approved` or better, and a `TransparencyArtifact` carries `model_id` and no status at all — so the only way `03` FR-RATE-60's second mode can ever deploy is if the approximation is an artifact with a lifecycle. The objections in the options column stand as the work the answer creates, and FR-MODEL-96 names all three: `approximates_model_id` on the approximating Model's spec, that field joining the `spec_hash` payload (FR-MODEL-86, so `n` increments with it), and §4.8's `status ≥ fitted ⟹ diagnostics_id` met by diagnostics of the surrogate **against the source model's predictions** — FR-MODEL-36's quantity, recorded as what it is rather than dressed as diagnostics against observed claims. Until built, `approximating_model_id` stays `None` and the artifact keeps the coefficients, which is FR-MODEL-87's declared-and-unbuilt state with a trigger attached. **The deadline is real**: after anything references a transparency artifact by identifier, this becomes a migration rather than a decision. **Built 2026-08-19** (W5): `approximates_model_id` on `GlmSpec` joining `spec_hash` at `v5`, the surrogate reserved and fitted inside the `model.transparency` Job with diagnostics against the source model's predictions, and FR-MODEL-102 appended for the invariant that keeps a surrogate identifiable from its spec alone. The deadline the row states is met: nothing references a transparency artifact by identifier yet. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-11**~~ ✔ | ~~What must a GLM approximation prove before a Rating Version may **deploy** in `approximation` mode?~~ **DECIDED 2026-08-18: a dislocation run against the same version in `exact` mode, inside a workspace-declared premium-deviation threshold, with the fidelity statement as the pre-check.** | Carved out of OQ-MODEL-3 when it was decided, 2026-08-17. FR-MODEL-36's fidelity statement is descriptive — it is computed, stored and shown, and nothing refuses anything on it — so a mode whose deployed prices are, by construction, not the model that was validated currently has no floor. **A numeric floor on the artifact:** a workspace-declared minimum R² and maximum absolute relativity deviation, checked at the Rating Version's approval beside the other evidence (`03` FR-RATE-40). Cheap, and refuses the mode where the surrogate is plainly poor — but any single threshold is arbitrary, and coefficient agreement is not premium agreement. **A dislocation-based gate:** run `03` FR-RATE-46's existing dislocation report with `exact` as baseline and `approximation` as candidate, and threshold *that*. It measures the quantity that matters, on the actual book — but costs a portfolio run per approval and requires `exact` mode to be runnable at approval time. **Nothing beyond the statement:** the approver reads the fidelity numbers and decides, which is how every other piece of evidence is treated. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `03` **FR-RATE-61**, **Phase 2**, with the deployment path it gates — it needs FR-RATE-46 built and nothing in Phase 1 deploys a Rating Version, so an owner and a phase are the whole of what is deliverable today. The reason is the one the options column reaches: the approval question is *how different are the prices we will charge*, and R² answers a question about coefficients — a surrogate can agree on coefficients and disagree on premium wherever exposure is thin. Spike S2's p99 of 1.09 ms for `exact` (OQ-RATE-2) is what makes the baseline affordable, so the prerequisite is measured rather than assumed. Two details the requirement fixes that this row had left loose: the threshold is a maximum absolute percentage deviation **at a declared portfolio quantile**, because a mean deviation averages away the tail that a dislocation gate exists to catch; and the fidelity statement runs **first** and stays descriptive, so a plainly poor surrogate is refused before a portfolio run is spent on it. Where the workspace setting lives is left to the slice that builds it — inventing a home for it now would be specifying a field nothing can populate for a phase. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-12**~~ ✔ | ~~May an `interaction` Factor cross a **continuous** operand?~~ **DECIDED 2026-08-18: no — refused by name, and no product term at any intent.** | Raised 2026-08-18 (W5, the interaction slice) by building the crossing and finding the choice unavoidable at the first continuous operand. **Refuse it, and say so** — what this slice implements: the refusal names the operand and the remedy (band or group it, then cross the result), so the actuary gets a rateable structure by construction. **Allow a product term** — legitimate GLM practice and the thing an analyst reaches for when the effect really is a smooth slope; but `03`'s rating DAG is a graph of *tables*, and a varying slope has no cell, so the model would fit and could not be deployed as a rate table — the failure arriving at the rating slice rather than at the factor. **Allow it only for `intent: diagnostic` factors** — the narrow version: a slope is fine for something never rated on, and FR-MODEL-3 already distinguishes intents. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `02` **FR-MODEL-97**, which ratifies what the interaction slice built rather than changing it: the refusal names the operand and the remedy — band or group it, then cross the result — so the actuary is left with a rateable structure by construction. The asymmetry is the whole argument and the requirement records it: refusing is additive to undo, and allowing is not, because a product term shipped today is a model somebody has fitted by the time `03`'s DAG of tables turns out to have no cell for a varying slope. **The third option is named as the likely eventual answer rather than dropped** — a product term for `diagnostic`-intent factors only (FR-MODEL-3), never rated on — and it stays out now because it should be decided against a rate table that exists; it needs no contract change when that day comes, only a widened refusal. Owner: the maintainer, at `03`'s rate-table slice. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-13**~~ ✔ | ~~Should the platform ever offer a **true prediction interval** — one carrying the process variance `φ·V(μ)` — and if so, under what name?~~ **DECIDED 2026-08-18: not until a named consumer asks; then `prediction_interval`, aggregate first.** | Raised 2026-08-18 (W5, the prediction slice) on discovering the requirement named a quantity its stated mechanism cannot produce. **Never offer one** — what this slice implements: for a frequency model on a single policy the process variance dominates so completely that the honest interval is very nearly "0 or 1 claims", which is true and prices nothing, and a reader who asked for uncertainty and got that will read it as a malfunction. **Offer it as a second, separately-named kind** — `prediction_interval` beside `confidence_interval_mean`, computed as `φ·V(μ)` from the stored dispersion; cheap, correct, and defensible for severity and burning-cost models where the interval is genuinely informative. **Offer it only for aggregate predictions** — over a portfolio the process variance averages away and a predictive interval on total expected claims is both meaningful and what a capital or reserving reader actually wants. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `02` **FR-MODEL-98**, which does the one thing this row could not do by staying open: it names the **trigger**, so "revisit when there is a consumer" has a written form rather than depending on somebody remembering. The first consumer of an aggregate predictive interval — `05-monitoring.md`'s portfolio work, or a capital or reserving reader — unblocks it, and at that point the second kind is `prediction_interval` computed as `φ·V(μ)` from `GlmFitResult.dispersion`, which is already stored, offered **for aggregate predictions first** because that is where the process variance averages away. `confidence_interval_mean` is never silently widened into it: `UncertaintyKind` is an enum a client matches on, and changing what a value means is worse than adding one. The case against the per-policy version is not cost — it is that the honest frequency interval is very nearly "0 or 1 claims", which prices nothing and reads as a malfunction. FR-MODEL-63 stands as already amended; FR-MODEL-98 states the boundary that amendment left implicit. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-14**~~ ✔ | ~~A **penalised** GLM (`alpha > 0`) reports standard errors and now a confidence interval computed from a covariance matrix `glum` itself warns is incorrect. What should a penalised fit report as its uncertainty?~~ **DECIDED 2026-08-18: report both, and state the basis they were computed on.** | Raised 2026-08-18 (W5, the prediction slice) from the warning `glum` emits on every penalised fit in the suite; `alpha` defaults to `0.0` so the default path is unaffected, and `02` §4.8's comparison journey fits at `alpha = 25.0` deliberately. **Report it anyway, as now** — the sandwich-free matrix is the unpenalised information matrix, which understates the shrinkage the penalty applied and so gives intervals that are too wide for the estimate actually reported; wrong in the conservative direction, but wrong. **Refuse the interval** with a fourth `UnavailableReason` — honest, and it would leave FR-MODEL-21's standard errors reporting from the same matrix the interval just declined to use, which is incoherent unless both go. **Bootstrap or a penalty-aware sandwich** — correct for both, and 200-odd refits per model is a different cost class from a fit. | **Maintainer accepted the recommendation, 2026-08-18, and it was delivered.** The recommendation was a *sequencing* rule — decide FR-MODEL-21 and FR-MODEL-63 together — and taking it seriously is what settled the answer: the interval inherits the matrix from the standard errors rather than introducing it, so **refusing the interval would have had to take the standard errors with it**, leaving a penalised fit with no uncertainty at all. That option was rejected on its consequence rather than on its honesty. Specified as `02` **FR-MODEL-99**: the numbers are reported as before, and every response carrying them states `UncertaintyBasis.unpenalised_information_matrix` instead of `information_matrix`. The third option — bootstrap or a penalty-aware sandwich — is the *correct* answer and is named with a trigger rather than deferred to nowhere: ~200 refits is a different cost class from a fit and so a Job rather than a fit-time step, built when the first consumer needs valid penalised inference. **Two things the build decided that the row had left open.** The basis is **derived from `GlmSpec.alpha`, never stored on the fit result** — the spec is pinned to the fit by `spec_hash` and both are immutable, so a stored copy could only ever agree or be wrong (`CLAUDE.md` §2) — and it is derived from `alpha` rather than from `glum`'s warning text, which the fit swallows inside `catch_warnings` and which a patch release may reword. *Found while writing the tests:* `l1_ratio` alone does not make a fit penalised, so reading the basis off it would have labelled every elastic-net default approximate; the test pins that. The `Uncertainty` validator refuses an interval with no basis, so the qualification cannot be omitted by a future caller rather than merely being present in this one. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-15**~~ ✔ | ~~`GlmDiagnostic.aliasing` is `tuple[str, ...]` — collinear terms named — while `docs/contracts/schemas/diagnostics.schema.json` declares an array of untyped `object`. Should an aliasing entry be a bare term name, or a record such as `{term, aliased_with, reason}`?~~ **DECIDED 2026-08-21: bare term names — FR-MODEL-109, delivered the same day.** | Found 2026-08-19 by widening `test_generated_and_authored_agree_on_scalar_types` from 6 slugs to 11 while applying `OQ-OVR-8` — the divergence had been invisible because `diagnostics` was outside the check's parametrize list. **Neither side is obviously the error**, which is why this is a question and not a fix: the model's comment argues the name is the point ("2 terms aliased" tells a reader something is wrong and not which factor to fix), and an object entry would say strictly more. FR-MODEL-51 asks only for "a VIF/aliasing report" and does not decide it. Pinned meanwhile by `test_the_diagnostics_divergence_is_exactly_the_known_one`, so a *new* divergence in `diagnostics` still fails. Two adjacent unguarded float channels into a `DecimalStr` field were found by the same audit and are recorded here rather than lost: `ReconcileRequest.tolerance` is a bare `Decimal` (a JSON number is still coerced, then stringified into the job parameters), and `pricing-core`'s `ReconciliationResult` is an unvalidated frozen dataclass. | **Maintainer decided 2026-08-21, following the recommendation.** `GlmDiagnostics.aliasing` stays `tuple[str, ...]`: the field is read by a human deciding which factor to drop, and a name is what they act on — `aliased_with` is recoverable from the VIF report beside it, and the reason for a rank-deficient term is always the same reason. The hand-authored `diagnostics.schema.json` now declares the array of strings the model always produced; the pin (`test_the_diagnostics_divergence_is_exactly_the_known_one`) is deleted rather than relaxed and `diagnostics` joins `COMPARED_SLUGS`. The object form `{term, aliased_with, reason}` is not shipped; if W6b's diagnostic view needs those fields, that is a new requirement raised at that point, when the answer stops being a guess about what a reader wants. The two float channels the same audit found stay parked, named in the options column rather than fixed here. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-16**~~ ✔ | ~~A paired quantile interval covers `Y`; `UncertaintyKind.confidence_interval_mean` covers `E[Y\|x]`; and FR-MODEL-98 fixes the platform at exactly one kind, naming `prediction_interval` (`φ·V(μ)`, aggregate-first) as the only future second. What does a quantile-pair response call itself?~~ **DECIDED 2026-08-19: a third member, `quantile_pair_interval`.** | Raised 2026-08-19 (W5, the paired-quantile slice) on finding FR-MODEL-78's deliverable has no name in the enum FR-MODEL-98 closed. **Reuse `confidence_interval_mean`:** no contract change, and it is a lie — the pair covers the outcome, not the mean, and FR-MODEL-98 says that value is "never silently widened". **Use FR-MODEL-98's reserved `prediction_interval`:** the right *quantity*, and it collides with a reserved name whose stated computation is `φ·V(μ)` from a GLM's dispersion offered for aggregates first — a client matching the value would get a per-row quantile pair where the requirement promised an aggregate variance interval. **A third member, `quantile_pair_interval`:** names the estimator as well as the quantity, so a reader comparing a GBM bound with a GLM bound can tell they are not the same kind of claim; costs an enum member, which FR-MODEL-98 correctly calls a contract change. | **Maintainer decided 2026-08-19, at the recommendation.** Specified as `02` **FR-MODEL-101**. FR-MODEL-98's argument against a second kind was that shipping one *before a consumer exists* puts two numbers on a screen with nothing saying which to trust; here the consumer exists and is explicit, because FR-MODEL-78 makes the pair opt-in at 2–3× the fit cost and nobody receives one without having asked. What survives is the naming argument, and it points at a distinct member rather than at either existing value: `confidence_interval_mean` would be false, and `prediction_interval` is reserved for a different estimator at a different granularity, so taking it would leave FR-MODEL-98's named trigger with no name to fire into. **FR-MODEL-98 is amended by addendum, never edited** (`CLAUDE.md` §14) — its boundary holds and this is the second door it did not anticipate. Two consequences the decision carries into the contract: `basis` is **forbidden** on this kind, because `UncertaintyBasis` describes a covariance matrix and a pair of quantile fits has none; and `interval_models` is **required**, so a reader can reach the two Models the bounds came from. | maintainer | **decided** (in spec) |
| **OQ-MODEL-1** | ~~Ship `expression` custom objectives in Phase 1, or templates only?~~ **DECIDED 2026-08-15: templates in Phase 1, expressions in Phase 2, certification machinery built in Phase 1 for both.** | Templates only: the §4.5 catalogue already covers Tweedie tuning, capping, and asymmetric losses — i.e. most of the stated need — with zero parser/sandbox risk. Expressions in Phase 1: the headline differentiator lands early, but the AST parser, SymPy derivation, and certification are all new and all security-relevant at once. | **Maintainer accepted the recommendation.** Phase 1 ships the `template` catalogue and gates `kind: expression` behind `expression_objectives_enabled`, off throughout (**FR-MODEL-75**); the certification machinery of §4.7 is nonetheless built now, against templates, with `analytic_vs_numeric` derivative agreement standing in for `symbolic_vs_numeric` and every other check unchanged (**FR-MODEL-76**). *Sharper than when the question was written:* the restricted parser is no longer the risky, deferred part — `pricing_core.data.expressions` was built for `01` FR-DATA-10 in W4 and translates to Polars rather than sandboxing `eval`. What Phase 2 actually adds is the SymPy derivation, a second compilation target (gradient/hessian kernels), and the review path for a user-authored loss. So the first loss a user writes meets machinery that has been certifying for a phase, rather than being the input that proves it. | maintainer | **decided** |
| **OQ-MODEL-2** | ~~GBM prediction intervals — how?~~ **DECIDED 2026-08-15: `uncertainty: unavailable` with a typed reason by default; opt-in paired quantile models; the variance approximation is never shipped.** | Paired quantile models: honest, 2–3× fit cost, and the quantiles can cross. Variance-model approximation: cheap, easy to misread as a genuine predictive interval. `uncertainty: unavailable`: honest and free, but violates the spirit of R5 for GBMs specifically. | **Maintainer accepted the recommendation.** R5 is met by an explicit, typed statement of absence — `no_interval_models_fitted`, `interval_models_not_approved`, `interval_models_stale` — rather than by a number that reads like a measurement and is not one (**FR-MODEL-77**). Intervals are available on request as paired quantile models: separate Models in the same family, fitted with the `quantile` template at declared alphas and linked by `interval_for`, so the 2–3× cost is a visible choice (**FR-MODEL-78**). The crossing problem is answered rather than avoided — crossing is detected and reported, never silently reordered, because a reordered pair still does not describe one distribution and hiding that is the failure mode this whole question is about. | maintainer | **decided** |
| ~~**OQ-MODEL-3**~~ ✔ | ~~Can a Rating Version rate on the GLM approximation instead of calling the GBM?~~ **DECIDED 2026-08-17: yes — both modes, and the mode is the Rating Version's rather than the step's.** | Rate on the approximation: fully tabular, inspectable, fast, and it is what most insurers actually deploy — but the deployed prices are then *not* the model that was validated. Call the GBM: exact, but the rating structure contains a black box on the p99 latency path. | **Decided as recommended, and the finding this row was elevated for is what the decision fixes.** Specified as `03` FR-RATE-60: `RatingVersion.model_reference_mode` (§4.3) is the declaration, every `model_call` step's `mode` (FR-RATE-10) must equal it, and a version whose steps disagree is refused at save time with `MODEL_REFERENCE_MODE_INCONSISTENT` — added to §4.3's invariants and to §5.1's error codes. The field's two names were one concept at two levels: the version-level field is now the source and the step-level one derived, which is also all the published contract could ever express — `rating-version.schema.json` enumerates `exact \| approximation` and has no `mixed`. **FR-RATE-10 was ahead of its question rather than wrong** and stands as written. **The residual is carved out rather than closed:** what an approximation must prove before a Rating Version may deploy in `approximation` mode is now **OQ-MODEL-11** — FR-MODEL-36's fidelity statement is descriptive and nothing gates on it. **OQ-MODEL-10 is unblocked by this**, and its row records what the answer now has to account for. | maintainer | decided |
| **OQ-MODEL-4** | ~~Auto-detect interaction candidates from SHAP interaction values?~~ **DECIDED 2026-08-15: surfaced as suggestions with evidence, never auto-added.** | Auto-detection surfaces genuine structure an actuary would miss, and is a well-worn path to overfitting and to rating structures nobody can defend. | **Maintainer accepted the recommendation.** TreeSHAP interaction pairs are ranked on the transparency artifact and surfaced in the factor workbench with each candidate's exposure share and holdout lift — the two numbers that say whether a suggestion is worth taking and over how much of the book (**FR-MODEL-79**). The platform never writes a Factor into a Model Spec: an interaction becomes rateable only as an explicit `interaction` Factor with intent and a written rationale, and the model document names it as an authored decision. The detection is kept because the structure it finds is real; the automation is refused because a rating basis nobody chose is one nobody can defend. **Amended 2026-08-23:** the two-numbers clause is withdrawn and OQ-MODEL-31 replaces it. A per-pair exposure share is `1.0` by construction, and holdout lift was named here and in FR-MODEL-79 without ever being defined — so the decision was accepted on the strength of evidence that did not exist. The decision itself is unaffected; the evidence beside the suggestion is what reopened. **OQ-MODEL-31 decided 2026-08-23:** what stands beside a candidate is its **holdout strength ratio** (**FR-MODEL-128**) — the ranker's own statistic recomputed on the holdout partition and published against the in-sample value, so the evidence this row promised is now defined rather than named. | maintainer | **decided** |
| **OQ-MODEL-5** | ~~Which credibility standard for `credibility_weighted` grouping?~~ **DECIDED 2026-08-15: both — limited fluctuation as the default, Bühlmann–Straub selectable, the choice recorded per grouping.** | Limited fluctuation (classical): simple, familiar to UK GI actuaries, one parameter (standard for full credibility), theoretically weak. Bühlmann–Straub: defensible, handles heterogeneous exposure properly, needs variance components that are awkward to estimate on thin cells and harder to explain in a model document. | **Maintainer accepted the recommendation.** Limited fluctuation is the default because it is what a reviewer expects to see, and it is stored with the `(p, k)` pair its standard came from — 1 082 claims is ±5 % at 90 % confidence, and a reviewer who cannot see `(p, k)` cannot tell that number from a house number. Bühlmann–Straub is selectable and persists EVPV, VHM and `k` in the grouping evidence so `Z` can be re-derived rather than taken (**FR-MODEL-80**, `02` §4.3 and `grouping.schema.json`). Recorded **per grouping**, not as a workspace or platform setting: which standard suits a book is a modelling judgement that belongs beside the grouping it justifies.  **Built 2026-08-15 (W5): the default half only.** `limited_fluctuation` is implemented — shrinkage `Z = sqrt(min(n/n_full, 1))` toward the portfolio rate, then merging within `merge_tolerance_relativity`. `buhlmann_straub` is accepted by the contract and **refused by the implementation** with a named error rather than silently substituted, so the unbuilt half is visible. Its variance components (FR-MODEL-80's `credibility_components`) are unimplemented; owner is the slice that builds them. | maintainer | **decided** |
| **OQ-MODEL-6** | ~~Hard gate on factor count / exposure-per-parameter, or diagnostic warning only?~~ **DECIDED 2026-08-15: a diagnostic always; a gate only where a workspace configures one, unset by default.** | Hard gate: prevents a class of overfitted models reaching approval, and will block legitimate large models on large books. Warning: relies on the Approver noticing. | **Maintainer accepted the recommendation.** Every fit records factor count, fitted-parameter count and the exposure- and claims-per-parameter ratios in its diagnostics, beside whatever thresholds are in force. `modelling.max_factor_count` and `modelling.min_exposure_per_parameter` are workspace settings (`07` FR-PLAT-45) that are unset by default; where set, spec validation refuses with `MODEL_SPEC_EXCEEDS_COMPLEXITY_LIMIT` before compute is spent, and the refusal is audited (**FR-MODEL-81**). No platform-wide constant: a large book supports a large model, and whether *this* model is overfitted is a judgement for the Approver holding the diagnostic (`06`) — governance belongs in approval, not in a number chosen in a spec. | maintainer | **decided** |
| ~~**OQ-MODEL-17**~~ ✔ | ~~On a rebuild (`should_fit=False`), `model.transparency` pays a full GLM fit plus a full type-III diagnostics pass — one refit per factor — for numbers it then discards, because the surrogate Model already exists. Should the Job skip that compute and reuse the surrogate's already-fitted numbers instead?~~ **DECIDED 2026-08-21: skip the compute on `should_fit=False` and reuse the stored numbers — FR-MODEL-110.** | Found 2026-08-19 in the final whole-branch review of FR-MODEL-96 (fix round), reading `backend/src/app/worker/model_handlers.py`'s `_transparency` handler: `build_glm_approximation` and `compute_diagnostics` run unconditionally, before `model_service.reserve_model` reports whether `should_fit` is `True`, so a rebuild against an unchanged spec pays for a full GLM fit (`glum`) and a full type-III diagnostics sweep — one refit per factor, FR-MODEL-51 — and then discards both: `store()` only ever persists them when `should_fit` is `True` (FR-MODEL-66). It is correct, not slow by accident: a comment at the call site notes the fit is deterministic under `spec.seed`, which `approximation_spec` carries over from the GBM, so the discarded numbers agree with the ones the first build already stored. Nobody has costed how large that discard is. **Skip the compute when the surrogate is already fitted, reusing its stored numbers:** `reserve_model` already returns `should_fit`, so the handler could branch on it before fitting and, on `False`, load the existing surrogate's `Diagnostics` and the latest `TransparencyArtifact`'s `glm_approximation` block (`r_squared`, `deviance_explained`, `worst_regions`) instead of recomputing them — cheapest, but only correct because the source Model and the surrogate's spec are both immutable once fitted; it would stop being self-evidently current if that ever changed. **Keep recomputing, for a fresh fidelity measurement every time:** today's behaviour, and the one honest argument for it — FR-MODEL-36 wants the fidelity statement to describe the model as fitted, and a cached number describes a past build event rather than the model. Costs a full fit and a full type-III pass on every rebuild regardless of whether anything could have changed. **Make it conditional — skip only when nothing that could change the numbers has changed:** correct in principle, but needs its own invariant proving nothing between builds could disagree, which is the kind of condition that erodes quietly. | **Maintainer decided 2026-08-21, at the recommendation.** The handler branches on `reserve_model`'s `should_fit` before `build_glm_approximation` and `compute_diagnostics`; on `False` it loads the surrogate's stored `Diagnostics` and the latest `TransparencyArtifact`'s `glm_approximation` block instead of paying a full GLM fit plus one type-III refit per factor for numbers it then discards. No new invariant is needed: the source Model and the surrogate's own spec are immutable once fitted, so `spec_hash` (FR-MODEL-66) already guarantees the numbers a rebuild would recompute are identical to the ones stored at the first build — and FR-MODEL-36's fidelity argument for recomputing only bites if the source model could change between builds, which it cannot. **Owner: Phase 1b, and the trigger is its measurement of the transparency Job's cost against `07`'s job-latency NFRs**, before which this is the kind of cost a measurement would attribute to the wrong cause. | maintainer | **decided** (in spec) |
| **OQ-MODEL-7** | ~~How are protected-characteristic proxies detected, and what happens when detection fires?~~ **DECIDED 2026-08-15: the `prohibited` flag is the whole of Phases 1–2; a Phase 3 proxy assessment produces evidence, never a block.** | Needs (a) a statistical test — e.g. mutual information or AUC of the factor predicting a protected characteristic on a reference population — and (b) a policy. We do not hold protected characteristics, so any test needs an external reference dataset the insurer supplies. | **Maintainer accepted the recommendation.** FR-MODEL-5's `prohibited` flag stands as the only treatment through Phases 1–2. From Phase 3, an optional proxy assessment consumes an insurer-supplied reference table, measures each candidate Factor's association with the characteristic (mutual information and exposure-weighted AUC, with the reference population and its date recorded), and attaches the result to the approval request (**FR-MODEL-82**). It never refuses a Factor: whether an association amounts to unlawful discrimination is a legal judgement, and a platform that answered it automatically would be answering a different question — the same reasoning `04` FR-OPT-24 applies to price change. **The decision is made now; the delivery is a Phase 3 deliverable** (`CLAUDE.md` §0 — a later phase's capability is a spec change, not code). | maintainer | **decided** (delivery in Phase 3) |
| ~~**OQ-MODEL-18**~~ ✔ | ~~A Custom Metric never gets differentiated, so §4.7's `ObjectiveCertificate` battery includes checks — `analytic_vs_numeric_gradient`/`hessian`, `convexity`, `branch_discontinuity` — a metric has no gradient or hessian to run them against. Should a Custom Metric's certificate run the full nine-check battery anyway (each derivative check reporting `not_applicable`), or a reduced, metric-specific set?~~ **DECIDED 2026-08-19: a reduced certificate, sharing §4.7's check vocabulary rather than its check list.** | Raised 2026-08-19 in the custom-metrics decision gate, on FR-MODEL-42's argument extended to metrics: a metric that early-stops a fit changes when boosting halts and therefore changes the model, so it needs evidence before submission the same way an objective does. **Full nine-check battery, unused checks marked `not_applicable`:** one `CheckStatus` enum and one certificate shape for both artifacts, but four of nine checks would read `not_applicable` on every version ever certified — a status that exists to answer "not askable here" is not the same as a check nobody wrote, and a reader learns nothing from a field that is always the same value. **A reduced, metric-only check set (`finiteness`, `direction_holds`, `scale_behaviour`, `smoke_evaluation`), sharing `CheckStatus`, `SamplingSpec`, `CertificateOutcome` and `CertificateResult.outcome_of` with §4.7 rather than restating them:** no `not_applicable` padding, and `direction_holds` is a check an objective certificate has no use for (an objective has no declared direction to hold), so the two lists were never going to be the same list. **No certificate at all**, on the argument that a metric does not change fitted coefficients: rejected outright — early stopping decides *which* round's coefficients are kept, which is exactly the lever FR-MODEL-42 already treats as consequential. | **Maintainer accepted the reduced-certificate recommendation.** Specified as **FR-MODEL-105** and §4.7's `MetricCertificate` addendum: four checks (`finiteness`, `direction_holds`, `scale_behaviour`, `smoke_evaluation`), the derivative and convexity checks absent rather than `not_applicable`, and the check vocabulary — not the check list — shared with `ObjectiveCertificate` unchanged. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-19**~~ ✔ | ~~Does a Custom Metric define its own value computation — a metric-specific template catalogue, or its own `expression` grammar mirroring FR-MODEL-40 — or does it name an existing `ObjectiveTemplate` (§4.5) and reuse that template's loss as its value?~~ **DECIDED 2026-08-19: a metric names an `ObjectiveTemplate` and reuses its loss, evaluated as an exposure-weighted mean.** | Raised 2026-08-19 in the custom-metrics decision gate. A metric's *value* is a per-observation loss aggregated over a partition — the same shape an objective's loss already has — so the question is whether that shape gets defined twice. **A metric-specific template catalogue, parallel to §4.5:** guarantees a metric can express anything a metric author wants, independent of what objectives ship, but duplicates every template `pricing-core` already implements and certifies, and a `capped_gamma` metric and a `capped_gamma` objective would drift the moment one catalogue is edited without the other. **An `expression`-only path for metrics:** the same AST grammar (§4.6) reused for values instead of gradients, but Phase 1 has not shipped `expression` objectives (OQ-MODEL-1) and a metric-only exception to that gate is a second thing to keep off. **Name an `ObjectiveTemplate` and its parameters, reusing the template's loss as the metric's value (exposure-weighted mean):** no second catalogue, no second grammar, and the loss is the one FR-MODEL-39's analytic gradient/hessian were already certified against — a metric inherits that certification history rather than starting its own. | **Maintainer accepted the recommendation.** Specified as **FR-MODEL-103**, on OQ-MODEL-1's Phase-1-templates-only rule: in Phase 1 a Custom Metric is templates-only for the same reason an objective is, and §4.13's `template`/`params` fields are drawn from §4.5's catalogue rather than a metric-only vocabulary. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-20**~~ ✔ | ~~§5.1 declared one `POST /custom-metrics` row and left it "not built, deferred to Phase 1b" (FR-MODEL-45). Now that a metric gates early stopping (FR-MODEL-107), should this slice ship create only, or the full six-endpoint set FR-MODEL-95 built for `custom-objectives` — read, certify, read-certificate, submit, usage?~~ **DECIDED 2026-08-19: all six.** | Raised 2026-08-19 in the custom-metrics decision gate. FR-MODEL-95 was itself a correction: §5.1 originally declared five `custom-objectives` write endpoints and no way to read what they wrote, and an Approver who could not fetch a certificate was being asked to approve a verdict they could not see. **Create only, deferring the rest:** repeats exactly the gap FR-MODEL-95 closed, this time known in advance rather than found by a later audit — certification is a 202 job, so a caller with only `POST /custom-metrics` has no way to learn whether a metric it created ever certified. **All six, mirroring `custom-objectives`' shape (`FR-MODEL-38/42/46/47/95`) exactly:** a metric now sits on the fitting path's critical edge — it decides when boosting stops — so it needs the same governance visibility an objective has, not a lesser version of it because it shipped later. | **Maintainer accepted the recommendation.** Specified as **FR-MODEL-108** and §5.1's six `/api/v1/custom-metrics*` rows, replacing the row that read "not built, see the amendment below"; the amendment itself is struck through with a dated note (`CLAUDE.md` §0's rule for superseded text) rather than deleted. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-21**~~ ✔ | ~~LightGBM evaluates its builtin metrics before `feval`. When a Model Spec declares a builtin in `eval_metrics` *and* early-stops on a Custom Metric, `_fit_lightgbm` sets `params["metric"] = "None"` and the declared builtin is silently dropped — accepted by the spec, never evaluated, and nothing on the returned `GbmFit` tells the caller; XGBoost reports both. Does a *documented* silent drop satisfy FR-MODEL-106's requirement that `eval_metrics` be honoured?~~ **DECIDED 2026-08-21: record the drop on the fit — FR-MODEL-111.** | Found 2026-08-20 in the final review of the custom-metrics branch (FR-MODEL-45), immediately before merge. Already tested (`test_lightgbm_drops_a_builtin_eval_metric_rather_than_stop_on_it`, `packages/pricing-core/tests/test_gbm.py`) and named in FR-MODEL-107's 2026-08-20 amendment, so the asymmetry is documented rather than hidden — but FR-MODEL-106's own parenthetical describes the defect it was raised against as "a spec accepted, silently ignored, and reported to the caller as configured," which is close to what still happens here in one narrow combination. **Refuse the combination**, with a named `GbmFitError` at spec-validation or fit time: closest to FR-MODEL-106's intent — the caller learns immediately rather than receiving a curve missing a metric it asked for — but costs a refusal for a spec that would otherwise fit fine on XGBoost. **Record the drop on the fit result** — a field or note on `GbmFit` naming which declared metrics were not evaluated and why: the spec is still accepted, and the caller is told, at the cost of a new shape on the contract. **Leave it as-is**, on the grounds that it is tested and documented in FR-MODEL-107: costs nothing, but leaves the caller uninformed at runtime unless it consults the spec. A fourth option — reimplementing LightGBM's builtin in Python as an `feval` entry so both are reported — was considered and rejected in review: it would emit a curve labelled e.g. `rmse` that is not LightGBM's own `rmse`, and FR-MODEL-106 requires builtin names to be "passed to the backend's own metric vocabulary." | **Maintainer decided 2026-08-21, at the recommendation.** `GbmFit` and the persisted `GbmFitResult` gain `dropped_eval_metrics`, naming each declared metric the backend could not evaluate and why — the one reason that exists today: the backend evaluates builtin metrics before the stopping metric (FR-MODEL-107). The spec stays accepted: FR-MODEL-106's objection is to a spec "reported to the caller as configured" when it was not, and telling the caller is the cheapest honest fix — refusing punishes a portable spec for one backend's evaluation ordering, and leaving it silent is the condition FR-MODEL-106 exists to end. FR-MODEL-107 gains a dated addendum. **Owner: W5**, as the custom-metrics slice record already assigns, delivered before W5 closes. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-22**~~ ✔ | ~~Which offsets-from-model come after the GLM-to-GLM slice? FR-MODEL-24's 2026-08-21 amendment builds offset-from-another-model for GLM specs referencing fitted GLMs only.~~ **DECIDED 2026-08-21: (a) then (c), each as its own slice; (d) only if residual modelling stays GLM-shaped — FR-MODEL-112.** | (a) extend to GBM-referenced offsets — the referenced raw score minus its own `base_margin`, as η on the link scale — when a workflow needs it; (b) extend to `GbmSpec` declaring the offset (base_margin from η); (c) wire the peril-reconciliation scoring path to the resolver; (d) leave GLM-to-GLM as the whole capability. | **Maintainer decided 2026-08-21, at the recommendation.** (a) GBM-referenced offsets — the referenced raw score minus its own `base_margin`, as η on the link scale — are the next slice, when a workflow needs one, in Phase 1b. (c) The peril-reconciliation scoring path is then wired to the resolver; its owner is already W5 per the offsets slice's "not delivered, with owners" list. (b) A `GbmSpec` declaring the offset itself is **not scheduled**. (d) If residual modelling stays GLM-shaped in practice, GLM-to-GLM remains the whole capability rather than any of this. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-23**~~ ✔ | ~~**`spline`, `polynomial` and `offset` Factors are refused by name with no owner and no schedule. Which of the three, if any, does the platform commit to — and when?** FR-MODEL-88 contains them correctly (a refusal beats a raw column silently substituted) but containment is not a plan, and W5 closes with three arms of FR-MODEL-1's closed set counted among the evidenced because a test marks the refusal.~~ **DECIDED 2026-08-22: none of the three is committed to a slice, and they no longer share a verdict.** `offset` superseded — FR-MODEL-114. `spline` and `polynomial` stay declared and refused, gated on FR-MODEL-115, owner W30. | **(a) Commit to all three in Phase 2, one slice.** Each needs its own `Factor` contract field and a `resolve_factors` argument, so they share a shape and splitting them triples the contract churn. `offset` is the odd one — an offset is already expressible on a `GlmSpec` (FR-MODEL-24), so an `offset` *Factor* may be a duplicate mechanism rather than a missing one, and the slice would have to say which. **(b) Commit to `spline` only, supersede `polynomial` and `offset`.** A spline is what a UK pricing actuary actually asks for on a continuous rating variable; a polynomial is a worse answer to the same question, and `offset` is FR-MODEL-24's job. Smallest honest scope, at the cost of narrowing a set FR-MODEL-1 declared closed in Phase 0. **(c) Supersede all three and narrow FR-MODEL-1's set to five.** The refusals become permanent and the enum shrinks to what the platform means. Cheapest, and it forecloses a capability on no evidence about demand. **(d) Leave as is.** Rejected on its face: this is the silence §13 rule 1 forbids, and naming it here is the point of the question. | **Maintainer decided 2026-08-22 — not at the recommendation, which is refuted in three checkable places.** The decision: **`offset` superseded (FR-MODEL-114); `spline` and `polynomial` both left declared, both left refused, both gated on FR-MODEL-115, owner W30.** *(a)* rejected — it prices a spline at a polynomial's contract cost. *(b)* **rejected as written**, because its three supporting claims do not survive checking. "Same contract cost" is false: a polynomial needs `degree`, while a spline needs basis family, degree, interior and boundary knots, the placement method and its params, the derivation Dataset Version and per-knot evidence — if it is to be governed to the standard FR-MODEL-9, FR-MODEL-10, FR-MODEL-83 and FR-MODEL-85 hold a band boundary to. "Strictly worse" is false in the dimension this platform is built around: §4.6's grammar carries exponentiation and `03`'s expression step inherits it (FR-RATE-28), so a fitted quadratic is a closed-form rating step a reviewer reads in one line, whereas a cubic B-spline basis is not expressible in that grammar at all and must be tabulated back into a banded rate table (FR-RATE-8) — so the steps the recommendation called an artefact come back at the rating layer, now chosen by a machine and evidenced by nothing. And the supersession test it applies to `offset` applies at least as forcefully to `polynomial`: `derive_expression` already produces squared and truncated-power basis columns today (FR-DATA-9), so the capability exists — what a Factor-level basis adds is versioning, evidence and monotonic rationale as Factor properties, which is the real argument and the recommendation never makes it. *(c)* rejected — it forecloses a capability on no evidence, and the enum arms cost nothing to keep. *(d)* rejected on its face, as the question said. **The finding that decides it was in none of the four options and sits one layer down:** FR-MODEL-21 promises a relativity table for every *categorical* Factor and FR-RATE-16 seeds a rate table from that table, so **no continuous Factor can be rated or reviewed today** — including the `identity`-over-numeric one that already resolves. Committing either basis arm before that closes would ship a Factor an actuary can fit and cannot price. FR-MODEL-97, decided four days earlier, refuses a continuous product term because the rating DAG is a graph of tables and a varying slope has no cell; a spline main effect *is* a varying slope, so committing `spline` while that refusal stands would contradict it inside one spec. **`polynomial` is not left deferred without an owner** — "let the first real request decide" names no party and is the silence §13 rule 1 forbids; both arms are gated on a requirement that has an owner and a phase. One thing this decision deliberately does **not** settle, because it is a different enum with a different remedy: `FactorIntent.OFFSET` is live, selectable and consumed by nothing — OQ-MODEL-25. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-24**~~ ✔ | ~~**NFR-MODEL-4's "diagnostics ≤ 30 % of fit wall-clock" and FR-MODEL-51's type-III likelihood-ratio tests cannot both hold as written. Which moves?** Measured 2026-08-22: diagnostics cost **510 %** of fit wall-clock at 12 factors and **1 388 %** at 24, and **3 002 %** on the GBM path. Everything `compute_diagnostics` does *except* type-III fits the budget, at 9.0–9.5 %. The arithmetic is not a slow function: type-III **drops each factor and refits**, so a diagnostic containing *F* refits of the model cannot cost 30 % of one fit for any *F* above zero — and FR-MODEL-49 requires diagnostics computed once at fit time, so there is no later pass to move them to. A second question rides along: **which denominator the budget means**, since `fit_seconds` (the solve) excludes the factor resolution and design-matrix construction the caller also waits for, and the no-type-III figure is 32.1 % against wall-clock but 55.2 % against `fit_seconds`.~~ **DECIDED 2026-08-22: option (a), denominator fit wall-clock.** NFR-MODEL-4 re-scoped and re-set to 50 % at a named scale; type-III becomes NFR-MODEL-13, the GBM path NFR-MODEL-14. | **(a) Scope NFR-MODEL-4 to diagnostics excluding type-III, and give the type-III block its own budget.** Honest about what the 30 % was ever measuring — it fits everything else at 9.0–9.5 % — and keeps FR-MODEL-49's compute-once property intact. Costs a second number to maintain, and a reader must know both. **(b) Make type-III a separate Job the caller asks for.** The fit returns quickly, significance arrives on request. Costs FR-MODEL-49's "computed once at fit time, read thereafter" for this one block, which is a real weakening: a diagnostic a caller has to remember to ask for is one a model document can be assembled without. **(c) Replace the refit with a score or Wald approximation.** One fit total, budget met as written. But it answers a *slightly different question* — and FR-MODEL-51 chose the likelihood-ratio test deliberately, so this is a change to what the platform reports, not to how fast it reports it. **(d) Raise the budget to fit the measurement.** Rejected on its face: a budget rewritten to whatever was measured is not a budget, and NFR-MODEL-4 exists to stop diagnostics quietly dominating a fit. | **Maintainer decided 2026-08-22: option (a), denominator settled as fit wall-clock** — at the recommendation on the option, past it on every number. *(a)* **adopted**: it is the only option that changes no observable behaviour and loses no information. *(b)* rejected — it costs FR-MODEL-49's compute-once property for the one block a model document most needs. *(c)* rejected — it changes what the platform reports rather than how fast it reports it. *(d)* rejected on its face. The recommendation named no number, and each number it gestured at fails on the measurements it cites. **NFR-MODEL-4** is re-scoped, given the wall-clock denominator in its own text, and re-set to **50 % at or below 5 M rows × 60 factors**: the "9.0–9.5 %" that justified keeping 30 % is the two *small* arms only — the third is 32.1 % and it projects to 34.1 % at 5 M × 60, so a re-scoped 30 % would have shipped already breached. **The type-III budget is not a per-factor multiple.** 0.417× at F = 12 and 0.574× at F = 24 fit a growth law predicting 1 307 s at F = 60 against an observed > 2 400 s: two points cannot evidence an exponent, and the third falsifies the one they give. The rise is an artefact of a cold bench denominator carrying 41–56 % one-time cost; corrected, the multiples are 0.95× and 0.98× — flat at ≈ 1.0, which is exactly what the mechanism predicts, one full `fit_glm` plus one full `predict_glm` per factor. **NFR-MODEL-13** therefore bounds **one fit wall-clock per *tested* factor** — elements of `type_iii_tests`, not of `factors`, since interaction operands and failed refits are skipped. **NFR-MODEL-14** prices the GBM path per **scoring pass** rather than per factor: its 3 002 % is roughly 625 full-population passes of which 560 are partial dependence, so the driver is grid points, and a per-factor figure would mean nothing outside that exact factor mix. Two things this exposed and did **not** fix, each recorded rather than tuned away: the sweep is uncapped and covers every factor where FR-MODEL-52 says declared ones (OQ-MODEL-26), and NFR-MODEL-13 is breached at 678 013 × 60 with the observation censored, owned by Phase 1b together with the warm-denominator run the corrected multiples rest on. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-25**~~ ✔ | ~~**`FactorIntent.OFFSET` is live, selectable and consumed by nothing — a Factor an actuary declares as an offset is fitted with a free coefficient.** FR-MODEL-3 declares the intent; `rateable()` tests `risk` only, and neither the GLM nor the GBM fit path reads intent at all. The declaration is accepted through the API and changes nothing about the fit — a silent mis-fit rather than a refusal. Surfaced 2026-08-22 while deciding OQ-MODEL-23.~~ **DECIDED 2026-08-22: (b) then (c) in shape, but on grounds the recommendation does not give, and applied to two arms rather than one.** `offset` superseded as an intent — FR-MODEL-116. `diagnostic` refused pending OQ-MODEL-27 — FR-MODEL-117. FR-MODEL-3 amended to state what every arm means at fit time. | **(a) Honour it**: the factor contributes its column to the linear predictor with its coefficient fixed at 1, which is what declaring an offset means. Correct, and the largest change — it needs a design-matrix path that GLM and GBM both respect. **(b) Refuse it by name** at fit time, so the declaration fails loudly rather than doing nothing. Smallest change and it stops the mis-fit today, but it leaves FR-MODEL-3 declaring an intent no mechanism honours. **(c) Supersede `FactorIntent.OFFSET`** as FR-MODEL-114 supersedes the type, leaving `OffsetSpec` the single way to declare an offset. **(d) Leave it** — rejected on its face: a silent mis-fit is the one outcome none of the others produce. | **Maintainer decided 2026-08-22 — at the recommendation's *shape* and not its *reasons*, and the question is understated in two checkable places.** The decision: **`offset` superseded as an intent (FR-MODEL-116), `diagnostic` refused pending OQ-MODEL-27 (FR-MODEL-117), FR-MODEL-3 amended to gloss all four arms at fit time.** *(a)* rejected on cost against demand, as recommended, and the recommendation is right that nothing asks for it — but not for the reason it gives. *(c)* **adopted, and its stated argument refuted.** The recommendation calls the intent arm "the duplicate mechanism FR-MODEL-114 removed from the type enum, left half-removed"; the two are in fact **incomparable — neither subsumes the other**. `OffsetSpec`'s `column` is a single **raw dataset column** validated against the version's schema, so it carries neither a banded or grouped offset column nor two offset terms at once, and an offset column gets none of the versioning, lineage or prohibition a Factor carries; a `Factor`, forbidding extra fields and holding no model reference, cannot express `OffsetSpec`'s `model` kind at all. A duplicate mechanism does the same work twice, and these two each do work the other cannot — so superseding on a duplication claim would have recorded a reason later discoverable as false. **The argument that does hold is a layer argument**: offsetness is a property of one fit, `Factor.intent` a property of a Factor reused across every spec that names it, so a Factor marked `offset` could never carry a free coefficient in a second model. *(b)* **adopted as the mechanism, not as an interim** — the refusal is permanent and is sited in `resolve_factors` beside FR-MODEL-88's type refusals, which answers the "where would it live" objection: one place, reached by every fit, predict, diagnostics and transparency path. *(d)* rejected on its face, as the question said. **Two findings sit outside all four options.** First, "consumed by nothing" is the wrong shape of overstatement. `FactorIntent` is read in exactly **two** places, and neither touches a design matrix: `rateable()`, which no production code calls yet because `03` is unbuilt, and the `factor.created` audit event, which records the declared intent without gating on it. So the platform **audits** a declaration that changes nothing — a governance trail attesting to a property the fit never had, which is worse than a value merely going unread. No arm changes a fit, and `risk`/`control` are correct only because *being fitted freely is what they mean*. Second, **`diagnostic` carries the identical defect** and the question names only `offset`; refusing one and leaving the other live would have fixed half a defect of one shape. `diagnostic` is **not** superseded with it, because the two fail differently — `offset` names a mechanism that exists at another layer, `diagnostic` one that exists nowhere — and FR-MODEL-3 gives it no gloss, so deciding what it means inside a refusal would be the silent resolution §10 forbids. That is OQ-MODEL-27. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-26**~~ ✔ | ~~**The GBM partial-dependence sweep caps nothing and covers every factor, where FR-MODEL-52 says declared ones. What bounds it?** Partial dependence runs one full-population scoring pass per grid point — ten quantiles for a numeric factor, but **every distinct level** for a categorical. At the 10 000 levels NFR-MODEL-3 itself names, a single column costs 10 000 passes, roughly 1.9 hours, which a per-factor budget records as one factor. Raised 2026-08-22 from OQ-MODEL-24's measurement, which attributed 90 % of the GBM diagnostics cost to this sweep.~~ **DECIDED 2026-08-22: (a) in substance with its mechanism replaced — the grid is capped at the 20 most-exposed levels and truncates rather than pools (FR-MODEL-118); (b) withdrawn as a no-op.** A live `IndexError` found in the same two functions is FR-MODEL-119 and OQ-MODEL-28. | **(a) Cap the categorical grid** at the top N levels by exposure, pooling the remainder and recording the pooling as evidence. Bounds the cost and keeps the shape readable; the pooled bar is a summary an actuary must be told about. **(b) Honour FR-MODEL-52's "declared factors"** so the sweep covers what the caller asked for rather than everything. Cheapest, and it is what the spec already says. **(c) Bound the budget instead**: state that NFR-MODEL-14 does not hold above a named level count. Honest, and it documents the cliff without removing it. **(d) Leave it uncapped** — the status quo, which makes a 60-factor diagnostics run unbounded in the level count of its worst column. | **Maintainer decided 2026-08-22.** The decision: **the categorical grid is bounded to the 20 most-exposed levels, the omission recorded on `PartialDependence` (FR-MODEL-118); the "covers every factor" half is withdrawn; NFR-MODEL-14's interim note stands until the cap ships.** *(b)* **withdrawn — it is a no-op, and this is the finding that retires half the question.** FR-MODEL-52 does say "declared factors", but the sweep already covers exactly those: the worker resolves `load_factors(factor_ids=list(spec.factors))` and hands that list straight to `compute_gbm_diagnostics`, so "every factor" and "the declared factors" are the same set and there was never a gap on that half. *(a)* **adopted in substance and refuted in mechanism.** "Pooling the remainder" is not implementable: a partial dependence bar requires the column to be *held at a value and scored*, and a synthetic "other" is a level the fitted model never saw, which `_encode` refuses with `UNSEEN_LEVEL_BEHAVIOUR_REQUIRED` precisely because FR-MODEL-32 forbids inventing a code for an unseen level. There is no value the column can be held at to represent the pooled levels, so the cap **truncates and names what it dropped** — count, exposure held, and reason — rather than pooling. Recording it is a **contract change**, which the recommendation does not mention: `PartialDependence` is frozen with `extra="forbid"` and had no field that could carry the statement. *(c)* kept as written — NFR-MODEL-14 already carries the note, so it needs no edit beyond a pointer. *(d)* rejected. **The cap is 20 because the measurement says so, not because a chart looks better**: NFR-MODEL-14 measured 0.0480 fits per scoring pass, so 20 passes is 0.96 of one fit, which puts the GBM per-factor block at the same order as NFR-MODEL-13's one-fit budget for the GLM's, and leaves a numeric factor's ten quantile points already inside it. The default **binds**, unlike `modelling.max_factor_count`, and the asymmetry is deliberate: that setting has no platform-wide constant because a large book legitimately supports a large model, whereas this cap bounds a *chart* and changes no fitted coefficient. **A defect outside the question, and larger than it first looked:** both functions open with `factor.source_columns[0]` while `Factor` requires an `interaction` to name none — but that `IndexError` was **masked by an earlier one**. `resolve_factors` requires a cross's operands to be supplied and gives them no term of their own (FR-MODEL-91); `fit_glm` iterates the resolved terms and never sees them, while the GBM encoder iterated the *factor list* and raised `KeyError` on the first operand. So **no GBM could fit an interaction at all** between 2026-08-18 and 2026-08-22 — one line of difference between two sibling paths, unseen because only the GLM suite ever fitted a cross. FR-MODEL-119 and OQ-MODEL-28. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-27**~~ ✔ | ~~**What does `FactorIntent.DIAGNOSTIC` mean?** FR-MODEL-3 lists the arm and glosses only `risk` and `control`, so it has never had a stated meaning at fit or rating time; FR-MODEL-117 refuses it in the meantime. Raised 2026-08-22 out of OQ-MODEL-25, whose remedy could not be extended to it without inventing the meaning the spec omits.~~ **DECIDED 2026-08-22: (c), superseded — FR-MODEL-120** — and reached without inventing the missing meaning, because both candidate meanings fail. FR-MODEL-117's stated ground for holding the question open is corrected in place. | **(a) Resolved and reported but held out of the linear predictor** — the reading the word suggests, and the only one that makes the arm distinct. Needs a design-matrix path that both fit paths respect, which is what OQ-MODEL-25 rejected for `offset` on cost. **(b) A second name for `control`** — fitted, never rateable. Costs nothing and makes the arm redundant, which is an argument for (c). **(c) Supersede it**, as FR-MODEL-116 supersedes `offset`, leaving a two-arm enum that says exactly what it means. **(d) Leave it refused indefinitely** — rejected on its face: FR-MODEL-117 is explicitly an interim, and a refusal with no owner is the silence §13 rule 1 forbids. | **Maintainer decided 2026-08-22 — at the recommendation's option and its layer argument, past its opening claim, and the arm is superseded without the meaning ever being invented.** The decision: **`diagnostic` superseded as a Factor intent (FR-MODEL-120), the capability it named re-sited on the Model Spec and gated with an owner, FR-MODEL-117 corrected in place.** *(b)* **rejected, and the recommendation's first sentence with it.** "The enum exists to answer one question — may this be rated on?" is false: it answers **two**, and the second is whether the term enters the linear predictor at all. `risk` is both, `control` is fitted-but-never-rateable, and *(a)*'s reading — resolved and reported, held out of the predictor — is a third distinct cell of that pair, not a synonym for `control`. Had the enum answered only one question, *(c)* would have been trivial and this question would not have needed asking. *(a)* **is a genuine capability and is not superseded with the arm** — only its siting is. *(c)* **adopted on the layer argument**, which the recommendation states correctly: hold-out-ness is a property of one fit, `Factor.intent` a property of a Factor reused by every spec that names it, so a Factor marked `diagnostic` could never be fitted in a second model. *(d)* rejected on its face, as the question said. **The finding that lets the arm be superseded without FR-MODEL-3's missing gloss being supplied**: it is not necessary to choose the meaning, because **both candidate meanings fail** — the distinct one is mis-sited, the redundant one is `control` already — so the supersession resolves the question rather than pre-empting it, and `CLAUDE.md` §10's bar against silently picking one is met rather than skirted. **Two things sit outside the options.** First, **FR-MODEL-117's ground was wrong, and wrong against the decision that wrote it**: it declined to supersede because for an arm naming a mechanism that exists nowhere "the supersession argument has nothing to point at" — but that is the *duplication* argument, which OQ-MODEL-25 explicitly refuted and did **not** decide on; the layer argument it did decide on needs no mechanism elsewhere to point at, only a property belonging to another layer. Second, the blast radius is **measured**: `rateable()` has no production caller in the repository, the factor workbench contains the string `intent` zero times so the arm was never selectable through the UI, and `03` FR-RATE-25 — the one cross-module obligation this enum carries — names `control` alone and needs no edit. The arm stays in `FactorIntent` and in the published contract, as FR-MODEL-114 and FR-MODEL-116 leave theirs, because artifacts are immutable and a stored row must stay loadable. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-28**~~ ✔ | ~~**What should permutation importance and partial dependence report for an `interaction` Factor?** A cross sources no column of its own, so neither block has a column to permute or to hold. FR-MODEL-119 makes both skip it and record the skip, having first made a GBM able to fit a cross at all — the crash that hid this question was in the encoder, not the diagnostics. Raised 2026-08-22 while deciding OQ-MODEL-26.~~ **DECIDED 2026-08-22: (a) in substance with its mechanism restated — the cross is permuted and swept *jointly through its operands* (FR-MODEL-121) — and the second half resolved by skipping the operands (FR-MODEL-122), which turns out to fix a live crash rather than save a scoring pass.** | **(a) Operate on the cross's resolved term** — permute the combined column, sweep its cells. Treats the interaction as the single feature the design matrix says it is, and is the smallest change. The cell count is the product of the operands' level counts, so FR-MODEL-118's cap matters more here, not less. **(b) Operate on the operands jointly** — permute both source columns under one shuffle, sweep the grid of operand pairs. Closer to what an actuary reads off a two-way, and it re-introduces the operands whose main effects FR-MODEL-91 excludes as collinear with the cross. **Either way the question has a second half**: an operand carries a source column, so it is already swept and reported as though it were a model factor — at full scoring cost, for a term the booster has no column for — while the GLM's type-III block skips exactly those operands and states why. The two paths disagree today and this question owns the disagreement. **(c) Report neither for a cross**, and say so in the artifact — the interim FR-MODEL-119 ships, made permanent. Honest and leaves a reviewer with no per-factor evidence for a term that may carry a lot of the model. **(d) Leave the `IndexError`** — rejected on its face. | **Maintainer decided 2026-08-22 — at the recommendation's option, with its mechanism replaced and its dismissal of (b) refuted, and the question's second half found to be understated by a wide margin.** The decision: **the cross is measured jointly through its operands (FR-MODEL-121); the operands are skipped and the skip recorded (FR-MODEL-122).** The recommendation's premise is **confirmed and is the one thing here that checking upholds**: the booster's entire feature order for the measured fit is `('area_x_fuel',)` — one column, one set of splits, one gain figure, and no column at all for either operand. *(a)* **adopted in substance, refuted in mechanism.** There is no combined column to permute: `predict_gbm` re-resolves the cross from the operands' **raw** columns on every call, and both blocks manipulate raw columns upstream of encoding, so the only path to the cross term runs through its operands. *(b)* **is therefore not an alternative on the permutation half — it is the implementation of (a).** A shuffle applied to both operand columns **under one shared order** permutes the *pairs*, which is exactly a permutation of the resolved cross column: measured, the observed cell set is identical before and after — `{(coastal, hybrid), (rural, petrol), (urban, diesel)}` — while 67.8 % of holdout predictions move, so it is a real degradation and not a null one; checked a second way, the **encoded design matrix** a joint shuffle produces is element-wise identical to the fitted one re-indexed by the same permutation, on a dense cross and a sparse one alike. **The dismissal fails because *(b)* names two different operations under one label.** Permuting the operands jointly leaves the matrix one column wide and re-introduces nothing; *re-introducing the operands' main effects*, which *(b)*'s own clause invokes against itself, means adding columns and is a **refit** — a third operation neither *(a)* nor the joint shuffle performs, and the only one FR-MODEL-91's collinearity argument bites on. "(b) measures a model that was not fitted" is therefore **false for the shuffle**, true for the refit that *(b)* never actually proposes, and true for *(b)*'s **grid**: the Cartesian product of operand levels contains cells the fit never saw, and holding `(rural, diesel)` on a diagonal book is refused `UNSEEN_LEVEL_BEHAVIOUR_REQUIRED` where `(rural, petrol)` scores. So the grid is the cross's **observed** cells (FR-MODEL-91) — FR-MODEL-32's wall, met the second time after it killed FR-MODEL-118's pooled `other` bar. *(c)* rejected: a per-factor evidence gap for the term that may carry much of the model, now that a measurement exists. *(d)* rejected on its face. The cell-count objection resolves as the recommendation says but for a stronger reason than it gives — FR-MODEL-118 bounds the most-exposed **observed** levels, and FR-MODEL-91 already holds the observed count far below the product the objection feared: three cells of nine on the book measured here. **The finding that outweighs the question as asked, and it is in the half the question called a second one:** an operand swept alone is not a wasted pass, it is a **live crash**. A GBM whose cross is *sparse* — 3 observed cells of 9, which FR-MODEL-91 says is what a real cross looks like — fits cleanly and then dies inside `compute_gbm_diagnostics` with `UNSEEN_LEVEL_BEHAVIOUR_REQUIRED` naming all six unobserved cells, because permuting one operand alone recombines the pair into cells the fit never saw. It reaches production (`load_factors` returns `ordered + operands`) and it dies **uncoded**, the raise landing outside the block that maps a `GbmFitError` to a platform error code — the same reader-facing failure FR-MODEL-119 recorded for the bare `KeyError` and believed it had removed. It survived a second time for the reason it survived the first: the suite's only cross is drawn from two independent columns, so every cell is populated and no shuffle there can produce an unseen pair. | maintainer | **decided** (in spec) |
| ~~**OQ-MODEL-29**~~ ✔ | ~~**`fit_glm`'s `seed` keyword is read by nothing, and `02` §5.2 publishes it.** NFR-MODEL-6 asks for reproducibility under "identical `spec_hash` **+ seed**", and the parameter a caller would reach for to supply that seed is inert: `grep '\bseed\b' glm.py` returns the declaration and two `spec.seed` uses in the CV path, there is no `**kwargs`, and `GeneralizedLinearRegressor` is constructed with no `random_state`. `spec.seed` is the live one. **Six non-test callers pass the argument** — `model_handlers.py:371` (the real fit path), `diagnostics.py:504`, `transparency.py:199` and three in `scripts/bench-model.py` — and five test sites pass `seed=1` against a spec seed of `0`, a disagreement silently discarded. It is unobservable today only because every one of those five leaves `select_by` at `"fixed"`, so `_fit_cv_path` never runs and no RNG is consulted. **The repository already documents the fact, in the wrong file**: `fit_ebm` carries the identical dead parameter and its docstring (`ebm.py:111-114`) calls it "`fit_glm`'s vestigial kwarg for call-site symmetry … deliberately **not** the reproducibility source", while `glm.py` and `02` §5.2 say nothing. Raised 2026-08-22 by the closure slice while evidencing NFR-MODEL-6's GLM half.~~ **DECIDED 2026-08-22: option (b) — the parameter is removed from `fit_glm` and `fit_ebm` and from `02` §5.2; `spec.seed` is the single seed for every fitter. Specified as FR-MODEL-123.** | **(a) Document it as vestigial**, in `glm.py`'s docstring and beside the §5.2 signature, matching what `fit_ebm` already says. Cheapest, truthful, and changes no call site — but it leaves a parameter whose only purpose is to be ignored, and the next reader still has to be told. **(b) Remove it** from `fit_glm` and `fit_ebm`, and from §5.2. Removes the trap at the source and makes `spec.seed` the single seed, matching `fit_gbm`, which never had the parameter and reads `spec.seed` directly. It is a **breaking change to a published signature** across nineteen call sites, six of them outside tests. **(c) Honour it** — let an explicit `seed=` override `spec.seed` for the fit. Makes the published contract true, and is the only option that gives NFR-MODEL-6's "+ seed" clause an instrument. But it creates a second seed that is *not* in `spec_hash`, so two fits with identical digests could differ — which is the precise thing NFR-MODEL-6 exists to forbid, and `GlmCvSpec`'s docstring already refuses on that ground. **(d) Leave it** — the status quo, in which the contract advertises a knob that does nothing. | **(b), with (a) as the interim.** (c) is self-defeating: a seed outside `spec_hash` breaks the requirement that motivates the question, and `GlmCvSpec` has already rejected exactly that duplication. (d) is what §0 forbids — the code has proven the published signature wrong, and quietly leaving them disagreeing destroys the record of which was believed. Between (a) and (b): the parameter has no reading under which it is correct, so (b) is the resolution and (a) is the holding position until a slice owns the nineteen call sites. **Severity is latent, not live** — nothing is mispriced today; the first caller to pass a `seed` differing from `spec.seed` *and* enable CV gets a silently different fit, and the published signature invites precisely that. Not fixed in the closure slice because nineteen call sites and a §5.2 contract change are not a test-marker slice's to take. **Maintainer decided 2026-08-22 — (b), as recommended.** Twenty call sites updated, four of them outside tests including the platform's own fit path; `fit_gbm` needed none, having never taken the parameter. The interim (a) was not taken: a parameter whose only correct behaviour is to be ignored is worth removing once rather than documenting forever. Pinned by a negative test asserting `seed=` now raises `TypeError`, because a deleted parameter and an ignored one are indistinguishable to a caller until one of them raises. | maintainer | **decided** (in spec) |
| **OQ-MODEL-30** | ~~`objective-certificate.result.checks` publishes `minItems: 8`; `CertificateResult` requires `1`. Neither number is right, and the shared type is why.~~ **DECIDED 2026-08-23: option (a), specified as `02` FR-MODEL-126.** Found 2026-08-22 by W32-1's scalar-constraint guard, on its first run. | `02` §4.7's dated 2026-08-18 amendment says **"All nine checks are emitted for every template, always"**, and the authored contract's own `$comment` from that same amendment calls `branch_discontinuity` a **ninth** named check (FR-MODEL-69) and adds it to the `name` enum — while leaving `minItems: 8` untouched. So **8 is a stale pre-amendment count, contradicted by the file it sits in** (nine enum members, a floor of eight). But the model's `1` cannot simply become `9`: `CertificateResult` is **shared** — `objectives.py:691` for `ObjectiveCertificate` and `metrics.py:213` for `MetricCertificate` — and FR-MODEL-105 gives a metric certificate **four** checks, reusing §4.7's vocabulary unchanged. `min_length=9` on the shared type would refuse every metric certificate the spec requires. **(a)** Per-artifact obligation: leave `CertificateResult` unbounded, enforce nine on `ObjectiveCertificate` and four on `MetricCertificate` where each is constructed, and correct the contract to `minItems: 9`. **(b)** Split the shared type into two. **(c)** Leave the model loose and correct only the contract to `9`, accepting that the floor is published and unenforced. | **Decided 2026-08-23 on the maintainer's instruction: (a), the recommendation.** The obligation belongs to the artifact, not to the container the two artifacts happen to share — nine checks for an objective certificate, four for a metric certificate, `CertificateResult` unbounded — and (b) would duplicate a shape `CLAUDE.md` §2 says must exist once. `minItems: 8` is wrong on every reading and becomes `9`. One thing the options list did not say, now in FR-MODEL-126: a battery that comes up short is a **failed run**, not a smaller certificate, because nine checks are what the approver is told they are reading. **Specification only — the code, the contract and the carve-out are a slice.** Correcting `minItems` before the `min_length` narrowings land would fail `test_the_escalated_constraint_disagreements_are_still_unresolved`, which is exactly what that test is for: the carve-out `UNRESOLVED_CONSTRAINT_DISAGREEMENTS["objective-certificate"]` must be removed in the same commit that makes the two sides agree. Owner: **W32**, and it is the same slice as backlog item 8 (`02` §5.3 called the four `CheckStatus` values "pass/warn/fail"), now corrected in the cell rather than only in the note beneath it. | maintainer | **decided** |
| **OQ-MODEL-31** | ~~**What evidence stands beside an interaction candidate?**~~ **DECIDED 2026-08-23: option (b) — a holdout strength ratio, specified as `02` FR-MODEL-128.** FR-MODEL-79 and OQ-MODEL-4 both promise "exposure share and holdout lift — the two numbers that say whether a suggestion is worth taking and over how much of the book". Neither is real. `ShapInteraction.exposure_share` is written as the literal `1.0` and defaults to `1.0` on the type, and it cannot be anything else: every row has a value for both features, so a pair's observed cells cross to the whole book. Holdout lift appears in no section of any spec, on no shape and in no computation. Raised 2026-08-23 by the W6b slice-map backlog (item 3). | The disanalogy with the shares FR-MODEL-125 corrected is structural: a partial-dependence point and a worst region are *per level*, so their shares partition exposure and a share below one means something; a candidate pair is a single object spanning the entire frame. So the **"over how much of the book" half of the promise is not answerable per pair at all** and is withdrawn rather than redefined. What remains open is *worth taking*, against the `strength` the ranker already computes — the mean absolute sum of the pair's off-diagonal TreeSHAP entries, on a capped in-sample slice. **(a) Strength alone.** Truthful and free; an actuary sees the ranking and nothing about whether it survives out of sample. **(b) A holdout strength ratio** — recompute the same statistic on the holdout split and publish it against the in-sample value, so a pair that is a fitting artefact shows as a collapse. **(c) Concentration** — the share of *exposure* carried by rows in the top decile of absolute interaction contribution, which is the nearest honest thing to "how much of the book". **(d) A/E spread across the cross's cells on holdout**, above an exposure floor. | **DECIDED 2026-08-23 — (b), with (a) as the interim; specified as `02` FR-MODEL-128, which also amends FR-MODEL-35 so one seed and one row cap apply to each partition and the two numbers are comparable rather than merely adjacent.** It is the only option that answers the surviving half of the question with a quantity already defined: no new statistic, no threshold to argue about, and the same code path run against a second frame — which is also why it is cheap. (c) and (d) each need a constant nobody has chosen (a decile, an exposure floor), and choosing one inside this decision would settle a second question silently; (c) is the better of the two if "how much of the book" must be answered, and it should be raised as its own question rather than folded in here. (a) is what the artifact truthfully carries today and is the correct holding position — it is not a resting place, because a ranking with no out-of-sample check is exactly the overfitting route OQ-MODEL-4's decision refuses at the *other* end of the same pipeline. **Not urgent and not silent**: nothing is mispriced, because an interaction only ever becomes rateable as an authored Factor. **No threshold is attached**: a ratio is ranked evidence, never an admission test, so FR-MODEL-79's refusal to write a Factor is untouched. The second pass costs a keyword argument rather than a pipeline — the holdout frame is already loaded beside the training frame at the call site. Owner: the slice that builds the factor workbench's suggestion panel; until it lands the artifact publishes `strength` alone and `holdout_strength_ratio` is absent rather than defaulted. | maintainer | **decided** (in spec) |
| **OQ-MODEL-32** | ~~**Does `02` §5.3 render one artifact library or three?**~~ **DECIDED 2026-08-24: option (a) — the two missing §5.3 rows were added, by the W32 closure proposal's Part D item 3.** FR-MODEL-127 opens *"The three artifact libraries §5.3 renders are listable"*, and W32-8 built all three list endpoints against it. When it did, §5.3 contained **one** library view — `Custom objective library` at `/objectives` — plus a Peril structure *detail* view at `/peril-structures/{id}`, and no custom-metric view at all, so the requirement's opening sentence could not be satisfied as written. Raised 2026-08-24 by W32-8, which had no standing to resolve it: spec-versus-spec is what `CLAUDE.md` §0 reserves. | **(a) Add the two missing §5.3 rows** — a Custom metric library and a Peril structure library — making the opening sentence true and giving `W6b` two more screens, both already backed by §5.1's own endpoint rows. **(b) Amend FR-MODEL-127's wording** to "the three libraries §5.1 exposes, of which §5.3 renders one", making the view layer's asymmetry deliberate. | **(a)**, and this was reached independently twice: W32-8 recommended it from the endpoint side, and the closure proposal's Part D reached it from the specification side without seeing that analysis. Three list endpoints whose data only one screen consumes is the same asymmetry in the other direction. `Custom metric library` at `/metrics` (`02-modelling.md:2576`) and `Peril structure library` at `/peril-structures` (`:2577`) are now registered. **Watch item for whoever touches this next:** `00-overview.md` §5.6 carried the identical one-of-three gap and must move with it or it contradicts §5.3. | maintainer | **decided** (in spec) |
| **OQ-MODEL-33** | ~~**Which artifact rows carry `usage_count`?**~~ **DECIDED 2026-08-24: option (a) — the prose is qualified to two libraries, by the W32 closure proposal's Part D item 4.** FR-MODEL-127's prose said *"`usage_count` is on the row, as §5.3 asks: the count of Model Specs referencing that artifact"*, unqualified, while §5.1 puts the field on objectives and metrics and **omits it from peril structures**. W32-8 built the endpoint table's reading — the field on two libraries, absent on the third — and asserted the absence rather than letting it be implicit. Raised 2026-08-24 by W32-8. | **(a) §5.1 is right; qualify the FR prose** to the two libraries it holds for. **(b) Grow `usage_count` onto peril structures**, making the unqualified prose true. | **(a)**, reached independently twice as with OQ-MODEL-32. The quantity FR-MODEL-127 defines is *undefinable* on a peril row: `PerilComponent` (`packages/model-schema/src/model_schema/perils.py:214-228`) carries `frequency_model`, `severity_model` and `burning_cost_model`, each an `ArtifactRef`, so the reference runs PerilStructure → Model. No Model Spec names a peril structure — `modelling.py:832`'s `peril` is a plain `str` label, not a ref. So *"the count of Model Specs referencing that artifact"* is **`0` by construction, forever**, the same defect class W32-9 deleted from `ShapInteraction.exposure_share` for being `1.0` by construction (FR-MODEL-79's 2026-08-24 amendment). **The guard rail, which is the load-bearing half and is why this row is kept:** the holding must **not** be rested on the peril block having no `/usage` route. `03-rating-engine.md:100` gives `model_call` a `peril_structure_ref` and FR-RATE-22 pins one per call, so a peril structure *does* have a real blast radius. Only the specific quantity this requirement defines is vacuous — an argument from the missing route would reopen this wrongly. | maintainer | **decided** (in spec) |
| **OQ-MODEL-34** | **Does FR-MODEL-102's `-approx` slug rule bind every surrogate, or only the ones the transparency Job builds?** Raised 2026-08-24 by W6b-1a's residue review. FR-MODEL-102 states the rule unconditionally — *"the surrogate's `model_family_slug` is the source model's own family slug with `-approx` appended"* — while its own enforcement half is scoped to one caller: an over-long slug is *"refused by name … before the transparency Job spends any compute fitting it"*. The requirement is inconsistent with itself before any code is read, and the platform holds the narrow reading. `approximation_spec` (`packages/pricing-core/src/pricing_core/modelling/transparency.py:94`) is the only writer of the suffix; its one caller is `backend/src/app/worker/model_handlers.py:970`, and the length refusal at `:976` sits on that same path. Nothing binds a **client-declared** surrogate: `reserve_model` (`backend/src/app/platform/modelling.py:340`) persists `model_family_slug=spec.model_family_slug` verbatim at `:426`, and `_refuse_mismatched_approximation` (called at `:377`) compares `dataset_version_id`, `split_ref` and `factors` and not the slug. That omission is **absent, not hidden**: the sibling `interval_for` guard ~90 lines earlier builds the same mismatch list with **four** fields, `model_family_slug` first (`:583`), against the approximation guard's three (`:669-673`). Across `backend/src packages frontend/src` the literal `-approx` has five occurrences and not one of them enforces or reads it: the constructor, one error message (`model_handlers.py:976`), one test slug, one fixture comment, and one assertion under `packages/model-schema/tests/`. *(Use the plain `packages` prefix. `packages/*/src` is not a prefix match and silently drops the constructor, leaving a clean three that reads as the suffix never being built in `pricing-core` at all — a false negative pointing the way that dismisses this row.)* FR-MODEL-102's headline survives — the iff at the type makes a surrogate identifiable *as* a surrogate — but not *whose* surrogate it is, which is what the frontend already renders as a bare unresolved id. | **(a) Enforce the suffix on the create path**: validate a client-supplied `model_family_slug` against the source model's in `reserve_model` and refuse a mismatch. **(b) Scope FR-MODEL-102's slug sentence to the transparency Job**, matching the wording to the platform and leaving a client-declared surrogate free to be named anything. Note this is a spec change that code cannot then honour selectively: worker and API enter through the same `reserve_model`, whose audit hardcodes `source=JobSource.API` (`modelling.py:442`) where `:831` in the same file distinguishes via `JobSource.SYSTEM if job_id else JobSource.API` — the seam a worker-only rule needs exists as a pattern but not on this path. **(c) Derive the slug server-side** for any spec carrying `approximates_model_id`, so the property holds on every route by construction rather than by validation. **Cost of (c), measured not assumed:** no extra query. `_refuse_mismatched_approximation` at `:377` already fetches the source spec and the slug is written at `:426`, same function and same transaction (`reserve_model` at `:340` is the only `def` between `:330` and `:440`). One real cost remains: the 64-character refusal must move *with* the derivation, or an over-long derived slug hits the `String(64)` column at the DB layer instead of being refused by name as the FR promises. **Option (c) resolves the slug half only.** The version half is untouched by all three — `approximates_model_id` has no companion version field, `interval_for` carries `model_version`, and `/models/:slug` resolves to latest — so a link derived from a slug alone can land on a source version the surrogate does not approximate. That needs a companion field whichever option is taken, and so is not a reason to prefer one. | **Recommendation: (c), with the version field raised as its own question.** (b) is cheapest and is what the platform already does, but it makes the spec describe two classes of surrogate where the domain has one, and `00-overview.md` §2's naming discipline is what spares a reader having to know which route produced a row. (a) enforces the same property at a later point and leaves a caller able to *fail* on a name the platform could have computed; it is the better answer only if a client-chosen surrogate name is itself worth preserving. (c) removes the disagreement rather than recording it, is the only option under which the unconditional sentence is true as written, and its cost is now known to be one moved refusal rather than a new lookup. **Reading trap for whoever takes this:** `backend/src/app/platform/model_specs.py:232-233` exempts surrogates from the response-column check and cites FR-MODEL-102 by name, so grepping the id lands there — but `validate_spec`'s only caller is `POST /model-specs/validate`, which reports and persists nothing. That code shows the intent and is *not* on the create path; reading it as the enforcement is how this gap stayed invisible, and it must stay as it is either way, since a surrogate has to be able to name `__gbm_prediction__`. | maintainer | **open** |

## RATE — rating engine ([`03-rating-engine.md`](specs/03-rating-engine.md))

| ID | Question | Options / trade-offs | Recommendation | Owner | Status |
|---|---|---|---|---|---|
| **OQ-RATE-1** | ~~Does ZEN Engine expression evaluation preserve exact decimal semantics for money?~~ **RESOLVED 2026-08-14.** | Research established that `zen-types` represents `Variable::Number` as `rust_decimal::Decimal` (96-bit mantissa, ~28 significant digits) and that all node inputs, outputs and expression results use that type system. Engine arithmetic is exact decimal; option (b) — relying on integer minor units being exactly representable in float64 — is unnecessary. | **Decided: ADR-0004 stands, unchanged.** The risk moved to the boundaries rather than disappearing, and is now specified rather than open: FR-RATE-56 (arbitrary precision verified on parse *and* serialise, with a startup self-check), FR-RATE-57 (no unguarded transcendental in a rateable path, because `maths-nopanic` returns 0 instead of raising), FR-RATE-58 (scale-28 ceiling checked at compile). The S1 spike is **re-scoped, not cancelled** — it now tests the binding boundary and the `maths-nopanic` reachability, which is a smaller and better-aimed piece of work. Evidence: [`research/track-a-findings.md`](research/track-a-findings.md) F1. | maintainer | **decided (in spec)** |
| **OQ-RATE-2** | ~~Is `model_call` in `exact` mode viable within the 50 ms p99 budget?~~ **RESOLVED 2026-08-14 by spike S2.** | Measured: a 500-tree × 60-feature booster scores a single row at **p99 1.09 ms** including `DMatrix` construction (0.33 ms predict-only) — roughly 2 % of the budget. `nthread=1` beats all-cores at the tail (p99 1.09 vs 1.48 ms; max 4.5 vs 19.9 ms). | **Decided: `exact` mode is viable.** Both modes stay supported, and **OQ-MODEL-3 is a genuine design choice rather than one forced by latency** — which was the outcome this spike existed to prevent. Threading recorded as NFR-RATE-14. A sustained-load test still belongs in Phase 2. | maintainer | **decided (in spec)** |
| ~~**OQ-RATE-3**~~ ✔ | ~~Rate tables as PostgreSQL rows or content-addressed parquet blobs?~~ **DECIDED 2026-08-18: rows up to a configurable cell count, spilling to parquet above it, under one contract.** | Rows: SQL-diffable, joinable to exposure for FR-RATE-17 weighting, natural for the editor's paging. Blobs: consistent with dataset storage, cheap for very large tables (vehicle × area can reach millions of cells), but every diff and every exposure-weighted view becomes a compute job. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `03` **FR-RATE-62**, **Phase 2** with the rate-table slice — a later phase is a spec change and not code (`CLAUDE.md` §0). The reasoning is the recommendation's: rows are what make the rest of §3 cheap — FR-RATE-17's cell diff is a SQL join and its exposure weighting is a join to the portfolio dataset — and a vehicle × area table in the millions of cells must not dictate the design for the many small tables that are the common case. **Two things the requirement fixes that this row left loose.** `storage` (`rows \| parquet`) is a **stored, immutable property of the version** rather than a runtime decision, so a reader never has to ask which form a past version took and raising the threshold cannot silently re-home versions already written. And what degrades above the threshold is *named*: FR-RATE-17's diff and its exposure weighting answer **202 with a Job** instead of 200 — every question a caller may ask is unchanged, only the latency and the status code differ. "A degraded diff experience" was the phrase this row could afford; a requirement cannot. | maintainer | **decided** (in spec) |
| ~~**OQ-RATE-4**~~ ✔ | ~~Mid-term adjustments and cancellations — same algorithm with a `purpose`, or a separate path?~~ **DECIDED 2026-08-18: the same algorithm for the risk price, with the pro-rata/refund/charge maths in a separately-versioned sub-graph mounted on `purpose`.** | Same algorithm: one structure to maintain and approve, but pro-rata, cancellation charges, and refund rules genuinely differ from new-business maths and would bloat every graph with MTA-only branches. Separate path: cleaner graphs, two things to keep consistent. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `03` **FR-RATE-63**, **Phase 2**. One risk price, because two algorithms that must agree about risk are two things that will disagree about it — and the disagreement reaches a customer as one price at renewal and another for the same cover mid-term. Separate policy-administration maths, because pro-rata, cancellation charges and refunds are not new-business maths and folding them in puts an MTA-only branch in every graph that never performs one. *Found while writing it up:* the answer mounts on `purpose ∈ {mid_term_adjustment, cancellation}` and **`cancellation` was not a value `purpose` had** — §2's Quote Context and §4.4's scoring input both enumerated four, so the recommendation as filed keyed on something that did not exist. Added in the same edit, dated. The requirement also states the failure mode this row implied and did not say: a Rating Version mounting no such sub-graph **refuses** an MTA or cancellation quote rather than pricing it as new business, which is the silent wrong answer the split exists to prevent. | maintainer | **decided** (in spec) |
| **OQ-RATE-5** | Multi-product bundling in Phase 2? | In-platform: needed for a genuine bundle discount priced on combined risk, requires a multi-product Quote Context and cross-product rating versions. Left to the Consumer System: much simpler, but the bundle discount is then unpriced and unmonitored. | Defer to Phase 4. Phase 2 ships single-product Rating Versions; a Consumer System bundling two quotes is a supported pattern, and cross-product pricing follows the optimisation work that needs the same joint demand modelling. | maintainer | deferred (Phase 4) |
| ~~**OQ-RATE-6**~~ ✔ | ~~Does the platform own instalment/APR calculation?~~ **DECIDED 2026-08-18: price the annual premium, offer `instalment_loading` as an optional final ladder rung, and leave APR and schedule generation downstream.** | It changes the price the customer actually compares, so the demand model in `04` is fitted on the wrong price without it. But it is billing-domain logic with its own regulatory surface (consumer credit). | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `03` **FR-RATE-64**, **Phase 2**. The loading is in because it changes the price the customer actually compares, and without it `04-optimisation.md`'s demand model is fitted against a price nobody was offered. APR and schedules are out because they carry a consumer-credit regulatory surface — disclosure, the regulated APR formula, rules about what may be charged — that belongs to a billing system with its own compliance obligations; taking it on would make every rating release a consumer-credit release. **The requirement draws the line where the maths stops being rating maths** and adds the refusal this row did not state: a Quote Context asking for an APR figure or a payment schedule is refused rather than answered approximately, because an APR that is nearly right is a compliance defect and not a rounding one. | maintainer | **decided** (in spec) |

## OPT — optimisation ([`04-optimisation.md`](specs/04-optimisation.md))

| ID | Question | Options / trade-offs | Recommendation | Owner | Status |
|---|---|---|---|---|---|
| **OQ-OPT-1** | Which solver family? | SLSQP (`scipy`): simple, no extra dependency, adequate for a few hundred smooth segment factors, degrades badly at thousands of variables and with non-smooth constraints. `cvxpy` + a conic solver: far more robust, gives genuine infeasibility certificates and shadow prices for free (FR-OPT-12), but constrains objectives to convex forms — which excludes some blends users will want. | Start with SLSQP in Phase 4 for the segment-factor formulation, and specify the constraint set so that a convex reformulation stays available. Revisit with `cvxpy` if segment counts exceed ~1 000 or infeasibility diagnosis (NFR-OPT-4) proves unreliable. | maintainer | deferred (Phase 4) |
| **OQ-OPT-2** | Competitor position: first-class concept or just a dataset column? | First-class: enables market-response modelling, needs a competitor data model, ingestion cadence, and matching logic we cannot validate without the data. Column: zero platform cost, user does the joining, but then "rank in market" cannot be a constraint or a monitored metric. | Dataset column in Phases 1–4, with the *convention* documented (column naming, semantic type `competitive_position`) so it can be promoted to first-class later without a migration. | maintainer | open |
| **OQ-OPT-3** | Expected claims in the profit objective — platform models or externally supplied? | Platform models: internally consistent, everything traces to an approved artifact. External (reserving-derived): matches what the finance function believes, and will differ from our models in ways that get noticed in a pricing committee. | Support both: the profit definition (FR-OPT-9) names its expected-claims source, which may be a Peril Structure or a supplied column on the portfolio dataset. Force the choice to be explicit rather than picking a default that silently disagrees with finance. | maintainer | open |
| **OQ-OPT-4** | How is demand-model endogeneity handled? | Historic prices were set by a structure already conditioned on risk, so naive elasticity is biased toward zero (or worse). Options: instrumental variables (needs a valid instrument, rarely available), randomised price testing (see OQ-OPT-5), or documenting the caveat. | Document prominently and *quantify*: FR-OPT-2's price-variation report is the honest surface for it. Specify an `identification_note` field on every Demand Model that is required before it can be used in optimisation. Real identification needs OQ-OPT-5. | maintainer | open |
| **OQ-OPT-5** | Should the platform support randomised **price testing** on live traffic? | It is the only clean answer to OQ-OPT-4, and it means deliberately quoting some customers a different price to learn from it — operationally involved and ethically loaded, with FCA fair-value implications. | Do not build it in Phases 1–4. Specify the *capability boundary* now (a `price_test` purpose in the Quote Context that the platform refuses to serve) so that if it is ever added it is a deliberate, governed feature and not an emergent one. | maintainer | deferred (post-Phase 4) |
| **OQ-OPT-6** | ~~GIPP: what is the correct ENBP when channels differ?~~ **RESOLVED 2026-08-14.** | Not a judgement call — **the rule prescribes it.** ICOBS 6B.2.5R(1) requires the channel the customer used when they **first purchased** the policy; 6B.2.5R(2) falls back to the channel most commonly used by new-business customers where the original is unavailable or unidentifiable. | **Decided: follow the rule.** This corrected a real design defect — the spec and the `GippCheck` contract keyed ENBP off the *renewal* channel, which diverges from the rule for precisely the customers it targets: those who switched channel between purchase and renewal. Now FR-OPT-18/29, with the fallback share reported as a data-quality signal. My earlier recommendation ("same-channel with an explicit mapping") was close but wrong on which channel. | maintainer | **decided (in spec)** |

## MON — monitoring ([`05-monitoring.md`](specs/05-monitoring.md))

| ID | Question | Options / trade-offs | Recommendation | Owner | Status |
|---|---|---|---|---|---|
| **OQ-MON-1** | Is 1 % trace sampling enough for segment-level A/E? | At 1 % of 50 M quotes, a segment holding 0.5 % of business gets ~2 500 quotes/year — far too thin for a per-level A/E with usable CIs. Options: raise the rate (storage, NFR-RATE-12), stratify sampling by segment (complex, biases need correcting), or compute A/E from a **full batch re-score** of the exposure dataset rather than from traces. | Compute A/E from a full batch re-score of the exposure dataset (`03` FR-RATE-36) — the population is policies, not quotes, so it is far smaller, complete, and already needed for `01`. Keep trace sampling for *quote-level* metrics (conversion, declines, latency, constraint activation) where full coverage is unaffordable and sampling is statistically fine. | maintainer | open |
| **OQ-MON-2** | PostgreSQL or a dedicated time-series store for monitoring metrics? | Postgres: one fewer system, adequate at weekly cadence, partitioning handles growth. Dedicated TSDB (Timescale/Prometheus): better for daily per-replica operational metrics, another system to run and back up (NFR-OVR-9 wants a simple local stack). | PostgreSQL with declarative partitioning for pricing metrics, and the existing observability backend (OpenTelemetry → whatever the operator runs) for operational metrics. Do not add a TSDB to the required stack. | maintainer | open |
| **OQ-MON-3** | Who owns the claims-development pattern used for maturity adjustment? | User-supplied reference data: simple, defers the actuarial work to the user, and the pattern is exactly what a reserving team already has. Platform-fitted from triangles: better UX, meaningful new scope (triangle construction, tail fitting), overlaps reserving which is explicitly out of scope. | User-supplied as a Reference Table (`development_pattern`), referenced by the Monitor's `maturity_policy`. Ties off with `01` OQ-DATA-4 — the platform warns about maturity and consumes a pattern, it does not derive one. | maintainer | open |
| **OQ-MON-4** | Automated champion/challenger on live traffic? | It measures what shadow scoring cannot — realised conversion and claims under the challenger — but it means real customers get the challenger price, which is a live pricing experiment with fair-value implications (same family as OQ-OPT-5). | Shadow scoring only (`03` FR-RATE-54). Champion/challenger is the same governed decision as price testing; group it with OQ-OPT-5 rather than deciding it separately. | maintainer | deferred (post-Phase 4) |
| **OQ-MON-5** | What is the right default cadence per metric family? | One cadence for everything is wrong in both directions: weekly A/E is noise on a low-frequency peril, monthly drift is far too slow to catch a broken feed. | Specify per family: `input_drift` daily, `operational` hourly (from the metrics backend), `price_portfolio` weekly, `demand` weekly, `model_ae` monthly with a 6-month maturity floor. Make these the template Monitor defaults created by FR-MON-4, and document the reasoning in the template. | maintainer | open |

## GOV — governance ([`06-governance.md`](specs/06-governance.md))

| ID | Question | Options / trade-offs | Recommendation | Owner | Status |
|---|---|---|---|---|---|
| ~~**OQ-GOV-1**~~ ✔ | ~~Audit hash chain: per workspace or global, and is a self-held chain enough?~~ **DECIDED 2026-08-18: per workspace and self-held, claimed as tamper-evident and never tamper-proof, with an optional chain-head anchor.** | A chain the platform operator controls end to end detects *storage-layer* tampering but not a determined operator who can recompute it. External anchoring (publishing the chain head to an append-only external store, a customer-controlled bucket, or a transparency log) closes that gap at the cost of an external dependency and an operational ritual. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `06` **FR-GOV-40**, **Phase 3**. Per workspace was already FR-GOV-24's shape, so the decision confirms it rather than changing it — a global chain serialises writes across workspaces and detects nothing a per-workspace chain misses. **The substance is the claim discipline**, which is where a governance feature usually goes wrong: a self-held chain detects modification *below the application* and does not detect the platform operator, who can recompute every subsequent hash — so the UI and the dossier must say tamper-evident **and against whom**, because a claim an auditor can falsify in one question is worse than no claim at all. The anchor operation (`POST /api/v1/audit/anchor`, declared in §5.1) closes the operator gap only to the extent the customer automates it into a store we do not control, so it is offered and documented and never described as on by default. | maintainer | **decided** (in spec) |
| ~~**OQ-GOV-2**~~ ✔ | ~~Are platform roles authoritative, or is IdP group membership?~~ **DECIDED 2026-08-18: hybrid — the IdP owns identity and role membership, the platform owns scope.** | IdP-authoritative: joiner/leaver processes work automatically, which large insurers require; but artifact-scoped permissions (FR-GOV-4) do not map cleanly to IdP groups. Platform-authoritative: precise scoping, and a leaver keeps access until someone remembers. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `06` **FR-GOV-41**, **Phase 3**, with the mapping itself remaining configuration (`07` FR-PLAT-4). The joiner/leaver argument decides the role half: a leaver removed from a group loses access without anyone here remembering to act, which is what large insurers impose and what a platform-authoritative model quietly fails. Scope stays here for a reason the options column understated — **scope names artifacts that exist only in this platform**, so an IdP administrator cannot express "motor-* but not household-*" against objects their directory has never heard of, and a mapping that pretended otherwise would widen access silently every time a model family was created. *Stated in the requirement because an implementation gets it wrong in exactly one place:* a granted role with no scope assigned holds its permissions over **no artifacts**, not over all of them. | maintainer | **decided** (in spec) |
| ~~**OQ-GOV-3**~~ ✔ | ~~Can an Admin override a flag (FR-GOV-17)?~~ **DECIDED 2026-08-18: yes, made maximally expensive and permanently visible.** | Allowing it creates a single bypass of every gate the platform has, which is exactly what an auditor will look for. Disallowing it means a false-positive flag — a genuinely invalidated dataset that was re-validated, a transparency artifact that failed to generate — blocks a legitimate change with no recourse. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `06` **FR-GOV-42**, **Phase 3**: two distinct Admins, a mandatory written justification, an entry in the exception log (FR-GOV-35), a line in the dossier's approval history (§4.4 §16), and a badge on the artifact that **never expires**. Refusing overrides is the cleaner rule and the wrong one — flags come from other modules and can be false, and a governance system with no recourse is bypassed *outside* itself, in a spreadsheet nobody audits. **The permanence is the requirement and the ceremony is not**: two approvers make an override deliberate, but only the permanent badge reaches the next reader, who is the person an auditor asks why this model was approved with a flag against it. *Added while writing it up:* an override never **clears** the flag — the underlying condition stays raised and only re-running the check lowers it, without which "override" would have quietly become "dismiss". | maintainer | **decided** (in spec) |
| ~~**OQ-GOV-4**~~ ✔ | ~~Do we need formal model risk tiering?~~ **DECIDED 2026-08-18: yes — `risk_tier` on Model Family and Rating Algorithm, which the Approval Policy may key on.** | Tiering (tier 1 models needing more approvers and more frequent attestation) is standard in UK insurer model governance and maps poorly onto per-artifact-type policy, which cannot distinguish a headline motor frequency model from a windscreen burning-cost model. | **Maintainer accepted the recommendation, 2026-08-18**, and this row moves from `deferred` to decided without changing phase. Specified as `06` **FR-GOV-43**, **Phase 3**. Per-artifact-type policy cannot tell a headline motor frequency model from a windscreen burning-cost model, so a workspace wanting two approvers on the first must impose them on both and then watch the rule be resented into irrelevance. Two details the requirement fixes that the recommendation left open: the tier sits on the **family and the algorithm, never on the version**, so it is a standing statement about a line of business rather than a per-fit judgement someone could set to 3 on the morning they want a faster approval (and changing it is audited under FR-GOV-20); and an `ApprovalPolicy` entry **without** a `risk_tier` applies to every tier, which is what lets FR-GOV-37's floor and §4.2's defaults stand unchanged and gives a workspace that never tiers anything no change at all. | maintainer | **decided** (in spec) |
| ~~**OQ-GOV-5**~~ ✔ | ~~How much commentary is required before an approval can be submitted?~~ **DECIDED 2026-08-18: exactly two Commentary Blocks, with a minimum length and no default text.** | Requiring commentary in defined dossier slots (purpose, limitations) improves documentation and reliably produces boilerplate. Requiring none produces dossiers that are all numbers and no reasoning — precisely what a reviewer complains about. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `06` **FR-GOV-44**, **Phase 3**: §4.4 §1 *Purpose, scope and intended use* and §15 *Limitations and known issues*, mandatory before submission; everything else optional. Ten mandatory slots reliably produce ten paragraphs of boilerplate, which is worse than silence because it looks like documentation and an approver stops reading it. **The no-default-text rule is the half that does the work** — a template with placeholder prose is a boilerplate generator with extra steps — and the minimum length is a blunt instrument used anyway, because it costs a real author nothing and stops "N/A" being a purpose statement. *Interaction stated rather than left to be discovered:* FR-GOV-45's new §18 and §19 are **not** part of this mandatory two — they serve a TAS-facing reader rather than the approver — which is what stops "exactly two" drifting to four. | maintainer | **decided** (in spec) |
| ~~**OQ-GOV-6**~~ ✔ | ~~Does TAS 200 (Insurance) cover pricing and premium rating, or only reserving, capital and Solvency II actuarial-function work?~~ **DETERMINED 2026-08-18 by reading TAS 200 v2.0: it covers pricing, via the `Pricing frameworks` scope item.** | TAS 100 applies regardless — it is the general standard. TAS 200 is the insurance-specific layer on top, revised to v2.0 (published 2024-09-20, effective 2025-01-01) with strengthened **assumption-setting** requirements and new material on the FCA's Consumer Duty. If pricing is in scope, `02`'s factor/banding/grouping rationale and `01`'s validation evidence must meet those requirements and `06` §4.4's dossier sections may need to expand; if not, TAS 100 alone governs and the bar is lower. The scope statement is in the standard text and could not be established from the FRC's public summaries. | **Resolved by doing the lookup the recommendation asked for, 2026-08-18** — the row was right that this is a lookup with a spec consequence rather than a design choice, and the conservative interim position it recommended turns out to have been the wrong way round. TAS 200 v2.0 §1.2 lists *"Pricing frameworks — Technical actuarial work to support pricing frameworks"* among the work in scope, and the glossary defines a **pricing framework** as the product pricing principles *and the methodologies, assumptions and models implementing those principles* that support an insurer's premium rates or product charges. `insurer` is any undertaking effecting or carrying out contracts of insurance or reinsurance — **no life/general split** — so general insurance premium rating is in, and the models, assumptions and rationale this platform produces are framework components by the standard's own definition. Effective for work completed on or after **1 January 2025** (§1.3); TAS 200 work is also TAS 100 work (§1.4). **Two findings the question did not anticipate.** The unit of scope is the *framework*, not the quote — a scoring call is not technical actuarial work. And there is **no pricing-specific provisions section**: sections 2–6 cover valuation, capital, transformations, audit and with-profits, so what binds is §1 *Provisions for all work in scope* — P1.1 (document material factors from customer-outcome obligations, which is where Consumer Duty enters), P1.2 (assumptions consistent with those used for business planning, reserving and capital), P1.3 (a persistent experience-versus-assumption gap considered and documented), P1.4 (communications describe material inconsistencies). Specified as `06` **FR-GOV-45**, with §4.4 gaining sections **18** and **19** to carry them — appended rather than inserted, because section numbers are identifiers. Verified against the FRC's published PDF rather than a summary; the row's own note that public summaries do not state the scope was correct. | maintainer | **decided** (in spec) |
| ~~**OQ-GOV-7**~~ ✔ | ~~Does `06` §3.3's per-artifact evidence table or `06` §4.2's `ApprovalPolicy` defaults decide what a submission actually requires?~~ **DECIDED 2026-08-18: §3.3 is a floor per artifact type; §4.2 may add to it and never remove from it.** | Raised 2026-08-17 (W5, the model lifecycle) by making `EVIDENCE_INCOMPLETE` a refusal something raises: the check has to read a list, and there are two. **§3.3 is the requirement, §4.2 is the configuration** — plausible, and it makes every workspace's editable policy unable to *relax* a floor it cannot see; a submission would then be refused for missing evidence the policy does not mention, which is the error message nobody can act on. **§4.2 is the whole answer** — what the code does now, and it means an insurer can edit its way to approving a GBM with no transparency artifact, which `02` R3 forbids outright. **§3.3 is a floor, §4.2 may only add to it** — the two documents then say different things without contradicting each other, at the cost of a rule that lives in neither table and must be written down a third time. | **Maintainer accepted the recommendation, 2026-08-18.** Specified as `06` **FR-GOV-37** and **delivered in the same commit**, because the precondition this row set for itself had already fired twice — transparency became a checkable evidence kind on 2026-08-17, and `peril_structure` added a fourth on 2026-08-18. The deciding case is `02` §4.8 R3, exactly as the recommendation argued: a transparency artifact for a non-GLM model is an invariant of the artifact, not a workspace preference. Three mechanisms answer the objection the options column raised against a floor — that a submission refused for evidence the policy does not mention is an error nobody can act on: the floor is **restated in §4.2's own text**, `PUT /api/v1/approval-policy` **refuses** an entry that drops below it with `POLICY_BELOW_EVIDENCE_FLOOR` naming the kinds, and submission checks the **union** so a policy stored before the floor existed cannot sit below it either. **The enforced floor is §3.3's checkable projection, and the remainder is named with an owner** — `model_comparison_if_predecessor` waits for a comparison that names its models in a queryable column rather than inside `payload`, and §3.3's rationale is unmodelled (Phase 1b) — because submission fails closed on an unverifiable kind, so a floor naming everything would have refused every model submission instead of raising the standard. *Found while building it:* the two tables disagreed a third way nobody had recorded — §4.2's `model` entry lists three evidence kinds and `DEFAULT_POLICY` shipped one, and the submission check answered a kind it spelled `transparency_artifact` while the spec spelled it `transparency_artifact_if_non_glm`, so a workspace that copied the name off the page got a fail-closed refusal for evidence it had. Both are resolved in §4.2 with a dated note rather than quietly aligned. | maintainer | **decided** (in spec) |
| ~~**OQ-GOV-8**~~ ✔ | ~~Does an `expression` Custom Objective need its own authoring permission, distinct from `model:fit`?~~ **DECIDED 2026-08-18: yes — `custom_objective:author`, granted by no built-in role by default.** | Raised 2026-08-18 (W5) by superseding `custom_objective:author`/`custom_objective:submit` in `06` §4.1: Phase 1's objectives are the §4.5 template catalogue, so authoring one is choosing how a model is fitted and `model:fit` is the right check. A Phase 2 `expression` objective is different in kind — author-written maths that a reviewer must read, whose blast radius (FR-MODEL-47) spans every model using it. **Separate permission**: an actuary may fit without being trusted to write a loss; costs a role migration and a screen that explains why a control is hidden. **Keep `model:fit`**: no new vocabulary, and the real gate stays where it already is — certification (FR-MODEL-42) plus a non-author Approver (FR-MODEL-46). | **Decided 2026-08-18 at the maintainer's direction, having first been deferred the same day — and the second look found the deferral rested on the wrong dependency.** The recommendation said the answer turns on how much of the review a certificate can carry. It does not: `02` FR-MODEL-42 analyses the *artifact* (convexity, domain, sampling) and FR-MODEL-46 gates *approval*, so both act after authoring and neither says anything about whether this principal should be writing loss functions at all — and a `draft` objective can already fit models whose numbers reach a pack. Certification is not a substitute for an authorisation decision, so the question was answerable now. Specified as `06` **FR-GOV-39**: `custom_objective:author` gates authoring, editing and versioning an `expression` objective; template selection stays `model:fit` and submission stays `model:submit`. The deciding test is §4.1's own — *would a role grant one and withhold the other?* — which fails for a template (choosing a cap is choosing how one's own model is fitted) and plainly passes for author-written maths that every model using it inherits (FR-MODEL-47). **Granted by no built-in role's default set**, which is the operative half: a permission every fitter holds is exactly the vocabulary-without-a-decision that §4.1 superseded. `custom_objective:submit` does **not** return — the same test fails for it, since FR-GOV-11 already keeps a submitter out of their own approval. **The enum member and its check land together in W30**, because a member nothing checks would recreate the defect this question came from. FR-GOV-38 stands, amended: its trigger is discharged before W30 rather than by it. | maintainer | **decided** (in spec) |

## PLAT — platform ([`07-platform.md`](specs/07-platform.md))

| ID | Question | Options / trade-offs | Recommendation | Owner | Status |
|---|---|---|---|---|---|
| **OQ-PLAT-1** | ~~Celery vs a transactional Postgres queue.~~ **DECIDED 2026-08-14: Celery.** | Maintainer chose Celery over the Postgres-queue prototype this file recommended. Reasonable: Celery is mature, Redis is already in the stack, and the throughput headroom is real. | **Celery** — but the transactional-enqueue property a Postgres queue would have given for free must now be built explicitly. Celery cannot enlist in the database transaction, so a task can publish and the transaction then roll back, leaving work with no audit record — which `06` R2 makes a correctness problem, not an inconvenience. **`FR-PLAT-51`** requires a transactional outbox: the job row is written with the domain change and the audit event, and a relay publishes only after commit. | maintainer | **decided** |
| **OQ-PLAT-2** | ~~Does Dagster earn its place?~~ **DECIDED 2026-08-23: no — it is dropped, and scheduled work is the tick specified as `07` FR-PLAT-61.** | It brings partitioned assets and backfills that `05` genuinely needs (NFR-MON-8) and that would otherwise be hand-rolled badly. It also brings a second scheduler, a second UI, and a second operational surface next to the Jobs model. | **Decided 2026-08-23 on the maintainer's instruction. The recommendation's own fallback is what the decision takes**, one phase earlier than it imagined. "Keep it, but confine it to scheduling and partitioning" is self-defeating: a Dagster forbidden to execute pricing work is a clock plus a table of which periods have run, and the platform must own both regardless, because a scheduled run must be a Job with the same outbox, audit and cancellation guarantees as any other (FR-PLAT-15, FR-PLAT-51). What is left to buy is a second scheduler, a second UI over state the Jobs UI already shows, and a second place a run can exist. **The two capabilities cited in its favour are specified rather than dropped** — catch-up, because FR-PLAT-61's schedules are period-anchored instead of interval-timed, and backfill for a Monitor created after the fact (`05` NFR-MON-8), as an explicit bounded request. Sensors go the same way: a deployment that should create monitors submits the Job in the transaction that records the deployment, rather than a poller discovering it. **Blast radius: specification only.** Dagster appears in no `pyproject.toml` and no `uv.lock`; the sweep removed it from `00`, `01`, `05`, `07`, `wf-04`, `skills-map.md` and the repo-architecture skill's tree. Two mentions are deliberately left: `NFR-PLAT-11` and the test behind it still block `dagster` on import, which is now a guard against a module that will never be installed — harmless, worth nothing as evidence, and left so the W-closure record that describes that test stays true. **This raised `OQ-PLAT-8`**: FR-PLAT-61's `(schedule_id, period)` key is precisely a key whose Job may have failed, and FR-PLAT-12's 24-hour window does not exist in the code. | maintainer | **decided** |
| ~~**OQ-PLAT-3**~~ ✔ | ~~Is the scoring service a separate application image?~~ **DECIDED 2026-08-18: one image with a role flag through Phases 1–2, a separate scoring image from Phase 3 — and the boundary that keeps the split cheap is enforced from today.** | Separate: much smaller attack surface and dependency set on the latency path, faster cold start, and it makes NFR-PLAT-9 structural rather than aspirational. Same image with a role flag: one build, one version, simpler release. | **Maintainer accepted the recommendation, 2026-08-18, and the enforcing half was delivered.** The image is Phase 3 and stays there; what this row correctly identified is that the *option* has to be protected in the meantime, and "specify NFR-PLAT-9 now" was the recommendation's own way of saying so. Specified as `07` **NFR-PLAT-11** and **built**: a Model is scored in a subprocess where `glum`, `scikit-learn`, `celery` and `dagster` cannot be imported, asserting the Poisson identity — so the design reconstructs, the base level resolves and the offset applies with the fitting stack absent. `xgboost` and `lightgbm` are deliberately *not* blocked: `02` FR-MODEL-62 scores a GBM by loading its JSON booster, so a boosting library is a scoring dependency by design and only the fitting libraries are what a second image sheds — a distinction found by checking the requirement against FR-MODEL-62 rather than by reasoning about what a scoring service obviously needs. *Found while building it:* **an import-linter contract is the wrong instrument**, which was learnt by writing one. `glum` and `sklearn` are already imported at their call sites, which is the discipline the requirement wants, but import-linter reads the AST and counts a function-scope import like a module-scope one — so the contract reported four violations against correct code, and the only ways to green it were to weaken it or to move modules with no other reason to move. The property is a runtime one, so the check creates the runtime; a second test proves the blocker blocks, without which the first would pass while importing `glum` freely. | maintainer | **decided** (in spec) |
| **OQ-PLAT-4** | ~~Are workspace resource quotas needed at all?~~ **DECIDED 2026-08-23: no — none is specified or built (`07` FR-PLAT-60).** | In a single-tenant deployment (FR-OVR-13) they mostly protect users from themselves — a 40-factor GBM on 50 M rows that starves the scoring workers. Not needed for isolation. | **Decided 2026-08-23 on the maintainer's instruction to resolve this question. The recommendation's *deferral* was not what was adopted.** It said skip quotas "until multi-tenancy is on the table (OQ-OVR-1)" — but OQ-OVR-1 was decided on 2026-08-15 (ADR-0006, FR-OVR-13): one tenant per deployment, permanently. The trigger is dead, so the deferral was already a rejection in everything but the record, and it is recorded as one. **The boundary that replaces it is the Job and the queue** — per-Job budgets (FR-PLAT-16), per-queue pools (FR-PLAT-13), the scoring pool independent of the compute pool (FR-PLAT-34). ADR-0006 §Neutral is amended to point here. **What this does not settle is enforcement, and three halves are unarmed today:** `wall_clock_s` is read by the worker and `memory_gb` is not; `platform.jobs.submit()` takes no budget argument, so the column is reachable only by a test that writes it directly; and the compose stack ships no worker service for a limit to apply to. Those are **W14**'s, and FR-PLAT-60 says so in the requirement rather than here. Reopened only on measurement from Job history — FR-PLAT-52 forbids the `workspace_id` metric label that would make it a dashboard — read at each phase-exit review. | maintainer | **decided** |
| **OQ-PLAT-6** | ~~How does the **browser** authenticate?~~ **DECIDED 2026-08-15: authorization code + PKCE in the SPA, for Phases 1–2.** | The SPA sent no credential at all, and the platform refuses an unauthenticated request (`07` §3.7) — so a browser got 401 on everything while every view and its tests passed, because the tests stub `fetch` and nothing exercised the real transport. The two real options were **PKCE in the SPA** (no new server component, standard for a public client; a token in JS is XSS-reachable and silent renewal is fiddly) and a **BFF session cookie** (token stays server-side, `SameSite`/`HttpOnly` removes the XSS reach, at the cost of a session-holding component the platform does not otherwise need). | **Maintainer accepted the recommendation.** PKCE adds no component the platform would not otherwise run, and `07` already specifies discovery and JWKS verification for the API side — so the decision costs one client-side flow and changes nothing a server does. Specified as **FR-PLAT-55**, owned by **W6b**. FR-PLAT-2's in-memory rule is what keeps the XSS exposure bounded, and is now load-bearing rather than advisory. The client library is W6b's to choose — `oidc-client-ts` is the default candidate, and hand-rolling PKCE is defensible only if silent renewal comes with it. **Revisit at Phase 3** if a deployment requires that no token exist in the browser at all; the BFF is then an addition rather than a rewrite, because the API verifies a bearer token either way. Until W6b ships, the frontend dev proxy is the only way in, and it is named so it cannot be mistaken for a mechanism. | maintainer | **decided** |
| **OQ-PLAT-5** | ~~Bundle an OIDC provider in the production stack?~~ **DECIDED 2026-08-23: local development yes, behind an opt-in profile; production no.** | Bundling Keycloak lowers the barrier for a small insurer with no IdP, and makes us responsible for operating and patching an identity provider — the highest-consequence component to get wrong. | **Decided 2026-08-23 on the maintainer's instruction; the recommendation is adopted, and its local half made concrete.** Splitting the question in two is what makes it easy: *production* is the recommendation verbatim — `deploy/` carries a **reference** Keycloak deployment the deployer operates and patches, not a component this project secures on an insurer's behalf (`07` **FR-PLAT-59**, W14). *Local* was not a preference but an unnoticed gap: FR-PLAT-1 stated in the present indicative that a provider runs in the local stack, and `deploy/docker-compose.yml` runs postgres, redis and minio and no provider — so the only way in is `dev_auth_enabled`, and W6b's PKCE flow (FR-PLAT-55) has nothing to authenticate against. **FR-PLAT-58** (W6b) closes it behind an **opt-in compose profile**, so the default `docker compose up` keeps three containers and both test suites keep running with no container; it is an *alternative* to `dev_auth_enabled`, never a replacement. One trap is written into the requirement: `examples/fremtpl2/seed.py` grants roles to principals it mints itself, so a real OIDC login — which the API keys on `(issuer, subject)` — would authenticate into **zero** workspaces unless the realm's subjects are what the seed grants against. FR-PLAT-1 keeps its id and carries a dated amendment saying which half was fact and which was intent. | maintainer | **decided** |
| ~~**OQ-PLAT-7**~~ ✔ | ~~A `PlatformError` raised inside a Job handler loses its `.code` before it is stored. Should `execute_job`'s generic exception handler preserve it, and how?~~ **DECIDED 2026-08-22: (a) — a dedicated `except PlatformError` clause before the generic one, storing the handler's own code, with retry behaviour unchanged.** | `backend/src/app/worker/tasks.py`'s `except Exception` around the handler call stores `JobError(code="JOB_HANDLER_FAILED", message=f"{type(exc).__name__}: {exc}")` for *every* unexpected exception, `PlatformError` included. `PlatformError.__init__` (`backend/src/app/errors.py`) calls `super().__init__(detail or title)`, so `str(exc)` is the error's title or detail — never its `.code` — and the stored message is not even a substring match for the code that was raised. Found on the FR-MODEL-96 branch: Task 4's two refusal tests (`test_the_transparency_job_refuses_a_hand_written_spec_that_disagrees_with_its_source`-shaped tests) had to call the handler function directly rather than go through `execute_job`, because no test going through the real dispatch path can assert on a specific error code from any handler — every handler's `PlatformError` looks identical to every other's once it reaches `JobError`. **(a)** add a dedicated `except PlatformError as exc` clause before the generic one, storing `JobError(code=exc.code, message=exc.detail or exc.title, ...)` — the handler's own code survives, and only a genuinely unexpected exception falls through to `JOB_HANDLER_FAILED`. **(b)** leave `JOB_HANDLER_FAILED` as the stored code for every handler failure, but put the original code in `JobError.detail` as a structured field — smaller diff, but a caller reading only `.code` (which is the contract's documented field) still cannot branch on it. **(c)** leave it as is, and require every negative test to call the handler directly — costs nothing today, but it is a standing gap for every future handler-raised `PlatformError`, not only this branch's two. | **Maintainer accepted the recommendation, 2026-08-22, and it is built.** `execute_job` now carries `except PlatformError as exc` immediately before the generic clause, storing `JobError(code=exc.code, message=exc.detail or exc.title, retryable=False, trace_id=current_trace_id())`. `JOB_HANDLER_FAILED` is untouched and correct where it now remains — a genuinely unexpected exception, which is a handler bug rather than a refusal the handler named. Nothing about retry changed: `retryable` is the same literal `False` the generic clause has always set, and the new clause deliberately does not read `PlatformError.status_code`, so a handler raising a 429 or a 503 is still a deterministic refusal of *this* Job. **The clause ordering was checked rather than assumed, and the check moved where the risk is.** The question implied the danger was the two clauses already there; it is not. `PlatformError`, `JobCancelled` and `JobBudgetExceededError` are three independent direct subclasses of `Exception` — verified against the MRO, none is a subclass of another — so those clauses are genuine siblings and their order is free. `except Exception` is the load-bearing one, because it is a *base* of `PlatformError`: put the new clause after it and it never runs, and every named handler error keeps arriving as `JOB_HANDLER_FAILED` with the fix apparently in place. The comment at the clause records that reasoning, which the same slice had just been taught to write down after finding `np.linalg.LinAlgError` to be a subclass of `ValueError` where two clauses read as siblings. **Three tests in `backend/tests/test_model_jobs.py` hold it, all marked FR-PLAT-11** — `07` §3's requirement that a failed Job records a typed error code, which is what this defect broke, and the mark `backend/tests/test_worker.py`'s `test_a_failing_handler_records_a_typed_error` already carries for the other half of the same behaviour. The named-code test drives `model.fit` through `execute_job` with a model id that does not exist and asserts the stored code is the handler's own `NOT_FOUND`; a positive control raises `RuntimeError` from the same job kind and asserts `JOB_HANDLER_FAILED` is still reached, without which a change that collapsed everything into the new clause would leave the first test green; and the third raises a `PlatformError` carrying `RATE_LIMITED` at 429 and asserts `retryable` is still `False`. All three were run against the pre-change worker first and fail there, storing `JOB_HANDLER_FAILED` in place of the handler's code. **One part of FR-PLAT-11 is still not met and is not made to look met:** `PlatformError.errors` carries the field-level cause, and this clause does not propagate it into `JobError.detail`. That is outside option (a)'s text, so it was left undone and recorded rather than done quietly. | maintainer | **decided** |
| **OQ-PLAT-8** | *(raised 2026-08-23)* ~~What does an `Idempotency-Key` naming an already-finished Job mean, and does FR-PLAT-12's 24-hour window exist?~~ **DECIDED 2026-08-23: option (c) — the window is withdrawn, keys are permanent, and a terminally failed Job releases its key. Specified as `07` FR-PLAT-64.** | FR-PLAT-12 specifies a 24-hour window; `platform.jobs.submit()` applies none and returns the existing Job whatever its status. A failed Job's key is therefore spent forever and a succeeded one is honoured indefinitely — the sentence is unimplemented in both directions at once. FR-PLAT-61 makes it load-bearing: `(schedule_id, period)` is exactly a key whose Job may have failed. | **DECIDED 2026-08-23 — (c), specified as `07` FR-PLAT-64.** Keys are permanent, but a *terminally failed* Job releases its key so a retry under it is a fresh Job, while a success or a cancellation does not. It is the only option that separates "has this period been done?" from "may it be attempted again?", which is what a failed scheduled period needs. (a) implement the window as written re-runs anything older than a day, which is the duplicate the key exists to prevent; (b) permanent keys with no release leaves no retry path but a new key. **What the decision reaches:** the window was stated platform-wide in `00` §5.4, which FR-PLAT-47 binds the API to, and a second index over the same header is `01` FR-DATA-8's Ingestion Run — both are amended rather than left to diverge. The code delta is a `status <> 'failed'` term on the partial unique index plus a status filter on the lookup, which would otherwise find several rows where it expects at most one; the unreferenced `idempotency_window_hours` setting that asserted the withdrawn window is deleted. | maintainer | **decided** (in spec) |
| **OQ-PLAT-9** | ~~**How does a principal's chosen workspace reach the API?**~~ **DECIDED 2026-08-23: option (a) — a verified `Workspace-Id` header, specified as `07` FR-PLAT-65.** FR-PLAT-63 (2026-08-23) requires a multi-membership principal to choose, and the choice to be verified against its own memberships on every request; it does not say how the choice is transported. Raised by the W6b slice-map backlog, item 2. | The question is live rather than a formality because the codebase already argues one way: the request-scope dependency carries the comment that a header-supplied workspace "would make the scope a claim rather than a fact", repeated on the job path — and today a principal with several memberships is refused outright. **(a) A verified header.** The browser sends the selection; the dependency checks it against the memberships the identity already holds and refuses a value that is not among them. Cheap, one code path, and the invariant's objection arguably does not reach it — a choice among facts the platform holds is not a claim. But every caller must remember to send it, and a forgotten header is a request that silently runs somewhere. **(b) A path prefix** — `/workspaces/{id}/…` on every scoped route. The workspace becomes part of the resource identity, so a URL is complete: shareable, loggable, and impossible to forget. It is the largest change, touching every route and the generated client. **(c) Bound into the session at login.** The selection is made once, stored server-side, and no request carries it. Nothing to forge and nothing to forget, but a principal cannot hold two workspaces open in two tabs, and switching becomes a stateful operation rather than a parameter. | **DECIDED 2026-08-23 — (a), the verified header; specified as `07` FR-PLAT-65.** It is the only option that does not change the shape of every route or of the session, and the invariant it appears to contradict is about *unverified* scope: the thing the comment refuses is trusting the caller, and FR-PLAT-63 does not — a header naming a workspace the principal does not belong to is refused with `WORKSPACE_SCOPE_DENIED`, so the scope stays a fact. The forgotten-header risk is real and is answered by refusing rather than defaulting, which is what makes (a) safe: a caller that forgets gets `WORKSPACE_SELECTION_REQUIRED`, not someone else's data. (b) is the better design in the abstract and would be the recommendation on a greenfield surface — it is refused here only on cost, and the cost is a client regeneration and every route in five specs. (c) is refused on the two-tab case, which is an ordinary thing for an actuary comparing two workspaces to do. **The header is named `Workspace-Id`** — unprefixed like `Idempotency-Key` and `If-Match` rather than reviving the `X-` convention RFC 6648 retired, and deliberately unlike `x-dev-workspace-id`, the development header FR-PLAT-55 confines to `local`/`dev`, because a production selector one word from a dev-only one is a footgun. It is declared on the route and optional in the published contract but required in the handler, the shape `If-Match` already uses, so it reaches the generated client while its absence stays a typed platform error rather than a `422` outside the catalogue. Owner: **W32** for the API half; `W6b-11` is unblocked as a decision and still waits on that half. *Amended 2026-08-25: that half landed — `backend/src/app/api/deps.py` declares the `Workspace-Id` header and raises both refusals, migration `57547846f0a3` creates the `workspaces` table, and `backend/tests/test_workspace_selection.py` covers them. `W6b-11` waits only on its own dependency `W6b-10`. Mirrored in [`specs/07-platform.md`](specs/07-platform.md) §10.* | maintainer | **decided** (in spec) |
| **OQ-PLAT-10** | **Every layer of the contract guard is scoped to the intersection of its two sides, so a shape or keyword present on only one side is outside all of them by construction.** The type comparison intersects paths; the constraint comparison intersects paths and then keywords; `test_every_eligible_schema_is_compared` defines `eligible` as generated ∩ authored, so the completeness check is itself defined over the complement of the problem. The consequence that makes this a design question rather than a bug: **the guard is silent in exactly the same way whether a shape is one-sided deliberately or accidentally.** `model-comparison`, `backtest`, `objective-usage`, `custom-metric` and `metric-certificate` are generated-only on purpose — a shape's first written form, and `generate-contracts.py` says so in a comment beside each. **That those comments are themselves unchecked is part of the finding**: the one beside `peril-structure` still reads "No hand-authored Phase-0 counterpart" although an authored side was added in #133, so the only existing declaration of intent has already gone stale without anything noticing — which is the argument for deriving the declaration rather than narrating it in a comment. A shape whose authored side was deleted or never written would present identically, and no reader or test can tell the two apart. **And the blind spot moves.** The intersection is recomputed from whatever the two sides hold at the moment of the run, and the guard keeps no memory of what it compared last time — so a path that *leaves* the shared set is indistinguishable from one that was never in it. The comparison simply covers one path fewer and stays green. That is strictly worse than static one-sidedness: coverage can shrink one path at a time, indefinitely, with every check passing, and the shrinking is itself the thing nothing reports. A worked instance at discriminator-arm granularity, measured at `7e09eb4` once W32-1b landed and reproducible against that tree: in `model-spec`, moving `family` from the glm arm to the xgboost arm of the *authored* contract takes the type comparison from 146 compared `(arm, path)` keys to 145 with disagreements at **0 before and 0 after**. `family` is shared in the `model_type=glm` arm before the move and shared in **no arm at all** after it — the path does not disagree, it stops being compared, and every check stays green. This is the one case arm attribution does *not* close: W32-1b gives arm-level type and bound disagreement on paths shared *within* an arm, and a field that leaves the shared set entirely is this question rather than that slice's. Note that the shrink is one key out of 146, which is why a threshold expressed as a fraction of the walker's own output cannot catch it. Note the boundary: W32-1b delivers arm-level **type disagreement on shared paths**; arm-level **existence** is this question and is not covered by it. Measured at `946725f`: 26 authored, 25 generated, 15 on both sides, 11 authored-only, 10 generated-only. `validation-report` demonstrates it on a slug added the same day — `empty_layers.[]` is model-only, `overall`, `counts.*` and `results.[].evidence_blob` are contract-only, and the whole suite is green. | **(a) Leave it and document the scope.** Honest and free; every count published must then say which of the two frames it means, because "the guard compares everything in scope" and "21 of 36 shapes are out of scope" are both true. Does nothing about the indistinguishability. **(b) Declare one-sidedness rather than infer it.** A slug that is generated-only on purpose says so in one registry; anything one-sided and undeclared fails. Small, and it separates deliberate from accidental — which is the whole finding. Cost: a registry to keep current, and the same staleness risk any curated list carries, mitigated by the entry being derivable from the corpus. **(c) Require both sides for every shape.** Strongest, and wrong here: eleven authored-only slugs are later-phase (governance, optimisation, monitoring, rating), so this builds ahead of the phase for the sake of a guard. | **(b).** The residual is already bounded — all 11 authored-only slugs are later-phase and all 10 generated-only ones are first-written-forms — so nothing is wrong today; what is missing is any way to keep knowing that. (b) makes the intent writable and checkable at the cost of one registry, and leaves (c) available per shape as its phase arrives. Not done in W32-11: the finding came out of that slice, but acting on it is a change to the guard's contract rather than to the two shapes the slice was scoped to. **W32 goal borne on:** FR-PLAT-48 — that every published contract is checked against the code producing it. **Dispositioned, not delivered:** the guard reaches every shape W32 scoped it to, and this question is about what "every" can mean, raised inside W32's last slice and owned beyond it. It **subsumes finding F2 of the W32-11 plan** — that no authored-keyword completeness check exists — which is the keyword case of the same intersection: a `minItems` declared on the authored side alone is not compared, which is how `gbm.quantile_crossing` sat missing for months with a green suite, and is why Task 1 of that slice makes the two sides agree partly by not comparing them. F2 needs no separate row because a fix for one is a fix for both; it is named here so the next reader does not file it twice. | maintainer | open |
| **OQ-PLAT-11** | **When a shape is tightened, what revalidates the artifacts already stored under the looser one?** Nothing does today, and the read path is where it surfaces. A stored payload is validated on write against whatever the model said that day, then parsed again on read against whatever it says now — so a tightening that is correct going forward can make an existing row unreadable, and the failure appears at read time to a user who did nothing, far from the commit that caused it. The class is not hypothetical in this repository: `OQ-OVR-8` moved decimals from accepting JSON numbers to refusing them, and the certificate battery moved from a count floor to an exact set of names in this slice. Both are strictly narrowing, which is the direction that breaks reads. | **(a) Nothing — accept read-time failure.** Free, and defensible only while the corpus is small and pre-production. It fails silently in the direction that matters: the first symptom is an artifact an approver cannot open. **(b) Version the shape and keep readers for old versions.** Correct in general and what a governed artifact store eventually needs, but it is a data-model change across every stored artifact and it buys migration cost on every tightening. **(c) A revalidation sweep, run on demand, that parses every stored artifact against today's models and reports what no longer reads.** Does not prevent the break; makes it visible on the committer's clock instead of the user's, and turns "is anything unreadable?" into a question with an answer. | **(c) now, (b) when artifacts outlive a release.** (c) is a script and a report, costs nothing to keep, and would have caught both real instances at the commit that caused them. (b) is the real answer and is disproportionate before there is a production corpus to migrate. Whichever is chosen, the rule worth writing down is that a narrowing change to a stored shape is a migration event, not a model edit. | maintainer | open |
| **OQ-PLAT-13** | **`metric-certificate` has a model-side four-check floor and no authored contract at all**, so nothing compares it and nothing publishes it to an external reader. Raised 2026-08-24 as finding F3 of W32-11. It is one of ten generated-only slugs, but unlike the other nine it is not a first written form by design — `objective-certificate` beside it has both sides, and the two are siblings that FR-MODEL-105 and FR-MODEL-108 treat alike. **W32 goal borne on:** FR-PLAT-48. **Dispositioned, not delivered:** the floor itself is enforced model-side, which is what FR-MODEL-105 asks for; only the comparison and the publication are outstanding. | **(a) Author the contract now.** Small and obvious, and it makes the pair symmetric with `objective-certificate`. But writing a new authored contract is authoring a published promise, not comparing an existing pair, and the slice that found it was scoped to two other shapes. **(b) Leave it generated-only and let `OQ-PLAT-10`'s declaration mechanism cover it.** Cheapest, and wrong in one specific way: a declaration would record it as *deliberately* one-sided, which is the opposite of true — it is one-sided because nobody wrote the other half. **(c) Author it as part of whichever workstream next touches certificates**, so the contract is written by someone with the certificate in front of them. | **(c).** The shape is not in doubt and the cost is small, so this is a scheduling question rather than a design one; what makes (a) wrong here is only that a closing slice is the wrong place to add a published promise. Whoever takes it should note that (b) is actively harmful — recording this as intentional one-sidedness would convert an oversight into a decision, which is precisely the confusion `OQ-PLAT-10` exists to end. | maintainer | open |
| **OQ-PLAT-12** | **What tells the API that a workspace selection *changed*, so that a switch is audited once rather than never or on every request?** *(raised 2026-08-24 by W32-7 Task 4.)* FR-PLAT-63's fourth obligation says a switch is audited into both chains, and W32-7 built the mechanism — `platform.workspace_switch.record_switch` writes one event in the workspace left and one in the workspace entered, in id order so two principals switching in opposite directions cannot deadlock on the per-workspace advisory lock. What W32-7 did **not** build is the call site, and the reason is not effort: `require_caller` runs once per request and holds no memory of the previous one, so "the selection changed" is not a fact it can observe. A switch is a difference between two requests, and the platform stores nothing that spans two requests. | *(options amended 2026-08-24, the day they were filed: `(b)` is split, because its two mechanisms have opposite costs and one of them is worse than the option it would be built instead of.)* **Which option is right turns on a reading of FR-PLAT-63's fourth obligation that has not been settled, and every cost below flips with it. Reading 1: a switch is a human act. Reading 2: a switch is any difference in effective scope between two consecutive requests by the same principal.** **(a) Audit every selection, with `left=None`.** Needs no new state and loses nothing. It appends one FR-GOV-24 event and takes one per-workspace advisory lock (`backend/src/app/platform/audit.py:81-85`) on *every authenticated request a multi-membership principal makes*, turning the chain into a request log — burying the record of what was actually done, which is what the chain is for. **The objection is the chain and the lock, not that a write appears:** the bearer path is already not read-only, since `require_caller` calls `authenticate_bearer` on every request (`backend/src/app/api/deps.py:115`) and `_upsert_user` stamps `last_login_at` each time (`backend/src/app/auth/service.py:146`) — but that is one unlocked column on the user row, appending to no chain and serialising nothing against anyone. Faithful under Reading 2, enormously noisy under Reading 1. **(b1) Store the previous selection on the user row** (`last_workspace_id`). Cheapest to build — the same shape and flush as `last_login_at`, no new table, no expiry. **Measurably worse than (a) in the two-tab case:** a single principal-level "current workspace" cannot represent two concurrently open workspaces, so every request from either tab differs from the stored value and is recorded as a switch, and `record_switch` appends both the workspace entered and the workspace left (`backend/src/app/platform/workspace_switch.py:39-42`) — **two events and two advisory locks per request**, on exactly the two workspaces the tabs are using, so the tabs serialise against each other permanently, where (a) takes one lock on the workspace entered and never contends between them. OQ-PLAT-9 refused session-bound selection *"on the two-tab case, which is an ordinary thing for an actuary comparing two workspaces to do"*, and FR-PLAT-65 made the selection per-request so two contexts can hold two workspaces; (b1) reinstates a single current workspace at the principal level. It does not break scoping — the header still governs each request — it corrupts the *record*, silently, in the case the platform went out of its way to support. **(b2) Store it on a session or presence row.** Escapes (b1)'s pathology **only if the stored scope is per-tab**, and nothing the server holds is: a session cookie is shared across tabs, so (b2) degrades to (b1) unless the client mints and sends a per-tab identifier — which is (c)'s cooperation requirement under another name. Genuinely new state, with expiry, ownership and cleanup of its own. **(c) Make the switch an explicit act** — `POST /api/v1/me/workspace`, which audits and returns; `require_caller` audits nothing. Zero cost on ordinary requests, and the only option whose trigger granularity matches the act being audited, since the client is the only thing with tab granularity. `W6b-11`'s switcher already has the moment to call it. Cost: a client that only ever sends the header switches with no record — fatal under Reading 2, and under Reading 1 not a gap at all, because such a caller has not switched. | **No mechanism is recommended, because the choice is not derivable from the options: it turns on which reading of FR-PLAT-63's fourth obligation is correct, and that is a maintainer decision this row exists to surface rather than to make.** Under **Reading 1** (a switch is a human act) the answer is **(c)** alone, and (a) and (b) both record non-events. Under **Reading 2** (a switch is any difference in effective scope between consecutive requests) the answer is **(a)**, with **(b2)** admissible only as an optimisation that provably loses no event, and (c) inadmissible. **(b1) is not recommended under either reading** and is recorded so it can be refused explicitly: it is the cheapest thing to build, it is what "store the previous selection" means to a reader in a hurry, and it is measurably worse than the option it would be built instead of. **The recommendation filed earlier on 2026-08-24, "(b), reached through (c)'s surface", is withdrawn as un-implementable rather than wrong**: it composes (c)'s trigger from Reading 1 with (b)'s completeness argument from Reading 2, and its cheapest honest implementation is (b1). **What closes this row is one sentence settling the reading**; the mechanism follows from it without further judgement. **Nothing is urgent and nothing is being lost meanwhile:** there is deliberately no API to create `workspace_members` in W2 (`backend/src/app/db/models.py:470`), so no multi-membership principal can exist yet — the population every cost above is measured over is currently empty, every option stays fully reversible, and (a)'s degradation in particular is invisible until memberships are provisionable, which is also when undoing it becomes expensive. **Not decided in W32-7**, which shipped the mechanism and its tests and left FR-PLAT-63's fourth obligation the §13 verdict *deferred with an owner* rather than letting a green suite imply delivery. Consumed by `W6b-11`, and by `W6b-11b` if the slice map's P8 split is accepted — P8 is cut on this row being undecided, so a Reading 2 answer, being entirely backend, vacates `W6b-11b` and moves its obligation with the decision. That consequence is recorded so the dependency is visible, **not as an argument for either reading**. | maintainer | open |
| **OQ-PLAT-14** | **20 of the 39 `entity_ref` spellings the backend writes into the audit chain do not parse as an `ArtifactRef`, and nothing notices.** *(raised 2026-08-24 by W32-7 Task 4, which set out to ask a much smaller question and measured this instead.)* W32-7 writes `workspace:<slug>@1` for a switch event; `workspace` is not in `ARTIFACT_TYPES`, so `ArtifactRef.parse` rejects it. Measured on `backend/src` before recording that as the finding: **13 types other than `workspace` already appear in `entity_ref` and are not in the frozenset** — `actor`, `approval_policy`, `backtest`, `blob`, `job`, `model_comparison`, `model_family`, `principal`, `profile`, `role`, `service_account`, `setting`, `validation_report` — and **5 more use a listed type but carry no `@version`**, which `_REF_RE` also rejects: `dataset:{slug}`, `dataset_version:{version_id}`, `model:{model_id}`, `reference_table:{slug}`. Only 19 of 39 parse. `audit.record` and `AuditEvent` both type the column as a plain `str` and nothing parses it, which is why a majority-unparseable column has been invisible. The real question is therefore not whether to admit one more type: it is **what `entity_ref` is**, because the code has been answering "a name for whatever the event is about" while the type name answers "an `ArtifactRef`". | **(a) Widen `ARTIFACT_TYPES` until every written ref parses.** 13 new types plus a fix for the 5 unversioned spellings, in `refs.py` and the mirrored `artifact-ref.schema.json`. It also redefines the frozenset as "things that appear in `entity_ref`" rather than "things that are artifacts" — and `00` §2 makes a Workspace, a Principal and a Role the *scope and subjects* artifacts exist under, not artifacts. `@1` on a workspace is a version that will never exist. **(b) Declare that `entity_ref` names the subject of the event, which is an `ArtifactRef` when the subject is an artifact and a scoped `type:name` otherwise**, and give it a validator of its own that admits both. Writes down the contract the code has held all along, costs one shared regex and a test, and leaves `ARTIFACT_TYPES` meaning what it says. Cost: a consumer that parses the column must handle two shapes. **(c) Split the column** — `entity_ref` stays an `ArtifactRef` and is nullable, with a second `subject` column for non-artifact events. Cleanest model and the most expensive: a migration over every stored audit event, in a table `06` FR-GOV-24 hash-chains, so a backfill is a chain rewrite. | **(b).** The measurement is what decides it: at 19 of 39 parsing, "the chain holds `ArtifactRef`s" is not a rule being broken in one new place, it is a description that was never true, and (a) would spend a spec change on 13 types to make a frozenset mean something it should not. (b) makes the column's actual contract checkable, which is the thing genuinely missing — nothing today would notice a *typo* in an `entity_ref` any more than it noticed these 20. (c) is right in the abstract and disproportionate against a hash-chained table. Two rules worth carrying whichever is chosen: `ARTIFACT_TYPES` is never widened to make an existing string parse, and the 5 unversioned refs are a plain bug under any of the three — `dataset:{slug}` names no version, so the chain records which dataset was touched but not which version of it, and that is the fact an auditor came for. **Not decided in W32-7**, which was scoped to workspace identity and recorded the measurement rather than widening to act on it. | maintainer | open |
| **OQ-PLAT-15** | **`req-coverage.py`'s three inflation modes are not the three that were reported: one is already closed by another gate, one has a live instance the measurement could not see, and the one that is firing is unreachable by any change to the instrument.** Raised 2026-08-24 (W6b), measured against `e2ae7c6`; baseline `523 specified · 269 marked · 51.4%`. The denominator is built from ids wrapped in markdown bold (`scripts/req-coverage.py:45`) and the numerator by regex over raw source text (`:51`), so *claimed* means only "this byte sequence occurs in a file named `test_*.py`". **(1) The denominator mode is already enforced elsewhere.** `scripts/audit-docs.py:332-360` builds `defined` from bold-only spec ids and `referenced` from a word-bounded FR/NFR id pattern over every markdown line, then fails on the difference — the proposed guard's predicate exactly. Proven on deliberately broken input: an unbolded requirement row exits 1, the identical row with four asterisks exits 0. **(2) The numerator mode has a live instance, and the measurement that reported zero could not have found it.** The reported figure was `0 of 1638` markers *outside decorator position*, but the requirement's predicate is a marker on something pytest **collects**. `backend/tests/test_celery_broker.py:66` satisfies the proxy and violates the predicate — a real `@pytest.mark.req("FR-PLAT-51")` in true decorator position on the helper `async def _drain`, which pytest never collects — while the test it belonged to at `:96` carries no marker at all. An AST reader over all five `testpaths` roots rejects that one site of 1637 and loses no ids: 269 under regex, 269 under AST. **The site itself is repaired separately as a plain bug** — moving the decorator onto the collected test restores the traceability link and leaves this finding untouched, because the published figure reads 269 either way: the gap is that the instrument cannot tell the two arrangements apart, not that the count is wrong. **(3) The live mode is clause-conflation, and no instrument change reaches it**, because the unit is the id and the defect is inside the sentence. `FR-DATA-27` (`01-data-management.md:160`) is two clauses and its four markers evidence only the first. The per-requirement count is additionally **mislabelled**: `:47-48` appends one entry per marker *occurrence* and `:52` prints it as "N test file(s)". | **(a) A standing reporting rule** — every closure record quoting the figure states it is a starting list, not a bound. Costs nothing, closes no mode; it is not a third option beside the code changes but their release note, and shipping guards without it is net-negative. Necessary but **not sufficient** for a §13 discharge, since a blanket caveat is not a verdict on `FR-DATA-27`'s second clause. **(b) Fail when an id appears in a spec and is never bolded — strike this.** At the only scope where it is correct it re-implements `audit-docs.py` checks 2 and 3, a second source of truth for one rule, which `CLAUDE.md` §2 forbids; at any narrower scope it fails on first run against 1188 unbolded id occurrences across 365 ids in `docs/specs/`, and `docs/workflows/` holds 160 id references with no bolded ids at all. **(c) Parse markers with `ast` and require a collected test** — accept a decorator only on a function pytest will collect and run, and print every rejected site rather than silently omitting it. Live yield today is one stranded marker and one test that lost its traceability link. Its residual risk is converting future divergent spellings from *silently counted* to *silently dropped*, which printing rejections mitigates and `--strict-markers` does not, since that validates a marker's name and not its host. **(d) Move the unit from requirement to clause** — reaches the live mode and nothing else does, but it is a spec-format *and* marker-format change across the corpus, and the split is not at sentence boundaries. Its useful signal is a subset of (e)'s. **(e) Warn when a requirement's text carries a UI clause and every one of its markers is backend.** Both operands come from files the script already opens, so it needs no frontend reader and never touches the VCS-ignored generated client. It reaches `FR-DATA-27`. It must be a **warning, never a failure**: requirements naming the UI as *evidence of absence* (`FR-MODEL-116`, `FR-MODEL-120`) are blast-radius measurements, and a keyword detector flags both. Its real cost is first-run volume — **31 requirements on day one**, which reads as noise until triaged. **(f) Print a truthful per-requirement count** — a set, and a label naming what it counts. Not an inflation mode; a quantity mislabelled as another quantity. | **(c), (e) and (f), with (a) as their release note and (b) struck.** The deciding argument is that the instrument's problems were mis-attributed rather than mis-solved: (b) was priced as cheap new work when the invariant was already pinned in the gate one file away, and (c) was priced as purely preventive when it has a live yield — so the original ranking would have built the redundant guard and justified the useful one on its weakest case. (c) and (f) are small and their effect on the published figure is nil, which is the point: neither is bought to move the number. **The limitation belongs in the decision in plain terms — (c) and (e) do not close clause-conflation, and no cheap instrument change does.** Its legitimate discharge is (a) plus a named §13 verdict on each conflated clause. **Bounded-proof discipline:** what is proven is that `audit-docs.py` fails the denominator input before and after, and that a prototype AST reader loses no ids while rejecting one live site. **Nothing here establishes that a shipped (c), (e) or (f) fails its own broken input** — none of them exists yet. When each lands, the proof must vary the input along that option's own predicate, and each check must be shown to pass the other's input; otherwise what is proven is "some check fails", not "this check fails for this reason". | maintainer | open |
