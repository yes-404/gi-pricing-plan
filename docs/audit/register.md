# Global register of open findings

One row per open finding, keyed by the requirement or artifact id it concerns. Each row
names the work item that carried it, the phase, and the decision. A finding is removed
when the close resolves it, accepts it, or re-plans it with an owner.

| Finding id | Concerns | Work item | Phase | Decision |
|---|---|---|---|---|
| FR-DATA-57 (F6) | `unrun_layers` projection — Phase 2 validation-report successor | W7-4 | 2 | carry forward with an owner (Phase 2 validation-report workstream) |
| FR-DATA-52 (F7) | exposure-ordered top-20 deferral — trigger has not fired | W7-4 | 2 | carry forward with a trigger (a named exposure-ordered reader), unowned by design |
| 03 rating surface (F8) | compile, score, rate tables, deployment | — | 2 | carry forward — phase boundary |
| wf-01 §4 surfaces (F9) | bandings, Peril Structure, reconciliation | W7-5 | 2 | carry forward — phase boundary |
| Phase 2/3/4 unevidenced (F22) | FR-MODEL-6/40/82/115/121; FR-PLAT-15/23-29/31-36/49/50/56/60/61/64; FR-GOV-16/17/18/27-35/38-45; NFR-OVR-1..8/10/11; NFR-PLAT-1/2/5/6/8/9/10; NFR-GOV-1/3-7 | — | 2/3/4 | carry forward — phase boundary; owners are the later-phase workstreams named in the roadmap |
| FR-OVR-20 (F14) | `_minor` suffix rule — enforcement invisible to `req-coverage.py` | W6b-15 | 1b | fix before close — add a test naming FR-OVR-20 |
| FR-PLAT-59 (F17) | no IdP in prod — enforcement not marker-evidenced | W6b-14 | 1b | fix before close — add a marker to the repository-invariant test |
| FR-OVR-22 (F13) | route reachability — Vitest-enforced, Python-marker-blind | #136 | 1b | accept — alternative instrument (Vitest), positive control + mutation verified |
| FR-OVR-21 (F15) | §5.3 cell is prose | W6b | 1b | accept — declared-prose affordance |
| FR-PLAT-55 (F16) | browser PKCE — Vitest-enforced | W6b-10 | 1b | accept — alternative instrument (Vitest) |
| NFR-PLAT-4 (F18) | compose < 5 min — measured 27 s | W6b-14 | 1b | accept — measured, recorded |
| FR-OVR-9 (F19) | pseudonymisation — ingestion enforces; PII-guard gap has a roadmap home | W6b | 1b | accept — enforcement exists; the PII-guard gap is recorded in the roadmap |
| Cross-cutting OVR (F20) | FR-OVR-2/4/10/11/12/14/15/16/19 | W6b/W7 | 1b | accept — conventions, ADRs, audit checks |
| Measured NFRs (F21) | NFR-DATA-1/2, NFR-MODEL-1..5, 10..13 | W4/W5/W7 | 1b | accept — measured, not asserted |
| NFR-RATE-13/14 (F-W9-1) | validate-inbound-never-outbound; nthread=1 per model_call — design constraints whose measurement belongs to the scoring path | W9-3 | 2 | carry forward with an owner — the W11 scoring workstream; W8's measurements recorded (NFR-RATE-13 p99 0.070 ms, NFR-RATE-14 p99 1.626 ms) |
| FR-RATE-61 (F-W9-2) | approximation-mode Rating Versions cannot reach `approved` without a Dislocation Run against their own exact-mode baseline (needs FR-RATE-46) — orphaned at birth, no workstream row's numeric range ever named it | — | 2 | carry forward with an owner — W13 (FR-RATE-46, the Dislocation Run it needs, is W13's; the check itself specialises FR-RATE-40's general approval-evidence gate, which W11 builds) |
| FR-RATE-25 (F-W9-3) | **Six clauses, enumerated (`03:136`): (1) DAG acyclic and fully connected — enforced and tested, but under `FR-RATE-1`'s marker: `model_schema/rating.py:361`'s `_graph_invariants` (Kahn's-algorithm cycle check + input/output reachability) runs automatically inside `compile_bundle` via `RatingAlgorithm.model_validate` (`compile.py:408`), tested by `test_rating_algorithm.py`'s `test_a_cycle_is_refused`/`test_an_orphaned_step_is_refused` — none of it marked `FR-RATE-25`. (2) all references resolvable and at a sufficient maturity (FR-OVR-14) — the maturity half is enforced (`compile.py:424`'s `_APPROVED_OR_BETTER`/`PIN_NOT_APPROVED`) and tested, but the test is marked `FR-OVR-14`; the resolvable half is untested — every fixture's `FakeResolver` resolves whatever it is asked, so a genuinely-unresolvable pin is never exercised. (3) all types compatible — enforced transitively (`compile_bundle` re-runs `validate_algorithm`, `compile.py:260`, whose `_check_result_types`, `compile.py:113`, is `FR-RATE-13`'s), but no test drives a type mismatch through `compile_bundle` itself and nothing is marked `FR-RATE-25`. (4) all constraints satisfiable — no implementation anywhere: `constraint` (case-insensitive) occurs exactly once in `packages/pricing-core/src/pricing_core/rating`, in `compile_bundle`'s own docstring (`compile.py:390`) restating this clause, not implementing it. (5) no `control`-intent factor in a rateable path (`02` FR-MODEL-3) — no implementation anywhere: `git grep -in intent -- packages/pricing-core/src/pricing_core/rating` is empty; `FactorIntent`/`REFUSED_FACTOR_INTENTS` exist only in `02`'s own fit-time refusal (`pricing_core/modelling/factors.py`), which never reaches the rating DAG. (6) no unapproved custom objective transitively reachable — the pin-maturity loop rejects an unapproved *directly-pinned* custom objective (same code as (2)) but not one reached transitively, and it is wholly untested: every fixture in both `packages/pricing-core/tests/test_rating_compile_bundle.py` and `backend/tests/test_rating_version_compile.py` sets `custom_objectives: []`.** The repository's one and only `@pytest.mark.req("FR-RATE-25")` (`test_rating_compile_bundle.py:170`) tests none of the six: its docstring reads "a boundary-guard violation re-checked at compile is named," and its assertion is `EXPRESSION_UNGUARDED_DIVISION` — `FR-RATE-57`'s zero-division guard, re-run inside `compile_bundle`. W9's closure record (`docs/audit/work/W9/README.md:27`) verdicted `FR-RATE-22..27` delivered, citing "marker-evidenced (... 25: 1 ...)" — the count is correct and the verdict does not survive reading what that one test checks. Not a W11 concern: FR-RATE-25 is `03` §3.4, outside W11's own §3.7/§3.8/NFR scope. | W9-3 | 2 | carry forward — **owners decided (Ruling 29, `docs/plans/2026-08-29-w11-algorithm-pin-maturity.md`, under the maintainer's M2 delegation): W11, in Ruling 28's follow-up PR**, for clauses (1), (2)'s maturity half and (3) — point `FR-RATE-25`-marked tests at the mechanisms that already run, which that PR is already touching in `compile_bundle` and its tests (cheap); **the §14 review at W11's close**, for clauses (4), (5) and (6)'s transitive half — place them on a workstream or accept them as not-built with a reason, since validation code for these exists nowhere (not cheap). `03`/W9 is not reopened, same as F27: this is a missing check against a requirement W9 declared delivered, not a defect in what W9 built elsewhere. |
| FR-RATE-17/18/19/20 (F-W10-1) | Cell diffs, bulk operations, validation, CSV/XLSX import/export | W10-1 | 2 | carry forward with owners — W10-2 (diffs, validation: FR-RATE-17/19), W10-3 (bulk ops, import/export: FR-RATE-18/20). *W10-2 half resolved 2026-08-28 (PR #302 — FR-RATE-17/19 delivered and tested; the remaining FR-RATE-17 wiring is F-W10-2).* |
| FR-RATE-16 (F-W10-1-1) | seeded_from lineage metadata: implemented in W10-1 but test marker not separate (tested within FR-RATE-15 immutability suite) | W10-1 | 2 | *resolved 2026-08-28 (PR #302) — FR-RATE-16 markers added to the API tests; lineage delivered and tested.* |
| FR-RATE-17 (F-W10-2) | exposure-weight wiring at the diff endpoint: pricing-core accepts caller weights (DP1) but the endpoint passes none; the portfolio-dataset join is scheduled in no slice | W10-2 | 2 | carry forward with an owner — portfolio-dataset integration (wiring lands with the join) |
| DP3 diff cache (F-W10-2-1) | the DP3 ruling's Redis cache (keyed by version hash + portfolio identity) is unbuilt and its deferral was not declared in PR #302 — silent until the closure record | W10-2 | 2 | *resolved 2026-08-28 (PR #311) — the cache shipped with W10-3D: keyed by both versions' content hashes + the portfolio dataset version's identity (never a date), no TTL, fail-open both get/set, 200 read path only (the worker stores the diff as a blob instead).* |
| FR-PLAT-48 typed diff 200 (F-W10-2-2) | the W10-2 diff endpoint answered `dict[str, Any]` while the platform computes a typed RateTableDiff — the ADR-0002 divergence class, invisible to the drift check | W10-2 | 2 | *resolved 2026-08-28 (PR #311) — the diff 200 is declared `$ref RateTableDiff` in the generated OpenAPI, 202 as `$ref Job`; the deferral was recorded with the discharge.* |
| 03 §5.1 `POST /rate-tables/{slug}/versions` (F-W10-3) | Phase-0 row "New Rate Table Version with change note" has no route; version creation happens via the three create paths (seed-from-model creates the table or appends, bulk-operation, import-confirm), each change-note-required | W10 close | 2 | carry forward with an owner — the W15 rate-table editor slice (the manual editing entry point); the plan's W10-1 T3 create/GET/list endpoint tasks fold into this row |
| `.claude/roles/`+`.claude/skills/` CI gap (F26) | No workflow watches either path except `.claude/notes/**`; a PR touching only `.claude/roles/**` gets zero CI of any kind — confirmed twice independently (PR #327's merge SHA: zero runs in `gh run list`; PR #338 ran a `docs` workflow only because it also touched CLAUDE.md, an in-filter file, not because the filter learned about roles) | `nt-0010-0011-adoption` audit | 2 | carry forward — **owner decided: W11** (Ruling 29, under the maintainer's M2 delegation), to land before the charter amendments R6 is holding for the §14 review — which land into exactly this unwatched directory; two parts required together, neither hollow alone (a `paths:` filter naming both directories, and a check that verifies content, not just presence — task #21's spawn-input constraints read as a ready-made specification: named skills exist, cited paths resolve, no placeholder survives, no hardcoded local path, a `Never` section present); the related ruff-exclusion inversion (the vendored-only exemption silently also covering first-party skill code) is resolved separately by PR #355 once merged |
| 03 rating shapes vs hand-authored contracts (F27) | `rating-version`, `rating-algorithm`, `rate-table` ship in `model-schema` (`rating.py:104`, `:341`, `:651`; plus `RateTableVersion` `:818` and `RateTableDiff` `:684`) and each has a hand-authored contract under `docs/contracts/schemas/`, but nothing has ever compared the two: `grep -n "RatingVersion\|RatingAlgorithm\|RateTable" scripts/generate-contracts.py` returns zero (a true negative — `GENERATED_SHAPES` values are literal class names, consumed by `getattr(model_schema, name)` at `:174`), and `backend/tests/test_contracts.py` excluded all three as `"later-phase — 03 rating"`, a reason that expired when W9 and W10 built them. Live divergences, enumerated rather than counted because this row's first version said "two" and the second field alone turned out to be seven (a bare count in this area has now aged four times in one day; the list is the artifact). **`scoring.schema.json`**: `purpose` has five values in `03:63` and four at `:12` — `cancellation`, added 2026-08-18 with FR-RATE-63. **`rate-table.schema.json` vs `model-schema`'s `RateTableVersion`**: missing `storage` (added 2026-08-18 with FR-RATE-62 per `03:310`), `cells`, `created_by_operation`, `created_by_import`; and carrying `bulk_operations`, `diff_vs_previous`, `diff_vs_seed`, which the model does not define. `slug`/`version` come from the composed `common/artifact-envelope.schema.json` and are not divergences. **And one defect harder than missing-or-extra: the contract is unsatisfiable for half of what FR-RATE-62 defines.** Its `required` list carries `rows`, typed `array` with `minItems: 1`; `_cells_match_storage_mode` raises *"a parquet version addresses its cells by a BlobRef, never inline rows (FR-RATE-62)"* whenever `storage != ROWS`. So a parquet-stored `RateTableVersion` — the whole point of FR-RATE-62's spill-above-threshold rule — **cannot satisfy this schema at any value.** The file is not stale, it is wrong. **The code is not the stale side**: `RateTableVersion.storage: RateTableStorageMode` exists with a `_cells_match_storage_mode` validator, so FR-RATE-62 is implemented and only the Phase-0 hand-authored contract was left behind | W11 Slice 1 | 2 | **carry forward with an owner, and W9/W10 are NOT reopened.** Reopening a Work close is the maintainer's alone (`CLAUDE.md` §13) and this is a missing *check*, not a defect in what those workstreams delivered — reopening would re-litigate rather than fix. Three things are separated deliberately: (a) *landed with this row* — the three exclusion reasons now say what is true, because a false reason is what let this survive two closes; (b) *W11's, accepted* — Ruling 12's fourth obligation on Task 1.4, which keeps `scoring` from becoming the fourth uncompared shape, the only part that is in W11's own scope; (c) *unowned, needs its own authorisation* — the systemic check comparing every spec-declared shape against its hand-authored contract, and whether the three types join the generated tier. **Asked and answered: does this row own the `storage` correction itself, since the contract is wrong on `main` now?** No, and deliberately. A hand-patch of `storage` alone would leave six divergences in the same file, produce a contract that is wrong in fewer places and still wrong, and spend the evidence that motivates (c) — the divergence is structural, not a missed field. **The unsatisfiability makes that decisive rather than merely tidy**: adding the four missing fields still leaves `required` demanding `rows` from a parquet version, because `required` has to become *conditional on a field the contract does not yet have*. There is no partial patch that leaves this file correct. The correction *is* (c): regenerate from `model-schema`, which is what joining the generated tier means. Until then the contract is knowingly stale and this row is where that is written down. Assigning (c) a workstream row is re-planning. **Owner decided (Ruling 29): the §14 review at W11's close**, bundled with F29 and the mypy files-coverage gap (F33) as **one** gate-coverage item — one mechanism answers all three, so a partial fix on any single row is not the target shape. Sibling of F26: both are gaps where a green gate means *unrun*, not *passed* |
| NT-0010/0011 adoption pilot (F28) | Process findings from running W11 Slice 1 Task 1.1 end-to-end under the adopted process — P1, P1b, P2 through P14 — each enumerated with its own disposition in [`work/nt-0010-0011-adoption/pilot-findings.md`](work/nt-0010-0011-adoption/pilot-findings.md) §Dispositions. **One row rather than one per finding, deliberately**: they concern several different artifacts, and copying them here would create the second copy `NT-0003` names, which would then age against the record it was copied from. Precedent for one row carrying several owners is F-W10-1 | `nt-0010-0011-adoption` §15 step 6 | 2 | **Fixed** — P8 (the vendored `task-brief` regression, `5b82b18`, verified on both heading conventions), P9 and P10 (every ruling re-derived across narrative, Files, Steps and Acceptance; rule 5 landed), and P13 (its diffing clause is in rule 5 on `main`; this row first listed it as carried, which was wrong when written). **§15 step 7 follow-up, landed 2026-08-29 after acceptance** — P1 and P1b's operational half and P2 in `.claude/roles/watcher.md`; P2 and P3 and P6 in `.claude/roles/reporter.md`; P4, P11 and P14 in `docs/process/delivery-process.md`. **Still carried: P5**, because no document owns the stand-down procedure it is against — which is the finding. **Carried to the §14 review as proposals** — P7, P12, and P1b's working-note half, filed **unnumbered** in `docs/audit/plan-reviews.md` (PR #389) and deliberately not landed, because a review's output is a proposal. *(This row first said "including rule 6, drafted" — the writer's-half rule was never drafted under that number, and "rule 6" collided with a second, unrelated proposal.)* *No total is stated in this row: the first version said "two live divergences" for F27 and "thirteen findings" here, and both aged within the hour. The three lists above are the artifact.* **The pilot closing discharges no carried item**; the adoption's closure record lists each again with its resolution (`CLAUDE.md` §14) |
| Error codes across the spec/code boundary (F29) | Nothing checks error codes in either direction. `scripts/audit-docs.py`'s check 10 reads only `docs/specs/*.md` and tests only that no code is claimed as owned by two modules; it never opens `backend/src/app/errors.py`. So a code can be listed as owned by a spec and exist nowhere in the implementation, and a code can exist in `errors.py` owned by no spec, and the gate stays green either way. `PlatformError.__init__` catches the second direction at runtime, on the first raise; **nothing catches the first at all.** Verified: `MODEL_CALL_FAILED` is owned by `03` §5.1 as of `b826790` and absent from `errors.py`, while `REBASE_AMBIGUOUS`, `IMPORT_PARSE_ERROR` and `NO_RELATIVITIES` are present — so the gap is real and the spec-first case is legitimate, which is why the check must compare rather than forbid | W11 Slice 1 | 2 | carry forward — **owner decided (Ruling 29): the §14 review at W11's close**, bundled with F27(c) and the mypy files-coverage gap (F33) as **one** gate-coverage item — one mechanism answers all three. Constraint on that mechanism, from Ruling 21: it must compare, not forbid — an unregistered spec-declared code is the *designed* state until raised, so a check demanding registration would fire on day one. Sibling of F27: same shape (a document describing an artifact nothing compares it to), different pair. Raised by the decision-maker in PR #368's findings section |
| `balance-watch` optional dependency (F30) | `.claude/skills/balance-watch/scripts/balance_watch.py` imports `ceiling_meter` behind a `try/except ImportError`, and `ceiling_meter` **is not in the repository** — `git ls-files` returns nothing. Its only copy on disk is in a handover directory for a workstream that closed four days earlier. The live poller runs with `CEILING_METER_DIR` unset, so `m is None` and the `live_limit_events` block is dead code, **silently**: the arm banner reports interval, threshold, heartbeat, token file and log file, and says nothing about whether limit-event detection is active. `NT-0012`'s shape with borrowed *code* rather than a borrowed credential | W11 Slice 1 | 2 | carry forward — **owner decided: W11** (Ruling 29, as corrected by `docs/plans/2026-08-29-w11-f30-ceiling-meter-addendum.md`). Delete the `ceiling_meter` import, `CEILING_METER_DIR` and the `live_limit_events` block — **not** because the maintainer's manual five-hour relay superseded them, which is unestablished and was wrongly stated as decided, but because `ceiling_meter` is in no tracked file and its only copy is in the W6b handover two teams ago, so nothing can enable the branch. `SKILL.md` must say that session-limit detection is **performed by nothing**, and that filing a module in the repository is what it would take — never that something replaced it. **Unconditionally, independent of that: the arm banner must state whether limit-event detection is active** — a guard that degrades silently is the defect regardless of which branch is right |
| Roster publishing emits a constant (F31) | `watcher.md` says the watcher's roster state *"derives from TaskList + lead messages"* and publishes it as **the single source of team state for the reporter**. The script that did it (`update-roster.sh`, handover-local, never filed) is a **heredoc emitting a fixed string** — only the timestamp is substituted. Verified against the publish at 16:08:33Z on 2026-08-29: it reported the executor *"holding — waiting for #329 to merge"* when #329 was long MERGED, *"Sixteen PRs merged; main at 6554a76"* when `main` was `4ec7fd1`, and a gate that had already been accepted and closed. **Every substantive field was false and only the freshness timestamp was true**, so the file looked healthier the longer it was wrong | W11 restart cleanup | 2 | carry forward — **(b)'s owner decided: the §14 review**, as a fourth charter amendment beside R6's three (Ruling 29) — drop `watcher.md`'s clause describing derived roster state, which now has no implementation at all, the honest state and better than one that lies. **(a) needs no owner: already discharged** — `update-roster.sh` was always handover-local; "do not carry it forward" is satisfied by not carrying it, not by removing anything from this repository. **A successor either builds the derivation or the charter drops the claim** — the one thing it must not do is inherit a constant with a live timestamp. Same shape as pilot finding P6 (a documented mechanism nothing performs), sharpened: here a freshness indicator updates while the content it vouches for is frozen, which is `NT-0007`'s "a boundary metric reads zero by construction" in a different dress |
| Ruling 16's acceptance-test premise (F32) | Ruling 16 (`docs/plans/2026-08-29-w11-slices-2-4-rulings.md:145`, Acceptance test item 1) claims Ruling 10's `load_bundle` purity property "currently lives in a ruling and in no acceptance block anywhere" and "becomes expressible for the first time" in Slice 2. False against `docs/plans/2026-08-29-w11-1-evaluator-core.md`'s own Task 1.3 Acceptance section, filed earlier the same day, which already names it: "`load_bundle` is pure with respect to the cache (Ruling 10)... `lint-imports` staying green is the mechanical half" — naming a mechanical half implies a behavioural half was already owed. PR #406 (Task 1.3) added that test directly (`test_load_bundle_is_pure_with_respect_to_any_cache`, independently re-run against PR #406's tree and confirmed passing) rather than deferring to Slice 2 — correctly, since the property depends only on `load_bundle` itself, never on anything Slice 2 builds above it. | W11 Task 1.3 audit | 2 | *resolved 2026-08-29 (`docs/plans/2026-08-29-w11-algorithm-pin-maturity.md`, "Correction to Ruling 16") — Ruling 16's own author verified independently and confirmed F32 right on both halves: the property was already in an acceptance block, and was discharged in Slice 1, not Slice 2. Only Ruling 16's acceptance item 1 is withdrawn; item 2 (the degraded read) stands. Task 2.1 must not re-implement the purity test — it is in the tree and passing.* |
| mypy files coverage (F33) | `uv run mypy`'s configured `[tool.mypy]` `files` (`pyproject.toml:101`) lists three `src` trees only — `packages/model-schema/src`, `packages/pricing-core/src`, `backend/src`. Uncovered: every test file in the repo (no `tests/` directory is listed); `.claude/skills/`; and, wider than first reported, `scripts/` — the gate's own tooling (`audit-docs.py`, `req-coverage.py`, `generate-contracts.py`, `scope-audit.py`), unchecked by the strictest tool the gate runs. **Verified live, not asserted**: running `mypy` directly against `.claude/skills/balance-watch/scripts/balance_watch.py` (the exact file F30 concerns) finds two real errors nothing in CI reports today — the `ceiling_meter` import F30 already names (`import-not-found`), and a second, previously unrecorded one at its own line 111 (`"object" has no attribute "__iter__"`, `attr-defined`), both consequences of the same unguarded-optional pattern. Could not independently confirm an equally concrete instance on the test-file half within this audit's effort budget; the structural gap there is verified directly from `pyproject.toml`, the live defect is not. Same categorical shape as F27(c) and F29 — a mechanism checks some artifacts and not others, silently. | W11 Task 1.3 audit | 2 | carry forward — **owner decided (Ruling 29): the §14 review at W11's close**, bundled with F27(c) and F29 as **one** gate-coverage item, not an extension of F26 (F26 is a path-filter/CI-trigger gap; this is a checker-coverage gap inside CI that already runs — the two overlap on one directory and their remedies are disjoint). Sizing is planning work, not a one-line `files` edit: `--strict` over every test file is a real undertaking. |
| `no_maturity_concept` sentinel unevidenced (F34) | Ruling 28 part 1 and Ruling 22's already-merged list-mate both ship without a tripwire. Reverting `backend/src/app/platform/rating_versions.py:276` from the sentinel back to an invented `approved` leaves `uv run pytest -q backend/tests/` at 1082 passed, 0 failed; the identical hole exists at `:324` for the `rate_table` branch, whose own comment states the safety property in terms and has no test. Every test occurrence of the sentinel sets it in the pricing-core fake resolver and never checks the backend produces it. Not cosmetic: the mutation proof shows the status is read the instant the exemption is lifted, so a regression lands green, sits dormant, and admits every algorithm as approved on the day someone removes the exemption expecting enforcement to start. Parts 3 and 4 each got a tripwire; part 1 got neither. Found by the PR #416 audit | W11-1.5 | 2 | fix before close — a tripwire at each of the two sites, written TDD against the reverted value; in flight on PR #416 |
| NFR-RATE-2 measured failing (F35) | Trace overhead measured against a 20 % ceiling on a 200-step motor structure, robust across 5 of 5 runs spanning +497 % to +723 %, with 97.2 % of the added cost needing to go. The dominant cost is **not** third-party: inside a 99.3 ms trace-enabled `async_evaluate()` the engine's own summed per-node time is 10.3 ms across 191 entries, and the balance builds and marshals a 1,119,097-byte payload whose size is set by our own `to_wire(passThrough: True)` in `packages/pricing-core/src/pricing_core/rating/runtime.py`. Engine-side cost tracks payload bytes at about 1 to 1 and does not track node count, so the remedy lever is ours. A minimal per-node entry is about 130 B against a mean entry of 5,859 B (1,119,097 B over 191 entries; the two directly measured components of one mid-graph entry, input 2,965 B and output 2,992 B, sum to 5,957 B — the two figures are 1.7 % apart and nothing downstream turns on which is used), which extrapolates to roughly 1.6 ms against the 1.847 ms allowance. The requirement's correctness limb is separately discharged but marked `FR-RATE-41`, so `req-coverage.py` reports the whole id uncovered. NFR-RATE-2 appears in no workstream row anywhere in the roadmap | W11-1.5 | 2 | carry forward with an owner — the remedy is a design change to what the trace records per node, gated on Ruling 35's off-path capture landing first; add the missing marker with the next fix |
| `req-coverage.py` mislabels its own count (F36) | `scripts/req-coverage.py:52` appends one entry per **marker occurrence** and `:58` prints `len(claimed[rid])` as "N test file(s)". Measured at **PR #438's head**, its own NFR-RATE-2 marker included, replicating the script's own regex and `testpaths` roots over the 162 `test_*.py` files it walks: **239 of 329 printed rows are wrong (73 %)**; summed, **1,936 reported against 699 true distinct files — 2.77× inflation**. Largest overstatements **by reported count**: `FR-MODEL-37` 36 against 9, `FR-MODEL-127` 30 against 4, `FR-MODEL-53` 27 against 5, `FR-MODEL-56` 26 against 3, `FR-RATE-20` 26 against 4, `FR-PLAT-48` 24 against 2. The two figures that do not move are the ones the finding rests on: **73 % of rows wrong, 2.77× inflation**, identical at `19eaabc`, at `7952f76` and here. The headline `requirements marked` is `len(claimed)` over distinct ids and is **unaffected**; the only failure path is `set(claimed) - specified`, also over distinct ids, so **the exit code and every verdict are unaffected**. The defect corrupts strength-of-evidence claims only. Re-deriving this means walking the tool's own roots — `req-coverage.py:48-52` globs `test_*.py` under `testpaths` and is immune, but an ad-hoc marker grep is not: `packages/README.md:25` quotes `@pytest.mark.req("FR-OVR-7")` in prose and reads as a test file. Remedy already decided: **OQ-PLAT-15 option (f)**, decided 2026-08-26. Found by the PR #416 audit | W11-1.5 | 2 | fix before close — relabel to what is counted; no behaviour change |
| NFR-RATE-12 names no storage format (F37) | The requirement budgets trace storage at 1 % of 50 M annual quotes under 200 GB/year but names no storage format, so it is silent on the variable that decides it. Measured at the 200-step structure NFR-RATE-1 itself names: one persisted `Trace` is 1,032,135 B uncompressed, which is 2.6x the 400 kB per trace that 200 GB over 500,000 traces allows, and 14,523 B under gzip -6, which is 4 % of budget at 71.1x compression. The redundancy that makes tracing slow is exactly what makes it compress — each of 189 steps carries a near-identical copy of the accumulated context. Two caveats make the uncompressed case worse: FR-RATE-42 persists 1 % plus 100 % of declines and errors, so the annual count exceeds 500,000, and the 50 M is a quote count rather than step-count weighted. Trace size is super-linear in step count (113 kB at 63 steps, 1.03 MB at 200). Found by the PR #416 audit | W11-1.5 | 2 | fix before close is not available — this is a spec amendment plus a Slice 4 obligation. Amend NFR-RATE-12 to state the storage assumption its budget depends on; Slice 4's persistence task must specify compression and Task 4D must measure the compressed size at the 200-step scale |
| NFR-RATE-1's without-GBM half not established (F38) | `docs/research/w11-task-1-5-nfr-rate-1-2.md` recorded "Both halves PASS" with the without-GBM half at p99 9.410 ms against a 15 ms bound. Five runs of the committed harness give 9.410 PASS, 10.162 PASS, 14.076 PASS at 94 % of budget, 24.816 OVER and 26.725 OVER — two of five breach. The correct verdict is measured, verdict unstable across runs, not established. Load does not order the results (the quietest and the busiest both passed) but it widens the spread about fivefold: stdev 3.5-4.0 ms under load against 0.706 ms quiet, so a quiet run's narrow spread makes one run look more trustworthy than it is. The leaf plan required the distribution reported, and the harness printed stdev, p50 and p90 for every run while the note dropped them — but reporting them WOULD NOT have revealed this, and run 1's own recorded figures show why. Its mean is 6.915 ms and its max 12.959 ms, so its largest sample sits 6.044 ms above the mean; at the 3.5-4.0 ms stdev the audit measured on its own re-runs that is only 1.5 to 1.7 sigma, where the expected maximum of 1,000 samples is about 3.24 sigma for a normal distribution (simulated over 20,000 trials: mean 3.243, median 3.198; the 3.7 first written here was sqrt(2 ln 1000) = 3.717, which is the leading asymptotic term rather than the expectation, and about 15 % high) and further out for a right-skewed latency one. Run 1's spread was therefore necessarily narrow, near run 5's 0.706 ms, and printed it would have read as reassuring. So the criterion as written is satisfiable by a single quiet run that still gets the verdict wrong. WHAT ESTABLISHES THIS VERDICT IS REPETITION UNDER VARIED LOAD; the distribution explains the result once you have both, rather than revealing it. The acceptance criterion needs both and carried only one. The with-GBM half is PASS in 5 of 5 but its margin narrows from about 4.0x to about 1.8x. Found by the PR #416 audit finding 1 | W11-1.5 | 2 | carry forward with an owner — NFR-RATE-1's full-path and sustained-load halves are Slice 2 Task 2D's, which must re-measure the component half with the distribution and enough runs to establish a verdict. This half must not be booked as delivered on the present evidence |
| Frontend test run opens a TCP connection to port 3000 (F39) | `pnpm --dir frontend test` prints an `AggregateError` with `ECONNREFUSED ::1:3000` and `127.0.0.1:3000` before any test output, then runs 97 files and 602 tests to completion with no failures and exit 0. Something in the vitest run attempts a TCP connect to port 3000 as a side effect and fails; nothing has explained what opens it. Reproduced 2026-08-30 on an independent run at a different tree on a quiet box, printing the identical AggregateError at the head of the vitest output, so it is deterministic rather than a one-off. Harmless today because the connection is refused, and the shape of a test that turns flaky under different timing or in an environment where port 3000 answers — in which case the failure would be silent rather than logged. Found by the gate-runner during the PR #416 gate | W11-1.5 | 2 | carry forward with an owner — the frontend workstream. Diagnose what opens the socket before it becomes a flake nobody can reproduce |
| Test-database cleanup is teardown-only, so any interrupted run poisons the next (F40) | `backend/tests/conftest_db.py`'s `_empty_the_database_after_the_session` truncates the shared Postgres only AFTER a session ends, and there is no setup-side guard. The session scope is deliberate — its docstring records that truncating per test would cost a full-table lock 430 times over — but it assumes every run reaches teardown. Any run that does not (a killed gate, Ctrl-C, a crash, an OOM) leaves its rows behind, and the next run on that box fails. The damage is misattributed by construction: it surfaces as `UniqueViolationError` on `uq_users_issuer_subject` inside `backend/tests/test_api_datasets.py`, a file unrelated to whatever the interrupted run was doing, so nothing points the next person at the real cause. The box is shared across worktrees, so the victim is usually someone else. A second, smaller weakness compounds it: two tests INSERT hardcoded identity literals — `(https://provider.example, analyst)` and the same issuer with `owner` — with no randomisation, so they collide with any surviving row rather than generating a fresh key. Observed 2026-08-30 during PR #416's certifying gate: 2 failed / 2292 passed, reconciling against `--collect-only`'s 2297. Root cause was three gate runs killed mid-pytest, which the lead caused by merging register PRs while a gate was in flight. **The tests then passed in isolation** — because the completing run's own teardown had by then emptied the database — which is a misleading green the diagnosis nearly stopped at; `users` was verified at 0 rows directly against Postgres rather than inferred. Found by w11-executor-416fix | W11-1.5 | 2 | fix before close is not required — no shipped behaviour is affected. Empty the database at session START as well as teardown, so a poisoned box self-heals rather than failing a later unrelated run; and randomise the two hardcoded identity literals. Owner: whoever next touches `conftest_db.py` |
| NFR-RATE-9 (F41) | Two limbs, and only part of one is delivered (`03:805`): "the scoring endpoint targets 99.95 % monthly, degrading to the last-known-good cached bundle if metadata storage is unavailable." PR #435 (W11 Slice 2 Task 2A) delivers the slot's ref → hash memo — the half that makes the degraded read reachable at all, since ref → `Bundle` → hash is itself a metadata read — and marks four tests `NFR-RATE-9`. Those markers are the requirement's first anywhere: at `19eaabc` no `@pytest.mark.req("NFR-RATE-9")` existed in `backend/` or `packages/`, and `req-coverage.py` does not report the id at all. They are four occurrences in one file, `backend/tests/test_bundle_slot.py`, and they reach `main` only when #435 merges — at which point `req-coverage.py` begins counting NFR-RATE-9 among its marked requirements on the strength of part of one limb. The end-to-end degraded read over HTTP is Task 2B's; the 99.95 % availability target is measured against a deployed service and nothing is deployed until W14. Ruling 16 clause 5 (`docs/plans/2026-08-29-w11-slices-2-4-rulings.md:129-131`) says so in terms — the figure "is not discharged by W11 and must not be booked as though it were" — and its Disposition declares this row owed. Filed 2026-08-30 rather than at the close, on the lead's instruction: the row is owed either way, costs nothing to write now, and the close's owed list already runs to thirteen items — a list that lost NFR-RATE-13/14 for two workstreams even though a register row, F-W9-1, existed for them the whole time. An earlier draft of this row gave a different reason: that the overstatement was already live in `req-coverage.py`'s output. That reading was taken at `cea3094` in an audit worktree and written without naming the tree it came from. At `origin/main` nothing is overstated yet, and the correct tense is future. | W11-2 | 2 | carry forward with an owner — W14, for the availability target; the degradation mechanism is recorded as delivered in part, with the memo half in PR #435 and the end-to-end HTTP proof owed by W11 Slice 2 Task 2B. The W11 close must not read `req-coverage.py`'s marked count as evidence that either limb is discharged |
| FR-RATE-34 (F43) | Three limbs in one sentence (`03:161`), and they have three different answers. **L1 resolution** — "evaluates one Quote Context against the Rating Version currently live in the target environment" — is **not delivered**: Ruling 14 defers live resolution to W14 and replaces it with a 409 `NO_LIVE_RATING_VERSION` refusal, because `live` is a property of a Deployment (FR-RATE-23). **L2 response** — "returning the ladder, outputs, and (optionally) a Trace" — is W11 Slice 2 Task 2B's, and is reached through an explicit `rating_version_ref`, which is FR-RATE-35's mechanism rather than FR-RATE-34's; a marker on an explicit-ref test evidences L2's response shape and never L1. **L3 latency** — "Target p99 < 50 ms server-side (NFR-OVR-1)" — is measured by Task 2D, so its state is open rather than settled at the time of writing. Two hazards recorded because neither is visible from the requirement row: `req-coverage.py` reports FR-RATE-34 as marked once any limb is marked, so the marker count is not evidence about which limb; and **the row itself carries no dated amendment**, so it still asserts live-in-target-environment resolution as the shipped behaviour, with the divergence recorded only in `03` §5.1's `NO_LIVE_RATING_VERSION` note (`03:542`) and in `docs/audit/plan-reviews.md`. A reader who opens FR-RATE-34 alone concludes the default-live path is delivered. Filed 2026-08-30 against `e16c459`; `plan-reviews.md:980-1000` makes this row a binding condition on the W11 close. | W11-2 | 2 | split verdict. **L1: carry forward with an owner — W14**, with the deployment work that gives `live` a referent; the refusal branch is permanent, not a stub. **L2: delivered** by Task 2B. **L3: not settled by this row** — Task 2D measures it, and the verdict is finalised at the W11 close against 2D's figures, not here. The close must also decide whether L1 warrants a dated amendment to FR-RATE-34's own text: a register row records delivery, it does not stop a requirement misleading its next reader |
| FR-RATE-40 (F44) | Four limbs (`03:173`), of which W11 touches one. A Rating Version cannot reach `approved` without: (1) a passing **Regression Suite**; (2) a **Dislocation Run** against the current live version over an agreed portfolio; (3) a **change summary** (FR-RATE-27); (4) where the insurer has enabled it, a passing **GIPP check** (`04-optimisation.md`) (R4). Only limb (3) is enforced anywhere at `e16c459`: `approvals.submit` (`backend/src/app/platform/approvals.py:146`) refuses a blank summary with 422 at `:170-176` citing FR-GOV-10, and rating-version submission reaches it — `rating_versions.submit_for_review` (`:153`) calls `approvals.submit` at `:178`. W11 Slice 2 Task 2C adds the attribution that mechanism lacks: at `e16c459` there is **no `@pytest.mark.req("FR-RATE-40")` anywhere in `backend/` or `packages/`**, so `req-coverage.py` cannot see the one control that exists. Limbs (1), (2) and (4) have no implementation: `approvals.submit` validates exactly the change summary and the existence of an approval-policy entry, and nothing else — so `03` §5.1's endpoint row for `POST /api/v1/rating-versions/{id}/submit`, "evidence completeness checked (FR-RATE-40)", is one-quarter true as written. Limb (4) is additionally a permission with no failing case — "where the insurer has enabled it" — and is untestable until the enablement it is conditioned on exists. Filed 2026-08-30 against `e16c459`; `plan-reviews.md:980-1000` makes this row a binding condition on the W11 close. | W11-2 | 2 | carry forward with owners, per limb. **(3) delivered and, with Task 2C, attributed.** **(1) Regression Suite — W12**, the testing workstream that builds it. **(2) Dislocation Run — W13**, which builds FR-RATE-46; F-W9-2 already carries the FR-RATE-61 gate that specialises this limb, and the two resolve together. **(4) GIPP check — the optimisation workstream that owns `04`**, and it stays untestable until insurer enablement exists to condition it on. The W11 close must not read a single FR-RATE-40 marker as evidence of the gate: the marker Task 2C adds names limb (3) alone |

A carried finding is written here by the work-item close checklist
([`checklists/work-item-close.md`](checklists/work-item-close.md)) and by the phase close
checklist ([`checklists/phase-close.md`](checklists/phase-close.md)).

## NT-0005 discharges, recorded 2026-08-27

Two deferred NT-0005 items are discharged rather than filed. They are recorded here so
this register is the custody home NT-0005 asked for.

- **Item (c) expired.** The `W6b-9` to `W6b-1b` dependency, which the frozen slice map's
  dependency column does not carry: `W6b-1b` shipped as PR #194 and `W6b-9` shipped its
  checks. No roadmap row is needed.
- **Item (g) is placed.** The eight open questions that sat on no decision gate —
  OQ-OVR-10, OQ-DATA-12, OQ-DATA-13, OQ-PLAT-10, OQ-PLAT-11, OQ-PLAT-12, OQ-PLAT-13,
  OQ-PLAT-14 — are all named in the `docs/roadmap.md` §10 note of 2026-08-26, among the
  twenty questions placed or recorded that day.
