# Global register of open findings

One row per open finding, keyed by the requirement or artifact id it concerns. Each row
names the work item that carried it, the phase, and the decision.

A finding is not removed when it resolves. Its row is annotated in place — **Resolved
<date>**, naming the PR or commit that discharged it and quoting what the annotation
supersedes — because a deleted row leaves every citation to it dangling, and
`scripts/audit-docs.py` check 25 resolves finding citations against this file. A row is
removed only when the phase whose register carries it is archived.

An unowned row must name the event that next confirms or assigns its owner. Absent a named
event it decays to the next `CLAUDE.md` §14 plan review, which must give it a disposition
rather than merely list it; a row that reaches a review and leaves it unchanged is a review
finding, not a register one.

A Decision cell opens with one of: the register's own disposition vocabulary — `fix before
close`, `accept`, `carry forward`, `split verdict` — each with its qualifiers (`with an
owner`, `with a trigger`, `unowned`, `phase boundary`, `provisional owner`); or `CLAUDE.md`
§13's four verdicts — `delivered but untested`, `deferred with an owner`, `reassigned`,
`not started` — which are binding, already appear (F53, F54), and may not be linted away by
a register-local vocabulary; or the negated form F37 and F40 need — a decision that a fix is
**not available** or **not required** before close, which must state where the work goes
instead. Every row carries one of the five ownership shapes: workstream, event, trigger,
next-toucher, unowned-pending-authorisation.

Status is not a Decision. The five rows that write one there (F-W10-1-1, F-W10-2-1,
F-W10-2-2, F32, F28) are corrected when P4 gives status its own field, or sooner.

**Every data row begins with a literal `|`.** GFM itself does not require this — a table
row missing its leading pipe still renders — but `scripts/register-lint.py`'s parser treats
that leading `|` as the sole test for whether a line is even a candidate row: a line
without it is skipped (`if not line.startswith("|"): continue`) before it is counted, before
its fields are split, and before the `assert classified == seen` accounting guard that
exists specifically to make a silent row-loss loud can see it — that guard cannot detect a
class of loss its own counted population never included. The table-contiguity check added
after F52 (`a3b9c9e`) does report such a row as a structural problem, because it still
carries four or more unescaped pipes and so falls inside the swept range — the gap it
leaves is loud — but the row itself is still not parsed: no `Row` is produced, and none of
its five fields is read. A row loses its leading `|` only by mistake; nothing but this
sentence keeps one from being written that way.

| Finding id | Concerns | Work item | Phase | Decision |
|---|---|---|---|---|
| FR-DATA-57 (F6) | `unrun_layers` projection — Phase 2 validation-report successor | W7-4 | 2 | carry forward with an owner (Phase 2 validation-report workstream) |
| FR-DATA-52 (F7) | exposure-ordered top-20 deferral — trigger has not fired | W7-4 | 2 | carry forward with a trigger (a named exposure-ordered reader), unowned by design |
| 03 rating surface (F8) | compile, score, rate tables, deployment | — | 2 | carry forward — phase boundary |
| wf-01 §4 surfaces (F9) | bandings, Peril Structure, reconciliation | W7-5 | 2 | carry forward — phase boundary |
| Phase 2/3/4 unevidenced (F22) | FR-MODEL-6/40/82/115/121; FR-PLAT-15/23-29/31-36/49/50/56/60/61/64; FR-GOV-16/17/18/27-35/38-45; NFR-OVR-1..8/10/11; NFR-PLAT-1/2/5/6/8/9/10; NFR-GOV-1/3-7 | — | 2/3/4 | carry forward — phase boundary; owners are the later-phase workstreams named in the roadmap |
| FR-OVR-20 (F14) | `_minor` suffix rule — enforcement invisible to `req-coverage.py` | W6b-15 | 1b | fix before close — add a test naming FR-OVR-20 |
| FR-PLAT-59 (F17) | no IdP in prod — enforcement not marker-evidenced | W6b-14 | 1b | fix before close — add a marker to the repository-invariant test |
| FR-OVR-22 (F13) | route reachability — Vitest-enforced, Python-marker-blind | #136 | 1b | accept — alternative instrument (Vitest), positive control + mutation verified |
| FR-OVR-21 (F15) | §5.3 cell is prose | W6b | 1b | accept — declared-prose affordance |
| FR-PLAT-55 (F16) | browser PKCE — Vitest-enforced | W6b-10 | 1b | accept — alternative instrument (Vitest) |
| NFR-PLAT-4 (F18) | compose < 5 min — measured 27 s | W6b-14 | 1b | accept — measured, recorded |
| FR-OVR-9 (F19) | pseudonymisation — ingestion enforces; PII-guard gap has a roadmap home | W6b | 1b | accept — enforcement exists; the PII-guard gap is recorded in the roadmap |
| Cross-cutting OVR (F20) | FR-OVR-2/4/10/11/12/14/15/16/19 | W6b/W7 | 1b | accept — conventions, ADRs, audit checks |
| Measured NFRs (F21) | NFR-DATA-1/2, NFR-MODEL-1..5, 10..13 | W4/W5/W7 | 1b | accept — measured, not asserted |
| NFR-RATE-13/14 (F-W9-1) | validate-inbound-never-outbound; nthread=1 per model_call — design constraints whose measurement belongs to the scoring path | W9-3 | 2 | carry forward with an owner — the W11 scoring workstream; W8's measurements recorded (NFR-RATE-13 p99 0.070 ms, NFR-RATE-14 p99 1.626 ms) |
| FR-RATE-61 (F-W9-2) | approximation-mode Rating Versions cannot reach `approved` without a Dislocation Run against their own exact-mode baseline (needs FR-RATE-46) — orphaned at birth, no workstream row's numeric range ever named it | — | 2 | carry forward with an owner — W13 (FR-RATE-46, the Dislocation Run it needs, is W13's; the check itself specialises FR-RATE-40's general approval-evidence gate, which W11 builds) |
| FR-RATE-25 (F-W9-3) | **Six clauses, enumerated (`03:136`): (1) DAG acyclic and fully connected — enforced and tested, but under `FR-RATE-1`'s marker: `model_schema/rating.py:361`'s `_graph_invariants` (Kahn's-algorithm cycle check + input/output reachability) runs automatically inside `compile_bundle` via `RatingAlgorithm.model_validate` (`compile.py:408`), tested by `test_rating_algorithm.py`'s `test_a_cycle_is_refused`/`test_an_orphaned_step_is_refused` — none of it marked `FR-RATE-25`. (2) all references resolvable and at a sufficient maturity (FR-OVR-14) — the maturity half is enforced (`compile.py:424`'s `_APPROVED_OR_BETTER`/`PIN_NOT_APPROVED`) and tested, but the test is marked `FR-OVR-14`; the resolvable half is untested — every fixture's `FakeResolver` resolves whatever it is asked, so a genuinely-unresolvable pin is never exercised. (3) all types compatible — enforced transitively (`compile_bundle` re-runs `validate_algorithm`, `compile.py:260`, whose `_check_result_types`, `compile.py:113`, is `FR-RATE-13`'s), but no test drives a type mismatch through `compile_bundle` itself and nothing is marked `FR-RATE-25`. (4) all constraints satisfiable — no implementation anywhere: `constraint` (case-insensitive) occurs exactly once in `packages/pricing-core/src/pricing_core/rating`, in `compile_bundle`'s own docstring (`compile.py:390`) restating this clause, not implementing it. (5) no `control`-intent factor in a rateable path (`02` FR-MODEL-3) — no implementation anywhere: `git grep -in intent -- packages/pricing-core/src/pricing_core/rating` is empty; `FactorIntent`/`REFUSED_FACTOR_INTENTS` exist only in `02`'s own fit-time refusal (`pricing_core/modelling/factors.py`), which never reaches the rating DAG. (6) no unapproved custom objective transitively reachable — the pin-maturity loop rejects an unapproved *directly-pinned* custom objective (same code as (2)) but not one reached transitively, and it is wholly untested: every fixture in both `packages/pricing-core/tests/test_rating_compile_bundle.py` and `backend/tests/test_rating_version_compile.py` sets `custom_objectives: []`.** The repository's one and only `@pytest.mark.req("FR-RATE-25")` (`test_rating_compile_bundle.py:170`) tests none of the six: its docstring reads "a boundary-guard violation re-checked at compile is named," and its assertion is `EXPRESSION_UNGUARDED_DIVISION` — `FR-RATE-57`'s zero-division guard, re-run inside `compile_bundle`. W9's closure record (`docs/audit/work/W9/README.md:27`) verdicted `FR-RATE-22..27` delivered, citing "marker-evidenced (... 25: 1 ...)" — the count is correct and the verdict does not survive reading what that one test checks. Not a W11 concern: FR-RATE-25 is `03` §3.4, outside W11's own §3.7/§3.8/NFR scope. | W9-3 | 2 | carry forward — **owners decided (Ruling 29, `docs/plans/2026-08-29-w11-algorithm-pin-maturity.md`, under the maintainer's M2 delegation): W11, in Ruling 28's follow-up PR**, for clauses (1), (2)'s maturity half and (3) — point `FR-RATE-25`-marked tests at the mechanisms that already run, which that PR is already touching in `compile_bundle` and its tests (cheap); **the §14 review at W11's close**, for clauses (4), (5) and (6)'s transitive half — place them on a workstream or accept them as not-built with a reason, since validation code for these exists nowhere (not cheap). `03`/W9 is not reopened, same as F27: this is a missing check against a requirement W9 declared delivered, not a defect in what W9 built elsewhere. |
| FR-RATE-17/18/19/20 (F-W10-1) | Cell diffs, bulk operations, validation, CSV/XLSX import/export | W10-1 | 2 | carry forward with owners — W10-2 (diffs, validation: FR-RATE-17/19), W10-3 (bulk ops, import/export: FR-RATE-18/20). *W10-2 half resolved 2026-08-28 (PR #302 — FR-RATE-17/19 delivered and tested; the remaining FR-RATE-17 wiring is F-W10-2).* |
| FR-RATE-16 (F-W10-1-1) | seeded_from lineage metadata: implemented in W10-1 but test marker not separate (tested within FR-RATE-15 immutability suite) | W10-1 | 2 | *resolved 2026-08-28 (PR #302) — FR-RATE-16 markers added to the API tests; lineage delivered and tested.* |
| FR-RATE-17 (F-W10-2) | exposure-weight wiring at the diff endpoint: pricing-core accepts caller weights (DP1) but the endpoint passes none; the portfolio-dataset join is scheduled in no slice | W10-2 | 2 | carry forward with an owner — portfolio-dataset integration (wiring lands with the join) |
| DP3 diff cache (F-W10-2-1) | the DP3 ruling's Redis cache (keyed by version hash + portfolio identity) is unbuilt and its deferral was not declared in PR #302 — silent until the closure record | W10-2 | 2 | *resolved 2026-08-28 (PR #311) — the cache shipped with W10-3D: keyed by both versions' content hashes + the portfolio dataset version's identity (never a date), no TTL, fail-open both get/set, 200 read path only (the worker stores the diff as a blob instead).* |
| FR-PLAT-48 typed diff 200 (F-W10-2-2) | the W10-2 diff endpoint answered `dict[str, Any]` while the platform computes a typed RateTableDiff — the ADR-0002 divergence class, invisible to the drift check | W10-2 | 2 | *resolved 2026-08-28 (PR #311) — the diff 200 is declared `$ref RateTableDiff` in the generated OpenAPI, 202 as `$ref Job`; the deferral was recorded with the discharge.* |
| 03 §5.1 `POST /rate-tables/{slug}/versions` (F-W10-3) | Phase-0 row "New Rate Table Version with change note" has no route; version creation happens via the three create paths (seed-from-model creates the table or appends, bulk-operation, import-confirm), each change-note-required | W10 close | 2 | carry forward with an owner — the W15 rate-table editor slice (the manual editing entry point); the plan's W10-1 T3 create/GET/list endpoint tasks fold into this row |
| `.claude/roles/`+`.claude/skills/` CI gap (F26) | No workflow watches either path except `docs/notes/**`; a PR touching only `.claude/roles/**` gets zero CI of any kind — confirmed twice independently (PR #327's merge SHA: zero runs in `gh run list`; PR #338 ran a `docs` workflow only because it also touched CLAUDE.md, an in-filter file, not because the filter learned about roles). Plan review 11 re-verified this directly and found it unchanged: `.github/workflows/docs.yml`'s `paths:` filter still lacks `.claude/roles/**`/`.claude/skills/**` (`docs/audit/plan-reviews.md:2546-2554`). **Note added 2026-09-02 — this row is not merely still open, it has been incorrectly reported as discharged in a durable artifact.** PR #561's squash-merge commit (`f226891`, 2026-09-02T00:54:44Z) states: *"That is register finding F26's path-filter gap with a live instance."* It is not — #561 added `docs/**` to `python.yml`'s filter, a different workflow, for a different symptom (see **F71**, filed alongside this note). The mechanism, traced by the lead rather than left unexplained: plan review 11 item 11.2 labels this row *"F26 (the CI path-filter gap)"* — a generic label — and a later, differently-shaped path-filter gap was matched to that label without opening the row itself. Verified unchanged at this note's date: `docs.yml`'s filter is exactly as plan review 11 found it; nothing about `.claude/roles/` or `.claude/skills/` having no CI trigger has moved. A reader of `git log` alone would conclude this row is handled; it is not. | `nt-0010-0011-adoption` audit | 2 | carry forward — **owner decided: W11** (Ruling 29, under the maintainer's M2 delegation) — **W11 closed (twice) without landing it**; plan review 11 item 11.2 asks for a fresh owner (maintainer/lead decision, not made here) now that W11 cannot discharge it. Two parts still required together, neither hollow alone (a `paths:` filter naming both directories, and a check that verifies content, not just presence); the related ruff-exclusion inversion is resolved separately by PR #355. **#561 discharges none of this** — named here, dated, so the merge commit's claim does not stand unopposed in the record. |
| 03 rating shapes vs hand-authored contracts (F27) | `rating-version`, `rating-algorithm` and `rate-table` (`model-schema`) each have a hand-authored contract under `docs/contracts/schemas/` that nothing has ever compared — seven live divergences (FR-RATE-62, FR-RATE-63), one unsatisfiable at any value: a parquet-stored `RateTableVersion` cannot meet `rate-table.schema.json`'s `required: [rows]`. Full evidence: [`docs/audit/findings/F27.md`](findings/F27.md) | W11 Slice 1 | 2 | carry forward with an owner, and W9/W10 are NOT reopened (`CLAUDE.md` §13, maintainer's alone). (a) landed with this row; (b) W11's, accepted (Ruling 12); (c) owner decided (Ruling 29): the §14 review at W11's close, bundled with F29 and F33 as one gate-coverage item. Full reasoning: [`docs/audit/findings/F27.md`](findings/F27.md) |
| NT-0010/0011 adoption pilot (F28) | Process findings from running W11 Slice 1 Task 1.1 end-to-end under the adopted process — P1, P1b, P2 through P14 — each enumerated with its own disposition in [`work/nt-0010-0011-adoption/pilot-findings.md`](work/nt-0010-0011-adoption/pilot-findings.md) §Dispositions. **One row rather than one per finding, deliberately**: they concern several different artifacts, and copying them here would create the second copy `NT-0003` names, which would then age against the record it was copied from. Precedent for one row carrying several owners is F-W10-1 | `nt-0010-0011-adoption` §15 step 6 | 2 | **Fixed** — P8 (the vendored `task-brief` regression, `5b82b18`, verified on both heading conventions), P9 and P10 (every ruling re-derived across narrative, Files, Steps and Acceptance; rule 5 landed), and P13 (its diffing clause is in rule 5 on `main`; this row first listed it as carried, which was wrong when written). **§15 step 7 follow-up, landed 2026-08-29 after acceptance** — P1 and P1b's operational half and P2 in `.claude/roles/watcher.md`; P2 and P3 and P6 in `.claude/roles/reporter.md`; P4, P11 and P14 in `docs/process/delivery-process.md`. **Still carried: P5**, because no document owns the stand-down procedure it is against — which is the finding. **Carried to the §14 review as proposals** — P7, P12, and P1b's working-note half, filed **unnumbered** in `docs/audit/plan-reviews.md` (PR #389) and deliberately not landed, because a review's output is a proposal. *(This row first said "including rule 6, drafted" — the writer's-half rule was never drafted under that number, and "rule 6" collided with a second, unrelated proposal.)* *No total is stated in this row: the first version said "two live divergences" for F27 and "thirteen findings" here, and both aged within the hour. The three lists above are the artifact.* **The pilot closing discharges no carried item**; the adoption's closure record lists each again with its resolution (`CLAUDE.md` §14) |
| Error codes across the spec/code boundary (F29) | Nothing checks error codes in either direction. `scripts/audit-docs.py`'s check 10 reads only `docs/specs/*.md` and tests only that no code is claimed as owned by two modules; it never opens `backend/src/app/errors.py`. So a code can be listed as owned by a spec and exist nowhere in the implementation, and a code can exist in `errors.py` owned by no spec, and the gate stays green either way. `PlatformError.__init__` catches the second direction at runtime, on the first raise; **nothing catches the first at all.** Verified: `MODEL_CALL_FAILED` is owned by `03` §5.1 as of `b826790` and absent from `errors.py`, while `REBASE_AMBIGUOUS`, `IMPORT_PARSE_ERROR` and `NO_RELATIVITIES` are present — so the gap is real and the spec-first case is legitimate, which is why the check must compare rather than forbid | W11 Slice 1 | 2 | carry forward — **owner decided (Ruling 29): the §14 review at W11's close**, bundled with F27(c) and the mypy files-coverage gap (F33) as **one** gate-coverage item — one mechanism answers all three. Constraint on that mechanism, from Ruling 21: it must compare, not forbid — an unregistered spec-declared code is the *designed* state until raised, so a check demanding registration would fire on day one. Sibling of F27: same shape (a document describing an artifact nothing compares it to), different pair. Raised by the decision-maker in PR #368's findings section |
| `balance-watch` optional dependency (F30) | `.claude/skills/balance-watch/scripts/balance_watch.py` imports `ceiling_meter` behind a `try/except ImportError`, and `ceiling_meter` **is not in the repository** — `git ls-files` returns nothing. Its only copy on disk is in a handover directory for a workstream that closed four days earlier. The live poller runs with `CEILING_METER_DIR` unset, so `m is None` and the `live_limit_events` block is dead code, **silently**: the arm banner reports interval, threshold, heartbeat, token file and log file, and says nothing about whether limit-event detection is active. `NT-0012`'s shape with borrowed *code* rather than a borrowed credential | W11 Slice 1 | 2 | carry forward — **owner decided: W11** (Ruling 29, as corrected by `docs/plans/2026-08-29-w11-f30-ceiling-meter-addendum.md`). Delete the `ceiling_meter` import, `CEILING_METER_DIR` and the `live_limit_events` block — **not** because the maintainer's manual five-hour relay superseded them, which is unestablished and was wrongly stated as decided, but because `ceiling_meter` is in no tracked file and its only copy is in the W6b handover two teams ago, so nothing can enable the branch. `SKILL.md` must say that session-limit detection is **performed by nothing**, and that filing a module in the repository is what it would take — never that something replaced it. **Unconditionally, independent of that: the arm banner must state whether limit-event detection is active** — a guard that degrades silently is the defect regardless of which branch is right |
| Roster publishing emits a constant (F31) | `watcher.md` says the watcher's roster state *"derives from TaskList + lead messages"* and publishes it as **the single source of team state for the reporter**. The script that did it (`update-roster.sh`, handover-local, never filed) is a **heredoc emitting a fixed string** — only the timestamp is substituted. Verified against the publish at 16:08:33Z on 2026-08-29: it reported the executor *"holding — waiting for #329 to merge"* when #329 was long MERGED, *"Sixteen PRs merged; main at 6554a76"* when `main` was `4ec7fd1`, and a gate that had already been accepted and closed. **Every substantive field was false and only the freshness timestamp was true**, so the file looked healthier the longer it was wrong | W11 restart cleanup | 2 | carry forward — **(b)'s owner decided: the §14 review**, as a fourth charter amendment beside R6's three (Ruling 29) — drop `watcher.md`'s clause describing derived roster state, which now has no implementation at all, the honest state and better than one that lies. **(a) needs no owner: already discharged** — `update-roster.sh` was always handover-local; "do not carry it forward" is satisfied by not carrying it, not by removing anything from this repository. **A successor either builds the derivation or the charter drops the claim** — the one thing it must not do is inherit a constant with a live timestamp. Same shape as pilot finding P6 (a documented mechanism nothing performs), sharpened: here a freshness indicator updates while the content it vouches for is frozen, which is `NT-0007`'s "a boundary metric reads zero by construction" in a different dress. ***Verified still open 2026-08-31*** — review 9 (`docs/audit/plan-reviews.md:1979-1981`, proposal 5.3) drafted exactly this row's "charter drops the claim" branch and recommended applying it, but stated explicitly it is "not decided here." At `036337e` neither branch is yet taken: `watcher.md:11-24` still describes the derivation (with the UNIMPLEMENTED caveat) rather than dropping it, and no derivation has been built. The owner (the §14 review) has now acted — drafted, not applied — so this row's discharge is the maintainer's acceptance of proposal 5.3 followed by the role-file edit, not further register work. ***Amended 2026-09-01*** — the sentence above read *review 10* (`docs/audit/plan-reviews.md:1979-1981`, proposal 5.3); corrected to **review 9**: plan-reviews.md carries exactly one proposal 5.3, and it sits in review 9's section (prose `:1979-1981`, table row `:2066`; review 9 spans `:1233-2150`, review 10 begins `:2151`). Found auditing the NT-0016 landing package (PR #549). The discharge sentence's unqualified *proposal 5.3* is thereby review 9's |
| Ruling 16's acceptance-test premise (F32) | Ruling 16 (`docs/plans/2026-08-29-w11-slices-2-4-rulings.md:145`, Acceptance test item 1) claims Ruling 10's `load_bundle` purity property "currently lives in a ruling and in no acceptance block anywhere" and "becomes expressible for the first time" in Slice 2. False against `docs/plans/2026-08-29-w11-1-evaluator-core.md`'s own Task 1.3 Acceptance section, filed earlier the same day, which already names it: "`load_bundle` is pure with respect to the cache (Ruling 10)... `lint-imports` staying green is the mechanical half" — naming a mechanical half implies a behavioural half was already owed. PR #406 (Task 1.3) added that test directly (`test_load_bundle_is_pure_with_respect_to_any_cache`, independently re-run against PR #406's tree and confirmed passing) rather than deferring to Slice 2 — correctly, since the property depends only on `load_bundle` itself, never on anything Slice 2 builds above it. | W11 Task 1.3 audit | 2 | *resolved 2026-08-29 (`docs/plans/2026-08-29-w11-algorithm-pin-maturity.md`, "Correction to Ruling 16") — Ruling 16's own author verified independently and confirmed F32 right on both halves: the property was already in an acceptance block, and was discharged in Slice 1, not Slice 2. Only Ruling 16's acceptance item 1 is withdrawn; item 2 (the degraded read) stands. Task 2.1 must not re-implement the purity test — it is in the tree and passing.* |
| mypy files coverage (F33) | `uv run mypy`'s configured `[tool.mypy]` `files` (`pyproject.toml:101`) lists three `src` trees only — `packages/model-schema/src`, `packages/pricing-core/src`, `backend/src`. Uncovered: every test file in the repo (no `tests/` directory is listed); `.claude/skills/`; and, wider than first reported, `scripts/` — the gate's own tooling (`audit-docs.py`, `req-coverage.py`, `generate-contracts.py`, `scope-audit.py`), unchecked by the strictest tool the gate runs. **Verified live, not asserted**: running `mypy` directly against `.claude/skills/balance-watch/scripts/balance_watch.py` (the exact file F30 concerns) finds two real errors nothing in CI reports today — the `ceiling_meter` import F30 already names (`import-not-found`), and a second, previously unrecorded one at its own line 111 (`"object" has no attribute "__iter__"`, `attr-defined`), both consequences of the same unguarded-optional pattern. Could not independently confirm an equally concrete instance on the test-file half within this audit's effort budget; the structural gap there is verified directly from `pyproject.toml`, the live defect is not. Same categorical shape as F27(c) and F29 — a mechanism checks some artifacts and not others, silently. | W11 Task 1.3 audit | 2 | carry forward — **owner decided (Ruling 29): the §14 review at W11's close**, bundled with F27(c) and F29 as **one** gate-coverage item, not an extension of F26 (F26 is a path-filter/CI-trigger gap; this is a checker-coverage gap inside CI that already runs — the two overlap on one directory and their remedies are disjoint). Sizing is planning work, not a one-line `files` edit: `--strict` over every test file is a real undertaking. **Partial landing 2026-08-30**: `files` now also covers the repo-level `tests/` root, `examples/fremtpl2` and the two named `.claude/skills/` script dirs (`pyproject.toml`), all `--strict`-clean; landing it surfaced and fixed three more real bugs (a bare string where `MonotonicDirection.NONE` belonged, two unguarded reads in the balance poller, and a spurious-but-real-shaped mypy overload misresolution from `session`-name reuse nine times in one function of the demo seed script — found and fixed only after an earlier pass in this same landing had already treated the file as clean without re-running the check, `dev-commands`'s Verified log has both). Still uncovered: `packages/model-schema/tests` (144 errors), `packages/pricing-core/tests` (107), `backend/tests` (1243) — each needs its own scoped `mypy` invocation and cannot share one with either sibling (`Duplicate module named "conftest"`, confirmed, not assumed) — and `scripts/`, this row's own third named gap, not touched by this landing. Counts, the collision, and why are in `dev-commands`. This does not discharge F33; the §14 review at W11's close still owns the bundled disposition. It narrows what remains. ***Note added 2026-08-31*** — `scripts/` (this row's own third named gap) now carries more surface than when last measured: `scripts/audit-docs.py` gained checks 25 (#453), 26 (#451), 27 (#513) and 28 (#510) across the NT-0012/0013/0014 adoption, none of it inside `[tool.mypy]`'s `files`. Verified directly, not inferred: `uv run mypy` at `9e8783d` reports "Success: no issues found in 173 source files"; `uv run mypy scripts/audit-docs.py` run directly against the same tree reports 10 errors in 1 file (two `Match[str] \| None` assignment/unreachable pairs and a `str`/`tuple[str, tuple[str, ...]]` mismatch trio, `:1055-1206`) — none of which the gate ever sees. This does not change F33's disposition or its owner; it is the count this row already asks the §14 review to use, kept current rather than left at its 2026-08-30 figure. ***Note added 2026-09-02*** — two new instances landed today, and the workflow comment claiming coverage is quoted here for the first time rather than only described. `scripts/doc-id.py` and `scripts/_docid.py` (W37-2, PR #567, merged `2204ffb`) are the newest files in this gap; the executor who wrote them noted it directly rather than quietly fixing it ("`uv run mypy`'s bare invocation does not actually cover `scripts/` at all... `scripts/_docid.py` and `scripts/doc-id.py` are still held to `mypy --strict` in this PR (verified directly, both clean) — just not by the bare gate command"), and held both files to `mypy --strict` by hand as a substitute. `.github/workflows/python.yml` asserts the opposite of this row's own finding, in both its `push` and `pull_request` blocks, verbatim: "`ruff check .` and mypy cover this directory" — true of `ruff` (path-agnostic), false of `mypy`. **Reproduced by mutation, not only by reading config**: appended an unambiguous type error (`int = "a string"`) to `scripts/audit-docs.py`; `uv run mypy` still reported "Success: no issues found in 181 source files" and exited clean. **Positive control**: the identical line appended to a covered file (`packages/pricing-core/src/pricing_core/progress.py`) was caught immediately ("Found 1 error in 1 file"), confirming the gap is `scripts/`'s absence from `files`, not `mypy` failing generally. Both mutations reverted; nothing here is left in the tree. |
| `no_maturity_concept` sentinel unevidenced (F34) | Ruling 28 part 1 and Ruling 22's already-merged list-mate both ship without a tripwire. Reverting `backend/src/app/platform/rating_versions.py:276` from the sentinel back to an invented `approved` leaves `uv run pytest -q backend/tests/` at 1082 passed, 0 failed; the identical hole exists at `:324` for the `rate_table` branch, whose own comment states the safety property in terms and has no test. Every test occurrence of the sentinel sets it in the pricing-core fake resolver and never checks the backend produces it. Not cosmetic: the mutation proof shows the status is read the instant the exemption is lifted, so a regression lands green, sits dormant, and admits every algorithm as approved on the day someone removes the exemption expecting enforcement to start. Parts 3 and 4 each got a tripwire; part 1 got neither. Found by the PR #416 audit | W11-1.5 | 2 | fix before close — a tripwire at each of the two sites, written TDD against the reverted value; in flight on PR #416 |
| NFR-RATE-2 measured failing (F35) | Trace overhead measured against a 20 % ceiling on a 200-step motor structure, robust across 5 of 5 runs spanning +497 % to +723 %, with 97.2 % of the added cost needing to go. The dominant cost is **not** third-party: inside a 99.3 ms trace-enabled `async_evaluate()` the engine's own summed per-node time is 10.3 ms across 191 entries, and the balance builds and marshals a 1,119,097-byte payload whose size is set by our own `to_wire(passThrough: True)` in `packages/pricing-core/src/pricing_core/rating/runtime.py`. Engine-side cost tracks payload bytes at about 1 to 1 and does not track node count, so the remedy lever is ours. A minimal per-node entry is about 130 B against a mean entry of 5,859 B (1,119,097 B over 191 entries; the two directly measured components of one mid-graph entry, input 2,965 B and output 2,992 B, sum to 5,957 B — the two figures are 1.7 % apart and nothing downstream turns on which is used), which extrapolates to roughly 1.6 ms against the 1.847 ms allowance. ***Corrected 2026-08-30 — the sentence this replaces was stale, and what it concealed is worse than what it said.*** It read: *"the correctness limb is separately discharged but marked `FR-RATE-41`, so `req-coverage.py` reports the whole id uncovered."* The marker was added by `e16c459` (#438): `packages/pricing-core/tests/test_rating_score.py:494-495` now carries **both** `@pytest.mark.req("FR-RATE-41")` and `@pytest.mark.req("NFR-RATE-2")`, and that test does correctly check the clause it is marked for — traced-vs-untraced equality, the R3 property. **So the id is no longer reported uncovered, and the defect inverted rather than closed.** `req-coverage.py` has no per-clause granularity, so one marker on a two-clause requirement now reads as whole-requirement coverage while the ≤ 20 % latency clause has zero test assertions anywhere. The requirement went from *visibly* uncovered to *invisibly* half-covered, which is the worse of the two states. Same mechanism as F43's precedent. NFR-RATE-2 appears in no workstream row anywhere in the roadmap | W11-1.5 | 2 | carry forward with an owner — the remedy is a design change to what the trace records per node, gated on Ruling 35's off-path capture landing first. *(The "add the missing marker" clause this decision used to end on is discharged — `e16c459` added it. What replaces it is not a marker but a mechanism: nothing distinguishes a requirement whose every clause is tested from one where a single marker covers the easy half.)* ***Amended 2026-08-31*** — the gate this row's remedy was waiting on has opened, and the clause this row measures has not moved. `003f9d4` (#485, W11 Task 4B) lands Ruling 35's off-path trace capture, moving `FR-RATE-42`'s sampled production off the serving request. **This is not a partial fix and must not read as one**: Task 4B's own commit message says so directly — *"`POST /api/v1/score` now scores untraced for FR-RATE-42's purposes (FR-RATE-41's caller-requested `ctx.options.trace` is unchanged)"*. The explicit, synchronous `ctx.options.trace=True` path is exactly what this row's +497% to +723% measurement was taken against (FR-RATE-41), and Task 4B leaves it untouched. So: the precondition for building the remedy is now met; the measured clause itself is unchanged and still failing. **Shares its remediation lever with F55** — both point at trimming `TraceStep.consumed`/`.produced` to each step's own declared `consumes`/`produces` instead of the engine's full per-node accumulated context, which this row already named as "a design change to what the trace records per node" and F55 independently found and sized. |
| `req-coverage.py` mislabels its own count (F36) | `scripts/req-coverage.py:52` appends one entry per **marker occurrence** and `:58` prints `len(claimed[rid])` as "N test file(s)". Measured at **PR #438's head**, its own NFR-RATE-2 marker included, replicating the script's own regex and `testpaths` roots over the 162 `test_*.py` files it walks: **239 of 329 printed rows are wrong (73 %)**; summed, **1,936 reported against 699 true distinct files — 2.77× inflation**. Largest overstatements **by reported count**: `FR-MODEL-37` 36 against 9, `FR-MODEL-127` 30 against 4, `FR-MODEL-53` 27 against 5, `FR-MODEL-56` 26 against 3, `FR-RATE-20` 26 against 4, `FR-PLAT-48` 24 against 2. The two figures that do not move are the ones the finding rests on: **73 % of rows wrong, 2.77× inflation**, identical at `19eaabc`, at `7952f76` and here. The headline `requirements marked` is `len(claimed)` over distinct ids and is **unaffected**; the only failure path is `set(claimed) - specified`, also over distinct ids, so **the exit code and every verdict are unaffected**. The defect corrupts strength-of-evidence claims only. Re-deriving this means walking the tool's own roots — `req-coverage.py:48-52` globs `test_*.py` under `testpaths` and is immune, but an ad-hoc marker grep is not: `packages/README.md:25` quotes `@pytest.mark.req("FR-OVR-7")` in prose and reads as a test file. Remedy already decided: **OQ-PLAT-15 option (f)**, decided 2026-08-26. Found by the PR #416 audit | W11-1.5 | 2 | fix before close — relabel to what is counted; no behaviour change |
| NFR-RATE-12 names no storage format (F37) | The requirement budgets trace storage at 1 % of 50 M annual quotes under 200 GB/year but names no storage format, so it is silent on the variable that decides it. Measured at the 200-step structure NFR-RATE-1 itself names: one persisted `Trace` is 1,032,135 B uncompressed, which is 2.6x the 400 kB per trace that 200 GB over 500,000 traces allows, and 14,523 B under gzip -6, which is 4 % of budget at 71.1x compression. The redundancy that makes tracing slow is exactly what makes it compress — each of 189 steps carries a near-identical copy of the accumulated context. Two caveats make the uncompressed case worse: FR-RATE-42 persists 1 % plus 100 % of declines and errors, so the annual count exceeds 500,000, and the 50 M is a quote count rather than step-count weighted. Trace size is super-linear in step count (113 kB at 63 steps, 1.03 MB at 200). Found by the PR #416 audit | W11-1.5 | 2 | fix before close is not available — this is a spec amendment plus a Slice 4 obligation. Amend NFR-RATE-12 to state the storage assumption its budget depends on; Slice 4's persistence task must specify compression and Task 4D must measure the compressed size at the 200-step scale |
| NFR-RATE-1's without-GBM half not established (F38) | `docs/research/w11-task-1-5-nfr-rate-1-2.md` recorded "Both halves PASS" with the without-GBM half at p99 9.410 ms against a 15 ms bound. Five runs of the committed harness give 9.410 PASS, 10.162 PASS, 14.076 PASS at 94 % of budget, 24.816 OVER and 26.725 OVER — two of five breach. The correct verdict is measured, verdict unstable across runs, not established. Load does not order the results (the quietest and the busiest both passed) but it widens the spread about fivefold: stdev 3.5-4.0 ms under load against 0.706 ms quiet, so a quiet run's narrow spread makes one run look more trustworthy than it is. The leaf plan required the distribution reported, and the harness printed stdev, p50 and p90 for every run while the note dropped them — but reporting them WOULD NOT have revealed this, and run 1's own recorded figures show why. Its mean is 6.915 ms and its max 12.959 ms, so its largest sample sits 6.044 ms above the mean; at the 3.5-4.0 ms stdev the audit measured on its own re-runs that is only 1.5 to 1.7 sigma, where the expected maximum of 1,000 samples is about 3.24 sigma for a normal distribution (simulated over 20,000 trials: mean 3.243, median 3.198; the 3.7 first written here was sqrt(2 ln 1000) = 3.717, which is the leading asymptotic term rather than the expectation, and about 15 % high) and further out for a right-skewed latency one. Run 1's spread was therefore necessarily narrow, near run 5's 0.706 ms, and printed it would have read as reassuring. So the criterion as written is satisfiable by a single quiet run that still gets the verdict wrong. WHAT ESTABLISHES THIS VERDICT IS REPETITION UNDER VARIED LOAD; the distribution explains the result once you have both, rather than revealing it. The acceptance criterion needs both and carried only one. The with-GBM half is PASS in 5 of 5 but its margin narrows from about 4.0x to about 1.8x. Found by the PR #416 audit finding 1 | W11-1.5 | 2 | carry forward with an owner — NFR-RATE-1's full-path and sustained-load halves are Slice 2 Task 2D's, which must re-measure the component half with the distribution and enough runs to establish a verdict. This half must not be booked as delivered on the present evidence |
| Frontend test run opens a TCP connection to port 3000 (F39) | `pnpm --dir frontend test` prints an `AggregateError` with `ECONNREFUSED ::1:3000` and `127.0.0.1:3000` before any test output, then runs 97 files and 602 tests to completion with no failures and exit 0. Something in the vitest run attempts a TCP connect to port 3000 as a side effect and fails; nothing has explained what opens it. Reproduced 2026-08-30 on an independent run at a different tree on a quiet box, printing the identical AggregateError at the head of the vitest output, so it is deterministic rather than a one-off. Harmless today because the connection is refused, and the shape of a test that turns flaky under different timing or in an environment where port 3000 answers — in which case the failure would be silent rather than logged. Found by the gate-runner during the PR #416 gate | W11-1.5 | 2 | carry forward with an owner — the frontend workstream. Diagnose what opens the socket before it becomes a flake nobody can reproduce |
| Test-database cleanup is teardown-only, so any interrupted run poisons the next (F40) | `backend/tests/conftest_db.py`'s `_empty_the_database_after_the_session` truncates the shared Postgres only AFTER a session ends, and there is no setup-side guard. The session scope is deliberate — its docstring records that truncating per test would cost a full-table lock 430 times over — but it assumes every run reaches teardown. Any run that does not (a killed gate, Ctrl-C, a crash, an OOM) leaves its rows behind, and the next run on that box fails. The damage is misattributed by construction: it surfaces as `UniqueViolationError` on `uq_users_issuer_subject` inside `backend/tests/test_api_datasets.py`, a file unrelated to whatever the interrupted run was doing, so nothing points the next person at the real cause. The box is shared across worktrees, so the victim is usually someone else. A second, smaller weakness compounds it: two tests INSERT hardcoded identity literals — `(https://provider.example, analyst)` and the same issuer with `owner` — with no randomisation, so they collide with any surviving row rather than generating a fresh key. Observed 2026-08-30 during PR #416's certifying gate: 2 failed / 2292 passed, reconciling against `--collect-only`'s 2297. Root cause was three gate runs killed mid-pytest, which the lead caused by merging register PRs while a gate was in flight. **The tests then passed in isolation** — because the completing run's own teardown had by then emptied the database — which is a misleading green the diagnosis nearly stopped at; `users` was verified at 0 rows directly against Postgres rather than inferred. Found by w11-executor-416fix | W11-1.5 | 2 | fix before close is not required — no shipped behaviour is affected. Empty the database at session START as well as teardown, so a poisoned box self-heals rather than failing a later unrelated run; and randomise the two hardcoded identity literals. Owner: whoever next touches `conftest_db.py` |
| NFR-RATE-9 (F41) | Two limbs, and only part of one is delivered (`03:805`): "the scoring endpoint targets 99.95 % monthly, degrading to the last-known-good cached bundle if metadata storage is unavailable." PR #435 (W11 Slice 2 Task 2A) delivers the slot's ref → hash memo — the half that makes the degraded read reachable at all, since ref → `Bundle` → hash is itself a metadata read — and marks four tests `NFR-RATE-9`. Those markers are the requirement's first anywhere: at `19eaabc` no `@pytest.mark.req("NFR-RATE-9")` existed in `backend/` or `packages/`, and `req-coverage.py` does not report the id at all. They are four occurrences in one file, `backend/tests/test_bundle_slot.py`, and they reach `main` only when #435 merges — at which point `req-coverage.py` begins counting NFR-RATE-9 among its marked requirements on the strength of part of one limb. The end-to-end degraded read over HTTP is Task 2B's; the 99.95 % availability target is measured against a deployed service and nothing is deployed until W14. ***Amended 2026-08-30: the HTTP half is DELIVERED and this row's "owed" clause above is superseded.*** Task 2B landed as `fd0e248` (#443) — after this row was filed in `7952f76` (#436) — and carries `@pytest.mark.req("NFR-RATE-9")` at `backend/tests/test_score.py:587`, an end-to-end degraded read over HTTP. Five markers now exist, not four. **Only the 99.95 % availability limb remains open**, and it is genuinely unmeasurable pre-deployment: a repo-wide search for `99.95` or `uptime` outside the spec sentence finds no measurement code. The row was stale for the length of one merge — filed against a state that a PR merged hours later discharged, and undisturbed since. Ruling 16 clause 5 (`docs/plans/2026-08-29-w11-slices-2-4-rulings.md:129-131`) says so in terms — the figure "is not discharged by W11 and must not be booked as though it were" — and its Disposition declares this row owed. Filed 2026-08-30 rather than at the close, on the lead's instruction: the row is owed either way, costs nothing to write now, and the close's owed list already runs to thirteen items — a list that lost NFR-RATE-13/14 for two workstreams even though a register row, F-W9-1, existed for them the whole time. An earlier draft of this row gave a different reason: that the overstatement was already live in `req-coverage.py`'s output. That reading was taken at `cea3094` in an audit worktree and written without naming the tree it came from. At `origin/main` nothing is overstated yet, and the correct tense is future. | W11-2 | 2 | carry forward with an owner — W14, for the availability target; the degradation mechanism is **delivered in full** — the memo half in PR #435, the end-to-end HTTP proof in PR #443 (see the 2026-08-30 amendment above, which supersedes this row's original "owed by Task 2B" reading). The W11 close must not read `req-coverage.py`'s marked count as evidence that either limb is discharged |
| FR-RATE-34 (F43) | Three limbs in one sentence (`03:161`), and they have three different answers. **L1 resolution** — "evaluates one Quote Context against the Rating Version currently live in the target environment" — is **not delivered**: Ruling 14 defers live resolution to W14 and replaces it with a 409 `NO_LIVE_RATING_VERSION` refusal, because `live` is a property of a Deployment (FR-RATE-23). **L2 response** — "returning the ladder, outputs, and (optionally) a Trace" — is W11 Slice 2 Task 2B's, and is reached through an explicit `rating_version_ref`, which is FR-RATE-35's mechanism rather than FR-RATE-34's; a marker on an explicit-ref test evidences L2's response shape and never L1. **L3 latency** — "Target p99 < 50 ms server-side (NFR-OVR-1)" — is measured by Task 2D, so its state is open rather than settled at the time of writing. Two hazards recorded because neither is visible from the requirement row: `req-coverage.py` reports FR-RATE-34 as marked once any limb is marked, so the marker count is not evidence about which limb; and **the row itself carries no dated amendment**, so it still asserts live-in-target-environment resolution as the shipped behaviour, with the divergence recorded only in `03` §5.1's `NO_LIVE_RATING_VERSION` note (`03:542`) and in `docs/audit/plan-reviews.md`. A reader who opens FR-RATE-34 alone concludes the default-live path is delivered. Filed 2026-08-30 against `e16c459`; `plan-reviews.md:980-1000` makes this row a binding condition on the W11 close. | W11-2 | 2 | split verdict. **L1: carry forward with an owner — W14**, with the deployment work that gives `live` a referent; the refusal branch is permanent, not a stub. **L2: delivered** by Task 2B. **L3: not settled by this row** — Task 2D measures it, and the verdict is finalised at the W11 close against 2D's figures, not here. The close must also decide whether L1 warrants a dated amendment to FR-RATE-34's own text: a register row records delivery, it does not stop a requirement misleading its next reader |
| FR-RATE-40 (F44) | Four limbs (`03:173`), of which W11 touches one. A Rating Version cannot reach `approved` without: (1) a passing **Regression Suite**; (2) a **Dislocation Run** against the current live version over an agreed portfolio; (3) a **change summary** (FR-RATE-27); (4) where the insurer has enabled it, a passing **GIPP check** (`04-optimisation.md`) (R4). Only limb (3) is enforced anywhere at `e16c459`: `approvals.submit` (`backend/src/app/platform/approvals.py:146`) refuses a blank summary with 422 at `:170-176` citing FR-GOV-10, and rating-version submission reaches it — `rating_versions.submit_for_review` (`:153`) calls `approvals.submit` at `:178`. W11 Slice 2 Task 2C adds the attribution that mechanism lacks: at `e16c459` there is **no `@pytest.mark.req("FR-RATE-40")` anywhere in `backend/` or `packages/`**, so `req-coverage.py` cannot see the one control that exists. Limbs (1), (2) and (4) have no implementation: `approvals.submit` validates exactly the change summary and the existence of an approval-policy entry, and nothing else — so `03` §5.1's endpoint row for `POST /api/v1/rating-versions/{id}/submit`, "evidence completeness checked (FR-RATE-40)", is one-quarter true as written. Limb (4) is additionally a permission with no failing case — "where the insurer has enabled it" — and is untestable until the enablement it is conditioned on exists. Filed 2026-08-30 against `e16c459`; `plan-reviews.md:980-1000` makes this row a binding condition on the W11 close. | W11-2 | 2 | carry forward with owners, per limb. **(3) delivered and, with Task 2C, attributed.** **(1) Regression Suite — W12**, the testing workstream that builds it. **(2) Dislocation Run — W13**, which builds FR-RATE-46; F-W9-2 already carries the FR-RATE-61 gate that specialises this limb, and the two resolve together. **(4) GIPP check — the optimisation workstream that owns `04`**, and it stays untestable until insurer enablement exists to condition it on. The W11 close must not read a single FR-RATE-40 marker as evidence of the gate: the marker Task 2C adds names limb (3) alone |
| Shared test database (F45) | `backend/tests/conftest_db.py`'s session-scoped autouse teardown (`:251`) truncates every table in whatever database it is pointed at, and `test_database_url()` (`:38`) reads a single machine-wide `GIP_TEST_DATABASE_URL`. Two concurrent `pytest` runs therefore destroy each other's rows mid-run — no interruption involved, no error naming the cause, and the second run's failures present as flaky assertions in whichever tests happened to be between setup and assert. **The guard half is already fixed and is not carried**: `e16c459` (#438) corrected the `pgrep` wait-loop that could never fire, and `.claude/skills/reproducing-ci-locally` warns against that idiom at `:327-332`. It is stated here because the backlog entry that produced this row was itself stale by hours when it was read, and a row silent about the fixed half invites a second re-filing. **Two things remain, neither owned.** (i) The pre-flight "confirm nobody else is running `pytest`" is *an intention recorded in a skill*, enforced by nothing — no fixture, no lock, no gate step reads it, so it binds only a reader who opens the skill before running the suite. (ii) The structural fix — deriving the test database per session so two runs cannot address one database — is described in the same skill and assigned to no workstream. It would also discharge **F40**, this defect's interrupted-run sibling. | W11 | 2 | carry forward — unowned, needs its own authorisation. Recorded rather than fixed because the remedy changes how every developer and every CI job addresses the test database, which is wider than any W11 task's scope; folding it into a scoring task would put an infrastructure change inside a diff reviewed against scoring |
| Test blob bucket (F46) | Eight fixtures named `api_settings` exist under `backend/tests/`. `conftest.py:39` is the base; the other **seven shadow it** — `test_api_approvals.py:53`, `test_api_audit.py:21`, `test_api_jobs.py:25`, `test_api_me.py:15`, `test_api_service_accounts.py:18`, `test_api_settings.py:15`, `test_metrics.py:19`. Each constructs `Settings(...)` without a test bucket, so the `BlobStore` those tests build addresses **production `gip-blobs`** rather than the `GIP_TEST_BUCKET` default (`gip-test-blobs`) that `conftest_db.py:81` reads. **Stated as a trigger, not as a present breakage**: none of the seven currently reads back a blob another fixture wrote, so nothing fails today. The day a test added to any of them does, it gets a 404 whose cause is two files away and which reproduces on no other machine. Found 2026-08-30 while diagnosing Task 2B's own bucket defect — the diagnosis was wrong in its first form (it claimed four hardcoded sites that "could not diverge"; there are eleven sites giving three answers, and `conftest_db.py:81` was already env-aware in the very grep output that was pasted to argue otherwise), which is why this row enumerates the seven rather than counting them | W11 Slice 2 | 2 | carry forward with an owner — the same owner as F45(ii). Both are one defect wearing two names: the test suite addresses a shared external resource by a machine-wide default, so a per-session fix for the database is the shape of the fix for the bucket, and solving either alone leaves the other's failure mode intact |
| `asyncio.get_event_loop()` (F47) | **85 call sites across 14 files, every one under `backend/tests/` and none in any `src/` tree** — `test_rating_version_compile.py` alone holds 30, `test_api_datasets.py` 22. **83 of the 85 are the `asyncio.get_event_loop().run_until_complete(...)` idiom**: a synchronous test driving async setup, so no loop is running at the call and the deprecated implicit-creation path is the one taken. **Measured rather than asserted, 2026-08-30**: `uv run python -V` reports **3.13.5**, and on that interpreter `python3 -W error::DeprecationWarning -c "import asyncio; asyncio.get_event_loop()"` raises `DeprecationWarning: There is no current event loop`. So this is not a future risk — **the suite emits these warnings on every run today**, and `filterwarnings` is configured in no `pyproject.toml`, `pytest.ini` or `setup.cfg`, so all of them are swallowed. When the deprecated path is removed the suite converts from green to 85 simultaneous errors on an interpreter bump that changes no project code, with no intermediate state anyone is obliged to look at. **The blast radius is what makes this a register row rather than a chore**: a defect failing one test is found by that test; a defect failing every test at once on an unrelated change reads as a broken environment, and is diagnosed as one | W11 | 2 | carry forward — unowned, needs its own authorisation. Not W11's: it touches 14 test files across every module, so folding it into a scoring task would make that task's diff unreviewable against its own scope. Two candidate shapes for whoever takes it, neither decided here — the migration itself (`asyncio.run()` at the call, or an async test), or, if sizing proves that large, a `filterwarnings` entry promoting this one warning to an error, which converts a silent future bump into a visible present failure. Recording the second as an option is not proposing it: it turns the suite red immediately, which is a decision with an owner and a schedule, not a tidy-up |
| NFR-RATE-11 rate limits (F48) | NFR-RATE-11 (`03:807`) requires, among four clauses, "per-client rate limits". **The vocabulary exists in three places and the feature in none.** `rate_limit_rps` is accepted on the create body (`api/service_accounts.py:65`), persisted (`db/models.py:394`), and returned on read (`:90`, `:113`, `:175`) — and **consulted by nothing**: no dependency, middleware or handler reads it. `RATE_LIMITED` is a registered error code (`errors.py:58`) **raised nowhere under `src/`**; its only occurrence in the suite is `test_model_jobs.py:976`, where it stands for an *upstream model provider's* 429 propagated into a job error — a different concept sharing the name, and therefore not evidence for this clause. A caller can set a limit through the API, read it back, and be rate-limited by nothing. **The requirement is not wholly unevidenced, which is the reason this row exists**: three `@pytest.mark.req("NFR-RATE-11")` tests (`test_score.py:214`, `:231`, `:563`) all exercise the *authentication and scoped-credential* clauses — a 403 for an account without `score:execute`, a refusal of a key relabelled to another environment, and the permitted/unpermitted pair. `req-coverage.py` therefore reports NFR-RATE-11 covered, and it is, on two clauses of four. Same shape as **F36**: a marker proves a requirement was touched, never that every clause under it was | W11 Slice 2 | 2 | carry forward with an owner — **provisionally W14 (deployment)**, for a reason the §14 review should confirm or overturn rather than inherit: a per-client limit enforced in one replica's memory is not a limit, so "per Consumer System" points at a gateway or a shared counter, and an in-process limiter built in W11 would satisfy the words while delivering something the clause does not mean. The fourth clause (quote inputs never logged in full) is settled separately by Ruling 36 and is not carried here |
| Session links in published history (F49) | Every agent's commit template emitted a `Claude-Session: https://claude.ai/code/session_…` trailer, and the repository went public on 2026-08-30 with those trailers in it. **Measured, not estimated**: `git log origin/main --grep='claude.ai/code/session'` returns **73 commits**; a per-commit body scan deduplicated by SHA returns the same 73, and the raw occurrence count is 113 because a squash carries several. **14 distinct session ids**, the largest contributing 21 commits — this was every session on the team, not one. **No tracked file contains one**, so a checkout exposes nothing; the links live only in commit messages. The trailer is configured **nowhere in this repository** — no `commit.template`, no `.claude/` setting — so it came from the agent runtime's own convention and no repo change could have prevented it. Latent rather than new: on a private repository the links leaked nothing beyond collaborators, and publication converted an unchanged practice into a disclosure. Nobody did anything differently; the context moved underneath them. | W11 | 2 | **accept, with instrument** — the maintainer's ruling, 2026-08-30. The 73 stay in history. Removing them means rewriting a public `main`, which breaks every clone and invalidates every SHA cited across this register, the plans and the closure records — a cost out of proportion to an inert internal handle in a commit message. **The instrument is a squash-time strip**, and it is proven rather than intended: `git log <sha> -1 --format=%B \| grep -v 'Claude-Session:' > body.txt` then `gh pr merge <N> --squash --body-file body.txt`, verified on `98eca40` and `c8d3c81`, both of which reached `main` carrying zero links from branches that carried three between them. The merge is the only point where one person controls what lands, so it is the right place for the filter. **The prohibition is landed in `docs/process/delivery-process.md` §15 and `.claude/skills/git-hygiene`** in the same change, because a rule that lives only in a session dies with it — this one did, for the length of a day, which is why a member spawned before it emitted the trailer while the rule was notionally in force |
| `bundle_slot.py` immutability argument (F50) | `backend/src/app/platform/bundle_slot.py:28-31` justifies serving the `ArtifactRef → content_hash` memo (`hash_for`) without a metadata read on the grounds that "artifacts are immutable (FR-OVR-1), so a given `rating_version` ref names one immutable version and compiles to one `Bundle` content hash. The mapping cannot change under the memo." **That argument is wrong as stated.** `row.bundle` is mutable: `compile_rating_version` (`backend/src/app/platform/rating_versions.py:440-444`) rewrites it — including `content_hash` — on every compile, and `POST /api/v1/rating-versions/{id}/compile` (`backend/src/app/api/models.py:1215-1244`, verified: only `submit_for_review` checks draft status, `compile_rating_version` checks none) carries no already-compiled refusal, so a recompile of an already-compiled version re-points a pinned ref to a new hash. `_rating_compile` (`backend/src/app/worker/rating_handlers.py:41-48`) captures `prior_hash` **before** compiling precisely because the call overwrites it, and audits `before`/`after` — the repository already treats "changed hash under an unchanged ref" as a normal, audited event, which the docstring's "cannot change" denies. **Identified in Ruling 41 §3** (`docs/plans/2026-08-30-w11-reopen-hooks-and-bundle-resolution-rulings.md`), which found it safe only *today*, because `hash_for` is read solely inside the NFR-RATE-9 degradation branch (`api/score.py:174-175`) where serving a last-known-good bundle is the specified behaviour — not because the memo's mapping is immutable. | W11 | 2 | carry forward, unowned. Fix is a docstring correction at `bundle_slot.py:28-31` restating the true safety argument (safe because the memo is read only on the degradation branch, not because the mapping cannot change); it is not urgent by itself, but it is the reasoning a later session would build a happy-path shortcut on, which is why it is named rather than left implied. NFR-RATE-1 remediation (Ruling 41 §2, which would touch this same module) is explicitly not part of the W11 reopen (`docs/plans/2026-08-30-w11-reopen-direction.md` §3), so nobody is currently positioned to fix it as a side effect of other work. ***Resolved 2026-08-30*** — Ruling 42 §7 assigned the fix to W11 Slice 3 Task 3B once the reopen made Ruling 41 §2 the thing to build; `6a44dd4` (`feat/w11-3b-batch-handler`, audited in the range `origin/main...feat/w11-3b-batch-handler`) corrects `bundle_slot.py:28-31` in place, dated and citing F50/Ruling 41 §3, replacing the false immutability claim with the true one — the memo is safe only because `hash_for(ref)` has exactly one call site, the NFR-RATE-9 degradation branch, never the happy path. Verified against the diff, not the executor's report: the corrected paragraph is present at that path and quotes the sentence it supersedes, matching this row's own prescribed fix exactly. Closed by the auditor per this task's delegated authority (`.claude/roles/auditor.md`, Owns: register deferral rows) |
| Research note false premise (F51) | `docs/research/w11-task-2d-nfr-rate-1-full-path.md:74-75` states as fact: "the slot is keyed on `content_hash`, and the only way to learn a ref's content hash is to fetch the bundle." **False at the tree it was written against** (`5d99042`, and still false at `daa6fbe`/`e882727`): `compile_rating_version` writes `content_hash` into `row.bundle` (`rating_versions.py:440-444`), `record_bundle_blob` merges `blob_sha256` into that same dict (`:163`), and `_fetch_bundle` (`api/score.py:126-127`) reads the dict and keeps only `blob_sha256`, discarding `content_hash` it already has in hand. The false universal is the premise the note's no-happy-path-shortcut framing rests on, and it is what Ruling 41 §2 (same plan file) refutes — the ruling re-verified every one of the note's *measurements* and found them holding; only this one claim about the code's structure is wrong, and the note's headline conclusion (correctness/latency trade, no way around the fetch) was built on it. Filed as its own row rather than folded into F50: different artifact (a merged research note, not production code), different correction path (a research artifact needs a ruling before it is edited, per this row's own dispatch), and a distinct risk — a reader who does not also read Ruling 41 takes the note's premise as still true. | W11 | 2 | carry forward, unowned. The note's measured figures are not in question and need no correction; only `:74-75`'s premise sentence does, and `CLAUDE.md` §12/this role's charter treat a research artifact as not the auditor's to silently edit — a correcting annotation (dated, quoting what it supersedes, per the register's own write-once convention) needs the same ruling-then-file path Ruling 41 itself came from, not a register row alone. ***Resolved 2026-08-30*** — Ruling 42 §7 assigned this to W11 Slice 3 Task 3B, as "a dated correcting annotation quoting what it supersedes," the ruling-then-file path this row said was owed. `6a44dd4` (`feat/w11-3b-batch-handler`, audited in the range `origin/main...feat/w11-3b-batch-handler`) adds exactly that at `docs/research/w11-task-2d-nfr-rate-1-full-path.md:76-89`: dated, citing F51/Ruling 41 §2, quoting the false premise sentence verbatim before correcting it, and explicitly leaving the note's measured figures untouched. Verified against the diff. Closed by the auditor per this task's delegated authority (`.claude/roles/auditor.md`, Owns: register deferral rows) |
| NFR-RATE-5 linearity clause (F52) | `docs/specs/03-rating-engine.md:886`: *"Batch scoring ≥ 1 M risks/hour per worker (NFR-OVR-2), linear in workers."* Two clauses. **W11 Task 3D** (`docs/research/w11-task-3d-nfr-rate-5.md`, PR #478) measured the throughput clause — 5,093,947 risks/hour/worker, PASS at 5.09× budget, against the handler directly with real Postgres/MinIO I/O — and left the linearity clause **not started**: only one worker ran; nothing under W11 attempts a multi-worker throughput measurement. That disposition is stated plainly in the research note and is not a defect in Task 3D, whose own acceptance standard (`docs/plans/2026-08-29-w11-3-batch-scoring.md:619`) permits exactly this outcome — *"If only one was, say so and mark the linearity untested rather than implying it."* The row exists because **no plan or roadmap document anywhere names an owner for a follow-up multi-worker run**: every planning document that allocates NFR-RATE-5 (the W11 Slice 2–4 plans, the decision-points and readiness notes) sends the whole requirement to Slice 3 and none splits its two clauses or assigns the second beyond it. Separately: W11's original 2026-08-30 closure record (`docs/audit/work/W11/README.md` §3.1/§3.2) omits NFR-RATE-5 from both its FR and NFR tables entirely — it is named in the plan's own scope table (`w11-3-batch-scoring.md:410`) but appears in neither "named in the row" nor "further touched" at that close, because Slice 3 had not yet run. The forthcoming `## 10. Second close` will need to add NFR-RATE-5 to its NFR table for the first time, split at its two clauses per this row and the research note, rather than folding it into one line | W11 Slice 3 Task 3D | 2 | carry forward — unowned. No later workstream currently names a multi-worker throughput measurement; the W11 second close (`docs/audit/work/W11/README.md` §10, not yet written) or the next `phase-review` should either assign an owner or accept the gap explicitly, rather than the linearity clause going unmentioned the way NFR-RATE-5 itself went unmentioned at the first close |
| `scoring_traces` `UPDATE` revocation, untested against the app role (F53) | `835988d1de4c` (W11 Task 4A, merged `25c5688`) revokes `UPDATE` on `scoring_traces` at the grant level — `GRANT SELECT, INSERT, DELETE ON scoring_traces TO {APP_ROLE}` / `REVOKE UPDATE ON scoring_traces FROM {APP_ROLE}` / `REVOKE UPDATE ON scoring_traces FROM PUBLIC` (`835988d1de4c_scoring_traces_row_plus_blob_body.py:77-79`), and Task 4B's `complete_pending_trace` (`traces.py`, audited PR #485) correctly relies on it never being loosened, replacing a pending row by `DELETE` + `INSERT` at the same id rather than `UPDATE`. **The revocation itself is untested.** Every session the test suite opens connects as the table *owner* (`conftest_db.py:35`'s `DEFAULT_TEST_DSN`, `gipricing`, the same role Alembic runs migrations as and therefore the creating/owning role for every table it creates) — and Postgres ownership carries implicit privileges a `REVOKE` on a named grantee or `PUBLIC` does not touch. This repository already documents that exact trap for a sibling case: `test_an_artifact_cannot_be_rewritten_from_the_owner_connection` (`backend/tests/test_artifact_immutability.py:339`) states *"`REVOKE UPDATE` from a table's owner does nothing, because ownership carries implicit privileges"* and proves append-only enforcement against the owner connection only via a **row trigger**, not the grant. `scoring_traces` has no such trigger — `835988d1de4c` adds the grant alone — so nothing in the suite, run as any role it actually uses, demonstrates that `gip_app` (the runtime role) is refused an `UPDATE`. `test_the_application_role_holds_only_select_and_insert` is the nearest existing check and does not cover this: it inspects `information_schema.table_privileges` for `APPEND_ONLY_TABLES` only (`scoring_traces` is not a member — it holds `DELETE` too, a different grant shape) and is metadata inspection, not an attempted write. **Attribution:** this is Task 4A's gap (the migration that first revoked `UPDATE`), inherited rather than introduced by Task 4B, which only ever composes `DELETE`+`INSERT` and adds no grant of its own. | W11 Task 4A | 2 | **delivered but untested** — the DDL revocation is real and Task 4B's reliance on it (delete + reinsert, `created_at` preserved, both tested at the *code* level: `test_completing_a_pending_trace_that_reproduces_marks_it_complete`) is sound conditional on it holding. What is missing is either (a) a metadata check extended to cover `scoring_traces`'s `{SELECT, INSERT, DELETE}` grant shape (proves the grant is *declared* correctly, same limit as the existing check), or (b) a connection-as-`gip_app` test attempting a real `UPDATE` (proves *enforcement*, the stronger and currently entirely absent claim). Carry forward, **unowned** — no workstream currently names either fix; the next W11 close or the `.claude/skills/contract-guard`-adjacent DB-invariant tooling review should assign an owner or accept the gap explicitly rather than let it go unmentioned the way this row exists to prevent |
| Service Account key issuance never mints a key for any granted environment but the first (F54) | `backend/src/app/api/service_accounts.py:180` (`create_service_account`) and `:246` (`rotate_key`) both call `generate_key(body.environments[0])` / `generate_key(account.environments[0])` — list-order first only. Neither route, nor any other in the file (`create_service_account`, `rotate_key`, `revoke_key` are the whole router — `grep -n '@router\.'`), accepts an environment parameter or otherwise offers a way to mint a key scoped to any environment other than index 0. FR-PLAT-3 (`07-platform.md:73`) grants an account *"named environments"*, plural, with no maximum (`service_accounts.py:63`, `Field(min_length=1)`), so an account created e.g. `["uat", "dev"]` is schema-legal and `dev` is a real granted scope — but no API call, including `rotate_key`, can ever produce a key whose `parsed.environment` is `dev`. The auth-time check that exists for exactly this purpose, FR-PLAT-30 (`07:141`, *"A `uat` key can never score against `prod`"*, enforced at `auth/service.py:210-217` — `if parsed.environment not in set(account.environments)` raises `ENVIRONMENT_SCOPE_DENIED`), can therefore only ever fire against a forged or hand-crafted key; against a legitimately issued one, `parsed.environment` is always `environments[0]` by construction, so the check's pass branch is exercised on every legitimate call and its refusal branch is exercised on none. Found and deliberately not ruled by **Ruling 44 §4** (`docs/plans/2026-08-30-w11-4b-trace-environment-ruling.md`), which states it explicitly: *"it is not created, worsened or relied upon by PR #485 [...] It is reported to the lead for the findings register with an owner, and is not ruled by this record."* **Attribution: `main`**, in key issuance (`service_accounts.py`), not Task 4B (W11) — Task 4B only reads `caller.environment` post-authentication and neither writes nor calls `generate_key`. Verified independently against the tree at `origin/main` `049b6c1`, not taken from the ruling's relay. | — | 2 | **not started** — no fix, workaround, or owner exists anywhere in the roadmap or open plans for this specific gap as of this filing. Recorded **unowned**; needs the lead or maintainer to name an owner and a work item (the natural fix is `rotate_key` gaining an `environment` selector, or `create_service_account` minting one key per granted environment, either a spec change first per `CLAUDE.md` §0 since neither shape is currently specified) before it can be scheduled. Filed as its own row rather than folded into any Task 4B row because it is a `main` defect Task 4B neither introduced nor depends on. |
| `TraceStep.consumed`/`.produced` carry the full accumulated context, not the step's own declared inputs/outputs (F55) | `_build_trace` (`packages/pricing-core/src/pricing_core/rating/score.py:660`) builds each `TraceStep` from the zen-engine's own raw per-node trace entry — `consumed=dict(entry.get("input") or {})` and `produced=dict(output)` at `:686-687` — which carries the **full accumulated evaluation context at that node**, not only the variables the algorithm step itself declares via `consumes`/`produces`. **Verified independently against `origin/main` `1ccec01`** (this branch's base), not taken from the research note's relay: for a compiled 50-`n_expr` structure, step `s_v047` declares `consumes=['v046']` (one input) via `algorithm.steps`, but `result.trace.steps` reports that same step's `consumed` dict holding **61 keys** — every prior input and computed variable, `channel`…`v046` — and `produced` holding 62. Consumed-key count grows monotonically with step position across the whole chain (12 keys at the first computed step, 63 at the last of 50), confirming the note's "worse than linear" characterisation rather than an artifact of one probed step. **Found by W11 Task 4D** (`docs/research/w11-task-4d-nfr-rate-12.md` §2, PR #501) while sweeping trace size against step count for the NFR-RATE-12 projection, and it is **the single largest driver of that projection's OVER-budget verdict** (~2.58× the 200 GB/year budget at the ~200-step reference structure) — a `TraceStep` carrying only its own declared consumed/produced fields would be substantially smaller. Task 4D itself is a projection, not an implementation change, so it does not fix this; it is filed here per Task 4D's own note (§6) asking that the finding "not be read as merely an artifact of this benchmark's fixture." | W11 Slice 4 Task 4D | 2 | carry forward — unowned. The remedy — trimming `TraceStep.consumed`/`.produced` to each step's own declared `consumes`/`produces` rather than the engine's full per-node context, or another slimmer trace-step shape — is a design change to `pricing_core.rating.score._build_trace` and to the `Trace` schema's semantics, out of Task 4D's scope (measurement, not remediation) and named by no workstream. It is also NFR-RATE-12's largest available remediation lever if the decision-maker chooses to bring the projection back under budget by shrinking trace size, rather than lowering the sample rate or raising the budget — a choice this row does not make. ***Cross-referenced 2026-08-31*** — F35 (NFR-RATE-2's latency finding) names the identical fix as its own remedy: trimming `TraceStep.consumed`/`.produced` to each step's own declared inputs/outputs. F35's remedy was gated on Ruling 35's off-path trace capture, which landed in W11 Task 4B (`003f9d4`, #485); that clears the precondition for building this shared lever but builds neither row's fix. Nobody had connected the two rows until this cross-reference. |
| `audit-docs.py`'s notes scan root skips silently when absent (F56) | `scripts/audit-docs.py:52` defines `NOTES = REPO / ".claude" / "notes"` as one constant with two consumers: `check_notes` (`:220`, checks 16-20) guards on it at `:235-236` — `if not NOTES.is_dir(): notes.append("no docs/notes/ directory — checks 16-20 skipped"); return` — and `check_finding_citations` (`:415`, check 25) puts it in `scan_dirs` at `:505`, filtered by `if d.is_dir()`. **Absence is a skip, not a failure, in both places, and the second skip prints nothing at all.** Verified live rather than assumed: loading the script as a module and repointing `NOTES` at a non-existent path, then calling `main()` directly, returns exit code **0** and prints *"no docs/notes/ directory — checks 16-20 skipped"* followed by **"All checks passed."** — check 25 silently loses one of its three scan roots with no corresponding line anywhere in the output. **So any rename or move of `docs/notes/` that misses updating this one constant silently disables checks 16-20 and weakens check 25, with a green gate** — a hole open today for any rename, not only the one `docs/plans/2026-08-31-nt-0016-investigation.md` proposes. Same class as plan review 10's `fastapi-service` finding (a skill and a test both asserting a check that did not exist, its body unable to fail): a gate whose absence-case is a silent skip reports the same green as a gate that ran — `CLAUDE.md` §13's *"a check that has never printed a failure has not been tested"*, and two instances inside two days argues the class is worth naming here rather than treating each as its own coincidence. Found by the decision-maker, verified independently at source (`scripts/audit-docs.py:52,235-236,505`) and by direct reproduction before this row was filed | NT-0016 investigation | 2 | carry forward with an owner — `docs/plans/2026-08-31-nt-0016-investigation.md` Slice 1 (merged as a proposal, `826d636`, #508) names this exact defect, cuts a scan-root hard-fail as its first slice specifically because this hole blocks the notes-directory move safely, and specifies the TDD test and acceptance criteria. Proposal only as of this filing — nothing scheduled, the acceptance line undated — so the owner is named, not yet discharged |
| §7 retry-cap instrumentation is unbuilt while the caps it gates are already enforced (F57) | `delivery-process.md` §7 (`:131-134`) conditions adoption of the retry caps (≤1 Project/Phase/Work, ≤2 Slice) on logging "every replan/audit-fix loop iteration and every per-slice re-audit count and gate re-run from the first slice run under this process," and revisiting the numbers "once a workstream's worth of data exists, not before" — `delivery-process.core.json:183` mirrors the same four log items under `guards.retry_caps.instrumentation`. **No such logging exists anywhere.** `retry_counters` (the counter the caps would be read against) is deliberately absent from artifact B — `write_runtime_state.py:19-24`, Ruling 47(c): "a `0` from a counter nothing increments is indistinguishable from a true zero," so it is withheld rather than shipped as a false zero — and the hook that would increment it, NT-0014 script C2, does not exist (`delivery-process.core.json:263`: "ships with NT-0014 script C2 (adoption slice G, blocked, not built)"). NT-0014's own adoption plan names the dependency explicitly: "**Pilot** — first live workstream after C2 lands runs with counters on; spec §7's 'revisit when a workstream's worth of data exists' clock starts then" (`docs/notes/0014-machine-readable-process-core.md:151-152`). So the caps have been in force since `delivery-process.md` was adopted with no mechanism anywhere collecting the data their own adoption was conditioned on revisiting them against — not an oversight, a not-yet-reached dependency, but a real exposure while it lasts: nothing today can say whether ≤1/≤2 are the right numbers. Ruling 40 §6 (`docs/plans/2026-08-30-w11-reopen-hooks-and-bundle-resolution-rulings.md:254-256`) recorded slice G "still blocked — on slice E, because C2 has nothing to increment until artifact B, the runtime state file, exists, and E is not started." **E has since landed** (`b551060`, #506, merged 2026-08-31), clearing that specific blocker per Ruling 40's own terms — verified against `036337e`, not assumed from the ruling's relay. | NT-0014 adoption | 2 | carry forward with an owner — slice G, the next-scheduled adoption slice (C2 alone, per Ruling 40 §6) and now unblocked. Until it lands and a full workstream runs under live counters, this row stays open: the §7 caps remain enforced with no data collected toward the revisit their own adoption text names as the condition. ***Amended 2026-08-31*** — **slice G has since landed** (`9e8783d`, #516, verified against `origin/main` at that SHA, not assumed from the PR title): `scripts/hooks/retry_cap_hook.py` now increments `retry_counters` on every `record` call and refuses on breach, so the counter this row names finally has a writer. This does not close the row — no live workstream has yet run a fix/replan loop through the hook (`python3 .claude/skills/watcher-runtime-state/scripts/write_runtime_state.py show` reports no runtime state file at all, `~/gi-pricing-plan.local/handover/runtime-state.json`, 2026-08-31), so the ≤1/≤2 revisit still has zero cycles of data behind it. The trigger for closing this row is now purely "a workstream's worth of data exists," per the adoption note's own words — no further blocker remains |
| Artifact B has no live writer; the watcher charter's "each cycle" claim does not hold (F58) | `.claude/skills/watcher-runtime-state/scripts/write_runtime_state.py` (slice E, merged `b551060`, #506) is real and tested, and `.claude/roles/watcher.md:49-56` states the watcher "writes `position` and `in_flight_expensive_verifications` to `$RUNTIME_STATE_FILE` (default `~/gi-pricing-plan.local/handover/runtime-state.json`) ... each cycle." **Four facts checked directly against the running session at `036337e`, 2026-08-31, rather than assumed:** (1) `~/gi-pricing-plan.local/handover/runtime-state.json` does not exist (`ls`: "No such file or directory"). (2) No process anywhere on the machine is running `write_runtime_state.py` (`ps aux` has no match). (3) The three watcher-class processes actually running — `balance_watch.py` (pid 5632), `reporter-cycle.sh` (pid 5678), `watch-external-prs.sh` (pid 96190) — contain no reference to `write_runtime_state.py` (`grep` empty on all three sources). (4) The charter's "each cycle" sentence, quoted above, is current as of this filing. **Not a defect in slice E**: its brief (`docs/notes/0014-machine-readable-process-core.md` impact-matrix row 10, `:118`) was the script and the charter's "Amend" — writing the mechanism and describing it — not standing up a live invocation. No adoption task (§6, `:139-154`) or impact-matrix row (§5, `:103-137`) assigns *wiring a running process to call it on a schedule*; row 21 (`:135`) covers hook/config wiring for C1-C4 only, a different mechanism (Claude Code hooks intercepting agent actions, not a cron-style cycle). **Same shape as F31** — a charter asserting a mechanism nothing runs — filed one day after F31's own lesson ("the one thing it must not do is inherit a constant with a live timestamp") was applied to amend this very file. This is milder than F31 in one respect (nothing here is a stale constant masquerading as fresh — there is simply no file to read) and identical in the respect that matters: a reader of `watcher.md` alone believes a mechanism runs that does not. | — | 2 | carry forward, **unowned-pending-authorisation**. The fix is wiring one of the three live watcher processes (or a fourth) to invoke `write_runtime_state.py cycle` on its own schedule; no slice or workstream currently names this task. **Not slice G**: G's scope is C2 alone (Ruling 40 §6), a hook-triggered write path on replan/fix events, distinct from the periodic `cycle` invocation this row concerns — G landing would not discharge this row. Needs the lead or maintainer to name an owner (a new slice, a fold into F's or G's scope, or the next §14 review) before it can be scheduled |
| `03` §5.2 declares `KeyFilter`'s home as `pricing_core/rate_tables/operations.py`; the code imports it from `model-schema` (F59) | `docs/specs/03-rating-engine.md:725` places `KeyFilter = dict[str, list[str]]` inside the `# pricing_core/rate_tables/operations.py` fenced block (header `03:724`), stating it as a type defined in that module. The code disagrees: `packages/pricing-core/src/pricing_core/rate_tables/operations.py:39` **imports** `KeyFilter` from `model_schema.rating`, where it is actually defined at `packages/model-schema/src/model_schema/rating.py:728`. **This is not, today, a violation of `CLAUDE.md` §2 or ADR-0002** — `operations.py` does not redefine the shape; it correctly imports the single-source-of-truth definition, and ADR-0002's one-way `model-schema` → `docs/contracts` seam is undisturbed. The risk is prospective rather than present: `03` §5.2 is written as an implementation reference, and a reader implementing "from the spec" rather than from the code would create a second `KeyFilter` inside `pricing_core/rate_tables/operations.py`, which is exactly the duplication `CLAUDE.md` §2 states causes a mispricing ("nobody hand-writes a shape that already exists in `model-schema`... a shape defined twice will diverge"). Filed as its own row per proposal 4.2 (plan review 10, `docs/audit/plan-reviews.md:2430`) rather than folded into F60, because its severity class differs: F60's four items are spec text lagging the code (an omission), this one actively misattributes a shape's canonical owner (a misdirection) with a live governance rule on point. | Plan review 10 (proposal 4.2) | 2 | carry forward with an owner — the decision-maker, per `CLAUDE.md` §0 ("whether the spec or the code is in error is a real question, and the answer has often been the spec... a decision-maker's ruling, not a register row's"). The evident fix is correcting `03:725` to state `KeyFilter` is `model-schema`'s (imported, not defined, by `operations.py`), since the code already conforms to §2/ADR-0002 and reversing that would be the actual duplication the rule exists to prevent — but that correction is the ruling's to make, not this row's |
| `03` §5.2's rating-engine signature blocks omit four live exports (F60) | Four separate gaps between `03` §5.2's fenced signature blocks/prose and the code, found running plan review 10's delegated two-direction pass (proposal 4.2, `docs/audit/plan-reviews.md:2430`) and independently verified against this tree: **(1)** `03:698-699` declares `pricing_core/rating/runtime.py`'s only export as `load_bundle` (`runtime.py:562`). The module also exposes public `to_wire(graph, payloads=None) -> dict[str, Any]` at `runtime.py:341` (full signature: `graph: JdmGraph, payloads: Mapping[str, Any] \| None = None`), the function that builds the ZEN engine's wire payload; `compile.py`'s `to_jdm` docstring (`compile.py:357-361`) calls it load-bearing — *"`pricing_core.rating.runtime.to_wire` is what a live `zen.ZenEngine` call actually needs, verified rather than assumed"*. **(2)** `03:724-736` declares `pricing_core/rate_tables/operations.py` as exporting `KeyFilter` plus 10 `def` signatures (`uplift_table`, `uplift_by_filter`, `floor_and_cap`, `rebase_to_level`, `decide_storage_mode`, `export_to_csv`, `export_to_xlsx`, `import_from_csv`, `import_from_xlsx`, `import_confirmed`). The code additionally exposes six public (non-underscore) functions absent from that block — `check_model_approved:113`, `extract_relativity_table:142`, `seed_from_model:168`, `validate_rate_table:269`, `diff_vs_previous:383`, `diff_vs_seed:395`. **Verified wired, not stubs**: `diff_vs_previous`/`diff_vs_seed` are called directly by the platform layer serving the published `GET .../diff` endpoint (`backend/src/app/platform/rate_tables.py:294,296`, routed from `backend/src/app/api/rate_tables.py:314`); `seed_from_model` is called by `seed_rate_table_from_model` (`api/rate_tables.py:118`, imported at `platform/rate_tables.py:68`), and `seed_from_model`'s own body (`operations.py:180,206`) calls `check_model_approved`, `extract_relativity_table` and `validate_rate_table` in turn — so all six are reachable from a published endpoint, four directly imported into the platform layer and two transitively through `seed_from_model`'s call graph. This is shipped drift, not an unbuilt stub. **(3)** `03:692-696`'s `compile.py` block omits `assert_integer_minor_round_trip()` (`compile.py:67`), while FR-RATE-56's prose (`03:220`) requires *"a startup self-check [that] asserts the round-trip; failing it prevents the service starting"* — the function is exactly that check (its own docstring names FR-RATE-56) and is wired at `backend/src/app/main.py:82` as a startup call, so the requirement names a behaviour §5.2 never names a function for. **(4)** `build_scoring_result` (`score.py:707`, and in the module's own `__all__` at `score.py:219`) is discussed at length in §5.2's prose (`03:779-786`, the FR-RATE-37 "shared post-evaluation tail" paragraph) but carries no formal signature in the fenced block, which shows only `score_one`/`score_batch` (`03:701-706`) — a declared public export with prose discussion and no signature entry. **Spot-checked matching, no drift**: `score_one`, `score_batch`, `load_bundle`, `validate_algorithm`, `compile_bundle`, `to_jdm`, `bundle_hash`, `apply_factor`, `reconcile_ladder` all match their fenced signatures exactly against the code at the cited lines. **Confirmed expected absences, not drift**: `pricing_core/rating/analysis.py` and `rating/testing.py` do not exist anywhere in the tree (`ls packages/pricing-core/src/pricing_core/rating/`: only `__init__.py`, `compile.py`, `runtime.py`, `score.py`) — consistent with plan review 10 §4c's own finding that the unpublished `dislocation-runs` (W13, FR-RATE-46-49) and `regression-runs` (W12, FR-RATE-43-45) endpoints are the reason, not drift. | Plan review 10 (proposal 4.2) | 2 | carry forward with an owner — the decision-maker, per `CLAUDE.md` §0, to rule each of the four sub-items' correct side before W12 opens (proposal 4.2's own stated urgency, since W12 builds against these shapes): **(1) and (4)** read as pure spec omissions (the code is right, `03` §5.2 is incomplete) since neither `to_wire` nor `build_scoring_result` conflicts with anything the block does state; **(2)** is the more consequential of the four — six functions are live behind published endpoints and undeclared, so the fenced block understates the module's actual public surface; **(3)** ties to a numbered requirement (FR-RATE-56) whose named behaviour has no named function anywhere in §5.2, which a reader implementing from the spec alone would not know to look for. This row does not choose which correction each sub-item takes (spec amendment vs. code change) — that is the ruling's, not the register's |
| C2's `PreToolUse` layer is bypassable exactly as C3's dissolved git hook was, and unlike C3 it has no CI-equivalent backstop (F61) | NT-0014 adoption slice G (`9e8783d`, #516) built `scripts/hooks/retry_cap_hook.py` — C2, the retry-cap hook — after Ruling 40 §3 dissolved its sibling C3 (the verify-gate hook) specifically because *"a git hook cannot satisfy `CLAUDE.md` §13 in principle... `--no-verify` bypasses it with no trace... no broken-input proof can establish otherwise,"* with the state it would have enforced already checked by CI, "the stronger check." C2 passes Ruling 40's own literal test for needing a hook at all (§2: *"a hook is needed only to intercept an action that leaves no artifact"* — a fix/replan decision is exactly that), and `record`'s own refusal is unconditional and independently mutation-proven: verified directly in this audit by mutating `_would_breach`'s `current >= cap` to `current > cap` (`scripts/hooks/retry_cap_hook.py:148`) and re-running `tests/test_retry_cap_hook.py` — 4 of 11 cases failed, matching PR #516's own reported figure exactly, then reverted. **But Ruling 40 §4 never asks whether C2's *registration* point is bypassable the same way C3's was, and it is**: `.claude/settings.json` is a session-scope Claude Code hook, disableable per-session (`disableAllHooks`) or overridden by the gitignored `.claude/settings.local.json` (`.gitignore:82`) exactly as C3's git hook was uninstalled-by-clone and `--no-verify`-bypassable — the two escape hatches PR #516's own body names in the same sentence it uses to distinguish C2 from C3 ("nothing stops an actor from hand-editing `retry_counters`... disabling hooks for a session... or simply never calling `record`"). **The asymmetry is the finding**: C3 was disqualified for being a bypassable local mechanism *with* CI sitting behind it as a stronger, unbypassable backstop; C2 is a bypassable local mechanism *with no backstop at all* — nothing in this repository re-derives `retry_counters` from durable artifacts (`delivery-process.core.json:249`'s own design note: *"Neither stops a direct hand-edit of the runtime state file or a session with hooks disabled -- no CI-equivalent backstop exists behind C2 (Ruling 40 §4)"*; Ruling 47(b) assigns that reconciliation to "a future watcher cycle," not built anywhere yet — consistent with F58, which independently finds no live watcher-cycle writer of any kind exists today). Ruling 40 §4 asserts hooks are "the only registration point that reaches" the acting agent without weighing this against §3's own bypass reasoning three paragraphs earlier in the same ruling — the two sections were not reconciled against each other. **This is not closed by the executor having disclosed the gap in the PR body and the process-core note**: honesty about a limitation is not the limitation's remedy, and C2 has now shipped and is in force | NT-0014 adoption slice G | 2 | carry forward with an owner — no slice or workstream today builds the re-derivation cycle (Ruling 47(b)) that would give C2 a CI-equivalent backstop, and none is scheduled. Two dispositions are open, not one: (a) build the reconciliation — a mechanism that periodically or on-demand recomputes `retry_counters` from a durable, harder-to-forge source (e.g. the `record`/`hook` invocation's own audit trail, if one is added) and flags a mismatch; or (b) the lead or maintainer explicitly accepts the residual gap as proportionate, in writing, rather than by silence. Needs the lead to name which, and by when — absent a named event this decays to the next `CLAUDE.md` §14 phase review per this register's own header rule |
| `03` §4.4's `timing_ms` example: four keys, `score_one` emits two (F62) | `03:419`'s `ScoringResult` worked example showed a four-key `timing_ms` breakdown (`model_call`/`table_lookups`/`expressions`); `score_one` emits only two (`total`/`evaluate`). Full evidence: [`docs/audit/findings/F62.md`](findings/F62.md) | W11 Task 1.4 | 2 | carry forward with an owner — the decision-maker, per `CLAUDE.md` §0, to rule which side is correct. ***Resolved 2026-08-31*** (Ruling 54, `docs/plans/2026-08-31-f62-timing-ms-ruling.md`, merged `e241bb4`, PR #528): `03:419`'s example corrected to the two keys `score_one` emits; the engine is not extended. Left appended, not opened, deliberately — the two-key shape is pinned by no test, so this row is meant to keep surfacing on `register-owed.py` until that test lands; a reader seeing it there is seeing the open residual, not a stale entry. Full reasoning: [`docs/audit/findings/F62.md`](findings/F62.md) |
| `get_eta`'s Z-only stamp vs `eta.md`'s GB-local convention (F65) | `get_eta` (`.claude/skills/reporter-cycle/scripts/reporter.py:170`) requires a `Z`-suffixed `**Updated:**` stamp; `eta.md`'s own header mandates GB-local time (maintainer instruction 2026-08-29). Verified via a full `conversations.history` query (212 messages, the reporter's whole operating history): the STALE branch has never fired; 87% of real ETA posts degrade to "staleness unknown" instead. Full evidence: [`docs/audit/findings/F65.md`](findings/F65.md) | — | 2 | carry forward, unowned-pending-authorisation — a code-side fix (stdlib `zoneinfo`) is recommended over changing the documented convention; assigning a workstream and owner is the maintainer's or lead's call. Falsifiable: discharged by a genuine STALE post, or superseded once a fix lands and is re-verified. Full reasoning: [`docs/audit/findings/F65.md`](findings/F65.md) |
| `register-owed.py W11` finds 10 open rows the closure record's owed-list sections never name — possible F41 recurrence at scale (F63) | `python3 scripts/register-owed.py W11` at `f99b55d` returns 30 matched rows (29 owed, 1 excluded as opening with a resolution marker) — reproduced independently against the current tree, same result. **Every one of the 20 distinct F-ids `docs/audit/work/W11/README.md`'s own findings sections (§6, §10.6, §10.7) cite is among the 30**, with one footnote: §10.7 also names F54, but that row's own Decision cell states its attribution is `main`, "not to any W11 task" — so F54's absence from the 30 is correct exclusion, not loss, and "20 of 20" needs that qualifier to be exact. **Ten further matched rows appear nowhere in the closure record**: F-W9-2, F-W9-3, F26, F30, F31, F34, F36, F37, F39, F40. Checked each one's actual filing commit against the first close (`1da81cd`, 2026-08-30 11:58): all ten predate it — F-W9-2 (`71d8ca5`, 08-29 12:19), F26 (`f6343d4`, 08-29 15:09), F30 (`97fcb16`, 08-29 16:56), F31 (`9891be1`, 08-29 17:16), F-W9-3 (`06bdac5`, 08-29 18:26), F34/F36/F37 (`6e548f8`, 08-30 01:16), F39 (`80203a7`, 08-30 01:56), F40 (`4948923`, 08-30 02:44) — so every one existed as an open finding before §6's table, which quotes `CLAUDE.md` §14's "every open finding" obligation on its own header line. **Not uniform in strength**: F26, F30, F31, F34, F36, F37, F39, F40 and (more partially) F-W9-3 each carry an explicit W11 ownership statement in their own Work item or Decision column ("owner decided: W11 (Ruling 29...)"; Work item `W11-1.5`). F-W9-2 is the weak one — its Decision column's only "W11" token is explanatory prose ("the check itself specialises FR-RATE-40's general approval-evidence gate, which W11 builds"), and its own filing commit's title states "owner W13" — a plausible genuine false-positive rather than an omission, not the other nine's shape. **`docs/audit/work/W11/README.md` §10.6's own scope sentence, read directly**: "None of these are in the reopen's requirement scope; each is checked for whether anything in this reopen's work changed its state" — and it revisits exactly the 4 items §6 already named (NFR-RATE-13/F-W9-1, F35, F48, F27(c)/F29), never a fresh sweep. §10.7 adds one paper-cut and states "No other new register rows are filed by this audit," discussing F53/F54 only because they were filed earlier the same day against adjacent work. **Neither closing pass attempted a full register sweep for every W11-attributed row; both were scoped to what the closing session already knew or had itself filed.** Two readings are live and this row does not choose: **(a)** F41's own failure recurring at roughly 4x scale — a hand-compiled owed list (built before this tooling existed) losing at least 8-9 genuinely open, W11-attributed findings the same way F41 itself was lost, or **(b)** legitimate, if undisclosed, scoping — a narrower reading of "open finding" that nobody wrote down as a rule. This row records the discrepancy; it does not reopen anything, and `docs/audit/work/W11/README.md` is not edited by it. | — | 2 | carry forward with an owner — the maintainer. Reopening a Work close is the maintainer's alone (`CLAUDE.md` §13); this row is not that. Needs a ruling on which of the two readings holds and, if (a), an owner to carry the 9 substantiated rows (all but F-W9-2) into a superseding note or the next relevant closure record. Absent a dated ruling this decays to the next `CLAUDE.md` §14 phase review, which must give it a disposition rather than merely list it |
| `register-lint.py` (check 29) was blind to 11 of 59 register rows and printed `OK (0 violations)` throughout (F64) | `scripts/register-lint.py`'s `parse_register`, as it shipped in `fa87086` (#519, check 29's own introduction) and stood through `890b06e` (#520, F62's own filing), reset `in_table = False` on every line not starting with `\|` — including the two blank lines the register's one data table carries for readability, after F51's row and after F52's row. Once reset by the first of those, the parser only re-enters table mode on a separator row (`\|---\|---\|...`) or the table's own header row (naming its five column headings) — neither reappears — so every row from F52 onward was silently skipped: not read, not counted, not linted. **Verified directly against the pre-fix code**: running that `parse_register` (`git show 890b06e:scripts/register-lint.py`) over the register as it stood at `890b06e` returns **48 rows, not 59** — 11 short, exactly F52, F53, F54, F55, F56, F57, F58, F59, F60, F61, F62. `main()` printed `"OK (0 violations)"` at every one of those commits regardless, because zero violations were found among the rows it did see, with no signal anywhere that 11 rows (19% of the register at that point, including every row filed in the two days before this one) went unexamined. **This directly undercuts an assurance given in this session**: told "`register-lint.py` returns 0 violations, so your F62 row passes check 29," that run had not examined F62's row at all — true only by coincidence, not by verification. Fixed in `f99b55d` (#521, alongside `scripts/register-owed.py`) by removing the blank-line reset, with a regression test; verified independently against the current tree: `parse_register` now returns 59 rows, F62 included, 0 violations — confirming both the fix and that F62's row was genuinely clean, just not for the reason originally stated. Filed for the record: a check merged, audited, and reported clean by two different people across two different commits was blind to a fifth of its own target corpus the entire time it ran. | NT-0015 P5 | 2 | accept — the defect is fixed and regression-tested (`f99b55d`, #521); no owner is owed further work. The event that would reopen this row is a future regression in `parse_register`'s blank-line handling — guarded today by `tests/test_register_lint.py`'s new coverage, not by this row |
| `audit-docs.py` check 1's link-checker has no code-span awareness (F66) | Its pattern scans raw file text for a bracket-label immediately followed by a parenthesised target, with no exclusion for fenced or inline code, so a doc quoting a regex, glob, or bracket-then-parenthesis construct verbatim fails the gate as an unresolvable link — reproduced live by an early draft of F65 and again by this row's own first draft. Measured: 0 of the 930 such matches in `docs/` today sit in a code span, so nothing currently committed trips it; the risk is forward-looking. Full evidence: [`docs/audit/findings/F66.md`](findings/F66.md) | — | 2 | carry forward, unowned — a fix (skip matches inside fenced/inline code spans) is recommended but out of scope for this filing; not fix-before-close, since nothing live is failing today. Falsifiable: discharged by a fix to check 1, verified against a re-run of this row's own reproduction. Full reasoning: [`docs/audit/findings/F66.md`](findings/F66.md) |
| Hygiene monitor died on its first clean sample — `set -e` plus a grep returning 1 (F67) | The 2026-08-31 hygiene monitor (`watcher-monitor-improved.sh`, a local handover artifact with no repo home) died within one sample of arming (15:04 UTC, PID 36246): `set -e` (line 16) met the uncommitted-changes grep pipeline returning 1 on a clean tree, exiting before the loop ever started — a monitor for abandoned work was dead long before the work it watches for could appear. Undiagnosed on 2026-08-31; identified and repaired 2026-09-01 (`\|\| true`, liveness-verified on a clean tree). Full evidence: [`docs/audit/findings/F67.md`](findings/F67.md) | — | 2 | accept — the defect is repaired in the local script (2026-09-01, recorded in the local `watcher-state.md`) and liveness-verified; no owner is owed further work. The event that would reopen this row is another monitor death read as silence — a static `hygiene-fingerprint-state.txt` mtime at a 60-second sample interval — or a monitor re-armed from a pre-fix copy |
| `audit-docs.py` check 28 has no plan-kind test for a ruling record, shielded only by the 2026-08-31 cutoff (F68) | `check_plan_acceptance_standard`'s own docstring says scope is the plan kind only, discriminated by the four documented suffixes in `docs/plans/README.md`, and warns explicitly against widening it to catch "a future ledger, ruling record or handover file." The code does exactly that: `_PLAN_KIND_EXCLUDED_SUFFIXES` (`-ledger`, `-final-review`, `-verified`, `-handover`) has no ruling-record entry, and `docs/plans/README.md`'s own four-kinds table has no fifth kind for one — a ruling record is filed as a plain `YYYY-MM-DD-<slug>.md`, indistinguishable by suffix from "the plan." Every instance on disk (the precedent `2026-08-30-nt-0017-maintainer-decisions.md`, and the 27 rulings files NT-0019 §5.2 counts in `docs/plans/`) predates `PLAN_ACCEPTANCE_STANDARD_CUTOFF` (2026-08-31) and is exempted as legacy, so the check has never actually been exercised against one. Surfaced when the lead filed a same-shaped ruling record and worked around the false failure rather than patching the shared gate. A positive-content alternative (`^#+ Ruling N` headings) was checked and does not generalise — the precedent file uses no such heading. Full evidence: [`docs/audit/findings/F68.md`](findings/F68.md) | — | 2 | carry forward with a trigger (NT-0019's S2 migration PR, which rewrites `audit-docs.py`'s parsers and roots and moves rulings out of `docs/plans/` into their own family — not S1, which is instruments only) — not fix-before-close, since nothing live is failing today and the one instance encountered was worked around in the record itself. Falsifiable: discharged by S2 landing, or by any ruling record filed in `docs/plans/` on/after 2026-08-31 passing check 28 without an added Acceptance Standard section. Full reasoning: [`docs/audit/findings/F68.md`](findings/F68.md) |
| `write_runtime_state.py cycle` cannot clear a field — "no active slice" is inexpressible (F69) | `_position_block_content` starts from a copy of the existing position content and overwrites only `phase`/`work`/`slice` when the matching CLI flag is not `None`; `--slice` defaults to `None` and has no separate clear flag, so omitting it silently carries the previous value forward. Empirically reproduced against the shipped script (89dd2b1): a state file seeded with `slice.value = "old-value"`, then `cycle --work W35` omitting `--slice`, leaves `slice.value` at `old-value`, byte-identical, while `work` updates correctly. This defeats the artifact's own stated design — position re-derived every cycle so a dead or unwired writer cannot masquerade as a healthy zero — for exactly this field. A second, separate claim relayed alongside this one (that the same script re-stamps `written_at` on every cycle regardless of content change) was retracted by the lead before filing and independently re-checked here: two identical `cycle` calls seconds apart leave `written_at` unmoved on both blocks, confirming the retraction; it is not part of this finding. Full evidence: [`docs/audit/findings/F69.md`](findings/F69.md) | — | 2 | carry forward, unowned — two fix shapes are live (an explicit per-field clear flag, or requiring the caller to pass an explicit empty value when its own re-derivation finds nothing) and the choice is not made here; not fix-before-close, since the watcher reported writing the stale value openly rather than being fooled by it. Falsifiable: discharged by either fix shape landing and the reproduction above no longer holding. Full reasoning: [`docs/audit/findings/F69.md`](findings/F69.md) |
| The ledger convention has lapsed; a frozen plan requires it anyway, and a correct escalation nearly went unrecorded (F70) | Four sources disagree: **16** `docs/plans/*-ledger.md` files exist, newest dated and last touched 2026-08-24 — nine days of silence spanning the entire post-2026-08-29 process adoption and all of W33-W36; `docs/process/delivery-process.md` (`CLAUDE.md` §15's named process authority) contains "ledger" zero times, case-insensitive; `docs/plans/2026-09-01-nt-0019-id-standard-map-plan.md` (frozen, merged `106e322`) uses it ten times, including **Slice W37-1's own acceptance line** — "verified by a diff recorded in the slice's ledger"; `.claude/skills/subagent-driven-development/SKILL.md`, the executor skill W37-1 runs under, describes the ledger as mandatory and load-bearing throughout, with no hint the practice lapsed. **Corrected 2026-09-02, twice over**: this row first read PR #562's silence (body, commit, comments — none mention ledger) as the gap having been "worked around silently." That was wrong. The lead reports the W37-1 executor did flag the exact same disagreement in a session report, correctly declining to revive the convention unilaterally — not independently verifiable by this review, but corroborated by a durable artifact this review found on its own: PR #562's squash-merge commit (`553bbef`, 2026-09-02T00:38:22Z) states the finding and the lead's ruling in full. **The real exposure, named at the lead's own request**: between the executor's report and that merge, the correct escalation existed nowhere any reader could check — durability depended on the merging commit message happening to be written in full, not on anything structural. No check anywhere (`audit-docs.py`, `register-lint.py`, `register-owed.py`, `scope-audit.py`) cross-references a plan's acceptance prose against ledger-file existence, which is the separate, still-open mutation. Same shape as **F58** (a charter asserting a mechanism nothing runs), checked against F58's own text rather than asserted by analogy. Full evidence: [`docs/audit/findings/F70.md`](findings/F70.md) | — | 2 | carry forward with a trigger — the lead ruled *proceed without a ledger* for W37 under the replan-vs-proceed call (`delivery-process.md` §5 step 4) and adopted this disposition as proposed; that disposition resolves W37's own exposure but does not fix the underlying four-way disagreement, which is this row. Proposed (not decided): discharged when NT-0019 lands `LG-` as a first-class family with its own directory, lint and §1.6 owner, making ledger existence mechanically checkable; a cheaper interim warn-only check (flag, don't fail, a frozen plan's acceptance text naming "ledger" with no matching file) is offered as an alternative, not a recommendation over the first. Falsifiable: discharged by either landing, or by a corrected mutation test showing such a plan is in fact caught today. Full reasoning: [`docs/audit/findings/F70.md`](findings/F70.md) |
| `python.yml` never triggered on `docs/**`, so a docs-only change could break any root test with CI green (F71) | `.github/workflows/python.yml` — the only workflow that runs the repository's root `tests/` suite — did not list `docs/**` in its `paths:` filter (either block); all thirteen files in that suite read `docs/` (verified by reading source, not grepping paths, since `test_register_owed.py` builds its path with a `pathlib` join a substring search would miss). `docs.yml` watches `docs/**` but does not run `pytest`. **Not hypothetical**: filing NT-0019 (PR #555, 2026-09-01) broke `test_audit_docs_notes_tombstone.py` (a frozen "18 working notes" literal, the corpus legitimately grew to 19) and `test_notes_move_citations.py` (the old `.claude/notes/` path exemption did not yet cover the note or the auditor's verification sweep) — both red on `main` for hours, invisible because nothing triggered `python.yml`. Fixed together in PR #561 (merged `f226891`, 2026-09-02T00:54:44Z): both tests corrected (count cross-checked against disk rather than a literal; the exemption narrowed to a documented, named two-member class) and `docs/**` added to `python.yml`'s filter in both blocks — landed as one commit because fixing only the tests would have left the next docs-only PR free to repeat the failure with the detector disarmed. Four broken-input proofs verified in the merge commit; `pytest tests/` 122 passed; `audit-docs.py`, `ruff`, `mypy` all clean; the `python` workflow itself green on the merged head. **Filed as its own finding, not folded into F26**: #561's own commit message calls this "register finding F26's path-filter gap with a live instance" — see F26's dated note, filed alongside this row, for why that attribution is wrong (different workflow, different symptom, different fix; siblings in shape, not the same finding). Full evidence: [`docs/audit/findings/F71.md`](findings/F71.md) | — | 2 | accept — the defect is fixed and verified (`f226891`); no owner is owed further work. The event that would reopen this row is a regression dropping `docs/**` from `python.yml`'s filter again, or a `docs/**` change breaking a root test with CI still green. Full reasoning: [`docs/audit/findings/F71.md`](findings/F71.md) |
| `written_at` records when a block's content last changed, not when the writer last checked it (F72) | Live instance, read directly from `~/gi-pricing-plan.local/handover/runtime-state.json` at filing time: `position.written_at` current to the minute (`2026-09-02T02:02:13Z`) while `in_flight_expensive_verifications.written_at` sits at `2026-09-01T20:06:20Z` with `entries: []` — six hours old, truthful (nothing changed since), and useless for the question a reader actually asks: is this being maintained right now, or is it a dead writer's residue. `write_runtime_state.py cycle` carries the whole old block, stamps included, forward unchanged whenever freshly re-derived content matches what's on disk, writing a new `written_at` only on an actual content change — re-proven empirically for this filing, as for F69: two identical `cycle` calls leave `written_at` unmoved on both blocks. **This is not a defect**: it is `docs/plans/2026-08-30-nt-0014-q1-q3-q4-rulings.md` **Ruling 47(d)'s own acceptance test** (a no-op cycle produces a byte-identical file), verified verbatim at its cited location, `docs/audit/work/nt-0012-0013-0014-adoption/README.md:114` — no mis-citation found, though one was looked for. The watcher's first draft read `watcher.md`'s "each cycle" claim as requiring `written_at` to move every cycle and filed this as a tool defect, which would have collided with Ruling 47(d) and forced a false choice; corrected here — "each cycle" describes the watcher unconditionally re-deriving its inputs and invoking `cycle`, not the timestamp `cycle` then writes. Behaviour satisfies the skill and the ruling; no field in the current schema (`written_by`/`written_at` are the only two per-block keys anywhere in the script or `SKILL.md`) records "checked and confirmed live" separately from "content last changed." **Same shape as F69** (the position block cannot express "no active slice" either) — different fields, same state-model-narrower-than-its-consumers shape; two instances, not one. **Amended 2026-09-02**: the gap is wider than the JSON. Reproduced directly — two identical `cycle` calls two seconds apart, against a canonical baseline, leave content byte-identical (Ruling 47(d) holds) **and leave the file's mtime unmoved too**, because `path.write_text`, the only statement that could advance it, sits inside the branch a genuine no-op never enters. **Nothing available on this file records liveness, not just `written_at`.** That matters because mtime is this repository's own established liveness idiom — `delivery-process.md:253` names "mtime staleness checks" as a watcher duty, and F67 records "`ps -p <pid>` and the state file's mtime are the adopted liveness proxies" directly — and for this one artifact, applying it gives a wrong answer silently. Full evidence: [`docs/audit/findings/F72.md`](findings/F72.md) | — | 2 | carry forward, unowned. **Amended 2026-09-02 — asks for a property, not a field.** The original proposal (a `checked_at` field inside the state file) is withdrawn and **ruled out, not merely revised, by Ruling 47(d) at its primary source** (`docs/plans/2026-08-30-nt-0014-q1-q3-q4-rulings.md`, part (d)): a field refreshed every cycle inside the file is a freshness token moving while content is frozen, which the ruling names as F31's shape "regardless of what the surrounding prose says" — no field inside `runtime-state.json` can satisfy this row without violating that ruling. Asks instead for a no-op cycle to be observable **without changing the state file's bytes** — two candidate mechanisms named without ranking (`os.utime()` on every cycle; a separate sidecar timestamp file), the choice left to whoever builds it. Either must leave `tests/test_watcher_runtime_state.py::test_a_cycle_with_no_change_is_byte_identical` passing unmodified — confirmed that test asserts content bytes only, never mtime, so neither candidate conflicts with it. Not fix-before-close: F58 already carries the separate "nothing invokes this on a schedule" gap. Falsifiable: discharged by either mechanism landing with the cited test unmodified, or by a corrected reading showing the existing artifact already answers the liveness question. Full reasoning: [`docs/audit/findings/F72.md`](findings/F72.md) |
| The lead-staleness nudge ladder cannot distinguish a legitimate long wait from a dead lead, and its disambiguating signal was withdrawn for cause and never replaced (F73) | `.claude/skills/reporter-cycle/scripts/nudge.py`'s three-signal AND-conjunction (the `.last_lead_status_ts` marker, `eta.md`'s stamp, `origin/main`'s newest commit) — confirmed verbatim against the shipped script's own docstring — nudges only when every signal is stale, which correctly fixed the single-signal design's documented false positive but cannot tell a legitimate long wait from a dead lead: during any sufficiently long build all three go stale *together*, for the same reason, not independently. The fourth signal that would disambiguate — is real work actually in flight — is architecturally `roster-state.md`, which `.claude/roles/reporter.md` already frames as something to read rather than poll for, but `.claude/roles/watcher.md:24` (read directly) forecloses using it: roster-state.md is the constant-with-a-live-timestamp **F31** found and the charter now explicitly disclaims. **Live instance, found in a log this review pulled itself, not asserted**: `~/gi-pricing-plan.local/handover/nudge.log` shows two nudges 30 minutes apart (`2026-09-02T03:15:02Z` and `03:45:01Z`), `main`'s logged age growing from 47.2 to 77.2 minutes across the gap — no merge landed between them — while `runtime-state.json`'s `position.slice` (`W37-4`) and its own `written_at` (`02:02:13Z`) stayed unmoved the whole window: three independent artifacts agreeing a build ran for over an hour and the ladder fired twice on entirely correct behaviour. **Citation corrected**: the reporter's write-up cited the shared pattern as "F69, F72 (bundled-data invariant), and this detector" — F72 is not the bundled-data invariant (it is this session's `written_at` finding, verified against the register directly); the actual bundled-data invariant is `backend/tests/test_lineage.py::test_no_reference_rows_are_bundled_in_the_repository`, a test, not a register finding, which fired correctly on W37-4's own fixture tonight and does not belong in this trio. **The corrected trio, same shape verified against each member**: F69 (`position.slice` cannot express "no active slice"), F72 (`written_at` cannot express "checked and unchanged"), and this row (the runtime-state ecosystem cannot express "work is in flight" generally — `in_flight_expensive_verifications` is scoped narrowly, per its own skill doc, to spec §8's announce/check protocol for one specific costly verification, not general work-dispatch state) — three instances of one shape in the same artifact family: the state model is narrower than its own consumers need. Full evidence: [`docs/audit/findings/F73.md`](findings/F73.md) | — | 2 | carry forward, unowned — **not** a case for reversing F31 (the withdrawal remains correct; a false signal removed and not replaced is still better than one kept) and **not** a threshold fix (the ladder has no better information available, which is the finding). Per the shared-shape framing: recommend extending artifact B's model with a general, re-derived-each-cycle "work in flight" signal — Ruling-47(d)-compliant by the same construction as `position` and `in_flight_expensive_verifications` already are — rather than patch `nudge.py` alone or rehabilitate `roster-state.md`; that signal, once built, is what the reporter's charter already anticipates reading. Sizing, field shape and ownership not decided here. Falsifiable: discharged by such a signal landing and the reporter's charter updated to read it, or by a corrected reading showing the existing artifacts already disambiguate. **Amended 2026-09-02 — two further states, neither discharged by the "work in flight" signal above.** (1) **Work complete, waiting on an external decision** — the state this amendment is itself written in: zero open PRs, `main` at `81e7c4e`, `eta.md`'s headline says the team stood down with only the maintainer's decision outstanding; `nudge.log`'s own last entry (`07:15:02Z`, all three signals stale) is a live instance. "Work in flight" reads correctly `false` here too, so it cannot be the disambiguating signal; this state needs a "named outstanding external dependency" signal instead. (2) **A relaying agent's published field is checked; its internal narrative is not** — the reporter's cycle posts `eta.md`'s headline verbatim by design and that field stayed correct; separately, in relaying nudges at `06:15:03Z` and `07:15:02Z`, it narrated "W37-4 still building" — first reported by the lead as recipient, **then confirmed by the reporter's own quoted text, supplied on request after this row's first push**: "W37-4 still building (no merges expected, origin/main stale as expected)" at 06:15, "Expected pattern continuing during W37-4 build" at 07:15 — still an author's account of its own output, not this review reading the channel, but independently corroborated by this review against `nudge.log` on two details neither party was asked for (the "third"/"fourth" nudge labelling and both marker-age figures, both matching to within a minute). Independently verified false by 2h09m and 3h09m respectively (`#573` merged `04:06:14Z`; twelve further PRs merged before the second relay, confirmed via `gh pr list`). A third instance, per the lead: the reporter's own hedge narrowing this to "accurate through 04:15" is itself independently verified false, by nine minutes. The mechanism: `report-spec.md`'s re-derivation discipline covers structured facts (open PRs, merges), never the free-text narrative around them — nothing checks the belief the way something checks the field, and the checked field is the one that stayed right. Full reasoning: [`docs/audit/findings/F73.md`](findings/F73.md) |
| `.claude/roles/reporter.md`'s Mechanism section describes a single-signal lead-staleness nudge; the shipped detector is a three-signal conjunction (F74) | `nudge.py`'s `lead_is_stale` (verified directly at line 150) is `all(is_stale(marker_age), is_stale(eta_age), is_stale(main_age))` since 2026-09-01 — confirmed live by `nudge.log`'s own format change (a single number through `2026-09-01T10:45:01Z`; three numbers, ANDed, from `2026-09-01T21:00:01Z` on) and by the repo-tracked `.claude/skills/reporter-cycle/SKILL.md`'s dated entry, which documents the same design in full. `.claude/roles/reporter.md`'s own `## Mechanism: Lead freshness nudge` section (lines 49-78, read in full, not only its `Owns` bullet) still states only the marker — the single-signal design the 2026-09-01 false positive (marker 25.4 min stale while the lead had merged six PRs in the same window) replaced. `~/gi-pricing-plan.local/handover/report-spec.md` §5 (local, untracked) names this exact gap and declines to fix it: "a finding against the charter under `CLAUDE.md` §15, not something to edit here." **Checked against and distinct from four open rows on the same code**: F26 (no CI over `.claude/roles/**`, why nothing caught this, not this defect), F33 (mypy coverage, unrelated artifact), F72 (a different field's staleness-expressiveness gap), F73 (takes the three-signal design as given and finds it cannot separate a long build from a dead lead — a design-adequacy question, not a documentation-accuracy one; its one quotation from `reporter.md` is accurate and from a different section). **One correction to how this was raised**: the working design is not local-file-only as first framed — the committed skill already documents it correctly, and `nudge.py` is code a respawned reporter runs, not prose it re-implements, so the real exposure is a wrong mental model for a reader who stops at the charter's own Mechanism section, not a mechanism that regresses on respawn. Full evidence: [`docs/audit/findings/F74.md`](findings/F74.md) | — | 2 | carry forward, unowned — the fix is narrow (replace reporter.md:49-78 with a pointer to `.claude/skills/reporter-cycle/SKILL.md`'s dated entry, the shape lines 13-17 already use for the rest of the mechanism), but who makes it is the open question this row records rather than decides: the lead, under `CLAUDE.md` §12's practice of fixing a stale role file in the same session it is found, or the maintainer, under NT-0019 §1.6's accepted-but-not-yet-operational standard that charters are maintainer-authored (`docs/notes/0019-one-id-per-document.md` §1.6, read directly, "Reference — charters" row: "maintainer; 'a role file that proves insufficient' → `FD-` → maintainer amends"). Absent that decision, decays to the next `CLAUDE.md` §14 plan review. Falsifiable: discharged by `reporter.md`'s Mechanism section being corrected to the three-signal design, or by a corrected reading showing it already matches. Full reasoning: [`docs/audit/findings/F74.md`](findings/F74.md) |
| The "never end your turn with outstanding background work" rule lives in one of seven role charters (`executor.md`) and the one skill it alone cites; the other five spawned roles have no version of it, including this one (F75) | `executor.md:29-42` states the rule in full (a backgrounded wait cannot notify a stopped agent turn, so the wait must block the agent's own turn in the foreground — "applies to everything you start... and a subagent you delegate to"), superseding an earlier version that failed twice more before this one was written. `.claude/skills/dev-commands:241-285` carries a fuller account — "Five occurrences on 2026-08-30 across four mechanisms — a backgrounded command, a background poller, a `Monitor` task, and a delegated subagent" — and is named by exactly one charter (`grep`, one hit: `executor.md:33`). A keyword sweep of all seven charters, reproduced independently under two different patterns — this review's and the dispatch's each reproduce exactly under their own pattern, not two attempts at one count (**corrected 2026-09-02**, `docs/audit/findings/F75.md` has both) — and the three-way zero for `auditor.md`, `decision-maker.md` and `planner.md` holds under both patterns and under occurrence-counting as a third, independent method; read directly, `reporter.md` and `watcher.md`'s few hits are incidental ("Monitor" as the harness tool's proper noun, unrelated verbs) — **none of the six non-executor charters contains the rule**. `lead.md` discusses the same 2026-08-30 incident but as justification for a different policy (dispatch fresh agents, since a charter fix cannot reach a running one) rather than a turn-discipline instruction for itself, consistent with its own "main thread, not a spawned role" framing — not extended to a claim that `lead.md` has the same gap. **Live instance, this session**: investigating F74 in this same dispatch, this auditor session dispatched a `gate-runner` subagent and called `ScheduleWakeup` expecting it to resume the wait, then ended its turn — `auditor.md` names no foreground-loop alternative and cites no pointer to `dev-commands` — and the dispatching lead had to send a direct message to resume it. No existing register row covers this (checked directly: no row anywhere mentions "turn discipline" or the 2026-08-30 stalls). Full evidence: [`docs/audit/findings/F75.md`](findings/F75.md) | — | 2 | carry forward, unowned — the fix is uniform and narrow across the five (add `executor.md`'s bullet, or a pointer to it and to `dev-commands`, to `auditor.md`, `decision-maker.md`, `planner.md`, `reporter.md`, `watcher.md`), but who makes it is recorded rather than decided, the same open question as F74 and for the same reason: the lead under `CLAUDE.md` §12, or the maintainer under NT-0019 §1.6's accepted-but-not-yet-operational standard for charters. One row for all five charters, deliberately, not five: one shared mechanism (the same lesson absent from all for the same reason) argues for the class, not per-charter exemplars. Kept as its own row rather than folded into F74 despite a shared dispatch: different artifact, different remedy, different evidence path — reasoning in the essay's "Why two rows, not one." Absent an ownership decision, decays to the next `CLAUDE.md` §14 plan review. Falsifiable: discharged when all five charters carry the rule or a pointer a fresh spawn would actually read, or by a corrected reading showing one already does. Full reasoning: [`docs/audit/findings/F75.md`](findings/F75.md) |
| An uncaught `HeaderError` in check 39 silently drops six later `audit-docs.py` checks, and every note from the ~30 that ran before it — the fix is already ruled and owned, this row is what tracks it (F76) | `scripts/doc-index.py:268-270`'s `_ROW_FIELDS` rejects `tree`, `corrected_by` and `relates`, which `docs/_templates/WK.md`/`SL.md` (verified directly) declare in the fenced block their own comment instructs an author to copy verbatim; `_parse_row_block` (`doc-index.py:289`) raises `HeaderError` on the first one it meets. `check_index_stable` (check 39, `audit-docs.py:2207`) calls `_doc_index.build_corpus(ROOT)` with no `try`, and is the last of ten checks `check_ids_30_39()` runs (`:2686`) immediately before six more with no exception boundary between any of them: checks 23, 25, 26, 27, 28, 29 (`:2689-2705`). **Not currently live on real content, verified rather than assumed**: `docs/roadmap.md` at `22adfe9` has zero `WK-`/`SL-` row blocks (NT-0019's roadmap restructure has not run), so `build_corpus` returns cleanly today and `audit-docs.py` exits `0`. **Fires on the templates' own first real use, proven by two reverted reproductions**: an isolated scratch file built from `WK.md`'s exact fenced block raises `HeaderError: unknown row field 'tree'` fed directly to `scan_roadmap_rows`; the same block appended to the real `docs/roadmap.md` and run end-to-end through `python3 scripts/audit-docs.py` produces a full traceback and exit `1` (captured in full, then reverted with `git checkout --`, confirmed clean and green again). **Answers the dispatch's own question**: a traceback, not silence — exit `1`, a full stack trace naming the exact file/line/field, materially less severe than the scan-roots precedent's silent exit-`0` false green it is compared to, though the audit's own structured report (every note from the ~30 checks that already ran, not only the six that never run) is lost either way. **Not a new decision**: `docs/plans/2026-09-02-w37-template-parser-conflicts-rulings.md`'s **Ruling 79** (merged PR #581, `b6a6f31`) already verifies this exact defect with its own probe, rules `_ROW_FIELDS` (not the templates) as the defect, states four fix obligations, and names **Owner: W37-6** with acceptance criteria — including the "sibling item" the dispatch asked noted alongside this row, Ruling 79 §4's own first acceptance bullet (extend `test_template_headers.py` to feed a template's block to the row parser that actually consumes it, the reason nothing caught this). PR #581's own squash-merge commit claimed a register row was "FILED SEPARATELY" for this; `grep -c` for it against `docs/audit/register.md` returns `0` at every tree checked. This row makes that claim true. Full evidence: [`docs/audit/findings/F76.md`](findings/F76.md) | W37-6 | 2 | **Resolved 2026-09-02** (W37-5b, PRs `#593` and `#607`), superseding the verdict below: Ruling 79's four fix obligations and its §4 acceptance items have landed — `_ROW_FIELDS` is now derived per family from the templates (`scripts/_docid.py`'s `row_template_fields`, wired through `scripts/doc-index.py` and `doc-id.py migrate`'s row emission, PR #593), and `check_index_stable`'s `build_corpus` call is guarded against both crash paths, `_doc_index.HeaderError` and the non-ISO-`created:` `ValueError` (`scripts/audit-docs.py:2226-2236`, PR #607). Each proven on broken input: `tests/test_audit_docs_ids.py` asserts all six checks after `check_index_stable` still run past a malformed header (both exception shapes) and names them; `tests/test_template_headers.py` feeds each template's own example block through the row parser that consumes it and asserts `kind:` is rejected on a `WK-` row. 302 tests pass at `d47a5f5` across the four touched files (`test_doc_id.py`, `test_doc_id_migrate.py`, `test_template_headers.py`, `test_audit_docs_ids.py`); `python3 scripts/audit-docs.py` exits 0. Original verdict, kept rather than deleted per this file's own "annotated in place" rule: carry forward with an owner — **W37-6** (Ruling 79 §3 point 5, open and active); the fix is Ruling 79's four obligations (derive `_ROW_FIELDS` per family from the templates via `_docid.py`, wire `tree`/`corrected_by`/`relates` into `_row_header_from_raw`, derive `doc-id.py migrate`'s row emission from the same template) plus its §4 acceptance items (a broken-input proof failing today and passing after, Ruling 70's mutation test applied to `doc-index.py`, a `kind:`-on-`WK-` rejection test) — not re-decided here. One structural observation, not filed separately: Ruling 79's fix closes this specific trigger; it does not add exception isolation to `check_ids_30_39()`'s six-later-check gap in general, so any other uncaught exception in checks 1-39 would reproduce the same blast radius — worth W37-6's or the lead's awareness, not sized or ruled here. Falsifiable: discharged when Ruling 79's §4 acceptance items land, or by a corrected reading showing this does not reach real content the way the essay's two reproductions demonstrate. Full reasoning: [`docs/audit/findings/F76.md`](findings/F76.md) |
| Ruling 84 §4's second acceptance item — no emitted `LG-` carries a `slice:` resolving to no roadmap row, reddening on a broken fixture — is vacuously true and untestable as stated (F77) | `_stamp_header` (`scripts/doc-id.py:888-953`) skips the `slice` key unconditionally for every caller (`:946-947`, `elif key in ("slice", "deliverable", "lands_in", "trigger"): continue  # not populated by this slice's migration — no data source for them`) and its signature carries no `slice` parameter at all, so no code path in `migrate()` can ever write a `slice:` value onto an emitted `LG-` record. Ruling 84 §4 (`docs/plans/2026-09-02-w37-guard-arithmetic-and-ledger-family-rulings.md:538-540`) asks for a check that reds "on a deliberately broken fixture carrying `slice: SL-99999`" — constructible only by hand-authoring a fixture outside `_stamp_header`'s own writer path, since the writer cannot produce the violating input itself. No such check or fixture exists at `d47a5f5` (`grep -n "SL-99999" tests/test_doc_id_migrate.py` returns nothing). Adjacent to, not identical with, obligations-list row 24 (checks 30-39 have never met a corpus): `scripts/audit-docs.py:1636-1637`'s `check_cross_references` docstring already discloses that `work:`/`slice:`/`phase:` resolving to roadmap rows is "not implemented against the live scope" pre-migration, for the same `_ID_SCOPE_ROOTS` reason. First disclosed in `614c92c`'s own commit body (closure-records' not-closed W5 headings become LG- ledgers, #599): "Whether that satisfies the acceptance item is an interpretation of a merged ruling and is routed rather than decided here." Not previously carried in this register. | W37-6 | 2 | carry forward, unowned — the interpretation question is **Ruled 2026-09-02** (Ruling 94, `docs/plans/2026-09-02-w37-vacuous-acceptance-item-ruling.md`, PR #614), so what remains is implementation with no owner named yet; next event: the lead names an owner when adopting the W37-5b closure record. Superseding the deferral below on the interpretation only: vacuously true does not satisfy Ruling 84 §4's second item (`CLAUDE.md` §13 — "a check that has never printed a failure has not been tested"), and the remedy is not a reading that treats it as satisfied by construction but a **substitution**: a check that counts the `slice:` values on emitted `LG-` records, requires every one to resolve to an `SL-` row, and **prints the count it checked** (a passing zero must say which zero it counted, per `NT-0007`), reddening on a **one-line mutation of `_stamp_header`** — removing `slice` from the skip tuple at `:946` so the template's `slice: SL-NNNNN` placeholder is emitted — rather than a fixture document, since a fixture cannot carry a key the writer itself refuses to emit. Ruling 84 is not edited; its other three items stand, and its third (`work:` resolves) is confirmed **live, not vacuous** — verified directly at `09b7e9b`: `_discover_roadmap(ROOT)`'s one `old_token == "W5"` draft (`WK`, phase `P1b`) matches all ten real `_discover_closure_records` `LG-` drafts' `work_token`, so today's real corpus resolves cleanly — but no *guard* yet exists that would red if a future `LG-` record resolved to neither `work:` nor `slice:`: `tests/test_doc_id_migrate.py::test_write_document_drafts_resolves_a_ledgers_work_and_phase_from_the_roadmap` asserts the opposite (an unresolved `work_token` silently omits `work:` rather than raising), and both that test's and `_write_document_drafts`'s own docstrings are now stale, still describing "the real corpus's `roadmap_drafts` is empty" — true before `4cbfa62` (row 2/3), not after. **Not yet built**: as of `09b7e9b` neither the substituted count-and-print check (Ruling 94 obligation 2) nor a guard for item 3's "neither axis" violation exists in `scripts/doc-id.py` or `tests/test_doc_id_migrate.py` — both are narrow, unassigned implementation gaps this row now tracks rather than the interpretation question Ruling 94 already closed. Original deferral text kept below per this file's "annotated in place" rule: deferred with an owner — the decision-maker (per `614c92c`'s own routing), to rule whether the item is unreachable-by-construction (satisfied vacuously) or instead obliges row 24's general check-33 cross-reference validation to run before W37-6 closes. Falsifiable: discharged by that ruling, or by a corrected reading showing a code path can write `slice:` onto an `LG-` record after all. |
| `_discover_roadmap`'s phase-spanning refusal — a work whose leading rows disagree on which milestone it belongs under — has no fixture exercising it (F78) | `scripts/doc-id.py:1917-1925` raises `NotImplementedError` naming every work id whose occurrences span more than one phase section rather than choosing a milestone silently (`unresolved_phase = [m.work_id for m in merged if m.phase_label is None]; if unresolved_phase: raise NotImplementedError(...)`). Disclosed by its own author in `4cbfa62`'s commit body (the roadmap and register's real shape convert, W37-6 rows 2, 3, #610): "Disclosed and not built: no fixture exercises the phase-spanning refusal. It is measured absent across every work's certain occurrences on the real tree today and the refusal code exists; the gap is named as a follow-up rather than the row being called done." Confirmed independently at `d47a5f5`: `grep -n "more than one phase\|unresolved_phase\|phase_uncertain" tests/test_doc_id_migrate.py` returns nothing. Harmless today — `_discover_roadmap(ROOT)` converts all 41 real works cleanly, none spanning phases — but untested code guarding an irreversible migration commit. | W37-6 | 2 | deferred with an owner — W37-6 (the executor who runs the migration), a fixture forcing two leading rows of one work id into different `## <n>. Phase <label>` sections and asserting the `NotImplementedError` names both, landed before or as part of that slice's own acceptance evidence. Falsifiable: discharged when such a fixture lands, or by a corrected reading showing the branch is unreachable by construction — it is not: `merged` is built from real per-id grouping with no upstream filter excluding a multi-phase spread. |
| `register-lint.py`'s `classified == seen` binary-count guard does not itself satisfy Ruling 83's three-bucket census property it inspired (F79) | Ruling 83 (`docs/plans/2026-09-02-w37-guard-arithmetic-and-ledger-family-rulings.md`) requires a census that refuses by naming every unmatched unit rather than comparing counts — "a guard may not derive its denominator from the same matcher it is checking," and a count-only assertion cannot distinguish a true zero-gap from two mismatched miscounts that happen to cancel. `scripts/register-lint.py`'s own `assert classified == seen` is exactly the count-comparison shape Ruling 83 §3 item 4 rules against, and this slice did not touch it. Disclosed in `d7c9b08`'s commit body (Ruling 83 census for five silent discovery functions, W37-5b, #608), which reused the *discipline* for the five new discovery-function guards but declined to retrofit `register-lint.py` itself: "deliberately not fixed: register-lint.py, which is wired into the gate as check 29, does not itself satisfy Ruling 83." Confirmed live: `python3 scripts/audit-docs.py` still runs `register-lint.py` as check 29 and reports "0 violation(s)" by the same binary tally at `d47a5f5`. | — | 2 | carry forward, unowned — the fix (retrofitting `register-lint.py`'s guard to `_reconcile_census`'s named-unmatched-unit shape, or an equivalent) is narrow but touches a script check 29 already depends on in the gate, so who takes it is recorded rather than decided here. Falsifiable: discharged when `register-lint.py` names its unmatched rows rather than only their count, or by a corrected reading showing the count comparison cannot silently cancel for this file's own row grammar. |

A carried finding is written here by the work-item close checklist
([`checklists/work-item-close.md`](checklists/work-item-close.md)) and by the phase close
checklist ([`checklists/phase-close.md`](checklists/phase-close.md)).

## F42 is retired, recorded 2026-08-30

**F42 names no finding and never will.** It was allocated for the `req-coverage.py`
mislabel, then withdrawn before it was ever filed here: **F36** already registered that
defect, in nearly the same words. The number is a tombstone, not a gap — a reader who finds
F41 followed by F43 has found this note, not an omission.

**Why it is tombstoned rather than re-used.** After the withdrawal the number was briefly
re-allocated to a second, unrelated finding — the concurrent-`pytest` truncation that is
`F40`'s sibling, where two runs collide with no interruption involved. That finding is
**F45**. Re-using a withdrawn number left one artifact carrying `F42` in two incompatible
senses at once, so a citation to it could not be resolved without knowing the allocation
history. `CLAUDE.md` §5 forbids exactly this for requirement ids and section numbers, and
the register's finding ids are held to the same rule: **never renumber, never re-use;
append, mark superseded, or leave a tombstone.**

The renumber cost one file and one task. The precedent — a register that breaks §5 while
the phase review argues from §5 — would have cost more, which is the ground the decision
was taken on.

## NT-0005 discharges, recorded 2026-08-27

Two deferred NT-0005 items are discharged rather than filed. They are recorded here so
this register is the custody home NT-0005 asked for.

- **Item (c) expired.** The `W6b-9` to `W6b-1b` dependency, which the frozen slice map's
  dependency column does not carry: `W6b-1b` shipped as PR #194 and `W6b-9` shipped its
  checks. No roadmap row is needed.
- **Item (g) is placed.** The eight open questions that sat on no decision gate —
  OQ-OVR-10, OQ-DATA-12, OQ-DATA-13, OQ-PLAT-10, OQ-PLAT-11, OQ-PLAT-12, OQ-PLAT-13,
  OQ-PLAT-14 — are all named in the `docs/roadmap.md` §10 note of 2026-08-26, among the
  twenty questions placed or recorded that day.
