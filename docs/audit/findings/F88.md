# F88 — discovery does not reach two populations §5.2 routes: one aborts every real run, one is silent

**Raised** 2026-09-02 by the W37-5c stamp-path executor, on the lead's instruction. Work item
**W37-6**, phase 2.

**Three limbs, one defect, and they are filed together deliberately.** Limbs 1 and 2 are
*"NT-0019 §5.2 says where this file goes, and no `_discover_*` function reaches it"*; limb 3
is the same shape one level up — a state the tooling can reach and cannot see — and it was
produced by this slice while filing these very findings. One finding for the shared
mechanism is worth more than three rows nobody connects. Limbs 1 and 2 differ only in whether
the migration **stops** or goes **blind** — the axis F80–F82 and F84 already sit on — and
limb 3 is blind in the same sense, about the register rather than about the migration.

## Limb 1 — `_discover_vendored_skill_manifests` aborts every real run, and would skip 25 if it did not

`scripts/doc-id.py`'s `_discover_vendored_skill_manifests` decides "already stamped" with
`if _docid.parse_header(skill_md) is not None: continue`. **That predicate is wrong for this
question**, and the two consequences are independent:

**It reads someone else's front matter as this migration's stamp.** `parse_header`'s own docstring
says so: *"A key outside the closed field set is not an error here — it lands in `.extra`
verbatim."* Every real `SKILL.md` opens with the harness's own block (`name:`, `description:`), so
`parse_header` returns a `Header` and the manifest is skipped as already-migrated. **Measured at
`359936b`, against the shipped symbols:**

| | |
|---|---|
| `.claude/skills/*/SKILL.md` | **46** |
| of those, `_is_vendored_skill_manifest` true | **28** |
| of the 28, `parse_header` returns a `Header` → skipped as "already stamped" | **25** |
| of the 28, `parse_header` raises `HeaderError` | **3** — `create-adaptable-composable`, `planning-with-files`, `vue-best-practices` |

**And it never gets that far.** The three that raise do so *inside the discovery function*, so
`_discover_vendored_skill_manifests(ROOT)` propagates `HeaderError` and a real `migrate()` run
aborts before the stamp loop is entered. Reproduced directly:

```
_discover_vendored_skill_manifests(ROOT) -> HeaderError:
  .claude/skills/create-adaptable-composable/SKILL.md:6: indented line — nested mappings
  are not in the closed field set (NT-0019 §1.5): '  author: github.com/vuejs-ai'
```

**This is F80/F81/F82 class and has had no id.** Those three are unconditional guards that refuse
to migrate a governed thing with no discovery code, and each was filed. This is a fourth stop with
the same effect on a run and none of the same visibility: it presents as a stack trace from a
YAML parser rather than as a guard naming what it cannot handle.

**It is distinct from F83, and the distinction matters.** F83's population is files that cannot
carry front matter **in any form** (59 `.json`, 1 `.yaml`, 2 more non-`.md` under `docs/`, plus
these 3 manifests) and its remedy is the exemption register now shipped in `audit-docs.py`'s
`UNSTAMPABLE_EXEMPTIONS`. **That register cannot discharge this limb**: it is read by check 35,
while the abort is a `parse_header` call inside `doc-id.py`'s discovery, which never consults it.
Two mechanisms on the same three files. Confirmed with the register's author.

## Limb 2 — `docs/audit/phases/1b/register.md` is discovered by nothing, silently

§5.2's row: *"`audit/register.md`, `audit/phases/1b/register.md` → one `findings/register.md`;
… the phase register's rows merge in with `phase: P1b` (NT-0003: no second copy)"*.

`_discover_register` reads `docs/audit/register.md` and nothing else — its own docstring says
*"Matched only at the legacy path"*. **Nothing reaches the phase register.** No guard fires, no
census covers it, and `migrate()` completes and reports success. That is F84's shape exactly, in a
population F84 does not range over, and it was found *by* F84's census: the file is a declared
exception there, carrying this reason, so that the gap is listed rather than folded into a `CR-`.

## Limb 3 — nothing stops two register rows claiming one finding id

**Added on the lead's instruction, after this slice produced the collision itself.** On
2026-09-02 two executors filed different findings as `F87` within an hour. Each had computed
the next free id from `main` correctly; neither could see the other's unmerged PR; there is
no allocator.

**The lead's account was that nothing would have caught it before the second merge
overwrote the first's file. Half of that is wrong, and the half that is wrong is the half
that would have been checked.** Measured with `git merge-tree` against the two real
commits:

```
added in both
  our    100644 4edba56... docs/audit/findings/F87.md
  their  100644 4278a53... docs/audit/findings/F87.md
```

**git reports the file collision as an add/add conflict and blocks the merge.** The essay
was never going to be silently overwritten. A check asserting "no two findings files share
an id" would therefore duplicate a guarantee git already gives, and would have been written
in the belief that it closed this gap.

**What has no guard is the register.** Two rows citing one id are inserted at different
offsets, merge cleanly, and `audit-docs.py`'s F-id citation check resolves the id to its one
file and passes — so the register can carry two rows for one finding and nothing says so.
That is the real hole, and it is one line of the same class as limbs 1 and 2: *a state the
tooling can reach and cannot see.*

**Discharged in this commit rather than carried**, because it is cheap and because leaving
it open would mean filing a finding about invisible states while leaving one open in the
document that reports them. `tests/test_findings_ids.py` asserts two invariants:

| | |
|---|---|
| every `docs/audit/findings/F<n>.md` heading names its own id | the mistake a renumber makes — this very finding was renumbered twice |
| no F-id is claimed by more than one register row's Finding-id cell | the state git permits and the audit passes |

Both are proven on deliberately broken input built in `tmp_path`, **never by mutating the
real tree** — which is limb 3 declining to commit the defect limb 1 of `F89` reports.
Measured at the filing tree: 76 register rows citing an id, 76 distinct; 25 findings files,
zero heading mismatches.

## Pinned by symbol, not by a pasted figure

At the branch head of **PR #639** — *unmerged at the time of filing, and this citation is to a
branch, not to `main`*:

- `tests/test_doc_id_migrate.py::test_exactly_one_discovery_writer_claims_the_closure_readmes`
  enumerates every `_discover_*` in the module **by name** and asserts the set that raises on the
  real corpus is exactly `{"_discover_vendored_skill_manifests"}`. A fourth abort appearing, or
  this one being fixed, reds that assertion rather than passing quietly.
- `scripts/doc-id.py`'s `_AUDIT_CLOSURE_CENSUS_EXCEPTIONS["docs/audit/phases"]` carries limb 2's
  reason, and
  `tests/test_doc_id_migrate.py::test_audit_closure_declared_exceptions_are_exactly_the_unclaimed_real_files`
  asserts the declared set equals the unclaimed set exactly, so the entry cannot outlive the gap
  it describes.

Reproduce limb 1's split with the shipped symbols rather than these numbers:

```python
vend = [p for p in sorted((ROOT / ".claude" / "skills").glob("*/SKILL.md"))
        if doc_id._is_vendored_skill_manifest(p)]
# then _docid.parse_header(p) on each, counting Header / HeaderError
```

## Not proposed here

The disposition is the lead's. Naming the shapes without choosing: the "already stamped" test
could ask for **this migration's own marker** (`family:` present) rather than "front matter
parses", which is the positive-idempotency reading the rest of the module already uses; and the
three that raise need a disposition that is not `parse_header`'s to make — the exemption register
enumerates them by path if a fix wants a reconciled list rather than a second copy. Limb 2 needs
either a discovery function or a declared decision that the phase register is not migrated.

## Falsifiable

**Limb 1** is discharged when `_discover_vendored_skill_manifests(ROOT)` returns without raising
**and** returns the manifests that have not been stamped by *this* migration rather than the ones
whose front matter fails to parse — proven by a manifest carrying foreign front matter being
returned, not skipped. **Limb 2** is discharged when the phase register is claimed by a discovery
function or refused by a named decision, and its declared exception is removed in the same commit.
Re-opened if a later change adds a §5.2 route with no discovery behind it, which is the event that
recreates both.

## Amended 2026-09-02 (PR #649) — limb 1 discharged; limbs 2 and 3 untouched

**Limb 1 is discharged.** `_discover_vendored_skill_manifests` no longer calls
`_docid.parse_header`. It classifies with `_front_matter_state`, the module's own textual
classifier, which was written for exactly this failure and whose docstring already named
these same three files: *"a classifier that crashes on the very files it exists to classify
cannot report them"*, and *"`"stamped"` is decided by `family:`, the one key every family's
template carries and no harness block does — a positive test for this migration's own output"*.
Both of this limb's consequences fall to that one substitution, which is why they were filed as
one defect.

It returns a `_VendoredManifestScan` — `to_stamp`, `deferred`, `already_stamped` — rather than a
list, so `len(to_stamp) + len(deferred) + len(already_stamped)` **is** the population and a
manifest cannot leave through an unnamed bucket. That was the shape of the second consequence:
not a wrong count, a bucket with no name.

**Measured on the real corpus at the branch that lands this, with the predicate rather than the
figure.** Population:
`{p.parent.name for p in (ROOT / ".claude" / "skills").glob("*/SKILL.md")} & set(_docid._VENDORED_SKILLS)`
— **28**, and every one classifies `foreign`, so the partition is **0 + 28 + 0**. The 25 that
`parse_header` reads are no longer counted as stamped, and the 3 that made it raise are deferred
by the same rule as the other 25 rather than by a second mechanism.

**Verified as a run, not as a function.** `migrate()` was called against a git-tracked
`git archive` snapshot of the real corpus with `_write_document_drafts` replaced by a sentinel
that raises: it **reaches the first write**, tree byte-unchanged. The shipped `doc-id.py` at
`c888b61`, run against that identical snapshot, aborts at
`.claude/skills/create-adaptable-composable/SKILL.md:6`. Reaching the sentinel proves every
pre-write call executed without enumerating them, so a call no list names cannot be missed — the
independent confirmation of W37-5c §2's *"no sixth abort point"*, arrived at by a different
method.

**Reaching the first write is not completing the run, and the first thing past it is a
mid-write crash — disclosed here rather than fixed.** `migrate()` run to completion on a
throwaway snapshot of the real corpus (never the repository) raises `KeyError: 'RS'` at
`_write_document_drafts`, **after writing 125 of the 290 document drafts and deleting none**
— 165 never reached, and no rollback: `_write_document_drafts` applies each write directly
with `Path.write_text` in a loop, `migrate()` wraps none of it in `try`, and the deletion
pass runs only after the loop completes, which is why nothing is deleted.

**It pre-exists this commit and is revealed by it, not introduced.** The shipped
`doc-id.py` at `c888b61`, run on a `c888b61` snapshot with only `_discover_vendored_skill_
manifests` neutralised to its own empty answer, raises the identical `KeyError` after the
identical 125 writes.

**Corrected: this passage first said 126.** That figure counted
`scripts/__pycache__/register-lint.cpython-312.pyc` — a bytecode cache the import machinery
wrote when `migrate` path-loads `register-lint.py` — as a migration output. 125 is measured
by tracing every `Path.write_text` the real writer performs (125 calls, 125 distinct paths,
none repeated, none outside `docs/`), and it agrees with the first `RS` draft sitting at
index 125 of the 290, read inside `_write_document_drafts`. A reconstruction of the same
figure from the discovery call order gave 126 and 291; it disagreed with the instrumented
run by one on both and is discarded.
`_discover_closure_records` emits two `RS-` document drafts from `docs/audit/closure-records.md`
(*"Independent audit —"* and *"W4 mid-workstream scope findings —"*), and the research family has
no entry in either `_DOCUMENT_FAMILY_DIR` (`ADR`, `CR`, `LG`, `PL`, `RFC`, `RL`) or
`_MIGRATE_TEMPLATE_FILENAME` (those six plus `REFERENCE`), though `docs/_templates/RS.md`
exists and `_docid._FAMILY_OF` carries `RS`. **It is task #34's failure mode one layer down** —
a partial migration, not a clean abort — and it was unreachable while limb 1 stopped the run
before it, which is why no record names it. **Fixed, and the sentence this replaces was wrong.** It read *"which directory and template
the `RS-` family takes is a disposition, and `CLAUDE.md` §13 keeps that with the lead."*
**NT-0019 §1 states both** — *"| Document | Research | `RS` | `docs/research/` | one spike,
measurement or audit | frozen | draft → active → closed \| retired | `spike` ·
`measurement` · `audit` |"* — `docs/_templates/RS.md` exists, and D13 routes `RS- kind:
audit` to `auditor`, which `_discover_closure_records` already sets. Nothing was open.

**The writer was incomplete, not the classifier.** The two records are audits that live in a
closure-records file; discovery emitting `RS` for them is correct and is unchanged. Making
discovery emit `CR` instead would have been the tempting repair and the wrong one.

**The set was closed rather than the instance**, because fixing one entry leaves the class
open. Every prefix a `_Draft(materialize="document")` can carry is derived twice and
independently — an AST walk over the shipped source (resolving a variable through its
assignments, and a *parameter* through its call sites, which is how
`_discover_headed_split_file` gets its `"CR"`) and an empirical run of every discovery over
the real corpus. They agree exactly:

| | |
|---|---|
| emittable with `materialize="document"` | **`{ADR, CR, LG, PL, RFC, RL, RS}`** |
| emittable but absent from `_DOCUMENT_FAMILY_DIR` | **`RS`** — the only one |
| emittable but absent from `_MIGRATE_TEMPLATE_FILENAME` | **`RS`** — the same one, so checking either table alone would have half-fixed it |
| in `_DOCUMENT_FAMILY_DIR`, not emittable | none |
| in `_MIGRATE_TEMPLATE_FILENAME`, not emittable | **`REFERENCE`** — legitimate: stamped in place, never moved into a family directory, and carrying no `id:` (§1.2) it has no padded filename either |

`_check_every_document_draft_is_placeable` refuses in the pre-write span when a document
draft names a prefix either table lacks. **It cannot fire on any corpus** — the emittable set
is a property of the module's source, not of the documents it reads — so it adds no
corpus-triggerable stop, and it converts what would be a `KeyError` inside the write loop
into a named refusal with the tree untouched. The test
(`test_every_emittable_document_prefix_has_a_family_dir_and_a_template`) is the primary
instrument and catches a branch this corpus never takes; the guard catches what the corpus in
front of it produced. Neither subsumes the other, and both are proven on deliberately broken
input — `test_the_placeability_guard_refuses_before_any_write` (a family removed from the
table: named refusal, tree byte-unchanged) and
`test_without_the_guard_the_same_input_is_a_mid_write_crash` (the same input, guard
neutralised: bare `KeyError`, files already on disk).

**With this, `migrate()` runs to completion on a throwaway snapshot of the real corpus** —
1,085 files written, 202 deleted — with no in-memory patching, and no seventh failure
behind it.

### Where this discharge departs from the Falsifiable clause above, and why

That clause asks for *"a manifest carrying foreign front matter being **returned**, not skipped"*.
Taken literally against the old `list[Path]` return, and with `migrate()`'s stamp loop unchanged,
that would have made a real run **prepend a second `---` block to 28 vendored files** — which
NT-0019 §4 step 5's own Reference section refuses (`_REFERENCE_FOREIGN_REASON`: the header must be
*merged*, which is W37-6 Task 1's work) and which `CLAUDE.md` §12 forbids as an edit to a vendored
file. A worse outcome than the abort, and an irreversible one.

**The clause's intent is satisfied exactly; its literal wording is satisfied by the partition.**
A foreign-front-matter manifest is returned — in `deferred`, with its reason — rather than
skipped, and it is not stamped. Recorded here rather than silently reinterpreted, because a
falsifiability clause that is departed from without saying so stops being one.

### Limb 1's replacement falsifiable clause — this supersedes the one in §Falsifiable

The clause above cannot be limb 1's acceptance test, because §"Where this discharge departs"
shows it is **destructive when met**: satisfied literally, against the old `list[Path]` return
and a writer that does `write_text(header + "\n" + body)` — a prepend, not a merge — it puts a
second `---` block into 28 vendored manifests. A test that causes the harm it was written to
prevent is worse than no test, because it passes. It is replaced, not reinterpreted.

**Limb 1 is discharged when all five hold on the real corpus, and re-opened when any stops:**

1. **Nothing in discovery raises.** `_discover_vendored_skill_manifests(root)` returns, and so
   does every other `_discover_*` — pinned as an equality (`raised == []`) by
   `test_exactly_one_discovery_writer_claims_the_closure_readmes`, so a *sixth* abort reds a
   test rather than surfacing inside the irreversible run.
2. **The return is an exhaustive partition.** `len(to_stamp) + len(deferred) +
   len(already_stamped)` equals a population derived **independently of the function under
   test**, so a manifest cannot leave through an unnamed bucket — which is what the second
   consequence actually was.
3. **A manifest whose foreign front matter *parses* is classified not-stamped**, in `deferred`,
   rather than already-migrated. This is the original clause's intent, kept: the predicate must
   ask whether *this migration* stamped the file, never whether its front matter parses.
4. **No vendored manifest is stamped** — its **leading front-matter block** is byte-identical
   before and after. NT-0019 §4 step 5 requires the header to be *merged*, which is W37-6 Task
   1's work, and `CLAUDE.md` §12 forbids doing it by prepend. **This is the clause the original
   lacked**, and its absence is what made the original destructive.

   **Corrected 2026-09-02, before merge, and the first wording was wrong in the same way the
   original clause was.** It read *"no vendored manifest is written by the run — byte-identical
   before and after"*, which **reds on correct behaviour**: NT-0019 §1.5 says the manifest *"is
   stamped and its own citations rewrite like any other file"*, and `_is_vendored_exempt`
   implements exactly that — only files **beneath** the boundary are exempt. Measured on a
   completed run over the real corpus: of 28 vendored manifests, **0** had their leading block
   changed and **2** (`secret-hygiene`, `writing-plans`) had body-only citation rewrites
   (`NT-0012` → `RFC-130`, `NT-0014` → `RFC-179`, `W10` → `WK-905`, `W11` → `WK-906`). The
   first wording would have called those two a violation. **Stamping and citation rewriting are
   different acts on the same file**, and an acceptance clause that cannot tell them apart is
   the defect this replacement exists to avoid.
5. **Every deferred manifest is named with its reason on the single reporting channel**,
   `MigrateResult.deferred_reference_stamps` — proven by reconciling the two instruments
   against each other (`test_vendored_manifest_deferrals_are_exactly_the_reference_censuss`)
   rather than asserted in a docstring.

*Violation: a `_discover_*` that raises on the real corpus.*

*Violation: a vendored manifest in none of the three buckets, or in two.*

*Violation: a manifest carrying `name:`/`description:` counted as already migrated.*

*Violation: a vendored `SKILL.md` whose leading front-matter block differs across a run.*

*Violation: a deferred manifest that appears in no report.*

**Limb 2 is untouched and this finding stays open on it**: nothing yet reaches
`docs/audit/phases/1b/register.md`, and its declared exception in
`_AUDIT_CLOSURE_CENSUS_EXCEPTIONS` still describes a live gap. **Limb 3** was discharged in this
finding's own filing commit and is unaffected.

**Pinned by symbol, at the tree that lands this**, in `tests/test_doc_id_migrate.py`:
`test_vendored_manifest_scan_partitions_the_real_corpus_without_raising` (the closed partition),
`test_a_vendored_manifest_whose_front_matter_parses_is_not_read_as_stamped` (this clause's own
test), `test_vendored_manifest_deferrals_are_exactly_the_reference_censuss` (the two instruments
this finding named now reconciled against each other, measured rather than asserted),
`test_the_old_predicate_reproduces_the_abort_on_the_real_corpus` and
`test_treating_foreign_front_matter_as_stamped_is_the_blind_failure` (the two consequences
restored one at a time by source mutation), and
`test_a_narrowed_selector_reds_the_partition_rather_than_shrinking_quietly` (the corpus-builder
itself, so the counts above cannot go quietly to zero).

**`UNSTAMPABLE_EXEMPTIONS` is not imported, and that is a decision rather than an omission.**
This finding's own text says the register *"cannot discharge this limb"*, and the reason it
cannot is not only that `doc-id.py` never consults it: the register answers *can this file carry
a governed header at all*, a stamp-set question, while this call answers *has this migration
already stamped it*, an idempotency question. Substituting one for the other is what produced the
defect; importing the register would substitute a different wrong predicate. At this tree the
three registered manifests need no separate treatment here — they classify `foreign` alongside
the other 25 — and the constant's own declaration names `audit-docs.py` check 30, not this
module, as the consumer it was made public for.
