# Example documentation suite

The fixture's stand-in for `docs/README.md` — a README **already** inside §4 step 5's own
roots, so it is stamped whether or not the six-README widening applies. It is here so the
fixture exercises both halves of the arithmetic (already in scope, and newly gained) rather
than only the interesting half.
