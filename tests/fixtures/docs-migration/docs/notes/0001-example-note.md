# NT-0001 — Example finding needs a follow-up fix

| | |
|---|---|
| **Raised** | 2026-08-10, fixture author — a worked example of the legacy note header `doc-id.py migrate` converts to front matter |
| **Status** | `accepted` — nothing left open in this fixture |
| **Deliverable** | A fixture illustrating the prose-table header this migration converts; no real code |
| **Owner** | fixture author |
| **Lands in** | This corpus only |
| **Trigger** | none — filed for the migration script's own test suite |

---

## 1. Background

This note cites `ADR-0001` for the decision it assumes. It also cites `wf-01`, a workflow
this miniature corpus does not itself carry a fixture for — `doc-id.py migrate`, run only
over this fixture root, has no record to resolve that citation against and must leave it
exactly as written rather than guessing.

## 2. Resolution

None required.
