# Closure records and historical findings

> A worked example of the legacy closure-records shape: one `###` heading per work or
> phase closure, each ending in the date it closed. `doc-id.py migrate` splits this file
> one closure record per heading (NT-0019 §4 step 2), each becoming its own `CR-` file.

### Example work closes, accepted 2026-08-18

**Accepted by the maintainer.** A worked example only.

| | |
|---|---|
| What was delivered | A fixture closure record |
| Evidence | None — this is test data |

### Example phase closes, accepted 2026-08-19

A second worked example, so the migration script has more than one record to split out of
this one file. Historical: the retired `.claude/notes/` directory is named here purely as
an example legacy-path form for the migration script's own test suite to exercise, per
`LEGACY_FORM_PATTERNS`'s "legacy claude-notes path" entry in `scripts/audit-docs.py` — not
a real citation of anything this corpus governs.
