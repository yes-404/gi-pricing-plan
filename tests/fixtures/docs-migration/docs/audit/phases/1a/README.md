# Phase record — 1a (Example phase)

Closed 2026-08-13. Written per the phase-close checklist.

The fixture's stand-in for `docs/audit/phases/1b/README.md`: the one record NT-0019 §5.2
routes to `closures/CR-0nnnn-*.md` with `kind: phase` rather than `kind: work`, so the
migration's two kinds are both exercised end to end rather than only the commoner one.

## Exit criteria

Met.
