# F1 — Example finding one

A worked example of the migration script's finding-essay shape: a bare `F<n>` heading,
no front matter, matched by `_discover_findings` on its filename (`F1.md`) and its own
`# F1 — <title>` heading, for the register row `docs/audit/register.md` also names
`(F1)`.

## Evidence

Fixture only — no real evidence is attached.
