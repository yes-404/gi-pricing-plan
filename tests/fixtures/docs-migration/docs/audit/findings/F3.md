# F3 — Example finding three, resolved

A third worked example, whose register row carries a resolution marker so
`_discover_findings`'s `status:` derivation (`closed` when the Decision cell opens with
or carries a resolution marker) has a positive case to prove against, not only the
`active` default `F1`/`F2` exercise.

## Evidence

Fixture only — no real evidence is attached.
