# WF-01 — Example journey

A worked example of the migration script's legacy workflow shape: a bare `# WF-0N —
<title>` heading, no front matter, matched by `_discover_workflows` on the heading
rather than the filename.

## Steps

1. Fixture only — no real journey is described.
