"""The rating path — validation, compilation and scoring (03 §5.2).

W9 builds this module in three slices: W9-1 the `RatingAlgorithm` contract (in
`model-schema`), W9-2 the save-time validation (`compile.py`), W9-3 the bundle
compilation. The engine is the confirmed substrate (ADR-0004).
"""
